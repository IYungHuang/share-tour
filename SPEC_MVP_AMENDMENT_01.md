# SPEC MVP 增修草案 01 —— 核心迴圈數值與取材尺度

**狀態**：**已完成並驗收**（v7 定稿；T0~T14 共 16 個 commit 序列全數執行並通過驗收，證據見 `MVP_AMENDMENT_01_VERIFICATION.md` §6，commit `0b9a30a`）
**修訂對象**：`SPEC_MVP_CORE_LOOP.md`、`SPEC_MVP_TIMELINE_UI.md`、`SPEC_MVP_POI_GATHERING.md`
**依據**：2026-09-11 雙軌審查與其後的企劃覆核，主張均已逐項複驗
**上位文件**：`CROSS_CUTTING_CONSTRAINTS.md`（牴觸時以其為準）

**v7 相對 v6 的變動**（最終覆核退回後修訂）：明定「合法素材域」與「契合卡集」不是同一件事，AC-A1-6.8 改驗玩家實際可出的完整合法牌組；把 Theme 契合度基準與最終加扣分拆開，移除混亂冒險在 Theme 側的殘留豁免；撤回與完整合法域互斥的「所有最高熱度行程 Theme ≤ 60」，改驗冒險連段不得消除 Theme 疲勞；保留預算赤字契約，將第二張絕景的採集代價收斂為體力；補齊所有分佈／最大值 AC 的客戶、哲學、母體、平手與比較基準；正式裁決美食朝聖採「低風險體力效率」補償；明文保留凍結中的 GPS 公尺契約與 AC-18.3；補 3 槽 UI、最近 POI、體力提示三條接線驗收；依 v5 標籤方案更正未使用標籤為 5/17（29%）。

**v7 覆核後勘誤（不改行為）**：刪除 AC-A1-4.4 說明中沿用舊標籤方案的標籤位估算；`≥16` 保留為產品下限，最終可達子集改由施工計劃的精確約束搜尋證明。

**v6 相對 v5 的變動**（施工計劃覆核回填）：AC-A1-6.8 未定義窮舉範圍，而 AC-A1-2.5 定義為「當局可達素材池」，兩者範圍不一致導致「調參」與「選卡」互為前提。拆為 **6.8a（全卡表，調參目標）** 與 **6.8b（當局可達子集，玩家實際經歷）**。v6 曾要求因京都窗口重疊而修訂 `manifest_geometry_check.dart`；該點已於 v7 撤回，因其實際承接凍結中的 GPS 公尺契約。

**v5 相對 v4 的變動**（第四輪覆核後）：v5 改由**標籤層拆開兩家**（午夜探索改排斥 `#拉車`，契合卡集重疊自 0.33 降至 0.11），並把規則反轉改到 **Hype 側**，Theme 側照扣。v5 當時用單一最高熱度並列解 `[5,4,4,3]` 論證「失去節奏互補是 no-op」；v7 已改為對全部並列解驗收，不再依賴該不完整論證。

**v4 相對 v3 的變動**（第三輪覆核後）：REQ-A1-14 補上代價條款並收斂為單一具名例外（無代價的規則反轉讓混亂冒險嚴格支配午夜探索）；新增 AC-A1-6.8 功率天花板；AC-A1-6.5 從累退稅改為不奪走任一哲學的可負擔絕景；清掉五處「正文已改、AC 未同步」的矛盾；附錄 A.2 的補償路徑由斷言改為待辦；待決問題 6 結案（實測 24%，未違反門檻，我先前誤算為 35%）。

**v3 相對 v2 的變動**（第二輪覆核後）：新增 §1.2「主題必須傳導到滿意度」—— v2 的六條 AC 全部停在 `finalTheme`，而社畜端 `themeRatio` 在 60 分即封頂，主題 60 與 92 同分，整套修訂做完可能對滿意度毫無影響；§1.1 定義契合度階梯（否則主題分只是從恆 100 變成恆 90）；§3 明訂槽位須連續佔用（留空中槽會消掉相鄰對判定，等同免費規避疲勞）並重新定義純度獎勵；新增 §6.5 哲學得改變規則正負號（混亂冒險與疲勞懲罰在現行卡表上直接對撞）；附錄 A 依實測重排；修正多條互斥、no-op 與依賴幀率的 AC。

**已更正的事實錯誤**：絕景為 **5 張**（v2 誤植 6 張）。

---

## 0. 為什麼要改

現行實作通過 383 條測試、`flutter analyze` 0/0，但**三條規則使整個核心迴圈的決策空間塌陷**：

| 症狀 | 複驗證據 |
|---|---|
| `finalTheme` 在任何合法提交下恆為 100 | `timeline_itinerary.dart:153` 累加素材全額 `themeValue`；`:197` 以 `baseTheme = 50` 為基底 clamp 至 100。素材 `themeValue` 值域 15~45，四槽最低組合即 80 → 130 → clamp |
| 客戶滿意度不可能失敗 | 主題得分恆滿；反無聊門檻 `totalHype < 30`（`client_review_engine.dart:43`）在實測 200~330 下不可達 |
| 正規採集路徑在虛擬移動下不可用 | `triggerRadiusMeters = 50`、`metersPerPixelAt() = 370.4` → 觸發窗 0.135 px；方向鍵 40 px/s 下單幀位移 0.67 px |

後果：5 種旅行哲學、4 個槽位加成、節奏互補、拉車疲勞全部無效；玩家不會失敗，因此無從歸因。**在此修訂落地前，任何「好不好玩」的玩家測試都測不到東西。**

本草案只處理**數值與行為契約**。微動作（Runner / Snapshot）不在本次範圍（見 §7）。

### 0.1 共通驗收定義

以下定義適用於本草案全部 AC，避免用未定義母體做出可被挑資料操縱的結論：

- **合法素材域**：玩家當局腰包中的全部素材。偏好與排斥只改變分數，**不禁止素材入槽**。
- **契合卡**：至少命中 1 個偏好標籤，且未命中任何排斥標籤的素材。契合卡集只用於內容覆蓋率與牌風差異稽核，**不是合法牌庫**。
- **合法行程**：3 或 4 張不同素材，放入固定 4 槽；3 槽只允許 `[0,1,2]` 或 `[1,2,3]`，4 槽全部填滿。同一批卡的不同槽位順序視為不同合法行程。
- **最佳滿意度**：除非 AC 另寫「最高熱度」，否則「最佳」皆指指定客戶的 `satisfaction` 最大值。若多個行程並列，全部都屬最佳解，不得任選其中一個代表。
- **最高熱度**：`TimelineItinerary.calculateStats` 產生的 `totalHype` 最大值，含相機槽位倍率與同標籤共鳴，但未扣客戶端疲勞、絕景與 Theme 係數。若並列，相關 AC 必須對全部並列解成立。
- **分佈母體**：每個 `(哲學, 合法行程)` 配對各算 1 筆；不得以抽樣、去重分數或只留每家最佳解取代。

---

## 1. 主題一致性

### 1.0 REQ-A1-00 標籤詞彙表對齊（阻擋級，其餘各條的前提）

素材標籤與旅行哲學標籤**必須**取自同一份受控詞彙表。

現況（已複驗 `travel_philosophy.dart` 與 `kyoto_night_catalog.dart`）：15 個偏好標籤中 **9 個在素材池零命中**（#孤獨 #放空 #偶然 #深夜食堂 #地道 #無人 #廢墟 #突發 #奇葩）；素材池的 #美食(6 張) #絕景(5) #拉車(6) #咖啡(4) 不是任何哲學的偏好；**「美食朝聖」哲學不偏好 `#美食`**。

且 5 種哲學中 **4 種沒有任何排斥標籤**（僅 `antiTourism` 有 `#大眾名店`）。沒有排斥就沒有「不該放進去的東西」，策展只剩加法，GDD Rule 11 無從成立。

行為契約：

- 每一種旅行哲學的每一個偏好標籤，在當局可達素材池中**至少有 2 張卡承載**。
- 每一種旅行哲學**至少宣告 1 個排斥標籤**，且其排斥標籤**聯集**在當局可達素材池中**至少有 4 張卡承載**。
- 未被任何哲學使用的標籤，其**獨有的卡片數**不得超過全池的三分之一。

> 度量是卡片覆蓋率而非標籤種類數：v3 方案下未使用標籤只有 4/17（24%，門檻未違反），但其中的 `#老街` 有 14 張卡（全池 44%），等於近半數卡片帶著一個不影響任何決策的標籤。數種類量不到這件事。

#### AC

- **AC-A1-0.1**　對每一種旅行哲學與其每一個偏好標籤，當局可達素材池中承載該標籤的素材數 ≥ 2。
- **AC-A1-0.2**　每一種旅行哲學的排斥標籤數 ≥ 1，且其排斥標籤**聯集**的承載素材數 ≥ 4。

> 聯集而非單一標籤：AC-A1-1.3 要求「四槽全排斥」，3 張卡湊不出四槽，v2 的兩條互斥。
- **AC-A1-0.3**　素材池標籤集合與哲學標籤集合的交集，涵蓋哲學標籤的 100%。
- **AC-A1-0.4**（回歸護欄）　僅帶未使用標籤的卡片數 ≤ 全池的三分之一。實測為 **0 張**，現況已通過；與 REQ-A1-02 同類，放入施工測試清單即可，不構成需求。

> 這條不做，§1 的差值制會在真實內容上空轉 —— 重演「元件正確、接線沒人測」的老毛病。

### 1.1 REQ-A1-01 Theme 反映契合度，而非素材數量

最終主題分數**必須**由「素材與所選哲學的契合程度」決定，且**不得因強力組合而觸頂截斷**。

行為契約：

- 基底 50。中性素材淨貢獻為 0。
- 命中偏好產生正貢獻，命中排斥產生負貢獻。
- 先計算只含「基底 + 哲學契合貢獻」的**契合度基準分**，再加入時段、節奏、疲勞與純度效果得到 `finalTheme`。兩個階段必須可分別驗收，避免同一個 `finalTheme` 同時被要求等於基準值與扣分後值。
- **疲勞前 Theme**：契合度基準分加上時段、節奏與純度效果後、尚未扣除疲勞且尚未 clamp 的值。此為驗收用中間量，不限定程式結構。
- 契合貢獻須**正規化**（按槽位數平均或等效手段），使「四槽全偏好且皆為最高契合度」的契合度基準分落在 **88~92**，而非溢出後被截斷。
- **契合度必須分級**，不得維持現行的二元 `any()` 判定。分級至少須反映：單張素材命中的偏好標籤數，以及該素材自身的 `themeValue`（值域 15~45）。
- 槽位時段加成、節奏互補、拉車疲勞在**任何** Build 上都必須能在最終分數上被觀察到。

#### AC

- **AC-A1-1.1**　以合成素材隔離時段、節奏、疲勞與純度效果後，四槽皆為中性素材時，契合度基準分等於 50。
- **AC-A1-1.2**　同一隔離條件下，四槽每張皆命中 ≥2 個偏好標籤且 `themeValue = 45` 時，契合度基準分介於 88 與 92；四槽每張僅命中 1 個偏好標籤且 `themeValue = 45` 時，契合度基準分 ≤ 75。
- **AC-A1-1.3**　同一隔離條件下，**對每一種**旅行哲學，均存在四槽全排斥合成組合使契合度基準分 ≤ 20。
- **AC-A1-1.4**　同一隔離條件下，**存在**一組素材與兩種哲學，使其契合度基準分相差 ≥ 25。
- **AC-A1-1.5**　對**每一種**旅行哲學，各建立一份恰有 1 組相鄰高風險對、疲勞前 Theme 落在 40~80 的合成行程；其 `finalTheme` 必須等於疲勞前 Theme − 10。
- **AC-A1-1.6**　對**每一種**旅行哲學，各建立一份契合度基準分 ≥ 88、恰有 1 組相鄰高風險對、疲勞前 Theme 落在 90~100 的強 Build；其 `finalTheme` 必須等於疲勞前 Theme − 10。

> AC-A1-1.6 證明 clamp 不再吃掉懲罰 —— 在**最需要看見懲罰的強 Build 上**也看得見。
>
> AC-A1-1.2 的下半條是 v3 新增：若維持二元判定，隨手挑 4 張偏好卡即得 90 分，主題分只是從「恆為 100」變成「恆為 90」，塌陷沒有解除。

### 1.2 REQ-A1-01b 主題必須傳導到滿意度

主題分數的變化**必須**在兩位客戶的滿意度上都造成可感知的差異。

現況（已複驗）：社畜端 `client_review_engine.dart:36` 為 `(finalTheme / minTheme).clamp(0.0, 1.0)`，`minTheme = 60` —— **主題 60 分與 92 分完全同分**；網紅端 `:110` 的 `themeFactor = 0.7 + 0.3 × theme/100`，全域擺幅僅 43%。

§1.1 把主題從恆 100 修成有分佈，若不同時修這兩處映射，滿意度可以一分都不動 —— 六條 AC 全綠而玩家無感，正是本專案反覆踩到的「元件正確、接線沒人測」。

行為契約：`minTheme` 的封頂映射與 `themeFactor` 的係數一併納入修訂範圍；主題須能單獨決定一次成敗。

- **AC-A1-1.7**　對兩位客戶**各自建立一組**參考行程；該行程在 `finalTheme = 90` 時滿意度落在 **85~95**。固定該客戶的成本、熱度、疲勞與絕景條件，只把 `finalTheme` 降至 30 時，滿意度下降 ≥ 20 分。

> 必須指定參考行程：網紅端是乘法模型（`client_review_engine.dart:113`），同一個係數在強行程上掉 36 分、在弱行程上只掉 14 分。讀作全稱量詞時對任何乘法係數都不可滿足。
- **AC-A1-1.8**　存在一組素材，僅因所選哲學不同而使同一位客戶的結果跨越一個評級（如 Perfect → Near Miss）。

---

## 2. 預算必須會爆

### REQ-A1-02 素材成本的內容不變式（回歸護欄）

任一城市圖資的素材池須滿足「最貴四張 > 社畜預算 × 1.3」與「存在負擔得起且不觸發反無聊的四張」。京都素材池**已滿足**（最貴四張 8,700 > 2,600；免費卡 10 張），刪掉本條今天玩家的能力毫無變化 —— 依 Rule 35 它是一條回歸測試而非需求，施工計劃將其放入測試清單即可，正文不再展開。

### REQ-A1-03 超支懲罰改為比例制

超支扣分**必須**與超支佔預算的比例成正比，不得與絕對金額成正比。

行為契約：**自 Perfect 惡化至 Rejected 的超支跨度 ≥ 預算的 30%，且分數隨超支比例單調遞減無跳躍。**

> v2 另外訂了「超支 10% 不再 Perfect」與「超支 40% 落入 Rejected」。三條合看只有唯一一組邊界能同時滿足，是零容差的刀鋒解；且現況其實已滿足前兩條（超支 200 円即掉出 Perfect、超支 800 円即 Rejected，`client_review_engine.dart:26-33`），真正壞掉的只有跨度 —— 現況跨度僅預算的 7.5%。故只保留跨度這一條。

### REQ-A1-04 反無聊門檻必須可觸及且隨客戶調整

反無聊懲罰的熱度門檻**必須**寫成客戶目標熱度的函數，不得硬編碼。

行為契約：門檻落在合法行程的熱度分佈之內；存在觸發與不觸發的合法組合。

#### AC

- **AC-A1-2.1**　對 `budgetWorker`，存在四槽組合使滿意度落入 Rejected。
- **AC-A1-2.2**　對 `budgetWorker`，存在四槽組合使滿意度落入 Perfect。
- **AC-A1-2.3**　對 `budgetWorker`，在主題得分為滿分的前提下，Perfect 的超支上界與 Rejected 的超支下界之差 ≥ 預算的 30%，且滿意度對超支比例單調遞減。
- **AC-A1-2.4**　對 `budgetWorker`，存在四槽組合觸發反無聊懲罰，且其總成本低於預算（證明「省錢」與「不無聊」構成真實取捨）。
- **AC-A1-2.5**　對 `budgetWorker`，以當局可達素材池窮舉 5 種哲學下的全部合法 3／4 槽行程；以 §0.1 的 `(哲學, 合法行程)` 為分佈母體，Perfect / Pass / Near Miss / Rejected **四級皆須出現**，且 Perfect 佔比 ≤ 15%。

> AC-A1-2.5 是分佈型斷言。前四條的存在性只能證明「不是恆為 100」，證不出分佈健康。

---

## 3. 刪除必須有代價

### REQ-A1-05 允許少交，並獎勵純度

行程表**必須**允許以少於 4 個槽位提交，且「少而純」在特定客戶與哲學下必須優於「多而雜」。

現況：`timeline_itinerary.dart:97,100` 要求四槽全滿才可提交。GDD Rule 11 要求「Theme Coherence 必須獎勵刪除」、第二十一節稱「知道什麼**不應該**放進去」是最重要的策展能力 —— 但玩家目前**不被允許不放**。所謂刪除只剩「從 6 張選 4 張」，那是挑選，不是刪除；被淘汰的素材不會在行程上留下任何痕跡。

行為契約：

- 可提交的槽位數為 3 或 4。
- **已填的槽位必須連續佔用，不得有中間空槽。**
- 少交一個槽位必然減少一份素材帶來的收益（佣金與熱度），此即刪除的代價。對以高風險追熱度的網紅局，**少交不得只因少一次疲勞曝險而成為固定最優解**。
- 純度獎勵僅在全部已填素材都是 §0.1 的契合卡時成立；任何中性或排斥素材都使獎勵失效。
- 純度獎勵處理的是「**要不要為了湊滿而塞第 4 張不合哲學的卡**」，不是「3 槽優於 4 槽」：4 槽全契合一樣拿得到獎勵。
- 最優槽位數**必須**隨客戶與哲學改變，不得存在恆定最優解。

> 連續佔槽是阻擋級：`timeline_itinerary.dart:167` 的相鄰對迴圈遇 `null` 即 `continue`，把空槽放在索引 1 或 2 會把行程切成不相鄰的兩段，相鄰對從 3 對降到 1 對甚至 0 對 —— **免費規避拉車疲勞**。高風險 Build 可藉留中間空槽清掉本應存在的疲勞，而 §6 加重疲勞後這條規避路徑的價值同步放大，兩節互打。

#### AC

- **AC-A1-3.0**　空槽位的槽位加成（`timeline_slot.dart` 的時段 +5 Theme 與黃昏槽相機倍率）一律不計入，且留空**晨曦**槽與留空**深夜**槽的損失必須不同。

> 連續佔槽之後 3 槽只能是 `[0,1,2]` 或 `[1,2,3]`，**黃昏槽永遠不可能空**，故 v3 拿黃昏槽當對照是測不出來的。另須注意：晨曦槽的加成條件是「低風險且帶 #散步 或 #早餐」、深夜槽是「高風險或帶 #深夜 或 #小酌」，後者承載卡遠多於前者，現況下 3 槽幾乎恆為 `[1,2,3]`（丟晨曦）。若要兩個方向都有人選，晨曦槽的加成條件須放寬。
- **AC-A1-3.1**　3 槽行程可通過提交前置檢查；2 槽不可；中間留空的 3 槽亦不可。
- **AC-A1-3.2**　存在一種哲學、一組恰含 4 張不同素材的手牌（3 張契合卡、1 張非契合卡）與一位客戶，使 3 槽純行程的最佳滿意度高於該手牌全部 4 槽排列。
- **AC-A1-3.3**　對另一位客戶，同一哲學與同一手牌的 4 槽最佳滿意度高於任何合法 3 槽排列。
- **AC-A1-3.6**　固定 `hypeInfluencer`，對五種哲學逐一以全卡表中 `riskLevel >= 3` 的全部素材為候選域，窮舉合法 3 槽與 4 槽行程；3 槽的全域最佳滿意度不得高於 4 槽的全域最佳滿意度。
- **AC-A1-3.4**　行程中混入一張非契合素材時，純度獎勵消失（3 槽與 4 槽各驗一次；中性與排斥素材各至少覆蓋一次）。
- **AC-A1-3.5**　客戶回饋文案在純度獎勵成立與失效時，文案內容可區分。
- **AC-A1-3.7（狀態接線）**　`CuratorRunController` 對 `[0,1,2]`、`[1,2,3]` 兩種 3 槽連續配置皆允許提交；2 槽與任何中間留空配置皆拒絕提交。提交結果必須使用 domain 回傳的同一份 `canSubmit`，不得在 state 重寫規則。
- **AC-A1-3.8（UI 接線）**　時間線 UI 對合法 3 槽配置啟用提交按鈕，將端點空槽呈現為刻意留白；對 2 槽與中間留空配置保持禁用並顯示可區分原因。原 `SPEC_MVP_TIMELINE_UI.md` AC-UI-1.4、AC-UI-2.5 的「四槽才可提交」由本條取代。

> v1 的「敘事落差」規則已刪除。它只禁止故事厚度單調不遞減，一個凹點（如 `[5,4,5,5]`）即可豁免，低故事素材並未因此成為必需品；刪掉它玩家創造旅行的能力毫無變化 —— GDD Rule 35 判它出局。
>
> AC-A1-3.6 刻意不對每一手四張牌作全稱斷言：若第四張昂貴、排斥或低效，刪掉它本來就是 REQ-A1-05 要獎勵的策展決策。本條只阻止「高風險追熱度」的全域最佳策略被疲勞量級固定成 3 槽。

---

## 4. 取材尺度必須讓遊戲走得完

**已裁決採甲案**：改用小尺度手繪京都夜間街區圖資；台灣真實地圖原地封存，留待 Reality Run 階段。

### REQ-A1-06 觸發判定一律以像素定義

P1 京都街區的**採集資格**觸發半徑必須以像素定義，不得經由公尺/像素換算取得。

現行採集入口實際使用 `DistrictAttraction.triggerRadiusMeters`，再透過 `metersPerPixelAt()` 換算距離；`OverworldPoiNode.triggerRadius` 是另一條遭遇／顯示通道，不能證明採集已採像素契約。P1 須在採集端新增獨立像素半徑，並由正式採集資格入口讀取。

GPS 的 `PoiMarker.triggerRadiusMeters`、`DistrictAttraction.triggerRadiusMeters`、`metersPerPixelAt()` 與 `findPoiProximityConflicts()` **全部保留**。它們承接凍結中的 SPEC C REQ-C-18 / AC-18.3，不因京都像素採集而修改、刪除或改義。

行為契約：

- 觸發窗口直徑不得小於「方向鍵持續按住 0.25 秒的位移」。
- 一次採集動作**只結算距離最近的一個 POI**，因此觸發窗口容許重疊。
- 若兩個最近 POI 等距，以 POI `id` 的字典序決定，確保結果可重播。
- 京都像素採集窗口的重疊規則與 GPS 公尺幾何護欄屬不同契約；不得用刪除 GPS 護欄解決京都窗口重疊。

### REQ-A1-07 單局地理尺度

單局可探索範圍**必須**能在整局 3~10 分鐘的預算內走完，且 POI 數量足以產生取捨。

#### AC

- **AC-A1-4.1**　三件裝備皆為 Lv.1，自降落點出發，在最終京都街區 POI 佈局中存在一條只用方向鍵的決定性路徑，可於 **4 分鐘邏輯時間**內完成 **6 次**採集；第 6 次後腰包滿載且 HP > 0。
- **AC-A1-4.2**　觸發窗口直徑（像素）≥ 方向鍵速度 × 0.25 秒。
- **AC-A1-4.3（正式入口接線）**　站在至少兩個觸發窗口的重疊處，透過正式採集入口發動一次採集，只能新增最近 POI 的 1 個 `gatheredPoiId`、1 張素材，並只扣 1 次 HP 與 Budget；等距時依 POI `id` 字典序結算。
- **AC-A1-4.3b（GPS 回歸護欄）**　SPEC C AC-18.3 的合法 Fake、違規 Fake、台灣 manifest 三組公尺幾何測試維持全綠；既有 GPS 公尺欄位與 `metersPerPixelAt()` 仍由原管線使用。
- **AC-A1-4.4**　可達 POI 數量 ≥ 16。
- **AC-A1-4.5**　可達 POI 的標籤分佈涵蓋**全部 5 種**旅行哲學的每一個偏好標籤；行前三選一仍可從全部 5 種哲學抽取，不得產生當局零對應素材的死牌。
- **AC-A1-4.6**　可達 POI 中，**每一種旅行哲學的契合卡集**至少含 1 張 `isSpotlight` 素材。

> AC-A1-4.4：16 張是保留採集取捨空間的產品下限，不由標籤種類數算式推導。先前對 12~14 張做的隨機搜尋不是窮舉，不得當成不可行證明；最終子集須以完整約束搜尋同時驗 §1.0 與本節全部 AC，若找不到則回報並退回 SPEC。
>
> AC-A1-4.2：v2 寫「單幀位移的 3 倍」，幀率是 Flame 的事，在 `domain/` 測不了，違反分層測試策略；且現況 35 px 已超標 17 倍，門檻形同虛設。
>
> AC-A1-4.5：行前三選一是從 5 種哲學無權重抽 3（`curator_run_state.dart:87`）。只保證涵蓋 3 種，玩家有實質機率抽到一手在當局地圖上完全沒有對應素材的死牌，違反 Rule 26/27。
>
> AC-A1-4.6：網紅客戶的絕景折半是 50 分量級的開關。僅保障「整張地圖 ≥1 張」不夠 —— 美食朝聖與反觀光各自只有 1 張絕景可用，那張若當局不可達，這兩家在網紅局就沒有絕景路徑。

---

## 5. 局外成長必須改變玩法

### REQ-A1-08 裝備效果不得為純數值

裝備升級**必須**改變玩家可做的選擇。

行為契約：

- 相機的槽位倍率，其最低等級不得使「將最高熱度素材置於加倍槽位」成為恆定最優解。
- 體力上限與腰包容量必須**輪流**先咬合 —— 兩者都要有成為約束的場合，玩家才會在「採幾張」與「採哪張」之間取捨。
- 高風險素材的體力代價必須顯著高於低風險素材。**現況不滿足**：`deltaHp = 10 + riskLevel × 2`（`curator_run_state.dart:269`），risk 5 為 20、risk 1 為 12，僅 1.67 倍，未達 AC-A1-5.3 要求的 2 倍。

### REQ-A1-09 升級節奏

三件裝備全數升滿所需局數**必須**落在可觀察留存的區間內。

### REQ-A1-09b 美食朝聖的補償路徑

美食朝聖因卡片成本偏高而承受預算劣勢；本次正式採用**低風險體力效率**作為補償。補償必須由所有哲學共用的風險／體力公式自然產生，不新增美食朝聖專屬規則。

驗收使用 §0.1 的契合卡集，各自按 `hypeValue` 由高至低排序；同值以素材 `id` 字典序排序。Lv.1 球鞋、初始 HP 100、無其他體力消耗下，美食朝聖應能先把 Lv.1 腰包裝滿；混亂冒險則應先被體力限制。

#### AC

- **AC-A1-5.1**　固定 `hypeInfluencer`、相機 Lv.1，以最終當局可達素材池的全部 6 張手牌組合乘上 5 種哲學為母體；每組窮舉全部合法 3／4 槽行程。若該組的**每一個**滿意度最佳解都把手牌中最高基礎 Hype 的素材之一放在黃昏槽，才計為「黃金槽答案已固定」；只要存在一個並列最佳解未如此放置就不計。該比例 ≤ **30%**。
- **AC-A1-5.2a**　Lv.1 裝備下，存在一條全低風險採集序列，使腰包先滿而體力仍有餘（考驗「採哪張」）。
- **AC-A1-5.2b**　Lv.1 裝備下，存在一條全高風險採集序列，使體力先耗盡而腰包未滿（考驗「採幾張」）。
- **AC-A1-5.3**　採集一張 riskLevel 5 素材的體力代價，至少為 riskLevel 1 素材的 2 倍。
- **AC-A1-5.4**　固定 Lv.1 裝備並對 `budgetWorker`，以當局可達素材池窮舉 5 種哲學的全部合法行程，取所有 `Pass` 行程 `earnedCoins` 的中位數；`ceil(三件裝備自 Lv.1 升至 Lv.3 的總價 / 該中位數)` 介於 6 至 10 局。偶數筆中位數取中間兩筆算術平均。
- **AC-A1-5.5（體力接線）**　一般採集與滿包換牌兩條正式入口中，行動前預覽顯示同一條 domain 規則算出的名目 HP 成本；行動後浮字等於實際 HP 下降量。最後一搏時，實際下降量為行動前剩餘 HP，不得顯示超過實際扣除值。
- **AC-A1-5.6（美食補償）**　依 REQ-A1-09b 的固定排序採集：美食朝聖前 6 張契合卡全部收入腰包後 HP 仍 > 0；混亂冒險最遲在第 5 張完成採集時 HP 歸零並進入 `nightEditing`，腰包尚未滿。

> AC-A1-5.1 不再使用可挑選的「10 組代表手牌」。全母體斷言才能防止調參後另挑 10 組讓比例過關；若執行時間過長，可使用能證明不改變最佳解的剪枝，不得抽樣。
>
> AC-A1-5.4 現行參數不滿足：滿級總價 4,500（`meta_equipment.dart:64-71`），社畜 Pass 佣金約 1,060、Perfect 約 1,560 —— **約 3~4 局即可全滿**。施工計劃須調整升級價梯或佣金係數。

---

## 6. 評分模型的五處失衡

### REQ-A1-10 絕景權重改為連續

絕景係數**不得**為二元開關。現況 `client_review_engine.dart:106` 為 `hasSpotlight ? 1.0 : 0.5` —— 這是全系統最大的單一權重（50 分量級），卻只有兩個值，把網紅客戶壓成一道是非題，抹平其他所有決策（違反 Rule 26）。

行為契約：係數隨絕景張數連續成長且有上限；「帶第二張絕景」必須構成需要付出體力的取捨。預算維持可赤字的軟資源，不作為採集資格硬上限；網紅評分不計成本，因此不能用預算宣稱其第二張絕景在採集階段有代價。

### REQ-A1-11 拉車疲勞改為比例

疲勞懲罰現為固定 15 Hype（`client_review_engine.dart:102`），對 `targetHype = 150` 僅 10 分量級，而高風險素材熱度通常 40~80 —— **帶疲勞仍然賺**，網紅「絕不能有拉車行程」的人設在數值上是空話。懲罰須與目標熱度成比例。

### REQ-A1-12 退件的收入必須顯著低於過關

`ReviewOutcome.rejected.commissionRate` 早已是 0.0，真正的漏洞是 `client_review_engine.dart:76-78` 的故事折現 `stats.totalStory * 5` **無條件加總**，故 Rejected 仍可入袋 60~100 幣。失敗必須有可感知的代價。

但**不得歸零**：3~10 分鐘一局的 roguelite，失敗局零產出會直接打擊 Exit Criteria ②「會不會主動開始第二局」。故要求的是「顯著低於」，不是「沒有」。

### REQ-A1-13 絕景素材必須有代價

京都素材池 5 張絕景中 4 張成本為 0 —— 帶絕景幾乎不用付錢，網紅端的痛苦取捨不存在（違反 Rule 12）。至少 1 張絕景須提高成本或風險（現況 `cost == 0 && riskLevel <= 2` 者為祇園白川與八坂之塔共 2 張，降到 AC 要求的 ≤1 張只需加稅其一）。

**但加稅不得成為累退稅**：實測伏見稻荷(risk 4)、大文字山(risk 5)、清水寺(1000 円) 三張現況已通過，代價會全部落在祇園白川(0 円/risk 1) 與八坂之塔(0 円/risk 2) —— 前者是慢旅行唯一的免費絕景，後者是**美食朝聖唯一的絕景、也是它唯一的免費卡**。且若把八坂之塔改成 risk ≥ 3，它立刻被疲勞規則（`timeline_itinerary.dart:190` 以 `riskLevel >= 3` 判定）咬到，美食朝聖唯一的網紅解法自帶疲勞。

### REQ-A1-14 混亂冒險的疲勞反轉（單一具名例外）

**對混亂冒險，且僅對混亂冒險**，相鄰高風險在 **Hype 側**不計為疲勞懲罰（`client_review_engine.dart:102`），而計為「冒險連段」。

**Theme 側照常扣分**（`timeline_itinerary.dart:190-193` 的 −10/對，對所有哲學一律適用）。這就是它的代價。最高熱度存在多個並列卡集與排列，不能從任一風險序列推定必有疲勞；因此不再要求「所有最高熱度行程必為低主題」，改驗只要形成冒險連段，就必須同步付出 Theme 疲勞。

理由：本卡表中高熱度素材多為高風險。混亂冒險（偏好 `#高風險 #拉車`）的最高四張基礎熱度有 3 組並列卡集，平均 riskLevel 為 3.75~4.00；REQ-A1-11 把疲勞改為比例後，它比其他哲學承受更多 Hype 疲勞風險。若不反轉 Hype 側效果，哲學身分與規則正面對撞。

> **v4 的代價條款（失去節奏互補）已刪除。** v4 只檢查最高熱度並列解之一 `[5,4,4,3]`，不足以證明所有最佳解都吃不到節奏互補。v7 不再使用這項不完整結論；Theme 側代價由 AC-A1-1.5/1.6、6.6、6.9 直接驗收。
>
> **v4 的反轉側別也是錯的。** 反轉 Theme 側等於給主題加分，會消除冒險連段應付出的 Theme 代價，與本條方向相反。
>
> 真正把兩家契合方向拆開的是標籤層：午夜探索改排斥 `#拉車`，大文字山（帶 `#拉車`）退出它的契合卡集，契合卡集重疊自 0.33 降至 **0.11**（見附錄 A.1）。該卡仍可合法入槽並承受排斥扣分。規則反轉只負責讓混亂冒險在網紅局活得下去。
>
> **本條是 MVP 階段的唯一具名例外。** 其餘哲學的規則改寫屬後續階段（見 §7）。

#### AC


- **AC-A1-6.1**　絕景係數單調遞增，**0 張時 ≥ 0.7，且任兩相鄰張數之間的增幅 ≤ 0.15**。
- **AC-A1-6.2**　固定 `hypeInfluencer`、Lv.1 裝備與同一組可達 POI：存在一個尚未採集、腰包未滿的狀態，同時可選第二張絕景或另一張非絕景素材；窮舉兩個選擇之後所有仍受 HP 與腰包限制的合法採集續局及最終合法行程，選第二張絕景所得的最高滿意度低於選非絕景素材。差異必須由 HP 消耗造成，不得以預算硬擋採集。

> 網紅端的評分完全不計 `totalCost`（`client_review_engine.dart` 的 `_evaluateHypeInfluencer`），且預算依既有 SPEC M3 可進入赤字。故本條只接受 HP 造成的採集機會成本。
- **AC-A1-6.3a**　對 `targetHype = 150` 的網紅客戶與混亂冒險以外的四種哲學，一組拉車疲勞扣除的 Hype ≥ 20。
- **AC-A1-6.3b**　對混亂冒險以外的四種哲學，建立未觸及 0／100 clamp 的網紅參考輸入，固定絕景張數、`finalTheme` 與其餘條件：新增一組疲勞造成的滿意度損失，不得小於 `totalHype` 增加 20 所造成的滿意度增益。

> 基準不得用中位熱度（實測 35）：3 槽相對 4 槽是「少一張卡的熱度，換少一組相鄰對」，若疲勞損失 ≥ 中位熱度，則凡丟掉的第 4 張低於中位（全池 32 張中有 12 張熱度 ≤ 30）時 3 槽即嚴格更優，與 AC-A1-3.6 兩側夾死無解。用最低熱度當下界已足以擋住「帶著疲勞仍然賺」。
- **AC-A1-6.4**　對兩位客戶分別窮舉當局可達素材池中的全部 `(哲學, 合法行程)`：Rejected 的最高 `earnedCoins` ≤ Near Miss 的最低 `earnedCoins` × 30%。兩個集合都不得為空。
- **AC-A1-6.5**　絕景素材中，`cost == 0 && riskLevel <= 2` 者 ≤ 1 張；且加稅後**每一種哲學仍保有至少 1 張負擔得起的絕景**（「負擔得起」定義為 `cost ≤ 社畜預算 × 25%`）。

> 有了門檻，施工就有唯一乾淨解：八坂之塔改為 `cost 500`、`riskLevel` 維持 2 —— 既滿足 ≤1 張，又保住美食朝聖唯一的可負擔絕景，也避開改 `riskLevel ≥ 3` 會讓它自帶疲勞的陷阱。

- **AC-A1-6.6**　同一份四槽行程，在混亂冒險與在其他哲學下，相鄰高風險對在 **Hype 側**的效果正負號相反；**Theme 側則一律為負**。
- **AC-A1-6.7**　混亂冒險的最佳四張熱度組合，在網紅客戶下可達 Pass 或以上。
- **AC-A1-6.8a**（全卡表）　在網紅客戶下，對五種哲學分別以**全卡表全部素材**窮舉合法 3／4 槽行程，不得先依偏好或排斥過濾；取各哲學的最佳滿意度，最高者與最低者相差 ≤ 15 分，且至少兩種哲學可達 Perfect。**這是調參的收斂目標，與選卡無關。**
- **AC-A1-6.8b**（當局可達子集）　同上條，但合法素材域改為當局可達素材池；仍不得依哲學先過濾。**這是玩家實際經歷的保證。**

> 必須分成兩條：參數在全卡表上調平之後，選出的可達子集只要少掉任一張絕景或高熱度卡，某一家就可能整組塌掉。若只有一條而範圍未定義，「調參」與「選卡」會互為前提。6.8a 單向餵給選卡；只有在找不到滿足 6.8b 的子集時才回退調參。
- **AC-A1-6.9**　對混亂冒險，以全卡表全部素材窮舉所有至少含 1 組相鄰高風險對的合法 3／4 槽行程；每份行程的 `finalTheme` 均須等於 `clamp(疲勞前 Theme − 10 × 相鄰高風險對數, 0, 100)`。冒險連段只能改變 Hype 側，不得減免、反轉或重複計入 Theme 側疲勞。
---

## 7. 不在本次範圍

- **微動作（Runner / Snapshot）**：先修數學，再做微動作 —— 在評分模型不可能失敗的前提下，微動作的三態判定驗證不出效果。
- **第 3 種以上客戶、第 6 種以上哲學、NPC、World Memory**：屬後續階段。
- **混亂冒險以外的哲學規則改寫**：REQ-A1-14 是 MVP 階段的唯一具名例外，其餘哲學在本次只調整權重，不改規則。
- **存檔事件日誌**：已另行裁決並施工完畢，不經本草案。
- **遙測**：`CROSS_CUTTING_CONSTRAINTS.md` 禁止，且禁止提前埋鉤子。Run/Retry/Fail 的行為分析應由本機事件日誌離線讀取，不上傳。

---

## 8. 待決問題

1. ~~**§1.0 詞彙表對齊的方向**~~ **已裁決**：改哲學標籤去貼合既有 32 張京都卡（成本較低）。具體方案見附錄 A。
2. **§1 正規化的具體手段**：按槽位數平均，或對總貢獻設軟上限。兩者在 3 槽提交（§3）下行為不同，須一併決定。
3. **§3 純度獎勵的量級**：須大到讓 3 槽在某客戶下勝出，又不能大到讓 4 槽永遠劣勢。
4. **§5 數值階梯的具體數字**：本草案只約束行為邊界。施工計劃須提出具體階梯並回頭驗證 AC。
5. **§6 各係數的具體數值**：同上。
6. ~~**§1.0 第三條契約無 AC 承接、實測 35% 超標**~~ **已結案**：v3 方案曾實測為 4/17（24%）；v5 將午夜探索的排斥標籤由 `#咖啡` 改為 `#拉車` 後，現方案的未使用標籤為 #咖啡、#老街、#絕景、#小憩、#浪漫，共 **5/17（29%）**，仍未違反三分之一門檻。已補 AC-A1-0.4，並把度量從標籤種類數改為卡片覆蓋率 —— 改度量的理由是它量得到真正的問題：一個標籤佔 14 張卡卻不影響任何決策時，該問的是「這些卡是否因此變成死卡」。實測為 0 張死卡，故此條通過 —— 但度量本身仍該是卡片覆蓋率，數種類會漏掉這個問法。
7. **既有測試的期望值更新**：本修訂會使多條既有 AC 的期望數字改變（例如滿意度不再恆為 100）。施工時須逐條檢視 `test/domain/core_loop/`，更新的是**期望值**而非斷言強度。
8. ~~**3 槽提交對 UI 的影響**~~ **已結案**：AC-A1-3.7/3.8 明訂 state 與 Widget 接線，並取代 `SPEC_MVP_TIMELINE_UI.md` AC-UI-1.4、AC-UI-2.5 的四槽限定；AC-A1-3.5 負責純度回饋文案。

---

## 附錄 A　詞彙表對齊方案（§1.0 的裁決結果）

**方向**：調整哲學標籤去貼合既有素材池，不改卡表。

**卡表實測**（32 張卡、17 種標籤、**5 張絕景**、10 張免費卡；風險分佈 1:17、2:8、3:4、4:2、5:1）。

### A.1 對齊後的哲學標籤

依實測窮舉搜尋所得（約束：每個偏好標籤 ≥2 張承載、排斥標籤聯集 ≥4 張、契合卡集 ≥8 張、最省四張 ≤ 預算 40%、至少 1 張絕景、兩兩契合卡集 Jaccard ≤ 0.45，並強制保留各哲學的招牌標籤）：

| 哲學 | 偏好 | 排斥 | 契合卡 | 最省 4 張 | 絕景 | 最高 4 張基礎 Hype 合計 | 該並列卡集的平均風險範圍 |
|---|---|---|---|---|---|---|---|
| 午夜探索 | #深夜 #小酌 | **#拉車** | 11 | 0 円 | 1 | 195 | 2.50 |
| 慢旅行 | #散步 #古蹟 | #高風險 | 10 | 0 円 | 2 | 215 | 1.25~1.50 |
| 美食朝聖 | #美食 #銅板美食 #早餐 | #高風險 | 11 | 750 円 | 1 | 210 | 1.75 |
| 反觀光 | #巷弄秘境 #怪談 | #大眾名店 | 16 | 0 円 | 1 | 210 | 3.00~3.50 |
| 混亂冒險 | #高風險 #拉車 | #散步 | 9 | 100 円 | 2 | 250 | 3.75~4.00 |

**兩兩契合卡集重疊**：最高為慢旅行 × 反觀光 **0.30**，午夜探索 × 混亂冒險自 v4 的 0.33 降至 **0.11**。這只度量內容提供的契合方向，不限制玩家選卡；實際最佳策略另由 AC-A1-6.8 對完整合法素材域驗收。

**驗收對照**：

- **AC-A1-0.1**（偏好標籤 ≥2 張）：最少者 #早餐 2 張 ✓
- **AC-A1-0.2**（排斥聯集 ≥4 張）：最少者 #大眾名店 4 張 ✓（午夜探索改排斥 #拉車，6 張）
- **AC-A1-0.3**（哲學標籤 100% 被涵蓋）：全部 12 個相異標籤皆存在於卡表 ✓
- **AC-A1-0.4**（僅帶未使用標籤的卡片數 ≤ 1/3）：未使用標籤為 #咖啡、#老街、#絕景、#小憩、#浪漫（5/17 = 29%），而**僅帶未使用標籤的卡為 0 張** ✓ —— 32 張卡每張都至少帶一個被使用的標籤，`#老街` 的 14 張卡全部同時帶有至少一個哲學使用的標籤，不構成死卡

### A.2 搜尋過程揭露的兩條內容事實

這兩條不是標籤配置能解的，屬卡表的內在性質，施工計劃須正面處理：

1. **飲食卡幾乎都要錢**：16 張含飲食標籤的卡中僅 1 張免費（`#早餐`，0 円 / risk 2 / hype 70），其餘自 100 円至 5,000 円。美食朝聖因此天生對預算型客戶不利 —— 這在主題上是對的（吃東西要錢），不該用標籤「修平」。

   **v7 已裁決補償路徑**：採低風險體力效率，使用所有哲學共用的風險／體力公式，不加美食朝聖專屬規則。AC-A1-5.6 直接驗 Lv.1：美食朝聖最高熱度前 6 張契合卡的風險總和為 9，應先裝滿腰包；混亂冒險對應風險總和為 21，應最遲於第 5 張耗盡 HP。多採換到的是手牌選擇權，不另加收入。
2. **高熱度卡幾乎都是高風險**：故 REQ-A1-11 把疲勞懲罰改為比例後會懲罰所有追熱度的玩家。這是 REQ-A1-14（哲學得改變規則）的由來。

### A.3 v2 三個被推翻的判斷

- v2 稱「`#絕景` 同時被美食朝聖與反觀光排斥是刻意的痛苦取捨」。**數值上不成立**：絕景係數是 ×0.5 → ×1.0（+100%），而主題的全域擺幅只有 +43%（`client_review_engine.dart:110`）—— 帶絕景永遠划算，代價可忽略，那不是取捨而是無腦題。v3 已取消絕景排斥。
- v2 稱「#老街 與 #怪談 的重疊服務 Rule 27」。單標籤重疊不是問題，**卡集重疊**才是；v2 實際造成 0.57 的同質度。
- v2 誤植絕景為 6 張，實為 5 張。

### A.4 午夜探索與混亂冒險的分離

v3/v4 的方案讓兩家共用同一對最高熱度絕景（伏見稻荷 0 円/risk 4/hype 75、大文字山 0 円/risk 5/hype 80），四張基礎熱度上限同為 250。v4 當時只記錄並列卡集之一 `[5,4,4,3]`；v7 複驗發現另有 `[5,4,4,2]`，故不再用單一並列解的節奏互補數量作結論。

v5 改在標籤層解決：**午夜探索排斥 `#拉車`**，大文字山退出它的契合卡集；該卡仍是合法素材，但會承受排斥扣分。

| | 契合卡 | 最高 4 張基礎 Hype 合計 | 風險序列 | 節奏互補 | 絕景 |
|---|---|---|---|---|---|
| 午夜探索（v4，排斥 #咖啡） | 15 | 250 | 多組並列，當時只記錄 [5,4,4,3] | 未完整窮舉 | 2 |
| 午夜探索（v5，排斥 #拉車） | 11 | 195 | [4,3,2,1] | 依排列不同 | 1 |
| 混亂冒險 | 9 | 250 | 平均風險 3.75~4.00 的 3 組並列卡集 | 依排列不同 | 2 |

契合卡集重疊自 0.33 降至 **0.11**。兩家預期形成「中熱度、有節奏互補空間、主題可高」對「高熱度、多數高風險、Theme 壓力大」；這只是內容假說，不能取代完整合法素材域上的 AC-A1-6.8。

混亂冒險多出的 55 點熱度，須由午夜探索省下的 Hype 疲勞與節奏互補的主題優勢抵銷；疲勞下界由 AC-A1-6.3a/6.3b 驗收，**AC-A1-6.8（五家滿意度極差 ≤ 15 分）是最終閘門**，量級須以窮舉決定。
