# HANDOFF — M5 快門手感原型

**最後更新**：2026-09-13 凌晨
**Worktree**：`/Users/appgongyong/share_tour/.claude/worktrees/m5-shutter-feel`
**分支**：`proto/m5-shutter-feel`（基底 `0d83843`）

> **這份交接只管原型分支。** 主線的交接是 `HANDOFF.md`（在 `feature/mvp-amendment-01` 上），它記到 M5 SPEC v4 為止，**不含本原型的任何內容**。接手時兩份都要讀。

---

## 0. 三十秒版

M5 SPEC 改到 v4、經三輪雙軌覆核仍未過，但**一行都沒實作**。原型的用途是回答唯一無法靠分析回答的問題：`decisiveMoment` 的 120 ms 完美窗是「難但可練」還是「抽獎」——那取決於裝置的輸入抖動。

**已確立**：機制可玩可學（最難難度＋絕景下完美率 37.5%），參考玩家模型被實測驗證。
**已抓到兩個規格層級的錯**（§4），都是覆核抓不到、上手就撞到的。
**仍未答**：抖動量測給過兩個矛盾讀數，儀器已修好但**還沒重跑** —— 原型的主目的目前是空的。

**下一步**：裝機 → 三檔各連續按 30 次以上 → 拿建議窗寬 → 據此改 v4 參數表（§6）。

---

## 1. 為什麼在 worktree，以及基底為什麼是 `0d83843`

主線 `feature/mvp-amendment-01` 有另一個 agent 並行施工（因果可視化 → 京都圖資 → 時段光照 → 地圖階層），使用者要求隔離開發。

**不能用 `EnterWorktree` 的預設基底。** 該工具預設 `worktree.baseRef = fresh`，會從 `origin/main` 開分支 —— 而 `origin/main` 停在 `f83e412`（M3 階段），缺 M4、因果、京都圖資、光照與全部今日 SPEC。故手動建立：

```bash
git worktree add -b proto/m5-shutter-feel .claude/worktrees/m5-shutter-feel HEAD
```

**但當時的 HEAD（`e847f04`）編譯不過**：並行 agent 提交了 `lib/main.dart` 呼叫 `switchManifest` / `switchMap`，而那兩個方法只存在於它**未提交**的檔案裡。故退到 `0d83843`（我自己最後一個 commit，含 v4 SPEC 與事件日誌 bugfix）。

`0d83843` 仍帶著兩個既有錯誤（非我引入）：

```
error   • switchManifest 未定義 • lib/main.dart:116
error   • switchMap 未定義      • lib/main.dart:120
warning • assets/audio/ 不存在  • pubspec.yaml:75
```

原型有自己的 entry point，`flutter run -t` 只編譯可達圖，所以不受影響。

> ### 一併更正主線交接裡的數字
>
> `HANDOFF.md` 與多個 commit message 記載「`flutter analyze` 0 issues、549 passed」。那些是在**主 checkout** 量的，而主 checkout 有並行 agent 的未提交檔案剛好補上缺口。**已提交狀態其實一直編譯不過**，綠燈是被未提交檔案遮住的。這是並行 agent 該收的尾。

---

## 2. 原型的範圍與跑法

```bash
# worktree 內首次要先跑（生成檔不進版控）
flutter pub get
dart run build_runner build --delete-conflicting-outputs

flutter test test/proto/          # 26 條，純領域，不需裝置
flutter analyze lib/proto test/proto

# 實機
flutter build apk --debug -t lib/proto/shutter_proto_app.dart
~/Library/Android/sdk/platform-tools/adb -s <serial> install -r build/app/outputs/flutter-apk/app-debug.apk
~/Library/Android/sdk/platform-tools/adb -s <serial> shell monkey -p com.sharetour.share_tour -c android.intent.category.LAUNCHER 1
```

**`adb` 不在 PATH 上**，完整路徑 `~/Library/Android/sdk/platform-tools/adb`。套件名是 `com.sharetour.share_tour`（不是 `com.example.*`）。

**實機**：`CPH2783` / serial `EMD6VSPZV8MFBMDI` / Android 16 (API 36)。另有 iPhone 12 但無線連不上，要線接。**模擬器量不出真實抖動**（觸控來自滑鼠事件），那一題只能用實機。

### 檔案

| 檔案 | 內容 | 之後去處 |
|---|---|---|
| `lib/proto/shutter_verdict.dart` | 三態判定、難度參數表、絕景構圖懲罰。**純 Dart 零框架相依** | 可搬進 `domain/core_loop/` |
| `lib/proto/latency_probe.dart` | 抖動統計與裁決 | 丟棄（或留 debug 用） |
| `lib/proto/shutter_proto_app.dart` | 原型 UI 與 entry point | 丟棄 |
| `test/proto/*_test.dart` | 26 條，含三組回歸測試 | 判定那半可留 |
| `PROTO_M5_FIELD_CHECK.md` | 六題實機驗證清單，含每題的「症狀 = 要改」判準 | 隨原型丟棄 |

**不接**：第二層分數、計分、文案三版本、存檔、因果面板、素材變體表。

---

## 3. 實機結果（兩次會話）

### 第一次（讀數不可用）

```
觀光客  45 次  P 8 / N 13 / F 24
抖動   σ 0.0 ms (n=0)          ← 我的 bug，見 §5.1
```

**F 24 全是逾時** —— 觀光客的普通窗無上限，依規格「只要放開就不可能失手」。這是 §4.2 的發現。
（當時逾時沒有獨立計數，這是**推斷**不是量測；現已補計數器。）

### 第二次（修可讀性後）

```
決定性瞬間＋絕景  32 次  P 12 / N 6 / F 14   每次 3.8 s
抖動  σ 12.8 ms (n=32)  建議窗寬 120 ms  裁決「可練的技巧」

攝影師            69 次  P 37 / N 11 / F 21  每次 6.4 s
抖動  σ 40.2 ms (n=69)  建議窗寬 121 ms  裁決「★ 抽獎」
```

### 怎麼讀這兩組

**手感數據可信且有用**：

| 難度 | 實測完美率 | 模型 σ=120（熟練） | 模型 σ=200（中等） |
|---|---|---|---|
| 決定性瞬間＋絕景 | **37.5%** | 38.3% | 24.1% |
| 攝影師 | **53.6%** | 68.3% | 45.4% |

兩檔都落在熟練與中等之間，相對關係正確。**`AC-M5-5.5` 依賴的參考玩家模型因此有實測支撐** —— 那三檔 σ 原本是我憑空訂的。

**抖動數據不可信**：σ 從 12.8 跳到 40.2（加樣本讓穩定的裝置特性跳三倍），而建議窗寬只從 120 變 121 —— 操作上等於沒差，紅字卻跳出來。原因見 §5.2 與 §5.3。

**節奏數據是髒的**：`每次 6.4 s` 混著探索、切難度、停頓。v4 估的 `decisiveMoment` 儀式是 2.4 s，實測 3.8 s —— 差的 1.4 秒是**人的再進入時間**（看結果、重新握好、決定再按），v4 §REQ-M5-09.1 沒算這一項。代入最壞情況 26 × 3.8 = 98.8 秒，超過 v4 的 90 秒上限。但連續空打與遊戲內（兩次取材之間本來就要走路）不可直接類比，須另量。

---

## 4. 兩個必須寫進 v5 的規格層級發現

### 4.1 `REQ-M5-03.3` 的遮擋規則過度套用，會讓功能整體失效

現行條文：

> **收縮的視覺表現不得環繞標的。** 取景框跟隨手指意味著手指壓在標的上；若收縮環也繞著標的，判定瞬間玩家的手指正好蓋住他必須盯的東西。

照做的結果是使用者第一次上手就說「**我看不懂怎麼玩**」。因為這個動作的核心是「在環與目標**吻合**的瞬間放開」，而「吻合」的意思是兩個東西重合 —— 指示一旦搬離標的，就沒有任何東西在吻合，剩下的只是一條不知道要幹嘛的進度條。我當時還同時畫了收縮框**和**下緣時間軸，兩個東西要看。

**而遮擋只存在於絕景。** 只有 `isSpotlight` 的 5 張要拖曳取景框、手指才會壓在標的上；另外 **27 張手指可以按在螢幕任何位置**，環繞標的畫沒有遮擋問題，而且是唯一能讓「吻合」看得出來的呈現。

**v5 要改成**：

- 非絕景：同心圓收縮（原型已實作 `_radiusAt`，吻合時刻恰好等於目標半徑 —— 這是整個動作唯一的視覺承諾）
- 絕景：指示畫在**取景框自己的邊框**上，框跟著手指走，指示就跟著視線走

這是「每一條規則本身都對，合起來把功能做死」的形狀，與 `CLAUDE.md` §9 記的任務 C 接線教訓同類。

### 4.2 `tourist` 的無障礙保障被逾時吃掉

規格說它的普通窗無上限、「只要放開就不可能失手」。實機 **24/45 次逾時** —— 一半以上的嘗試，在那個專門為無障礙設計的難度上失敗。

原因：畫面沒有任何時限的訊息。收縮通過目標後繼續縮到 0，但「縮完就結束」這件事從沒傳達。

原型的修法（可直接寫進 v5）：

- 通過吻合時刻之後**收縮圈轉紅**
- 放開時打出結果文字：`完美` / `普通` / `太早` / `太晚` / `逾時 —— 沒放開`
- 提示補上「（圈變紅＝已過頭，縮到 0 就逾時）」

**紅色是事後資訊**，不影響吻合前的預測，所以不會把技巧退化成「看到變色就按」的反應測驗。

**刻意不做**：進入完美窗時讓目標圈發亮 —— 那會把「預測等速收縮何時歸零」換成反應測驗，是不同且更簡單的技巧。

---

## 5. 儀器的三個 bug（皆已修，皆有回歸測試）

三個都是我的錯，記在這裡是因為它們是同一形狀的變體。

### 5.1 epoch 濾波殺掉全部樣本 → `n=0`

```dart
_latency.add((_stopwatch.elapsed - e.timeStamp).inMicroseconds / 1000.0);
...
if (millis < 0 || millis > 1000) return;   // ← 全部被丟
```

`e.timeStamp` 的 epoch 是**裝置開機時間**，`Stopwatch` 的是 App 啟動 —— 差了整個開機時長（可達數億毫秒）。濾波器用絕對值設界，於是每一筆都被當雜訊。

修法：第一筆當基準扣掉再濾波。σ 與 epoch 無關，但濾波器不是。

### 5.2 σ 被少數離群值主導 → 12.8 跳到 40.2

閒置之後的第一次按壓帶著主 isolate 從空閒喚醒的延遲，而 σ 對離群值極敏感，幾筆就能抬三倍。

修法：**MAD（中位數絕對偏差）為主統計**，σ 降為併列參考；並排除間隔 > 1500 ms 的那一筆（遊戲內取材是連續動作，不是間隔數秒的單點）。

### 5.3 裁決是硬懸崖，且與建議值自相矛盾

舊版在 `σ > 40` 二分。但判準是「窗寬 ≥ 3σ」，而 $3 \times 40 = 120$ **正好是絕對下限** —— 所以那個交界必然是「下限剛好不夠用」的點，一越過就跳紅字，實際建議值卻只差 1 ms。

修法：三段（充裕 / 臨界 / 抽獎），對照**現行窗寬**而非固定閾值；並明文寫「建議窗寬才是可行動的輸出，裁決只是摘要」。

### 還有一個量測污染沒完全解掉

讀本地時鐘發生在 Dart 處理事件時，故差值 = 真實輸入延遲 **＋ 主 isolate 排程延遲**，後者有一整個幀週期（60 Hz 下 16.7 ms）的量化誤差。已把讀時鐘移到 `_onDown` 最前面（按下那一刻動畫還沒啟動，isolate 是閒的），但**本量測仍是上界而非純輸入抖動**。

用它下「窗寬夠不夠」的結論是**偏嚴**的，這個方向安全 —— 但報告時要說清楚它量的是什麼。

---

## 6. 下一步（照順序）

1. **裝機重跑**。三檔各**連續**按 30 次以上（中間不要停超過 1.5 秒，否則會被閒置規則丟棄）。絕景另外單獨跑一輪。
2. **看新的四個數字**：`MAD / 穩健σ / 原始σ / p95`。若它們彼此分歧很大，就是還有離群值。**以「建議窗寬」為唯一可行動輸出。**
3. **據此改 v4 §REQ-M5-01.8 的參數表**，並在 §6 待決 1 寫明「已在 CPH2783 / Android 16 驗證」而非「已普遍成立」—— 一台裝置一次會話不足以推廣。
4. **驗 §4.2 的修法有效**：觀光客與攝影師各按 15~20 次，看逾時計數還會不會長。
5. **另量節奏**：連續空打不能類比遊戲內。要嘛接上大世界走路，要嘛明文承認 v4 的 90 秒預算只涵蓋儀式不涵蓋人的再進入時間。
6. **把 §4 的兩條寫進 SPEC v5**，然後才送第四輪覆核。

### 收尾時要做的事

原型是**丟棄式**的。`shutter_verdict.dart` 與它的測試可以搬進 `domain/core_loop/`（已經零框架相依），其餘刪除。

worktree 的清理：

```bash
git worktree remove .claude/worktrees/m5-shutter-feel   # 確認不再需要之後
```

分支 `proto/m5-shutter-feel` 建議保留一段時間 —— 它是 §3 那些數據的唯一來源。

---

## 7. 這輪學到的流程教訓

**兩個規格層級的發現（§4），六個覆核 agent 花了數小時都沒抓到；使用者上手 40 分鐘撞到兩個。**

它們都不是靠推論能發現的 —— 一個要真的看著畫面不知道要幹嘛，一個要真的按住不放。

合理的做法是**第一輪覆核之後就先做原型**，而不是把同一份 spec 改到第四版、派六個覆核 agent。手感本來就只能實機驗，而「看不懂怎麼玩」這種問題，一個能跑的原型半小時就會撞到。
