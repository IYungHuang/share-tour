# SPEC — 任務 C：即時 LBS GPS 追蹤與平滑位移

狀態：**待覆核 (v6)** — 已併入 `SPEC_C_AMENDMENT_01.md`（實地測試修訂）全部內容，含使用者 2026-09-07 的三項裁決
流程位置：`spec → 覆核 → plan → 覆核 → 執行計劃 → 覆核`
上位文件：`CROSS_CUTTING_CONSTRAINTS.md`（牴觸時以其為準）

---

## 1. 目的與範圍

### 1.1 目的
把裝置的真實 GPS 位置，轉換成地圖上一個**穩定、平滑、可信**的小人位置；並在權限缺失、精度劣化、定位服務被關閉或圖資未涵蓋時，仍讓遊戲維持可玩。

### 1.2 本 SPEC 涵蓋
權限與精度等級的取得與降級；位置串流的取得、節流、品質過濾、**品質標記**與靜止判定；範圍檢查、投影與**地理範圍分類遮罩查詢**（委派圖資模組）；平滑位移與相機跟隨；移動模式切換與位移的來源標記、分桶距離；**追蹤期間的裝置喚醒抑制**；**觸發適用性標記與幾何約束的量測資料**（供任務 A 使用）；**執行期更換投影層時的正確性**；可觀測狀態與診斷。

### 1.3 本 SPEC 不涵蓋

| 項目 | 歸屬 |
|---|---|
| 遭遇觸發判定、Mini-game | 任務 A |
| 成果規則：御朱印、圖鑑造訪狀態、獎勵限縮 | 任務 A／B |
| HP／YEN／SAN 的數值計算與衰減曲線 | 任務 A |
| 存檔格式與事件日誌結構 | 任務 B（約束見上位文件） |
| 防誤觸判定（原 v2 REQ-C-17） | 移出本 SPEC，改由 `keepAwakeActive`（REQ-C-16 規則 6）供畫面層／任務 A 自行判斷 |
| 里程數字的遊戲機制用途、成果載體（POI 打卡、探索網格計數） | 任務 A／B（2026-09-07 裁決：里程降級為顯示數字，見 §6 Q23） |
| **地方層地圖的資產、設計與切換時機** | **任務 D（新設）**。本 SPEC 只負責「換層時不出錯」 |
| 背景定位、A* 路徑導航、多城市圖資下載 | 第二階段 |
| 衛星健康度／訊號強度偵測 | **平台無證據來源，已刪除**（見 §6.1）。精度劣化改由 `acquisition` 維度表達 |
| 定位偽造的處置政策 | 未定；本 SPEC 僅要求標記誠實 |

> **邊界紀律**：本 SPEC 產出「可信的位置、來源標記與分桶距離」。任何「這樣會得到什麼獎勵」的規則都不屬於這裡。

### 1.4 名詞定義

| 詞 | 定義 |
|---|---|
| **Fix** | 一筆定位讀數（經緯度、精度、時戳、速度，及各欄位的**已量測旗標**） |
| **投影層 (Layer)** | 目前作用中的圖資模組。大地圖與地方層是不同的投影層 |
| **目標點 / 顯示點** | 經管線處理後小人「應該在」的像素座標 / 實際繪製的像素座標 |
| **來源模式 (sourceMode)** | 一筆位移的產生方式：`gps` 或 `virtual`。不可竄改 |
| **顯著位移** | 通過 §3.0 管線第 5 步的移動，才算「真的動了」 |
| **牆鐘 / 單調時間** | 見上位文件 CC-2 |
| **品質合格 Fix** | 精度 ≤ 30 公尺的 Fix（REQ-C-13 規則 16）；虛擬 Fix 一律合格 |
| **`keepAwakeActive`** | 追蹤中且螢幕被強制常亮的狀態（REQ-C-16） |
| **`positionErrorBound`** | 觸發判定用的位置誤差安全裕度，必填注入、無預設值（REQ-C-18） |

---

## 2. 前提與相依

### 2.1 阻斷項

| ID | 項目 | 現況 |
|---|---|---|
| PRE-1 | 根層具備 Riverpod 作用域 | **缺** |
| PRE-2 | 圖資模組提供反投影 | **缺** |
| PRE-3 | 道路吸附歸屬圖資模組 | **已作廢**（`SPEC_C_AMENDMENT_01.md` 修訂四：道路吸附整項刪除，此前提不再適用） |
| PRE-4 | 圖資模組提供有效地理範圍判定 | **缺** |
| PRE-5 | 圖資模組由外部注入 | **違反** |
| PRE-6 | `freezed` / `json_serializable` / `build_runner` | **已完成**（`d02b6e5`） |
| PRE-7 | 玩家降落點由圖資模組提供 | **違反**，硬編碼於通用引擎 |
| PRE-8 | 吸附語意升級為「最近路段」；**道路節點與 POI 的最小間距須大於（觸發半徑 + 吸附上限）** | **已作廢**（修訂四：道路吸附與道路節點列舉整項刪除，原本要修的洞改由 §2.2 規則 5 的分類遮罩守） |
| PRE-9 | `pubspec.yaml` 宣告 `vector_math` 為**直接相依** | **缺**。目前僅為 flame 的傳遞相依，自 `lib/` 直接 import 會觸發 `depend_on_referenced_packages`，違反 DoD-5 |
| PRE-10 | **POI 觸發半徑改以公尺定義** | **違反**。現為 35 像素；於大地圖（370.4 公尺/像素）等於 **12.96 公里**，站在新竹即觸發台北 101 |
| PRE-11 | 圖資模組不得暴露 `dart:ui` 型別（海洋顏色改為 ARGB `int`） | **違反** |

### 2.2 圖資模組契約

**單一圖資模組介面**，不做繼承拆分（見 §6.1）。須提供下列**能力**（確切的成員名稱與簽章屬 plan）：

| 能力 | 用途 |
|---|---|
| 投影：經緯度 → 像素 | 決定目標點 |
| 反投影：像素 → 經緯度 | 除錯點擊尋路 |
| 地理範圍判定（**分類遮罩查詢**） | 出界判定（REQ-C-05）；遮罩為模組資產，解析度由模組宣告 |
| 預設降落點 | 冷啟動起點（REQ-C-02） |
| 查詢任一像素處的**公尺/像素比例** | 把公尺門檻換算為像素 |
| **宣告本圖層的方向鍵移動速度** | REQ-C-10 規則 8。以像素/秒或移動倍率表達 |
| 列舉 **POI 節點** | REQ-C-18 的幾何約束驗證 |
| 底圖資產識別、地圖尺寸、海洋顏色 | 渲染 |

規則：
1. 介面**不得出現 Flutter 或 `dart:ui` 型別**；顏色以 ARGB `int` 表示。
2. 經緯度一律以 `double` 傳遞，**禁止裝入 `Vector2`**（其儲存為 Float32，台灣經度量級的量化間隔約 0.77 公尺）。像素座標可用 `Vector2`（13 萬像素處量化間隔 0.016 像素，遠小於抵達門檻）。
3. 向量型別統一為 `package:vector_math/vector_math.dart`，**不得使用 `vector_math_64.dart`**（兩者型別不相容，flame 全庫使用前者）。
4. 地理範圍判定以模組宣告的**分類遮罩**實作；方向鍵驅動的虛擬 Fix 與真實 GPS 的 Fix **共用同一條判定路徑，不得分歧**。
   > 吸附刪除後（修訂四），`containsGeo` 是唯一還在守「讓玩家待在合理位置」這條線的機制：吸附是把玩家拉到某個點，錯誤不可回復；遮罩只判斷位置合不合法，不改動座標，只標記 `coverage`。
5. 地理範圍的形狀由模組自決，邊界含入。
6. 公尺/像素比例在非線性地圖上隨位置變化，故查詢須帶像素位置。

**不再持有的能力（修訂四，v6 刪除）**：道路吸附（`snapToRoad`）、道路節點列舉（`roadNodes`）。介面不得保留設為 0 或空的殘留成員（AC-4.7）。將來若引入路網比對，新增的是「有序、含連通性的路段折線 + 弧長投影」能力，**不得復用 `snapToRoad` 或 `roadNodes` 之名**。

**解耦驗收**：必須能提供純數學、不含圖檔的 `FakeMapManifest`，並用它跑完本 SPEC 的**全部**邏輯測試。

---

## 3. 功能需求

> `[P0]` 第一版必須；`[P1]` 應該。**P0 需求不得依賴 P1 需求**。
> **門檻比較語意統一**：門檻值本身視為通過／合格。**唯一例外**：REQ-C-03 規則 7 的顯著性閘門以 `<=` 判為「未顯著移動」，即門檻值等於「未達標」，該處已明文標示。
> **閱讀順序**：REQ-C-14 定義了全文引用的狀態維度，故置於最前；其餘依編號。
> **空號**：REQ-C-09 已整條刪除（行為併入 REQ-C-01 規則 7 與 REQ-C-14 規則 4）。編號不重用，以免與前幾版的審核紀錄混淆。

---

### 3.0 處理管線（規範性）`[P0]`

本節是**行為契約**，不是實作設計。各需求描述片段行為，其**全域順序**由此釘死。順序改變會改變結果，故必須明訂。

```
 1. 接收原始 Fix
 2. 精度等級評估        （REQ-C-01，對原始串流評估）
 3. 已量測旗標檢查      （REQ-C-03 規則 1）      ┐
 4. 品質閘門            （REQ-C-03 規則 2~5）    ├ sourceMode == virtual 時整段跳過
 5. 連續丟棄保護        （REQ-C-03 規則 6）      ┘
 6. 品質標記判定        （REQ-C-13 規則 16、17。虛擬 Fix 一律合格）
 7. 顯著位移閘門        （REQ-C-03 規則 7。不合格者不參與，且不更新基準）
 8. 地理範圍檢查        （REQ-C-05）
 9. 投影                （REQ-C-04）
10. 大跨距判定          （REQ-C-07。不合格者不更新基準、不消耗不連續標記）
11. 更新目標點
12. 分桶距離累計        （REQ-C-13。用第 8 步之前的地理距離）
13. 平滑（每幀，與上列串流事件非同步）（REQ-C-06）
```

> 修訂四（v6）：道路吸附步驟已整段刪除。修訂二（v6）：新增第 6 步「品質標記判定」，不合格 Fix 仍更新顯示點與 `coverage`，但不推進 `motion`、不成為顯著性閘門與大跨距判定的比較基準、不消耗不連續標記。

**AC**
- AC-0.1 範圍外的 Fix 不進入第 9 步（以 fake 的呼叫計數斷言投影未被呼叫）。
- AC-0.2 精度等級評估在品質過濾之前執行（注入恆為 2000 m 的來源，其精度等級仍可被判定，而非因全數丟棄而無從判定）。
- AC-0.3 判定為未顯著移動的 Fix 不進入第 8 步之後的任何步驟。
- AC-0.4 `sourceMode == virtual` 的 Fix 跳過第 3~5 步，直接進入第 6 步（品質標記判定，一律標記為合格）。

---

### REQ-C-14 `[P0]` 可觀測狀態與診斷

**敘述**：定位狀態不是單一互斥列舉。把它們壓成一個 enum 必然產生無法表達的合法狀態。診斷與狀態合併為一條需求（見 §6.1 第 6 點）。

**規則**
1. 可觀測狀態由**五個獨立維度**組成，任一維度變化不得覆寫其他維度：

| 維度 | 值 | 定義於 | 優先級 |
|---|---|---|---|
| `permission` | `ready` / `serviceDisabled` / `denied` / `deniedForever` / `approximate` / `unavailable` | REQ-C-01 | P0 |
| `mode` | `gps` / `virtual` | REQ-C-13 | P0 |
| `coverage` | `inside` / `outside` | REQ-C-05 | 維度 P0，行為 P1 |
| `acquisition` | `acquiring` / `acquired` | REQ-C-02 | P0 |

   **初始值**：`permission = unavailable`、`mode = gps`、`coverage = inside`、`acquisition = acquiring`、`motion = still`。
| `motion` | `still` / `moving` | REQ-C-03 | P0 |

2. `motion` 由**逾時**判定，非由 Fix 到達判定：距上一次顯著位移超過 **30 秒**（單調時鐘）→ `still`；出現顯著位移即回 `moving`。
   > 平台距離門檻的語意是「沒移動就不推送」。若以 Fix 到達判定，靜坐的玩家零筆 Fix，`motion` 永遠停在 `moving`——而單元測試會餵 Fix 進去因此綠燈，真機必然失敗。
3. HUD 顯示優先序（高至低）：`permission` 異常 → `mode == virtual` → `coverage == outside` → `acquisition == acquiring` → 正常。同時成立時只顯示最高者。
4. **`acquisition` 為雙向**：距上一次「被接受的 Fix」超過 **45 秒** → 回到 `acquiring`。
   > 這是隧道、地下街、室內的表達方式。平台在該情境下常仍有推送，但精度劣化至被 REQ-C-03 丟棄——`accuracy` 是平台實測值，屬**真實證據**，與已刪除的自造心跳性質不同。不補這條，玩家在地下街走十分鐘小人不動，而 HUD 依優先序顯示「正常」，得到的訊息是遊戲壞了。
   > 複用既有維度而非新增機制：`acquiring` 的語意本就是「還沒有可信位置」，暫時失去與尚未取得是同一件事。
4. 所有狀態與診斷皆為不可變值物件。
5. 對外提供唯讀診斷快照，含五維度狀態，加上計數類欄位：
   `activeSubscriptionCount`、`powerMode`（`active`/`suspended`，定義於 REQ-C-11）、`acceptedFixCount`、`rejectedFixCount`、`rejectionsByReason`、`currentAccuracyMeters`、`realDistanceMeters`、`virtualDistanceMeters`、`secondsSinceLastSignificantMove`、**`accuracyGatedFixCount`**（REQ-C-13 規則 16，修訂二）、**`keepAwakeActive`**（REQ-C-16 規則 6，修訂一）。
6. 診斷資料**不得含原始經緯度**。

**AC**
- AC-14.1 `permission = ready`、`coverage = outside`、`mode = virtual` 可同時成立且互不影響。
- AC-14.2 `mode` 切換不改變 `permission`。
- AC-14.3 給定五維度的任一組合，HUD 顯示項目依優先序唯一決定。
- AC-14.4 **不推送任何 Fix**，經過 30 秒（單調時鐘）→ `motion = still`。
- AC-14.5 出現一次顯著位移 → `motion = moving`。
- AC-14.6 丟棄一筆低精度 Fix → `rejectedFixCount` +1 且 `rejectionsByReason['accuracy']` +1。
- AC-14.7 診斷快照序列化結果不含 `lat`／`lng` 鍵。
- AC-14.8 取得首筆被接受的 Fix → `acquisition = acquired`；其後 45 秒無任何被接受的 Fix → 回到 `acquiring`。
- AC-14.9 連續推送精度 300 m 的 Fix（全被丟棄）達 45 秒 → `acquisition = acquiring`，且 `permission` 與 `mode` 皆不變。
- AC-14.10 未經任何操作的初始狀態符合規則 1 所列的初始值。
- AC-14.11（新增，修訂二）精度不合格但仍被接受管線處理的 Fix → 診斷快照的 `accuracyGatedFixCount` 遞增，`rejectedFixCount` 不變（兩者為不同層級：後者是規則 1~5 的丟棄，前者是規則 16 的品質標記不合格）。

---

### REQ-C-01 `[P0]` 權限、服務與精度等級

**規則**
1. 檢查順序固定：**系統定位服務總開關 → 權限狀態 → （必要時）請求權限**。
2. 服務未開啟時不得發出權限請求，應引導開啟系統設定。
3. `deniedForever` 時提供設定入口，**並自動切換為方向鍵模式**。
4. **精度等級以平台的權威查詢為主判定**（`LocationAccuracyStatus` 的 `reduced` / `precise`）。
   輔助啟發式：連續 3 筆原始 Fix 精度**超過 500 公尺**。啟發式在**品質過濾之前**對原始串流評估（§3.0 第 2 步）。
5. `reduced` → `permission = approximate`：提示開啟精確位置並提供設定入口，期間視同定位不可用並自動切換為方向鍵模式。
   （iOS 的「暫時性精確度提升」請求延後至第二版：無真機無法驗證，違反 NFR-1。）
6. **`approximate` 的恢復路徑不得依賴 GPS 訂閱**。回到前景時以平台的精度等級查詢重新判定（不需訂閱、不耗電）。
   > 概略位置下權限查詢回報的就是「已授權」，若僅靠權限查詢輪詢，使用者去設定打開精確位置後將永遠無法脫離此狀態。
7. **服務狀態變化須被訂閱**（涵蓋原 REQ-C-09 的全部行為）：
   - 前景中服務被關閉 → 立即反映為 `serviceDisabled`，而非讓串流無聲死掉。
   - 服務恢復 → 重新走本需求的檢查流程，狀態依結果更新。
   - 恢復後首筆造成的位置不連續，依 REQ-C-02 規則 3 處理。
   > 平台**只**提供服務開關這一項證據，不提供衛星健康度或訊號強度。「有推送但品質不佳」屬 REQ-C-03，並由 `acquisition` 維度呈現給玩家。
8. 並發的權限請求需去重。

**AC**
- AC-1.1 服務關閉 → `serviceDisabled`，且未曾呼叫權限請求器。
- AC-1.2 權限未決 → 權限請求器恰好被呼叫一次；允許後 → `ready`。
- AC-1.3 `deniedForever` → `mode` 自動變為 `virtual`。
- AC-1.4 並發呼叫權限流程 3 次 → 權限請求器呼叫次數為 1。
- AC-1.5 平台精度等級回報 `reduced` → `permission = approximate`（不需等待任何 Fix）。
- AC-1.6 `approximate` 狀態下**GPS 訂閱已取消**，回到前景且平台回報 `precise` → `permission` 回到 `ready`（證明恢復路徑不依賴訂閱）。
- AC-1.7 app 前景中服務狀態變為關閉 → `permission = serviceDisabled`。
- AC-1.8 服務由關閉恢復為開啟 → 重新檢查權限，狀態依檢查結果更新為 `ready` 或對應的拒絕狀態。

---

### REQ-C-02 `[P0]` 位置串流與節流

**規則**
1. 位置來源為可替換的抽象；遊戲邏輯不得相依特定定位套件的型別。
2. Fix 欄位至少含：緯度、經度、水平精度、時戳、速度、速度精度、是否為模擬定位，**以及「精度是否確實量測」與「速度是否確實量測」兩個旗標**。
   > 平台在無法量測時以 `0.0` 作為佔位值。未檢查旗標而直接過濾，等於把「未量測」當成「零誤差」與「靜止」，會同時關閉精度閘門與速度閘門。
3. **冷啟動一律由圖資模組的預設降落點起步**，不使用平台的「最後已知位置」。
   冷啟動首筆被接受的 Fix **走獨立路徑**：重新投影後直接指定顯示點，**不經 REQ-C-07 的大跨距判定、不發傳送事件、不累計距離**。
   > 「最後已知位置」的唯一收益是開場少一秒空白，代價是要在整條管線傳遞一條「不得成為基準」的特例規則，且第一筆真實 Fix 的推算速度會是「三天前的東京 → 現在的台北 / 數秒」而被速度規則丟棄，接著要到第 6 筆才被連續丟棄保護救回，吃掉五筆合法 Fix。收益不值這個代價。
4. `acquisition` 的取值見 REQ-C-14 規則 4（雙向）。TTFF 冷啟在市區常需 5~15 秒，故初始為 `acquiring`。
5. 平台層距離門檻 **10 公尺**；Android 另設平台層更新間隔 **1 秒**（`iOS` 的 `CLLocationManager` 無此參數，不適用）。
6. 應用層節流語意為 **throttleLatest（leading + trailing）**，窗長 **1 秒**。
7. 生命週期：進入背景**超過 20 秒**才取消訂閱；恢復訂閱需 **2 秒** debounce。

**AC**
- AC-2.1 以 2 秒間隔推送 5 筆 → 下游收到 5 筆，順序內容一致。
- AC-2.2 1 秒內推送 10 筆 → 下游收到 2 筆（leading 與窗尾 trailing）。
- AC-2.3 背景 5 秒後返回 → 訂閱未曾取消。
- AC-2.4 背景 30 秒後返回 → 訂閱曾取消，恢復發生於 debounce 之後，訂閱數為 1。
- AC-2.5 dispose 後 `activeSubscriptionCount == 0`。
- AC-2.6 冷啟動時顯示點等於圖資模組的預設降落點，且未曾查詢平台的最後已知位置。
- AC-2.7 冷啟動首筆被接受的 Fix 即使距預設降落點數百公里 → **不發出傳送事件**，顯示點直接指定為新目標點，兩個距離桶皆不變。

---

### REQ-C-03 `[P0]` 品質過濾與靜止判定

**規則**（純函式）
1. **已量測旗標**：精度未量測者一律丟棄。速度未量測者不參與規則 5 的比較。
2. **精度閘門**：精度**超過 100 公尺**丟棄。
3. **時戳單調性**：時戳早於或等於前一筆被接受者，丟棄。
4. **時鐘異常**：時戳與前一筆相差**超過 1 天**，視為時鐘被調整或 NTP 回跳，**重置基準而非丟棄**。
5. **速度合理性**：推算速度**超過 350 km/h** 丟棄。**推算速度僅用兩點差分**；不再與裝置回報 speed 比較或取較小值（修訂五，v6）。
   > 都卜勒解算與位置解算共用同一組衛星幾何，裝置回報速度不是獨立證據源（實測：手機靜止時裝置回報速度中位數 **1.85 m/s**、53% 超過 1 m/s、最大 **14.08 m/s**，`hasSpeed` 全程為真）。取小值只會讓速度閘門更難丟棄——一個亂報低速的裝置可以整條關掉 350 km/h 閘門。`hasSpeed` / `hasSpeedAccuracy` 欄位保留於 `GeoFix`（診斷用），管線不再消費。
6. **連續丟棄保護**：連續丟棄**計數達 5** 後，**第 6 筆無條件接受並重置基準**。強制接受**僅豁免規則 5**；規則 1、2、3 仍生效。
7. **位移顯著性閘門**：
   `distance(基準, 目前) <= 0.75 × (基準精度 + 目前精度)` → 未顯著移動：不更新目標點、不累計距離。
   **基準凍結語意（規範性）**：判定為未顯著移動時，**比較基準（座標、精度、時戳）維持不變**，直到出現一次顯著位移才整體更新。
   > 若基準隨每筆前移：都市步行精度約 20 公尺 → 門檻 30 公尺，而平台每 10 公尺推一筆 → 每筆位移都小於門檻 → **永遠靜止、里程永遠為 0**。塞車、散步、逛街全部歸零。基準凍結才是正確語意。
8. 被丟棄或判定靜止者不影響顯示點，但計入診斷。
9. 首筆 Fix（無基準）只受規則 1、2、3 約束。

**AC**
- AC-3.1 精度 150 m → 丟棄；20 m → 接受；恰為 100 m → 接受。
- AC-3.2 精度未量測（旗標為否、值為 0.0）→ **丟棄**（不得視為零誤差）。
- AC-3.3 兩筆相距 1 公里、間隔 1 秒 → 第二筆丟棄。
- AC-3.4 時戳回捲 → 丟棄。
- AC-3.5 連續 5 筆被速度規則丟棄 → 第 6 筆接受，基準重置。
- AC-3.6 強制接受的第 6 筆若精度 200 m → 仍然丟棄。
- AC-3.7 時戳較前一筆早 2 天 → 基準重置且該筆被接受。
- AC-3.8 兩筆精度皆 20 m、相距 15 m → 不更新目標點，累計距離 0，`motion` 不變為 `moving`。
- AC-3.9 連續 5 筆沿直線、位置為 0 / 12 / 24 / 36 / 48 公尺、精度皆 20 m、時戳各間隔 12 s、速度未量測 → **恰有第 4 筆**判定為顯著位移（第 2、3 筆距基準 12 m、24 m 未達門檻 30 m；第 5 筆距新基準 12 m 未達門檻），累計 **36 m**。（修訂八，v6 改寫，修正 v5.1 的內部矛盾）
  > 此條是唯一能區分「基準凍結」與「基準前移」的驗收條件：凍結得 36 m，前移得 0 m。措辭寫「恰有第 4 筆」而非「自第 4 筆起」。

---

### REQ-C-04 `[P0]` 範圍檢查與投影

**規則**
1. 順序見 §3.0（範圍檢查 → 投影）。範圍檢查必須在投影之前：IDW 對範圍外輸入不報錯，只回傳落在錨點凸包內、看似合理但完全錯誤的點。
2. 兩者一律委派注入的圖資模組，通用引擎不得內含特定城市邏輯。

**道路吸附已於 v6 整項刪除（修訂四）**：`snapToRoad`、`snapLimitMeters`、`roadNodes` 不再是本 SPEC 的一部分，§2.2 不再持有此能力。理由：吸附的實際門檻換算為像素僅 0.135 px，是死碼；且「吸附到離散節點」這個形式本身沒有正確用法——節點密度低於 GPS 精度時會把玩家傳送到數百公尺外，高於時等於沒作用，而它的輸出（像素座標）是唯一被持久化的位置資料，錯誤不可回復。刪除後留下的邊界責任由 §2.2 規則 4 的分類遮罩承接（見 REQ-C-05）。

**AC**
- AC-4.1 給定 `FakeMapManifest` 與已知經緯度，目標點等於「範圍檢查通過後投影」的結果。
- AC-4.2 更換另一個 `FakeMapManifest`，相同經緯度產出不同目標點。
- AC-4.4 範圍外輸入 → 投影未被呼叫（同 AC-0.1）。
- AC-4.5 通用引擎的邏輯層不得 import 任何具名城市模組（靜態檢查；目錄邊界於 plan 定義）。
- AC-4.7（新增，修訂四）圖資介面不存在吸附與道路節點相關成員（靜態檢查）。
- AC-4.8（新增，修訂四）以 `FakeMapManifest` 提供一份標記部分區域為範圍外的遮罩：位於範圍外的座標 → `containsGeo` 為假、投影未被呼叫（同 AC-0.1）；方向鍵驅動的虛擬 Fix 與真實 Fix 在同一座標上得到相同判定。

> AC-4.3（距所有道路超過 50 公尺不吸附）、原 AC-4.6（PRE-8 幾何驗證）已刪除；後者的驗證邏輯改寫為 REQ-C-18 的 AC-18.3。

---

### REQ-C-05 `[P0/P1]` 圖資範圍外處理

**規則**
1. 範圍外的 Fix 不更新目標點，`coverage = outside`，不累計任何距離。
2. 暫停依位置驅動的遊戲判定。
3. **不自動切換移動模式**，僅提示可手動切換。
   > 自動切換會使本需求永不生效，且等同「人在國外即自動獲得全圖通行權」。
4. 回到範圍內自動恢復。
5. **分級**：規則 1（不投影、不更新目標點、不累計、標記 `coverage`）為 **P0**，因 §3.0 管線與 AC-0.1／AC-4.4 依賴它；規則 2、3（暫停遊戲判定、手動切換提示）為 **P1**。

**AC**
- AC-5.1 範圍外 → 目標點維持前值，`coverage = outside`，累計距離不變。
- AC-5.2 回到範圍內 → `coverage = inside`，目標點更新。
- AC-5.3 邊界上的點視為範圍內。
- AC-5.4 進入範圍外 → `mode` 維持不變。

---

### REQ-C-06 `[P0]` 平滑位移

**規則**
1. 顯示點與目標點為兩個獨立變數；GPS 只寫目標點。
2. **兩者不得共用同一個向量實例。**
   > 本條約束的是本 SPEC 邏輯層自有的裸 `Vector2`：`_rendered = _target` 在 Dart 是參考指派，之後改目標點會同時改掉顯示點，平滑靜默失效，而「顯示點趨近目標點」的測試照樣通過。
   > 渲染層另需注意：所用遊戲引擎的座標 getter 在不同元件上語意相反（一者回傳活參考、一者回傳副本），兩種失效模式都不會報錯。細節屬 plan。
3. 平滑必須與影格率無關。
4. 半衰時間 **1.0 秒**。
5. 抵達門檻**以公尺定義**（**2 公尺**），換算為像素。
   > 原以 1 像素定義：在大地圖（370.4 公尺/像素）等於 370 公尺——玩家在 370 公尺內移動會被判定為「已抵達」而完全不動。
6. 朝向僅在單幀位移超過**朝向門檻（5 公尺，換算為像素）**時更新，避免原地抖動亂轉。
7. **公尺→像素換算的求值契約**：以**當前顯示點**查詢公尺/像素比例，於**每次目標點更新時重算一次**，並沿用至下次目標點更新。
   > 非線性地圖上此比例逐點變化。不釘死求值點與重算時機，兩個工程師會寫出行為不同但各自「正確」的實作，且每幀重算會讓 NFR-4 的常數時間取決於投影演算法的成本。

**AC**
- AC-6.1 設定目標點後推進固定模擬時間，距離單調遞減。
- AC-6.2 **初始距離 100 像素**，以 dt=1/30 推進 30 次與 dt=1/120 推進 120 次（同為 1 秒），最終差距 **< 初始距離的 1%**。
- AC-6.3 **初始距離 100 像素**，經過 1 個半衰期後剩餘距離為 **50% ± 5%**。
- AC-6.4 已抵達後續更新不改變顯示點。
- AC-6.5 （併入 AC-6.9）
- AC-6.6 修改目標點向量後，顯示點的值不隨之改變（無別名）。
- AC-6.7 在公尺/像素比例為 1.0 與 370.4 的兩個模組下，抵達門檻對應的像素值分別為 2 與約 0.0054。
- AC-6.8 使用「比例隨位置變化」的 `FakeMapManifest`：目標點更新後，換算值依**當時顯示點**的比例重算；同一次更新期間比例不再變動。
- AC-6.9 位移小於朝向門檻（5 公尺換算後的像素）→ 朝向不變；超過 → 朝向更新。

---

### REQ-C-07 `[P0]` 大跨距位移處理

**規則**
1. 判準用**公尺與速度**：位移**超過 2 公里** 或 推算速度**超過 120 km/h**。
2. 觸發時不平滑，改發傳送事件，由畫面層播放過場後指定顯示點。
3. 事件酬載：**事件 UUID、單調序號**（上位文件 CC-3 規則 2 要求）、起點、終點、跨距（公尺與像素）、推算速度、UTC 時戳、成因、`sourceMode`。
4. **成因為二元**：
   - `continuousTracking`：訂閱從未中斷、時間連續、由真實移動造成。
   - `discontinuity`：其餘一切（背景恢復、服務恢復、切換移動模式、範圍恢復、除錯跳點）。
   細分原因為**保證非空、列舉固定**的附註欄位，供任務 A 日後區分用。
   > 不設為「可為空、不影響行為」：任務 A 很可能需要區分「切換模式」與「背景恢復」，若明文禁止依賴，屆時要回頭改 C。
   > 冷啟動首筆與換層**不在此列**：兩者各走獨立路徑（REQ-C-02 規則 3、REQ-C-15 規則 2），不發傳送事件。
   > 原五分法：從 virtual 切回 gps 時，`modeSwitch`、`resumeFromBackground`、`signalRecovered` 可同時成立而無優先序；且冷啟動與範圍恢復兩條實際路徑無任何成因適用。行為本來就只有二元，五分法沒買到行為差異卻買進重疊與遺漏。
5. **本 SPEC 不建議任何遊戲後果，只發事件。** 後果由任務 A 依**酬載**（跨距、速度、`sourceMode`、附註原因）決定。
   > 不寫「依成因決定」：二元成因的兩側目前都不該被懲罰（`continuousTracking` 是真實高速移動，`discontinuity` 的每條路徑也都不是玩家的錯），成因本身不提供可據以分歧的資訊。
   > 前一版寫「僅 `continuousTracking` 建議觸發後果」是錯的：高鐵 300 km/h 應被接受（低於 350 km/h 丟棄門檻）卻高於 120 km/h 傳送門檻，於是在一款以台灣為地圖的旅行遊戲裡，**搭高鐵等於連續被判定作弊**。
6. **不連續事件（`discontinuity`，含 `backgroundResume` 等附註原因）須同時提供給畫面層**，不得僅餵給任務 A（修訂六，v6）。
   > 實測：玩家鎖屏 13.6 分鐘、實際走了 773 公尺，回來後里程未動而 HUD 顯示正常——與 REQ-C-14 規則 4 當初要修的缺陷同構，且更嚴重：玩家會誤以為系統壞了。

**AC**
- AC-7.1 位移 5 公里 → 發出傳送事件，顯示點等於目標點。
- AC-7.2 位移 300 公尺、速度 5 km/h → 不發事件。
- AC-7.3 事件的跨距（公尺）與推算速度與輸入一致，且含 UUID 與單調序號。
- AC-7.4 背景 30 秒後恢復造成的大跨距 → 成因 `discontinuity`，附註原因為非空的固定列舉值。
- AC-7.5 連續追蹤中以 200 km/h 移動 → 成因 `continuousTracking`。
- AC-7.6 冷啟動首筆 Fix → **不發出傳送事件**（走 REQ-C-02 規則 3 的獨立路徑）。
- AC-7.7 本 SPEC 的邏輯層不得 import 遊戲數值模組（架構測試）。
- AC-7.8（新增，修訂六）`discontinuity` 事件（含 `backgroundResume`）同時可被畫面層訂閱，不僅限任務 A 的訂閱端。

---

### REQ-C-08 `[P1]` 相機跟隨

**規則**
1. 實作為**純邏輯狀態機**：狀態 `following` / `free` / `returning`；輸入 `(顯示點, 手勢事件, dt, zoom, viewportSize, mapSize)`；輸出目標相機中心。邊界限制為純函式。
2. 預設 `following`；手勢平移 → `free`；`free` 下無操作達 **3 秒** → `returning` → `following`。
3. 提供「回到我的位置」立即回到 `following`。
4. 相機中心不得越出地圖邊界（含地圖小於視口的退化情形）。
5. **縮放不算操作**，不重置回歸計時器，不改變狀態。

**AC**
- AC-8.1 手勢平移 → `free`。
- AC-8.2 停止操作滿 3 秒 → `returning`，完成後 `following`。
- AC-8.3 `free` 下顯示點移動 → 輸出相機中心不變。
- AC-8.4 任意狀態與 zoom 下，相機中心滿足邊界限制。
- AC-8.5 `free` 期間僅縮放 → 回歸計時器不重置。

---

### REQ-C-10 `[P0]` 虛擬定位來源

**規則**
1. 與真實來源實作同一抽象，下游不知差異。
2. **正式建置可用**：方向鍵移動，依方向與速度連續產生 Fix。
3. **僅除錯建置可用**：點擊尋路（反投影注入）、直接跳點。
4. 虛擬來源**繞過平台層距離門檻與應用層節流**，以 **15 Hz** 產生 Fix。
5. **虛擬 Fix 旁路整個品質閘門**（§3.0 第 3~5 步），直接進入顯著位移閘門。其精度欄位為固定小值且已量測旗標為真。
   > 品質閘門過濾的是**量測雜訊**——精度劣化、衛星跳點、時鐘異常——而合成的 Fix 沒有量測誤差，這些檢查對它不適用。
   > 更關鍵的是它會全滅：方向鍵速度以像素/秒表達，換算成地理速度後在大地圖上是每秒十餘公里，超出速度合理性門檻兩個數量級。這不是參數挑錯，而是「1 像素 = 370 公尺」的尺度下，任何看得出來在動的速度換算成真實世界都必然荒謬。若不旁路，玩家按方向鍵時每筆 Fix 都被丟棄，只有連續丟棄保護每 6 筆放行一次，小人會一頓一頓地抽動。
6. 每筆 Fix 帶正確的 `sourceMode`，不可由下游竄改。
7. 正式建置不得提供規則 3 的入口。建置旗標須以**可注入的值**表達，使單一建置下即可測試兩側行為。
8. **方向鍵速度依作用中投影層的尺度決定**，不得使用固定的真實速度。
   > 於大地圖（370.4 公尺/像素），真實步行 5 km/h 需 **4.4 分鐘**才移動 1 像素，台北到高雄需連續操作 5.8 小時。方向鍵速度應以「像素/秒」為基準由圖層提供，或由圖層宣告一個移動倍率。實際數值屬任務 D。

**AC**
- AC-10.1 點擊已知像素座標 → Fix 經投影後回到原像素，誤差 < 2 像素。
- AC-10.2 方向鍵移動 10 秒 → 產生約 150 筆 Fix（15 Hz），且顯示點位移符合該圖層宣告的方向鍵速度。
- AC-10.3 同一組下游測試對虛擬與真實來源皆通過。
- AC-10.4 release 旗標為真時，除錯來源的工廠**回傳空值**（不拋出）。
- AC-10.5 虛擬來源每筆 Fix 的 `sourceMode == virtual`。
- AC-10.6 虛擬 Fix 不被應用層節流合併。
- AC-10.7 以方向鍵速度產生的虛擬 Fix，其推算地理速度即使遠超速度合理性門檻，**仍不被丟棄**；且品質標記（一律合格）、顯著位移、範圍檢查、投影、分桶全部照常執行。

---

### REQ-C-11 `[P0/P1]` 省電

**規則**
1. 訂閱僅在需要時存在；不需要時完全取消。
2. 兩段模式：`active` / `suspended`。
3. `suspended` 觸發條件：背景超過 20 秒、權限不可用、`permission = approximate`、Mini-game 進行中、`mode = virtual`。
   > **本清單為封閉列舉**（修訂一，v6）：REQ-C-16 的 `keepAwakeActive` 述詞依賴本清單化簡（四個條件已被述詞的其他合取項排除，僅餘 Mini-game）。新增觸發條件時必須重新驗證該述詞的化簡是否仍成立，否則新增條件將造成無聲失效。
4. **不做常駐輪詢。** 定位可用性的恢復一律由「回到前景」與平台的服務狀態串流觸發。
   > 一個常駐計時器出現在標題為「省電」的需求裡並不合理；使用者去系統設定改權限，必然離開再回到 app。
5. 平台層距離門檻是主要省電手段（虛擬來源除外）。
6. **分級**：規則 1~3 為 **P0**（REQ-C-14 的 `powerMode` 與 AC-1.6 依賴其行為）；規則 4、5 為 **P1**。

**AC**
- AC-11.1 進入 Mini-game → 訂閱取消。
- AC-11.2 切換為 `virtual` → GPS 訂閱取消。
- AC-11.3 `suspended` 期間不產生任何位置更新。
- AC-11.4 `virtual` 期間回到前景且定位已可用 → 診斷快照的 `permission` 變為 `ready` 且 `mode` 維持 `virtual`（此組合即 HUD 提示可切回的依據，不需輪詢）。

---

### REQ-C-12 `[P0]` 隱私

> 本節為安全需求，逐字說明，不做壓縮。與上位文件 §4 一致。

**規則**
1. 原始經緯度僅存在於裝置執行期記憶體中。**永不離開裝置**（產品承諾）。
2. 禁止寫入存檔、事件日誌或任何未來的遠端服務。
3. 允許持久化並允許將來上傳者，僅限上位文件 CC-5 規則 3 的封閉清單：已投影的像素座標、POI 打卡結果、不含原始座標的事件欄位、依模式分桶的累計距離。
4. 正式建置不得將位置資料寫入日誌輸出。
5. 放寬此政策須經明確的產品決策與使用者同意流程，並補齊保存期限與刪除權。事後放寬遠比事後收緊容易，故第一版從嚴。

**AC**
- AC-12.1 本 SPEC 對外的持久化 DTO，序列化結果不含 `lat`／`lng` 鍵。
- AC-12.2 注入假 logger 與 release 旗標值，輸出不含任何座標字串。

---

### REQ-C-13 `[P0]` 移動模式與位移歸屬

**規則**

*模式切換*
1. 兩種模式 `gps` / `virtual`，玩家可隨時手動切換，即時生效。
2. 定位不可用時（`denied`／`deniedForever`／`serviceDisabled`／`approximate`／`unavailable`）自動切換為 `virtual`，記錄為自動切換。**不含**圖資範圍外。
3. 定位恢復可用時不自動切回，HUD 提示。
4. 手動切換的模式**持久化**跨進程；自動切換**不持久化**，重啟後依當時權限重新判定。
5. 切換造成的不連續走 REQ-C-07，成因 `discontinuity`。

*來源標記與分桶輸出*
6. 每一筆顯著位移都必須帶 `sourceMode`，且能一路傳遞至結算，不可中途遺失或被覆寫。
7. 對外輸出分桶累計距離：`realDistanceMeters`、`virtualDistanceMeters`，單位一律**公尺**，與投影層無關。
8. 分桶距離為**事件重播的衍生結果**，非獨立累加（上位文件 CC-3 規則 5 要求重播決定性）。
9. 僅顯著位移計入；被丟棄、判定靜止、或 `coverage = outside` 期間者一律不計。
10. **模擬定位標記**：`isMocked == true` 的 Fix，其位移標記為 `virtual`。
    此為**降低偽造誘因的措施，非完備防護**：Android 需 API 18+，iOS 需 15+ 且僅涵蓋軟體模擬，硬體級偽造與 iOS 14 以下無法偵測，平台不可用時預設為 `false`（偽陰性方向）。任務 A／B **不得**把它當成可靠的作弊防線。
11. **除錯建置須可用旗標覆寫規則 10**，否則模擬器與 GPX 除錯期間 `isMocked` 恆為真，開發機上無法驗證 `gps` 模式。
12. **對任務 A 的契約要求**：一次遭遇的模式歸屬以遭遇開始當下的 `mode` 為準，中途切換不改變歸屬。

*事件酬載（落實上位文件 CC-3 規則 2）*

13. 本 SPEC 產生三類持久化事件，**每一類都必須含**上位文件要求的五個欄位：事件 UUID、事件類型、UTC 時戳、單調序號、`sourceMode`。

| 事件類型 | 額外欄位 |
|---|---|
| 顯著位移 | 位移公尺數（§3.0 第 7 步之前的地理距離）、當時的 `coverage` |
| 移動模式切換 | 切換前後的模式、是否為自動切換、切換原因 |
| 大跨距傳送 | 見 REQ-C-07 規則 3 |

14. **依上位文件 CC-5，事件酬載不得含任何原始座標。** 位移事件只記公尺數，不記起訖點；傳送事件的「起點、終點」**一律以像素座標表達**，不得為經緯度。
15. 分桶距離為上述位移事件**重播的衍生結果**，非獨立累加（CC-3 規則 5 要求重播決定性）。故重播函式不得依賴當下時間、亂數或外部狀態。

*品質標記（修訂二，v6 新增）*

16. **品質標記**：精度 ≤ **30 公尺**（門檻值視為合格）的 Fix 標記為「品質合格」。不合格的 Fix：
    - 不產生位移事件、不累計任何距離桶
    - **不得成為顯著性閘門的比較基準**
    - **不得成為大跨距判定（REQ-C-07）的比較基準**
    - **不得消耗待處理的不連續標記**（`markDiscontinuity` 設定的標記須由第一筆合格 Fix 消耗）
    - **不推進 `motion`**
    - 仍更新顯示點與 `coverage`
    - 計入 `accuracyGatedFixCount`，不計入 `rejectedFixCount`

    > 「不得消耗不連續標記」防的是已發生過的缺陷：模式切換後首筆若為不合格 Fix 而消耗了標記，第二筆將走連續路徑，虛擬位置到真實位置的落差會被計入里程（實測曾灌入 13 萬公尺）。
17. **虛擬 Fix 一律標記為合格。** 合成 Fix 無量測誤差（REQ-C-10 規則 5 的理由逐字適用）。

**里程的角色（2026-09-07 使用者裁決，路徑 A）**：`realDistanceMeters` / `virtualDistanceMeters` **僅供畫面顯示，不得作為任何遊戲機制的輸入**。成果載體改由 POI 打卡與探索網格計數提供（屬任務 A／B，結構性規定見 `SPEC_C_AMENDMENT_01.md` §4.1），本 SPEC 不定義它們。里程機制在本 SPEC 中只保留品質標記（規則 16、17），不加冷啟動抑制；校準協定 T2~T4 降為附錄（§6.2），不列入 DoD。

**AC**
- AC-13.1 手動切換 → `mode` 即時改變。
- AC-13.2 權限被拒 → `mode` 自動變 `virtual` 並標記為自動。
- AC-13.3 定位恢復可用 → `mode` 維持 `virtual`。
- AC-13.4 `virtual` 模式移動 1 公里 → `virtualDistanceMeters` 約 1000，`realDistanceMeters` 為 0。
- AC-13.5 **合成路徑測試**：以 5 km/h 沿直線走 500 公尺（精度 20 m、每 10 m 一筆）→ `realDistanceMeters` ∈ [400, 550]。
  > 顯著性閘門最可能的失效模式是**少算真實里程**，而所有「靜止為 0」的 AC 都抓不到它。
- AC-13.6 判定為靜止的位移 → 兩桶皆不變。
- AC-13.7 `coverage = outside` 期間的位移 → 兩桶皆不變。
- AC-13.8 `isMocked == true` 且 `mode == gps` → 位移計入 `virtualDistanceMeters`。
- AC-13.9 除錯旗標開啟時，`isMocked == true` 的 Fix 仍依 `mode` 歸屬。
- AC-13.10 手動切換後重啟 → 維持手動值；自動切換後重啟 → 依當時權限重新判定。
- AC-13.11 每筆位移事件皆帶 UUID、事件類型、UTC 時戳、單調序號與 `sourceMode`。
- AC-13.12 三類事件的**序列化結果**皆不含經緯度鍵（`lat`／`lng`／`latitude`／`longitude`）；傳送事件的起訖點為像素座標。
- AC-13.13 對同一組位移事件序列重播兩次，得到相同的分桶距離；於不同時區、不同時間重播亦相同。

**以下三項為全體 AC 的通則，缺任一項視為未完成：時戳間隔、速度已量測旗標、是否共線。**

- AC-13.14 基準精度 24 m、當前精度 40 m、相距 96 m、間隔 5 s、兩筆速度旗標皆為否 → 顯示點更新、`motion` 不變、兩桶不變、`accuracyGatedFixCount` 為 1、`rejectedFixCount` 為 0。
  > 無標記閘門的實作會計入 96 m：`0.75 × (24+40) = 48 < 96`，顯著性閘門本就放行。
- AC-13.15 承上，其後第三筆精度 20 m、與**第一筆**相距 200 m、時戳為第一筆 +10 s、三點共線同向、速度未量測 → 計入 **200 m**（單一筆位移事件）。
  > 唯一能區分「不合格 Fix 是否帶走基準」的條件：帶走的錯誤實作得 104 m。時戳必須明寫——第二筆到第三筆為 104 m / 5 s = 74.9 km/h；若間隔寫 1 s，正確實作也會被速度規則丟棄。
- AC-13.16 兩筆精度**恰為 30 m**、相距 100 m、間隔 5 s、速度未量測 → 計入 100 m。同一組資料把當前精度改為 **30.1 m** → 不計入，`accuracyGatedFixCount` 為 1。
  > 兩半寫在同一條，否則「門檻值視為合格」的全文統一語意在此處沒有測試。
- AC-13.17 `markDiscontinuity(modeSwitch)` 後：第一筆精度 50 m 於 P1（距切換前最後已知位置 5 km）；第二筆精度 20 m 於 P2（距 P1 100 m、+5 s、速度未量測）→ 第一筆不產生任何事件、不成為任何基準；**第二筆**產生**恰好一筆**傳送事件（`discontinuity` / `modeSwitch`）、零筆位移事件，兩桶維持 0。
- AC-13.18 虛擬 Fix（精度 1 m）→ 標記為合格，行為與現行 AC-13.4、AC-10.7 一致。

---

### REQ-C-15 `[P1]` 執行期更換投影層

**敘述**：已裁決加入地方層地圖（大地圖負責城際、地方層負責街道尺度）。**地方層的資產、切換時機與方向鍵速度屬任務 D**；本 SPEC 只負責換層當下不出錯。
**列為 P1**：第一版尚無第二個投影層可換，觸發本需求的東西不存在。§2.2 的注入能力（AC-4.2）已涵蓋第一版所需。

**規則**
1. 作用中的圖資模組可於執行期更換。
2. 換層**走獨立路徑**：重置品質閘門與顯著性閘門的基準、以新模組重新投影當前地理位置、直接指定顯示點。**不經 REQ-C-07 的大跨距判定、不發傳送事件。**
   > 大跨距判準已改為地理位移與速度，而換層時地理位置完全不變（只有像素座標改變）→ 地理位移為 0 → 永遠不可能觸發。舊版要求「換層走傳送流程」是像素判準時代的殘留，會使該規則與其驗收條件皆無法成立。畫面層換層本就會有自己的過場。
3. **所有以公尺定義的門檻跨層通用**，不因換層而改變。以公尺定義、需換算為像素者（抵達門檻、朝向門檻）須於換層後依新圖層重新換算。
4. 分桶累計距離以公尺計，與圖層無關，換層不得清零或重複計算。
5. 換層後當前地理位置若落在新模組的範圍外 → `coverage = outside`，依 REQ-C-05 處理；**不得拒絕換層**（否則玩家會卡在舊圖層）。

**AC**
- AC-15.1 換層後，以相同經緯度取得的目標點來自新模組。
- AC-15.2 換層**不發出任何傳送事件**。
- AC-15.3 換層前後 `realDistanceMeters` 連續，無清零、無跳增。
- AC-15.4 換層後首筆 Fix 不因速度規則被丟棄（基準已重置）。
- AC-15.5 換層後公尺門檻換算出的像素值依新圖層的公尺/像素比例更新。
- AC-15.6 換層後位置落在新模組範圍外 → 換層仍成功，`coverage = outside`。

---

### REQ-C-16 `[P0]` 追蹤期間的裝置喚醒（修訂一，v6 新增）

**敘述**：核心迴圈是「真實移動累積成果」，而移動時玩家不注視螢幕。若背景逾時取消訂閱與螢幕自動休眠兩者相加，核心玩法只在玩家注視螢幕時成立（實測：螢幕自動休眠的 40 分鐘趟次僅 12 筆 Fix，螢幕常亮的 38.4 分鐘趟次有 142 筆，相差 11.8 倍）。

**前提已證實**：目標裝置（ColorOS）於螢幕常亮下未對前景 app 施加省電凍結，Fix 密度回到中位 8.2 秒。**此結論僅在目標裝置 / ColorOS 成立**，其他 Android 廠商與 iOS 未測（見 §6.2 Q25）。

**規則**

1. 定義衍生述詞：
   ```
   shouldKeepAwake = (mode == gps) ∧ (permission == ready)
                     ∧ (應用程式在前景) ∧ (玩家未關閉本功能)
   ```
2. 喚醒抑制狀態必須等於此述詞的值。
3. 述詞須為可在不啟動平台的條件下求值的純函式（NFR-1）。
4. 抑制的是**自動休眠**，不是使用者的主動操作。玩家按電源鍵鎖屏時依 REQ-C-02 規則 7 與 REQ-C-11 處理。
5. **須提供可關閉本功能的設定，預設開啟。**
   > 本需求沒有自動釋放路徑：一旦釋放，螢幕熄滅 → 20 秒後取消訂閱 → `motion` 永遠回不到 `moving` → 無法自動恢復。玩家唯一的耗電煞車只能是手動開關。
6. 述詞的值以 `keepAwakeActive` 之名同時進入狀態物件與診斷快照（REQ-C-14 規則 5）。
   > 畫面層需要它做互動決策（見下方交接說明），故它是對外契約而非僅診斷欄位。
7. 不得因本需求放寬 REQ-C-11 的任何一條。
8. 平台能力的取得方式屬 plan 裁決。SPEC 僅記載：Flutter framework 未提供此能力。

**述詞不含 `powerMode` 的理由（規範性註解）**：REQ-C-11 規則 3 的五個 `suspended` 觸發條件中，四個已被述詞的其他合取項排除（背景逾時↔前景、權限不可用與 `approximate`↔`permission == ready`、`mode == virtual`↔`mode == gps`），僅餘 Mini-game。若將 `powerMode == active` 納入述詞，Mini-game 期間會錯誤地釋放喚醒——而那正是玩家全程注視螢幕的時刻。

**與任務 A／畫面層的交接（修訂七：原 v2 REQ-C-17 防誤觸移出本 SPEC）**

v2 曾以 `motion == moving` 作為防誤觸觸發條件，但 `motion` 由顯著位移驅動，在「手機在口袋 + 精度差」時必為 `still`——正是它宣稱要防的情境，且它約束的是輸入層，落在 §1.3 的邊界紀律之外。改為：本 SPEC 以 `keepAwakeActive` 暴露「追蹤中且螢幕被強制常亮」的狀態，畫面層據此自行決定是否鎖觸控或要求二次確認。

以下具體危害交接給任務 A／畫面層，不記下來會被遺忘：

- `gps` 模式下虛擬來源已 `stop()` 且未被訂閱，**方向鍵誤觸不產生 Fix、不寫事件日誌**。
- 但 `VirtualLocationSource.setDirection` 會**記住方向**，該方向會在日後切回 `virtual` 時（含權限掉落**自動**切換）生效，小人自己開始走。
- 模式切換誤觸的後果不可逆：產生 `modeChanged` 事件（append-only）、改變後續位移的 `sourceMode` 歸屬，且依 REQ-C-13 規則 4 手動切換**持久化跨進程**——玩家口袋誤觸一次，重開 app 仍是 `virtual`。

**AC**

- AC-16.1 `mode` 由 `virtual` 切為 `gps`、`permission == ready`、在前景 → `keepAwakeActive` 為真。
- AC-16.2 `mode` 切回 `virtual` → 為假；`activeSubscriptionCount` 與 `powerMode` 行為不變。
- AC-16.3 `permission` 離開 `ready` → 為假。
- AC-16.4 進入背景 → 為假；回到前景且其餘條件成立 → 為真。
- AC-16.5 `powerMode` 變為 `suspended`（Mini-game 情境）而其餘條件不變 → **維持為真**。
- AC-16.6 玩家關閉本功能 → 為假，其餘五個狀態維度不受影響。
- AC-16.7 dispose 後 → 為假且抑制被釋放。
  > NFR-5 已補上「與外部副作用（含喚醒抑制）」，漏放的後果是玩家手機不再自動熄屏，且無從得知原因。

> **REQ-C-17（空號）**：v2 的防誤觸判定已於修訂七整條移出本 SPEC，改由本節規則 6 的 `keepAwakeActive` 承接。編號不重用，比照 REQ-C-09 的先例。

---

### REQ-C-18 `[P0]` 觸發品質契約（修訂三，v6 新增）

**敘述**：觸發正確性與里程正確性是不同的問題——里程是路徑積分，雜訊累加且只增不減；觸發是瞬時判定，雜訊不累加，里程高估不影響觸發正確性。本 SPEC 不裁決觸發半徑與遭遇規則（屬任務 A），但必須提供「這筆位置是否夠好到可用於觸發判定」的契約。

**規則**

1. 每筆位置對外提供**觸發適用性標記**，判準與 REQ-C-13 規則 16 的品質標記相同（精度 ≤ 30 m）。
   為**三個具名參數、目前同值 30 m**：`mileageQualityThreshold`、`triggerSuitabilityThreshold`、`gridEligibilityThreshold`，參數表三列、各自寫依據，另加一條斷言三者相等的測試。
   > 不共用單一參數：日後若放寬里程門檻，觸發契約與網格資格不應跟著漂移。真正的風險不是共用一個值，是**隱式**共用一個值——三個名字加一條相等性斷言，使「它們分家」成為一次刻意的編輯。
2. **`positionErrorBound` 為必填注入、無預設值**，由任務 A 於裁決觸發半徑時一併決定。
   本 SPEC 提供的是量測結果作為輸入資料：單裝置、單時段、24 筆、以自身中位數位置為真值的離散度上界 **33.0 m**。
   > 不給預設值的理由：安全裕度的大小應由誤觸發的代價決定，而只有任務 A 知道那個代價。無預設值使「忘記決定」成為啟動期失敗，而非靜默沿用一個沒人推導過的數字。
   > 本數字**不得**用以論證任何 POI 佈點合格，其樣本不足以支撐統計保證。
3. **幾何約束**：
   ```
   d(A, B) > r_A + r_B + positionErrorBound
   ```
   推導：玩家於 A 的觸發範圍內任一點（距 A 最遠 r_A），量測點偏離真位置最多 `positionErrorBound`，要求量測點距 B 超過 r_B。
   本約束為**對稱**式（含 r_A 與 r_B 兩項），故每對 POI 驗一次即可。它**不保證** A 自身觸發得到（偽陰性方向屬任務 A）。
4. 本 SPEC 不規定觸發半徑、停留筆數、冷卻時間。

**AC**

- AC-18.1 精度 30 m 的 Fix → 標記為適用；30.1 m → 不適用。
- AC-18.2 不適用的 Fix 仍更新顯示點與 `coverage`。
- AC-18.3 參數化幾何驗證（原 AC-4.6 改寫）：給定（POI 清單、各自觸發半徑、`positionErrorBound`），驗證每對 POI 滿足規則 3。
  - 以合法的 Fake 資料驗證 → 綠燈。
  - **以刻意違規的 Fake 資料（兩個 POI 相距 0.2 像素、r 各 50 m、e = 52.5 m）驗證 → 必須被抓到。** 否則規則本身未被驗證。
  - 以 `TaiwanMapManifest` 驗證 → **目前綠燈**，但測試名稱與註解須寫明：**綠燈源自 370.4 公尺/像素的尺度（最近一對 POI 相距 4969 m，為門檻的 32 倍），非佈點品質；地方層（任務 D）上線後必須重驗。**

---

## 4. 非功能需求

| ID | 要求 |
|---|---|
| NFR-1 | 所有邏輯需求須能在不啟動模擬器、不使用真實 GPS、不載入圖檔的條件下測試 |
| NFR-2 | 邏輯層不得相依 Flutter widget、`dart:ui` 型別、`FlameGame` 生命週期或 Flame `Component`。允許 `package:vector_math/vector_math.dart`（須依 PRE-9 宣告直接相依）。經緯度以 `double` 傳遞 |
| NFR-3 | 相對時序與速度推算用 Fix 自帶時戳；**逾時、冷卻、`motion` 判定一律用單調時鐘** |
| NFR-4 | 每幀位置計算為常數時間 |
| NFR-5 | 遊戲實例重建時所有訂閱與計時器，**與外部副作用（含喚醒抑制）**，完全釋放 |
| NFR-6 | 桌面開發環境、無 GNSS 裝置、模擬器無定位時 → `permission = unavailable`，自動切方向鍵模式，遊戲可操作 |

**AC**
- AC-NFR-6.1 注入回報「無定位硬體」的來源 → `permission = unavailable`，`mode` 自動為 `virtual`，遊戲可操作。

---

## 5. 參數表（已裁決）

門檻值本身視為通過／合格。**以公尺定義者跨投影層通用。**

| 參數 | 值 |
|---|---|
| 平台層距離門檻 | 10 m |
| 應用層節流窗 | 1 s（throttleLatest） |
| 精度丟棄門檻 | 100 m |
| 精度降級輔助啟發式 | 連續 3 筆 > 500 m（主判定為平台查詢） |
| 速度合理性門檻 | 350 km/h |
| 連續丟棄保護 | 計數達 5 後第 6 筆接受（僅豁免速度規則） |
| 時鐘異常門檻 | 1 天 |
| 位移顯著性係數 | 0.75 ×（基準精度 + 目前精度），基準凍結 |
| 靜止判定逾時 | 30 s（單調時鐘） |
| 平台層更新間隔（Android） | 1 s（`iOS` `CLLocationManager` 無此參數） |
| **品質標記門檻**（`mileageQualityThreshold`） | **30 m**（有效窗口 [27.5, 35]，實測確認） |
| **觸發適用性門檻**（`triggerSuitabilityThreshold`） | **30 m**（與品質標記門檻同值，斷言相等） |
| **網格資格門檻**（`gridEligibilityThreshold`） | **30 m**（與品質標記門檻同值，斷言相等） |
| **`positionErrorBound`** | 必填注入、無預設值；量測資料：離散度上界 **33.0 m**（任務 A 裁決） |
| 平滑半衰時間 | 1.0 s（**桌面實測確認**：跟隨滑順、有可接受的慣性） |
| 抵達門檻 | 2 m |
| **朝向更新門檻** | **5 m** |
| **`acquisition` 回退門檻** | **45 s 無被接受的 Fix** |
| 大跨距門檻 | 2 km 或 120 km/h |
| 相機回歸延遲 | 3 s（縮放不重置） |
| 背景取消訂閱門檻 | 20 s |
| 恢復訂閱 debounce | 2 s |
| 虛擬來源頻率 | 15 Hz |
| 方向鍵速度 | 由作用中投影層宣告（不得用固定真實速度）。大地圖暫定 **40 px/s，桌面實測確認速度合理** |

---

## 6. 裁決紀錄

| # | 問題 | 裁決 |
|---|---|---|
| Q1 | 無定位權限時的降級 | 方向鍵，隨時可切換 |
| Q1-a | 方向鍵的成果 | 成果限縮（規則屬任務 A／B） |
| Q2 | 大跨距是否觸發後果 | 要，但由任務 A 決定；C 只發事件 |
| Q3 | 久坐是否續扣 SAN | 不用，改由顯著位移驅動 |
| Q4 | 多城市圖資提示 | 不做 |
| Q5 | 速度門檻 | 350 km/h |
| Q6 | NFR-2 型別邊界 | 用 `vector_math`；**不拆介面**，改以 ARGB `int` 消除 `dart:ui` 相依 |
| Q7 | 平滑手感 | 半衰期 1.0 s |
| Q8 | 圖資資產 | 真實輪廓過渡圖（`cec1de8`） |
| Q9 | **尺度映射** | **加上地方層地圖**（大地圖城際、地方層街道）。地方層屬**任務 D** |
| Q10 | **訊號健康度** | **降級為僅偵測服務開關**，其餘刪除 |
| Q11 | 過度工程削減 | 全部採納 |
| Q12 | **三審的四項延後** | **全部採納**：換層降 P1、刪最後已知位置、模式持久化降 P1、刪 iOS 暫時精確度入口 |
| Q13 | **隧道／地下街的呈現** | **複用 `acquisition` 維度**（改為雙向），不新增機制 |
| Q14 | **AC-4.6 的門檻未決** | **參數化 + 台灣資料標記已知失敗**；觸發半徑不在本 SPEC 裁決（**v6 更新**：AC-4.6 已改寫為 AC-18.3，台灣資料組現為綠燈，見 REQ-C-18） |
| Q23 | **里程數字在遊戲中的用途** | **2026-09-07 使用者裁決：路徑 A，里程降級為顯示數字**，不驅動任何遊戲機制；成果載體改用 POI 打卡 + 探索網格計數（詳見 `SPEC_C_AMENDMENT_01.md` §4.1、§5.1） |
| Q-CC5 | `CROSS_CUTTING_CONSTRAINTS.md` CC-5 規則 3 要不要為探索網格 id、離線重播資料開例外 | **2026-09-07 使用者裁決：持久化格 id 集合，修改 CC-5**（已同步修改上位文件，見其 §4） |
| Q-SNAP | 是否刪除道路吸附與道路節點列舉 | **2026-09-07 使用者裁決：確認刪除**（修訂四，v6 已落實） |

### 6.1 v5 相對 v4 的變更（三審意見）

**修正的阻擋項**

1. **REQ-C-15（原 C-16）換層改走獨立路徑，並降為 P1**。大跨距判準改用地理位移後，換層時地理位置不變 → 位移為 0 → 永遠不可能觸發傳送，舊規則與其 AC 皆無法成立。第一版亦無第二個圖層可換。
2. **刪除「最後已知位置」與 `stale` 概念**。唯一收益是開場少一秒空白，代價是一條要在整條管線傳遞的特例規則，以及「冷啟動首筆的傳送起點是什麼」這個無解矛盾。改為由預設降落點起步，首筆走獨立路徑。
3. **朝向門檻補上數值（5 公尺）**。原規則引用一個全文不存在的參數，AC-6.5 寫不出測試。
4. **公尺→像素換算補上求值契約**：以當前顯示點求值、每次目標點更新時重算一次。非線性地圖上此比例逐點變化，不釘死會產生兩種各自「正確」的實作。
5. **修正 P0/P1 封閉性**：REQ-C-07 升 P0，REQ-C-05 規則 1 升 P0，REQ-C-11 規則 1~3 升 P0。原標記下若只做 P0，P0 自己的 AC 跑不完。
6. **補齊事件酬載（REQ-C-13 規則 13~15）**。上位文件 CC-3 要求每筆事件含 UUID、類型、UTC 時戳、單調序號、模式；v4 只有傳送事件補齊。
7. **§3.0 第 12 步補註分桶用哪一個距離**（當時為第 7 步之前的地理距離；v6 因插入品質標記判定步驟，現對應第 8 步之前）。兩種讀法差距達（已刪除的）吸附上限量級，直接決定 AC-13.5 是否成立。
8. **AC-4.6 改為參數化資料驗證**，台灣資料標記已知失敗直到 PRE-8 修復。
   > **v6 更新**：PRE-8 已隨道路吸附整項刪除而作廢（修訂四），AC-4.6 已改寫為 REQ-C-18 的 AC-18.3，台灣資料組現為綠燈（見該條附註）。本點原樣保留作為 v4→v5 變更的歷史記錄。

**改寫殘留的清理**

9. **整條刪除 REQ-C-09**。四條規則分別重複 C-01 規則 7、C-01 檢查流程、C-07 成因規定，且 AC-9.1 與 AC-1.7 逐字重複。行為併入 REQ-C-01 規則 7。編號留空號，不重用。
10. **C-16 改編為 C-15 並移至 C-13 之後**，修正編號斷裂與文件排序。
11. **修正「以像素定義者（僅抵達門檻）」的殘留句**：抵達門檻在 v4 已改為公尺定義。
12. **§2.2 補上遺漏的契約能力**：方向鍵速度宣告（AC-10.2 原本斷言契約上不存在的東西）、POI 與道路節點列舉（AC-4.6 需要）。同時改寫為能力描述，成員名稱與簽章下放 plan。
    > **v6 更新**：道路節點列舉已隨修訂四刪除，§2.2 僅保留 POI 節點列舉（供 AC-18.3 使用）。本點原樣保留作為 v4→v5 變更的歷史記錄。
13. **門檻語意的唯一例外已標註**（REQ-C-03 規則 7 以 `<=` 判為未達標）。

**矯枉過正的回補**

14. **`acquisition` 改為雙向**，45 秒無被接受的 Fix 即回到 `acquiring`。v4 把「刪除自造心跳」的理由過度延伸到「有推送但精度爛」的情境——後者的 `accuracy` 是平台實測值，屬真實證據。原狀況下玩家在地下街走十分鐘，小人不動而 HUD 顯示「正常」。
15. **傳送的細分原因改為保證非空、列舉固定**。原訂「可為空、不影響行為」等於禁止任務 A 依賴，但 A 很可能需要區分換層與背景恢復。規則 5 的「依成因決定」同步改為「依酬載決定」。

**AC 可測性**

16. AC-3.9 措辭由「自第 3 筆起」改為「恰有第 3 筆」。實算：基準凍結下只有第 3 筆顯著、累計 36 m；原措辭會誘導斷言 3 次而讓正確實作變紅。
17. AC-10.4「回傳空值或拋出」擇一為回傳空值；建置旗標須以可注入的值表達。
18. AC-11.4 的「可觀測提示」改為具體的診斷欄位組合。
19. 補上五個維度的初始值。

### 6.1.1 v5.1 的修正

20. **補回 v5 遺失的事件酬載段（REQ-C-13 規則 13~15、AC-13.12、AC-13.13）**。v5 的 §6.1 第 6 點宣稱已補齊，但本文實際缺漏——批次編輯中途失敗、重試時漏掉該段替換，而變更說明已寫入。規格因此宣稱修了一項並未修的東西，計劃又照著不存在的編號撰寫。
21. **REQ-C-10 規則 5 改為旁路整個品質閘門**，§3.0 管線補上分支，新增 AC-0.4 與 AC-10.7。原規則只保證虛擬 Fix 通過顯著性閘門，未處理品質閘門；而方向鍵速度換算成地理速度後超出門檻兩個數量級，方向鍵模式實質不可玩。
22. **明訂傳送事件的起訖點以像素座標表達**（規則 14）。原 REQ-C-07 規則 3 列了「起點、終點」卻未說單位，與 CC-5 的隱私封閉清單有張力。

### 6.1.2 v6 相對 v5.1 的變更（併入 `SPEC_C_AMENDMENT_01.md`，2026-09-07）

三趟真機資料（步行 38.4 分鐘、靜止 16.4 分鐘、螢幕自動休眠 40 分鐘）搭配三方（主筆、架構師、資深工程師）覆核後的收斂結果，加上使用者三項裁決。完整實測證據（F1~F9）與消融實驗見 `SPEC_C_AMENDMENT_01.md` §1、§4。

**新增需求**
1. REQ-C-16 追蹤期間的裝置喚醒抑制（`keepAwakeActive`）。
2. REQ-C-18 觸發品質契約（三個具名門檻參數、`positionErrorBound`、幾何約束）。

**修訂既有需求**
3. REQ-C-13 新增規則 16、17（品質標記機制，取代 v2 曾提出又撤回的三道防線）。
4. REQ-C-03 規則 5 刪除裝置速度交叉檢查（F4：都卜勒解算與位置解算共用衛星幾何，裝置回報速度非獨立證據源）。
5. REQ-C-04 整項刪除道路吸附與道路節點列舉；`containsGeo` 改為分類遮罩查詢（§2.2 規則 4），方向鍵與真實 GPS 共用同一判定路徑。
6. REQ-C-07 新增規則 6：不連續事件須同時提供給畫面層。
7. REQ-C-11 規則 3 加註「本清單為封閉列舉」。
8. REQ-C-02 規則 5 新增 Android 平台層更新間隔 1 秒。
9. AC-3.9 改寫，修正 v5.1 既有的內部矛盾（原「恰有第 3 筆」應為「恰有第 4 筆」）。
10. DoD 第 7 條拆為 7a（管線正確性）／7b（真實情境誠實性），見 §7。
11. v2 的 REQ-C-17（防誤觸）確認移出本 SPEC，改由 `keepAwakeActive` 承接；編號留空，比照 REQ-C-09。

**使用者裁決（2026-09-07）**
12. **里程角色**：路徑 A，降級為顯示數字，不驅動任何遊戲機制（REQ-C-13 新增框定文字）。
13. **`CROSS_CUTTING_CONSTRAINTS.md` CC-5 規則 3**：修改為允許持久化探索網格 id 集合（供任務 A／B），並為除錯限定的離線重播資料開例外；已同步修改上位文件。
14. **道路吸附刪除**：確認執行。

**方法論教訓**（記錄於 `SPEC_C_AMENDMENT_01.md` §10，供未來覆核參考）：一個機制「達成 0」與「正確地達成 0」在數字上長得一模一樣；調參資料不能同時證明參數穩健，須用消融實驗；參數的非單調性是耦合的訊號；樣本最大值不是上界。

### 6.2 待決問題（不阻擋 plan）

| # | 問題 |
|---|---|
| Q12 | 方向鍵掛機自動繞圖的防濫用 |
| Q13 | 定位偽造的處置政策 |
| Q14 | 最終手繪地圖完成後的控制點重標與三角化重算流程。控制點密度需高於現行 8 點——分片線性在非線性誇張下會在三角形邊界產生折角 |
| Q15 | 地方層地圖的資產與切換時機（**任務 D**）。大地圖的方向鍵速度已由實測確認為 40 px/s |
| Q16 | POI 觸發半徑的公尺數值（PRE-10 要求改用公尺，數值待任務 A 裁決） |
| Q17 | iOS 暫時性精確度提升請求（第二版） |
| Q18 | 移動模式的跨進程持久化（相依任務 B 的持久化層） |
| Q19 | 螢幕常亮的耗電量級與可玩時長上限（需一輪完整實地耗電數據） |
| Q21 | 精度丟棄門檻 100 m 在取樣密度提高後是否適當（分母已由 12 筆變 142 筆；路徑 A 之下影響降低） |
| Q22 | 品質標記門檻 30 m 是否過嚴（需 `accuracyGatedFixCount` 的實地資料） |
| Q25 | 其他 Android 廠商與 iOS 的螢幕常亮是否同樣使 Fix 密度回到 8 秒級（不阻擋第一版；REQ-C-16 的前提僅在目標裝置 / ColorOS 證實） |
| Q26 | REQ-C-16 規則 5 的關閉開關是否跨進程持久化（若持久化則相依任務 B 的持久化層，同 Q18 的先例） |

---

## 7. 完成定義 (DoD)

1. 所有 **P0** 需求的 AC 皆有對應的自動化測試且通過（REQ-C-09、REQ-C-17 為空號）。
   AC-18.3 的台灣資料組現為綠燈（見該條附註：綠燈源自地圖尺度，非佈點品質，地方層上線後須重驗）。
2. 全部邏輯測試可在無模擬器、無真機、無圖檔的條件下執行。
3. 存在純數學的 `FakeMapManifest`，並用它通過全部邏輯測試。
4. 通用引擎的邏輯層不存在對具名城市模組的引用（靜態檢查）。
5. `flutter analyze` 維持 0 errors / 0 warnings。
6. **合成路徑測試**（AC-13.5）通過：500 公尺步行路徑的里程落在 [400, 550]。
7. **真機實地測試，拆分為 7a／7b（修訂六，v6）**：

   **7a（管線正確性）**：全程前景、GPS 模式、不主動鎖屏。判定：
   1. **假顯著位移事件筆數為 0**——「假」定義為裝置未被移動期間產生的任何位移事件或傳送事件。三趟不同時段皆須為 0。
   2. **趟次有效性**：該趟的品質標記通過率須 > 50%，未達則該趟**判定為無效、不計入**（不是判定為通過）。
      > 沒有這條，室內窗邊必然拿到 0 筆綠燈，而機制有沒有用完全沒被驗證。測試環境本身要先被驗證，才有資格驗證被測物。
   3. **至少一趟為清除 GNSS 快取後立即開始**（冷啟動）。
      > 品質標記不擋冷啟動雜訊（實測殘留約 15%），這是唯一能量到該失效模式的測試。
   4. 每趟一併記錄 `accuracyGatedFixCount`、`acceptedFixCount` 與精度分佈（Q22 唯一的資料來源）。
   5. **（路徑 A 之下）**沿已知路線走完，**首次進入探索網格數與幾何真值相差 ≤ 1 格**（含端點半格的容許）。網格參數（`cellDegrees`、`gridScheme`）由任務 A／B 的 SPEC 定義，須滿足 `SPEC_C_AMENDMENT_01.md` §4.1 的四項結構性規定（純函式、格邊長 ≥ 2 × `positionErrorBound`、以度數定義、事件帶 scheme 版本號）。

   **7b（真實情境誠實性）**：手機收在身上、允許鎖屏、步行 500 公尺。判定：**未產生假里程**（中斷區間不計入）、且**中斷區間對玩家可見**（依 REQ-C-07 規則 6、AC-7.8）。

   **離線重播能力必須保留**：本次的消融、參數窗口等結論全部依賴它，也是未來所有參數爭論的唯一仲裁方式。落盤格式須符合 CC-5 的除錯例外（僅除錯建置、以除錯旗標守衛，轉換發生在寫入路徑上，原點與絕對時戳不落盤，不得隨 app 分發或上傳）。
8. 模式歸屬測試：`virtual` 模式全程遊玩一輪，`realDistanceMeters` 為 0，所有位移紀錄的 `sourceMode` 皆為 `virtual`。
