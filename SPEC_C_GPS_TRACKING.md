# SPEC — 任務 C：即時 LBS GPS 追蹤與平滑位移

狀態：**待覆核 (Draft v1)**
流程位置：`spec → 覆核 → plan → 覆核 → 執行計劃 → 覆核`

---

## 1. 目的與範圍

### 1.1 目的
把裝置的真實 GPS 位置，轉換成手繪地圖上一個**穩定、平滑、可信**的小人位置；並在訊號劣化或缺失時，仍讓遊戲維持可玩。

### 1.2 本 SPEC 涵蓋
- 定位權限與服務可用性的處理與降級。
- 位置串流的取得、節流、品質過濾。
- 經緯度 → 地圖像素座標的投影與道路吸附（透過注入的圖資模組）。
- 小人在畫面上的平滑位移與相機跟隨。
- 訊號中斷的容錯行為。
- 開發／除錯用的虛擬定位來源。

### 1.3 本 SPEC 不涵蓋（明確排除）
| 項目 | 理由 |
|---|---|
| 背景定位（app 關閉／鎖屏時追蹤） | 第二階段。涉及 iOS 背景權限審核與電量爭議 |
| 公路網 A* 路徑導航 | 第二階段。第一版用平滑插值即可 |
| 遭遇觸發判定 | 屬任務 A。本 SPEC 只負責**產出可信位置**，不判斷遭遇 |
| 軌跡錄製／重播 | 延後 |
| 多城市地圖切換流程 | 第二階段。本 SPEC 假設單一 manifest 已注入 |
| 外掛式定位偽造的處置政策 | 僅要求保留可辨識欄位，不定義懲罰 |

### 1.4 名詞定義
| 詞 | 定義 |
|---|---|
| **Fix** | 一筆定位讀數（經緯度 + 精度 + 時戳 + 速度） |
| **投影 (Project)** | 經緯度 → 手繪地圖像素座標 |
| **反投影 (Unproject)** | 手繪地圖像素座標 → 經緯度 |
| **吸附 (Snap)** | 把投影後的像素點修正到最近的合理道路位置 |
| **目標點 (Target)** | 經投影＋吸附後、小人「應該在」的像素座標 |
| **顯示點 (Rendered)** | 小人當前實際繪製的像素座標，逐幀趨近目標點 |
| **定位來源 (LocationSource)** | 產出 Fix 串流的抽象供應者（真實 GPS 或虛擬） |

---

## 2. 前提與相依

### 2.1 阻斷項（必須先完成，否則本 SPEC 無法實作）
| ID | 項目 | 現況 |
|---|---|---|
| PRE-1 | 應用程式根層具備 Riverpod 作用域 | **缺**，`lib/main.dart` 無 `ProviderScope` |
| PRE-2 | 圖資模組提供**反投影**能力 | **缺**，`OverworldMapManifest` 僅有單向 `projectGpsToPixel` |
| PRE-3 | 道路吸附歸屬於圖資模組，非通用引擎 | **違反**，`universal_overworld_game.dart` 直呼台灣專屬校準器 |
| PRE-4 | 圖資模組提供有效地理範圍 | **缺**，無法判斷玩家是否在圖資涵蓋範圍外 |
| PRE-5 | 圖資模組由外部注入，非在畫面內建立 | **違反**，`lib/main.dart` 內直接建立台灣 manifest |

### 2.2 圖資模組契約要求
本 SPEC 要求圖資模組（`OverworldMapManifest`）對外承諾以下能力。**通用引擎不得包含任何特定城市的演算法**。

| 能力 | 用途 | 本 SPEC 的驗收依據 |
|---|---|---|
| 投影：經緯度 → 像素 | 決定目標點 | REQ-C-04 |
| 反投影：像素 → 經緯度 | 除錯點擊尋路 | REQ-C-10 |
| 吸附：像素 → 修正後像素 | 讓小人沿路走 | REQ-C-04 |
| 有效地理範圍 | 出界判定 | REQ-C-05 |

**解耦驗收（本 SPEC 最重要的一條）**：必須能提供一個**純數學、不含任何圖檔與真實地理資料**的假圖資模組，並用它跑完本 SPEC 的**全部**邏輯測試。做得到＝解耦成立。

---

## 3. 功能需求

> 標記說明：`[P0]` 第一版必須；`[P1]` 第一版應該；`[P2]` 可延後。
> 每條需求的驗收條件（AC）皆須可寫成自動化測試。

---

### REQ-C-01 `[P0]` 權限與定位服務的取得與降級

**敘述**：遊戲啟動時取得定位能力；任何一環失敗都不得讓遊戲無法進行。

**規則**
1. 檢查順序固定為：**系統定位服務總開關 → 權限狀態 → （必要時）請求權限**。順序不可顛倒。
2. 系統定位服務未開啟時，不得發出權限請求，應引導使用者開啟系統設定。
3. 權限為「永久拒絕」時，提供前往應用程式設定的入口，**並同時讓遊戲以無定位模式繼續運作**。
4. 任何失敗狀態都必須轉譯成單一、明確的可觀測狀態，供 HUD 顯示。
5. 使用者在遊戲進行中於系統設定變更權限，回到前景時必須重新檢查並反映。

**可觀測狀態（列舉需完整且互斥）**
`ready` / `serviceDisabled` / `permissionDenied` / `permissionDeniedForever` / `unavailable`

**AC**
- AC-1.1 系統服務關閉 → 狀態為 `serviceDisabled`，且**未曾**發出權限請求。
- AC-1.2 權限未決 → 恰好發出一次權限請求；使用者允許 → 狀態 `ready`。
- AC-1.3 權限永久拒絕 → 狀態 `permissionDeniedForever`，且遊戲仍可進入大地圖（小人停在預設起點）。
- AC-1.4 從背景回到前景且權限已改變 → 狀態隨之更新。
- AC-1.5 連續呼叫取得權限流程多次，不得重複跳出系統對話框。

**測試層級**：純邏輯（注入假的權限查詢器）。

---

### REQ-C-02 `[P0]` 位置串流

**敘述**：以串流方式持續取得 Fix，並依應用程式生命週期啟停。

**規則**
1. 位置來源必須是可替換的抽象；遊戲邏輯不得直接相依特定定位套件的型別。
2. 對外提供的 Fix 至少含：緯度、經度、水平精度（公尺）、時戳、速度、是否為模擬定位。
3. 冷啟動時若可取得「最後已知位置」，先以其作為起點，避免開場數秒空白。最後已知位置**不參與**任何遊戲判定，僅供顯示。
4. 應用程式進入背景時停止訂閱；回到前景時恢復。
5. 平台層節流：僅在移動達到門檻距離時推送新 Fix（提案值 **10 公尺**，可調）。
6. 應用層節流：同一時間窗內多筆 Fix 只取最新（提案值 **1 秒**，可調）。

**AC**
- AC-2.1 串流推送 5 筆 Fix，訂閱端恰好收到 5 筆，順序與內容一致。
- AC-2.2 1 秒內推送 10 筆 → 下游恰好收到 1 筆（最新那筆）。
- AC-2.3 進入背景 → 訂閱被取消；回到前景 → 重新訂閱且無重複訂閱。
- AC-2.4 冷啟動有最後已知位置 → 小人立即出現在該位置，且該筆不觸發任何遊戲事件。
- AC-2.5 遊戲關閉時所有訂閱皆已釋放，無殘留。

**測試層級**：純邏輯（虛擬定位來源 + 假時鐘）。

---

### REQ-C-03 `[P0]` 訊號品質過濾

**敘述**：GPS 在城市峽谷、室內、隧道會產生跳點與低精度讀數。未過濾的話小人會亂飄，是本專案最可能被抱怨的問題。

**規則**（皆為純函式判定，輸入 Fix 序列，輸出接受／丟棄）
1. **精度閘門**：水平精度差於門檻的 Fix 一律丟棄（提案值 **100 公尺**）。
2. **速度合理性**：以前一筆被接受的 Fix 推算瞬時速度，超過門檻視為漂移並丟棄（提案值 **200 km/h**）。
3. **時戳單調性**：時戳早於或等於前一筆被接受 Fix 的，丟棄。
4. **連續丟棄保護**：連續丟棄達門檻筆數（提案值 **5 筆**）時，強制接受最新一筆並重置基準；避免因基準點本身錯誤而永久卡死。
5. 被丟棄的 Fix 不得影響小人位置，但需計入診斷統計。

**AC**
- AC-3.1 精度 150m 的 Fix → 丟棄。精度 20m → 接受。
- AC-3.2 兩筆 Fix 相距 1 公里、間隔 1 秒（≈3600 km/h）→ 第二筆丟棄。
- AC-3.3 時戳回捲的 Fix → 丟棄。
- AC-3.4 連續 5 筆皆被速度規則丟棄 → 第 6 筆強制接受，基準重置。
- AC-3.5 首筆 Fix（無前序基準）只受精度與時戳規則約束，不受速度規則約束。

**測試層級**：純函式，本需求應有最高測試密度。

---

### REQ-C-04 `[P0]` 投影與道路吸附

**敘述**：把被接受的 Fix 轉成地圖上的目標點。

**規則**
1. 順序固定為：**投影 → 吸附 → 產生目標點**。吸附必須在插值之前完成，否則小人會朝未修正的點移動再被拉扯。
2. 投影與吸附**一律委派給注入的圖資模組**，通用引擎不得內含任何特定城市邏輯。
3. 吸附有距離上限：與最近道路的距離超過門檻時不吸附，保留原投影點（提案值 **80 像素**，由圖資模組定義）；避免離島／山區的玩家被硬拉到公路上。

**AC**
- AC-4.1 給定假圖資模組與一組已知經緯度，產出的目標點等於「該模組投影後再吸附」的結果。
- AC-4.2 更換為另一個假圖資模組，相同經緯度產出不同目標點——證明無硬編碼。
- AC-4.3 距離所有道路皆超過上限的點 → 不吸附，目標點等於投影點。
- AC-4.4 通用引擎的原始碼不得出現任何特定城市校準器的引用（可用靜態檢查驗證）。

**測試層級**：純函式 + 假圖資模組。

---

### REQ-C-05 `[P1]` 圖資範圍外處理

**敘述**：玩家可能身處目前圖資未涵蓋的地區（載入台灣圖資但人在日本）。

**規則**
1. Fix 落在圖資有效範圍外時，不更新目標點。
2. 進入明確的「範圍外」狀態，HUD 給予提示。
3. 範圍外時暫停所有依賴位置的遊戲判定（含數值衰減）。
4. 回到範圍內時自動恢復，不需重啟。

**AC**
- AC-5.1 範圍外的 Fix → 目標點維持前值，狀態為範圍外。
- AC-5.2 範圍外 → 範圍內 → 狀態恢復，目標點更新。
- AC-5.3 範圍邊界上的點視為範圍內（邊界含入）。

---

### REQ-C-06 `[P0]` 小人平滑位移

**敘述**：小人不得因 GPS 更新而瞬移。顯示點需逐幀趨近目標點。

**規則**
1. 顯示點與目標點為**兩個獨立變數**；GPS 只寫目標點，永不直接寫顯示點。
2. 平滑必須**與影格率無關**：在 30fps 與 120fps 裝置上，同樣的真實時間內必須走過同樣的距離。
3. 提案收斂特性：約 **0.25 秒**內走完目標距離的一半（收斂率可調）。
4. 顯示點與目標點距離小於門檻（提案 **1 像素**）時視為抵達，停止計算。
5. 小人朝向僅在單幀位移超過門檻時更新，避免原地抖動亂轉。

**AC**
- AC-6.1 設定目標點後推進固定的模擬時間，顯示點與目標點的距離單調遞減。
- AC-6.2 以 dt=1/30 推進 30 次，與以 dt=1/120 推進 120 次（同為 1 秒），最終顯示點誤差在容許值內。
- AC-6.3 目標點不變且已抵達 → 後續更新不再改變顯示點。
- AC-6.4 位移小於朝向門檻 → 朝向不變。

**測試層級**：純函式（把平滑計算與 Flame 元件分離，才測得動）。

---

### REQ-C-07 `[P1]` 大跨距位移處理

**敘述**：玩家可能搭高鐵、飛機，或除錯時瞬間傳送。此時平滑會讓小人「飄」過整張地圖，觀感錯誤。

**規則**
1. 目標點與顯示點距離超過門檻（提案 **200 像素**）時，不使用平滑。
2. 改為：發出一個「傳送」事件，由畫面層播放過場後直接指定顯示點。
3. 傳送事件需可被外部觀察（供任務 A 的過場與音效使用）。

**AC**
- AC-7.1 距離 500 像素的新目標點 → 發出傳送事件，且顯示點在事件後等於目標點。
- AC-7.2 距離 50 像素 → 不發事件，走一般平滑。

---

### REQ-C-08 `[P1]` 相機跟隨

**敘述**：相機預設跟隨小人，但不得與玩家的手動平移打架。現行實作直接寫入相機位置，與跟隨機制互斥。

**規則**
1. 預設狀態：相機跟隨小人。
2. 玩家開始手勢平移 → 解除跟隨，進入自由檢視。
3. 自由檢視下無操作達門檻時間（提案 **3 秒**）→ 平滑回歸並恢復跟隨。
4. 提供明確的「回到我的位置」操作，立即恢復跟隨。
5. 相機在任何模式下都不得越出地圖邊界（維持現行邊界限制行為）。
6. 縮放不影響跟隨狀態。

**AC**
- AC-8.1 手勢平移後跟隨狀態為關閉。
- AC-8.2 停止操作滿門檻時間 → 跟隨狀態恢復。
- AC-8.3 自由檢視下小人移動 → 相機不動。
- AC-8.4 任何模式下相機中心點皆滿足邊界限制。

---

### REQ-C-09 `[P1]` 訊號中斷容錯

**敘述**：隧道、地下街、電梯會造成訊號中斷。不得懲罰玩家。

**規則**
1. 超過門檻時間未收到被接受的 Fix（提案 **15 秒**）→ 進入「訊號微弱」狀態，HUD 提示。
2. 進一步超過寬限期（提案 **60 秒**）→ 進入「訊號遺失」，暫停所有依位置驅動的遊戲判定與數值衰減。
3. 中斷期間小人停在最後有效位置，不做任何推測位移。
4. 恢復訊號時，第一筆 Fix 若造成大跨距，走 REQ-C-07 的傳送流程。
5. 中斷期間若有待處理的遊戲事件，需排入佇列並於恢復後處理，不得遺失。

**AC**
- AC-9.1 靜默 15 秒 → 狀態「訊號微弱」。
- AC-9.2 靜默 60 秒 → 狀態「訊號遺失」，且數值衰減暫停。
- AC-9.3 恢復訊號 → 狀態回 `ready`，衰減恢復。
- AC-9.4 中斷期間顯示點不變。

**測試層級**：純邏輯 + 假時鐘。

---

### REQ-C-10 `[P0]` 虛擬定位來源（開發／除錯）

**敘述**：沒有虛擬來源，開發只能靠真機走路，效率不可接受，且 TDD 無法進行。

**規則**
1. 虛擬來源與真實來源實作**同一個抽象**，下游程式碼完全不知道差異。
2. 支援模式：
   - **點擊尋路**：點擊地圖任一點 → 反投影為經緯度 → 產生 Fix 注入串流。
   - **方向移動**：以固定速度（提案：步行 5 km/h、車行 60 km/h）沿指定方向連續產生 Fix。
   - **直接跳點**：指定經緯度立即注入，用於驗證大跨距。
3. 可注入異常：低精度 Fix、跳點 Fix、訊號中斷，用於驗證 REQ-C-03 與 REQ-C-09。
4. 切換入口僅存在於除錯建置，正式建置不得包含。

**AC**
- AC-10.1 點擊已知像素座標 → 產生的 Fix 經投影後回到原像素座標，誤差在容許值內（反投影往返一致性）。
- AC-10.2 方向移動 10 秒 → 產生的 Fix 數量與位移距離符合設定速度。
- AC-10.3 下游邏輯在虛擬與真實來源下行為完全一致（同一組測試對兩者皆通過）。
- AC-10.4 正式建置的產物不含虛擬來源的切換入口。

---

### REQ-C-11 `[P1]` 省電

**規則**
1. 定位訂閱僅在需要時存在；不需要時完全取消，而非降頻空轉。
2. 兩段模式即可：`active`（大地圖前景探索）／`suspended`（背景、無權限、Mini-game 進行中）。
3. 平台層距離門檻是主要省電手段，不得為了追求滑順而取消它。

**AC**
- AC-11.1 進入 Mini-game → 訂閱取消。
- AC-11.2 離開 Mini-game → 訂閱恢復。
- AC-11.3 `suspended` 期間不產生任何位置更新。

---

### REQ-C-12 `[P0]` 隱私

> 本節為安全需求，逐字說明，不做壓縮。

**規則**
1. 原始經緯度資料只保留在裝置本機記憶體中，不得寫入任何遠端服務。
2. 存檔（持久化）只允許寫入「已投影的像素座標」與「POI 打卡結果」，**不得**序列化完整的原始 GPS 軌跡序列。
3. 不得將位置資料寫入應用程式日誌的正式建置版本；除錯建置的位置日誌必須以除錯旗標守衛。
4. 未來若加入社群分享功能，分享內容應為遊戲畫面截圖，不得包含原始座標。
5. Fix 需保留「是否為模擬定位」的欄位以供未來判定，但本階段不定義任何處置行為。

**AC**
- AC-12.1 存檔內容中不存在原始經緯度欄位（可用序列化結果的靜態檢查驗證）。
- AC-12.2 正式建置下，位置資料不出現在日誌輸出。

---

## 4. 非功能需求

| ID | 項目 | 要求 |
|---|---|---|
| NFR-1 | 可測試性 | 本 SPEC 的所有邏輯需求，必須能在不啟動模擬器、不使用真實 GPS、不載入圖檔的條件下完成測試 |
| NFR-2 | 分層 | 位置處理的核心邏輯不得相依 UI 框架或遊戲引擎型別 |
| NFR-3 | 效能 | 每幀的位置計算為常數時間；道路吸附若為線性搜尋，需在圖資模組內自行最佳化，不得成為每幀瓶頸 |
| NFR-4 | 可觀測性 | 提供診斷資訊：已接受／已丟棄 Fix 數、最近一次丟棄原因、目前精度、目前狀態。除錯畫面可讀 |
| NFR-5 | 資源釋放 | 遊戲實例重建（熱重載）時，所有訂閱與計時器必須完全釋放，不得重複訂閱 |

---

## 5. 提案參數彙總（皆可調，覆核時請裁決）

| 參數 | 提案值 | 影響 |
|---|---|---|
| 平台層距離門檻 | 10 m | 省電 vs 反應靈敏 |
| 應用層節流窗 | 1 s | 計算量 vs 反應靈敏 |
| 精度丟棄門檻 | 100 m | 過嚴會在室內完全不動 |
| 速度合理性門檻 | 200 km/h | 過嚴會誤殺高鐵玩家 |
| 連續丟棄保護 | 5 筆 | 防止永久卡死 |
| 吸附距離上限 | 80 px | 過大會把離島玩家拉上公路 |
| 平滑半衰時間 | 0.25 s | 過長會感覺遲滯 |
| 大跨距門檻 | 200 px | 決定何時改用傳送 |
| 相機回歸延遲 | 3 s | 過短會打斷玩家查看地圖 |
| 訊號微弱門檻 | 15 s | |
| 訊號遺失門檻 | 60 s | |

---

## 6. 待決問題（需你裁決才能進入 plan）

| # | 問題 | 影響 |
|---|---|---|
| Q1 | 無定位權限時，遊戲該進入何種模式？（A）唯讀觀看地圖，不可遊玩　（B）自動切虛擬模式可完整遊玩　（C）擋在權限牆直到授權 | 影響 REQ-C-01 的降級行為與整體可玩性 |
| Q2 | 「大跨距傳送」是否要消耗遊戲資源或觸發事件？（例如搭車移動要扣 YEN） | 影響 REQ-C-07 是否需與任務 A 耦合 |
| Q3 | 玩家不動時（久坐咖啡廳），SAN 衰減是否繼續？ | 影響 REQ-C-09 的暫停範圍定義；也牽涉任務 A |
| Q4 | 第一版是否需要支援「圖資範圍外」的多城市提示（例如提示可下載日本圖資）？ | 影響 REQ-C-05 的範圍 |
| Q5 | 提案參數是否全部接受？特別是速度門檻 200 km/h（台灣高鐵最高約 300 km/h，會誤殺） | 直接影響 REQ-C-03 |

---

## 7. 本 SPEC 的完成定義 (Definition of Done)

1. REQ-C-01 至 REQ-C-12 的所有 AC 皆有對應的自動化測試且通過。
2. 全部邏輯測試可在無模擬器、無真機、無圖檔的條件下執行。
3. 存在一個純數學的假圖資模組，並用它通過全部邏輯測試。
4. 通用遊戲引擎原始碼中不存在任何特定城市演算法的引用。
5. 靜態分析維持 0 errors / 0 warnings。
6. 真機實地測試一次：步行 500 公尺，小人無瞬移、無亂飄、無彈跳。
