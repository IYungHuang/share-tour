# SPEC — 任務 C：即時 LBS GPS 追蹤與平滑位移

狀態：**待覆核 (v3)**
流程位置：`spec → 覆核 → plan → 覆核 → 執行計劃 → 覆核`
上位文件：`CROSS_CUTTING_CONSTRAINTS.md`（牴觸時以其為準）

---

## 1. 目的與範圍

### 1.1 目的
把裝置的真實 GPS 位置，轉換成手繪地圖上一個**穩定、平滑、可信**的小人位置；並在權限缺失、精度劣化、訊號中斷或圖資未涵蓋時，仍讓遊戲維持可玩。

### 1.2 本 SPEC 涵蓋
- 定位權限、服務可用性與**精度等級**的取得與降級。
- 位置串流的取得、節流、品質過濾、**靜止判定**。
- 經緯度 → 地圖像素座標的範圍檢查、投影與道路吸附（透過注入的圖資模組）。
- 小人平滑位移與相機跟隨。
- 訊號健康度與中斷容錯。
- 移動模式（GPS／方向鍵）的切換，與**位移的來源標記與分桶輸出**。
- 診斷資料。

### 1.3 本 SPEC 不涵蓋（明確排除）

| 項目 | 歸屬 |
|---|---|
| 遭遇觸發判定、Mini-game | 任務 A |
| **成果規則**：御朱印發放條件、圖鑑造訪狀態與升級、獎勵限縮 | 任務 A／B |
| **SAN／HP／YEN 的衰減曲線與數值計算** | 任務 A |
| 存檔格式與事件日誌結構 | 任務 B（約束見 `CROSS_CUTTING_CONSTRAINTS.md`） |
| 背景定位（app 關閉／鎖屏時追蹤） | 第二階段 |
| 公路網 A* 路徑導航 | 第二階段 |
| 多城市圖資切換與下載提示 | 第二階段（已裁決 Q4） |
| 軌跡錄製／重播 | 延後 |
| 定位偽造的處置政策（懲罰、封鎖） | 未定；本 SPEC 僅要求標記誠實（REQ-C-13） |

> **邊界紀律**：本 SPEC 產出「可信的位置、模式標記與分桶距離」。任何「這樣會得到什麼獎勵」的規則都不屬於這裡。

### 1.4 名詞定義

| 詞 | 定義 |
|---|---|
| **Fix** | 一筆定位讀數（經緯度、水平精度、時戳、速度、是否模擬） |
| **投影 / 反投影** | 經緯度 ↔ 地圖像素座標 |
| **吸附 (Snap)** | 把投影後的點修正到最近的合理道路位置 |
| **目標點 (Target)** | 經範圍檢查、投影、吸附後，小人「應該在」的像素座標 |
| **顯示點 (Rendered)** | 小人當前實際繪製的像素座標，逐幀趨近目標點 |
| **定位來源 (LocationSource)** | 產出 Fix 串流的抽象供應者（真實 GPS 或虛擬） |
| **來源模式 (sourceMode)** | 一筆位移的產生方式：`gps` 或 `virtual`。不可竄改 |
| **顯著位移** | 通過 REQ-C-03 位移顯著性閘門的移動，才算「真的動了」 |
| **有效地理範圍** | 圖資模組宣告其投影可信的地理區域，形狀由模組自決 |
| **牆鐘時間 / 單調時間** | 見 `CROSS_CUTTING_CONSTRAINTS.md` CC-2 |

---

## 2. 前提與相依

### 2.1 阻斷項

| ID | 項目 | 現況 |
|---|---|---|
| PRE-1 | 應用程式根層具備 Riverpod 作用域 | **缺**，`lib/main.dart` 無 `ProviderScope` |
| PRE-2 | 圖資模組提供**反投影** | **缺** |
| PRE-3 | 道路吸附歸屬圖資模組，非通用引擎 | **違反**，`universal_overworld_game.dart` 直呼台灣專屬校準器 |
| PRE-4 | 圖資模組提供**有效地理範圍判定** | **缺** |
| PRE-5 | 圖資模組由外部注入 | **違反**，`lib/main.dart` 內直接建立 |
| PRE-6 | `freezed`／`json_serializable`／`build_runner` 依賴 | **缺**，`pubspec.yaml` 未加入。C 的 Fix、狀態、事件皆為不可變資料類 |
| PRE-7 | 玩家降落點由圖資模組提供 | **違反**，硬編碼於通用引擎 |
| PRE-8 | 吸附語意由「最近節點」升級為「最近路段」，且道路節點不得與 POI 同座標 | **違反**，5 個節點中 4 個與 POI 同座標；預設門檻 40px 大於 POI 觸發半徑 35px，會從數公里外瞬移並誤觸發遭遇 |

> 圖資錨點的自洽性問題已於 `cec1de8` 修復（過渡用等距底圖 + 線性推導錨點 + 回歸測試）。現行底圖為**過渡資產**，最終手繪圖完成後需重新量測錨點；屆時本 SPEC 的公尺門檻不受影響，此即 REQ-C-04 改用公尺的理由之一。

### 2.2 圖資模組契約

依 ISP 拆為兩層。**domain 只認 `GeoProjection`**，不得認識渲染用的介面。

| 介面 | 成員 | 相依 |
|---|---|---|
| `GeoProjection`（domain） | 投影、反投影、吸附、`containsGeo(lat,lng)`、`defaultSpawnPixel`、`metersPerPixelAt(pixel)` | 僅 `package:vector_math` |
| `OverworldMapManifest`（渲染層，繼承上者） | 再加 `assetPath`、`mapDimensions`、`oceanColor` | 可用 Flutter 型別 |

規則：
1. 吸附上限**以公尺定義**，由圖資模組換算為像素；且**必須小於該圖資中最小的 POI 觸發半徑**所對應的公尺數（PRE-8）。
2. 吸附語意為「最近**路段**上的投影點」，非「最近節點」。
3. `containsGeo` 的形狀（矩形／多邊形）由模組自決，通用引擎不得假設。邊界含入。
4. `metersPerPixelAt` 供本 SPEC 把公尺門檻換算為像素；非線性地圖上此值隨位置變化。

**解耦驗收（本 SPEC 最重要的一條）**：必須能提供一個**純數學、不含圖檔與真實地理資料**的 `FakeGeoProjection`，並用它跑完本 SPEC 的**全部**邏輯測試。

---

## 3. 功能需求

> `[P0]` 第一版必須；`[P1]` 應該；`[P2]` 可延後。每條 AC 皆須可自動化測試。
> **門檻比較語意統一**：門檻值本身視為**通過／合格**（即以「嚴格超過門檻」才觸發拒絕或特殊處理）。

---

### REQ-C-14 `[P0]` 可觀測狀態模型（四個正交維度）

**敘述**：定位狀態不是單一互斥列舉。「已授權」「範圍外」「訊號微弱」「方向鍵模式」可同時成立。把它們壓成一個 enum 必然產生無法表達的合法狀態。

**規則**
1. 對外的可觀測狀態由**四個獨立維度**組成，任一維度的變化不得覆寫其他維度：

| 維度 | 值 | 定義於 |
|---|---|---|
| `permission` | `ready` / `serviceDisabled` / `denied` / `deniedForever` / `approximate` / `unavailable` | REQ-C-01 |
| `signal` | `live` / `weak` / `lost` | REQ-C-09 |
| `coverage` | `inside` / `outside` | REQ-C-05 |
| `mode` | `gps` / `virtual` | REQ-C-13 |

2. 另提供一個衍生的 `motion` 維度（`still` / `moving`），由 REQ-C-03 的位移顯著性判定，**唯讀**。
3. HUD 顯示優先序（高至低）：`permission` 異常 → `mode == virtual` → `coverage == outside` → `signal` 異常 → 正常。同時成立時只顯示優先序最高者，其餘可於除錯面板查看。
4. 四個維度皆須為不可變值物件，變化以整體取代而非就地修改。

**AC**
- AC-14.1 權限為 `ready`、`coverage` 為 `outside`、`signal` 為 `weak` 可同時成立，且三者互不影響。
- AC-14.2 `mode` 切換不改變 `permission` 的值。
- AC-14.3 給定四維度的任一組合，HUD 顯示項目依優先序唯一決定。
- AC-14.4 狀態物件為不可變：修改嘗試無法改變既有實例。

---

### REQ-C-01 `[P0]` 權限、服務與精度等級

**敘述**：取得定位能力；任何一環失敗都不得讓遊戲無法進行。**含精度降級的偵測**。

**規則**
1. 檢查順序固定：**系統定位服務總開關 → 權限狀態 → （必要時）請求權限**。
2. 系統定位服務未開啟時，不得發出權限請求，應引導開啟系統設定。
3. 權限為 `deniedForever` 時，提供前往應用程式設定的入口，**並自動切換為方向鍵模式**（REQ-C-13）。
4. **精度降級偵測**：Android 12+ 僅授予概略位置、iOS 14+ 關閉精確位置時，權限查詢回報為已授權，但每筆 Fix 精度達數百至數千公尺，會被 REQ-C-03 全數丟棄，造成「顯示一切正常但小人永遠不動」。
   判定：連續 **3 筆** Fix 的精度皆**超過 500 公尺** → `permission = approximate`。
5. `approximate` 時：提示使用者開啟精確位置、提供設定入口，期間**視同定位不可用**，自動切換為方向鍵模式。若平台提供暫時性精確度提升請求，應提供觸發入口。
6. 使用者於系統設定變更權限或精度，回到前景時必須重新檢查並反映。
7. 並發的權限請求需去重，同一時間只有一個在途請求。

**AC**
- AC-1.1 系統服務關閉 → `permission = serviceDisabled`，且**未曾**呼叫權限請求器。
- AC-1.2 權限未決 → 權限請求器恰好被呼叫一次；允許後 → `ready`。
- AC-1.3 `deniedForever` → `mode` 自動變為 `virtual`，小人仍可移動。
- AC-1.4 背景返回且權限已變更 → 對應維度更新。
- AC-1.5 並發呼叫權限流程 3 次 → 權限請求器的呼叫次數為 1。
- AC-1.6 注入恆回報精度 2000 m 的來源，推送 3 筆 → `permission = approximate`（而非 `ready` 後靜止不動）。
- AC-1.7 精度回復至 20 m 並持續 3 筆 → `permission` 回到 `ready`。

---

### REQ-C-02 `[P0]` 位置串流與節流

**規則**
1. 位置來源為可替換的抽象；遊戲邏輯不得相依特定定位套件的型別。
2. 對外 Fix 至少含：緯度、經度、水平精度（公尺）、時戳、速度、速度精度（可為空）、是否為模擬定位。
3. 冷啟動時若可取得「最後已知位置」，先以其作為起點顯示。該筆**不參與**任何遊戲判定，並標記為 `stale`。
4. 開機後 **10 秒**仍無任何 Fix（TTFF 冷啟在市區常需 5~15 秒）→ 進入可觀測的「定位中」提示狀態，避免被當成當機。
5. 若無最後已知位置，初始顯示點取自圖資模組的 `defaultSpawnPixel`（PRE-7）。
6. 平台層節流：距離門檻 **10 公尺**。
7. 應用層節流語意固定為 **throttleLatest（leading + trailing）**：窗內第一筆立即放行，其餘合併於窗尾補發一次。窗長 **1 秒**。
   > 不採用 trailing-only 取樣，因其固定引入 1 秒延遲，與 REQ-C-06 的手感目標衝突。
8. 生命週期：進入背景**超過 20 秒**才取消訂閱；短暫背景（來電、下拉通知、切換到相機）不取消。恢復訂閱需 **2 秒** debounce，避免反覆重建串流造成 GPS 反覆冷啟。

**AC**
- AC-2.1 以 **2 秒**間隔推送 5 筆（大於節流窗）→ 下游收到 5 筆，順序與內容一致。
- AC-2.2 1 秒內推送 10 筆 → 下游收到 2 筆：第 1 筆（leading）與窗尾的最新筆（trailing）。
- AC-2.3 背景 5 秒後返回 → 訂閱未曾取消。
- AC-2.4 背景 30 秒後返回 → 訂閱曾取消，且恢復發生在 debounce 之後，訂閱數為 1。
- AC-2.5 dispose 後診斷值 `activeSubscriptionCount == 0`。
- AC-2.6 冷啟有最後已知位置 → 顯示點為該位置，且該筆標記 `stale`、不觸發遊戲事件。
- AC-2.7 冷啟無最後已知位置 → 顯示點等於 `defaultSpawnPixel`。
- AC-2.8 開機 10 秒無 Fix → 進入「定位中」狀態。

---

### REQ-C-03 `[P0]` 訊號品質過濾與靜止判定

**敘述**：GPS 在城市峽谷、室內會產生跳點與抖動。**未過濾會導致小人原地繞圈、累積假里程**。

**規則**（皆為純函式，輸入 Fix 序列，輸出接受／丟棄／靜止）
1. **精度閘門**：水平精度**超過 100 公尺**的 Fix 丟棄。
2. **速度合理性**：推算速度**超過 350 km/h** 視為漂移並丟棄（高於台灣高鐵營運最高速 300 km/h）。
   推算方式：兩點差分速度與裝置回報 speed（當 `speedAccuracy` 可用時）**取較小值**。裝置 speed 由多普勒推導，短間隔時通常較兩點差分可靠。
3. **時戳單調性**：時戳**早於或等於**前一筆被接受 Fix 的，丟棄。
4. **時鐘異常**：新 Fix 時戳與前一筆相差**超過 1 天**，視為裝置時鐘被調整或 NTP 校時回跳，**重置基準而非丟棄**（否則後續 Fix 會被單調性規則全數丟棄，症狀極難診斷）。
5. **連續丟棄保護**：連續丟棄**計數達 5** 之後，**第 6 筆無條件接受並重置基準**。強制接受**僅豁免速度規則（規則 2）**；精度（規則 1）與時戳單調性（規則 3）仍然生效。
6. **位移顯著性閘門（靜止判定）**：即使通過上述閘門，若
   `distance(prev, cur) <= 0.75 × (accuracy_prev + accuracy_cur)`
   則視為**未顯著移動**：**不更新目標點、不累計任何距離**，`motion = still`。
   > 這擋住最常見的情境：玩家靜坐，GPS 在 20 公尺內來回跳，每筆精度 20 公尺、推算速度極低、時戳遞增——三道閘門全部通過，結果小人原地繞圈並持續累積假里程。
7. 被丟棄或判定為靜止的 Fix 不影響顯示點，但須計入診斷統計（REQ-C-15）。
8. 首筆 Fix（無前序基準）只受規則 1、3 約束。

**AC**
- AC-3.1 精度 150 m → 丟棄；精度 20 m → 接受。精度恰為 100 m → 接受（門檻含入）。
- AC-3.2 兩筆相距 1 公里、間隔 1 秒 → 第二筆丟棄。
- AC-3.3 時戳回捲 → 丟棄。
- AC-3.4 連續 5 筆被速度規則丟棄 → **第 6 筆接受**，基準重置。
- AC-3.5 強制接受的第 6 筆若精度為 200 m → **仍然丟棄**（僅豁免速度規則）。
- AC-3.6 首筆 Fix 不因無基準而被速度規則丟棄。
- AC-3.7 兩筆精度皆 20 m、相距 15 m → **不更新目標點，累計距離為 0，`motion = still`**。
- AC-3.8 兩筆精度皆 20 m、相距 60 m → 更新目標點，累計距離約 60 m，`motion = moving`。
- AC-3.9 新 Fix 時戳較前一筆早 2 天 → 基準重置且該筆被接受，後續 Fix 不被連鎖丟棄。
- AC-3.10 裝置回報 speed 為 2 m/s、兩點差分為 300 m/s → 採較小值，不觸發速度丟棄。

---

### REQ-C-04 `[P0]` 範圍檢查、投影與道路吸附

**規則**
1. 管線順序固定為：**範圍檢查 → 投影 → 吸附 → 目標點**。
   > 範圍檢查必須在投影**之前**。IDW 對範圍外輸入不會報錯，只會回傳一個落在錨點凸包內、看似合理但完全錯誤的點。
2. 範圍檢查、投影與吸附**一律委派給注入的 `GeoProjection`**，通用引擎不得內含任何特定城市邏輯。
3. 吸附上限**以公尺定義**（提案 **1500 公尺**），由圖資模組換算像素。上限**必須小於**該圖資最小 POI 觸發半徑對應的公尺數，否則玩家會被從遠處瞬移到 POI 上並誤觸發遭遇。
4. 距離所有道路超過上限時不吸附，保留投影點。
5. 吸附對象為**路段上的投影點**，非路網節點（PRE-8）。

**AC**
- AC-4.1 給定 `FakeGeoProjection` 與已知經緯度，目標點等於「該模組範圍檢查通過後投影再吸附」的結果。
- AC-4.2 更換另一個 `FakeGeoProjection`，相同經緯度產出不同目標點。
- AC-4.3 距所有道路超過上限的點 → 不吸附，目標點等於投影點。
- AC-4.4 範圍外的輸入 → **不進行投影**（可用 fake 的呼叫計數斷言投影未被呼叫）。
- AC-4.5 通用引擎的邏輯層原始碼不得引用任何具名城市的模組（以 import 靜態檢查驗證；「邏輯層」的目錄邊界於 plan 定義，本 SPEC 僅要求該邊界存在且可檢查）。
- AC-4.6 吸附上限（公尺）小於該圖資最小 POI 觸發半徑（公尺）。

---

### REQ-C-05 `[P1]` 圖資範圍外處理

**規則**
1. Fix 落在有效地理範圍外時，不更新目標點。
2. `coverage = outside`，HUD 依 REQ-C-14 優先序提示。
3. 範圍外時暫停依位置驅動的遊戲判定，並**不累計任何距離**。
4. **不自動切換為方向鍵模式**（已裁決）。僅提示玩家「可手動切換」。
   > 自動切換會使本需求的暫停規則永遠不生效，且等同「人在國外即自動獲得全圖通行權」。
5. 回到範圍內自動恢復，不需重啟。

**AC**
- AC-5.1 範圍外的 Fix → 目標點維持前值，`coverage = outside`，累計距離不變。
- AC-5.2 範圍外 → 範圍內 → `coverage = inside`，目標點更新。
- AC-5.3 邊界上的點視為範圍內（由圖資模組保證，以 fake 驗證契約）。
- AC-5.4 進入範圍外 → `mode` **維持不變**。

---

### REQ-C-06 `[P0]` 小人平滑位移

**規則**
1. 顯示點與目標點為**兩個獨立變數**；GPS 只寫目標點，永不直接寫顯示點。
2. 兩者**不得共用同一個向量實例**。（`Vector2` 可變，別名會使兩者同步變動而靜默失效。）
3. 平滑必須**與影格率無關**：相同真實時間內走過相同距離。
4. 收斂特性：**半衰時間 1.0 秒**（已裁決，自 0.25 秒調高）。
   > 更新間隔實務上為 1~10 秒（受 10 公尺距離門檻影響）。半衰 0.25 秒會在 0.75 秒內走完 87.5% 後靜止數秒，觀感是抽搐而非走路。
5. 顯示點與目標點距離**不超過 1 像素**時視為抵達，停止計算。
6. 朝向僅在單幀位移超過門檻時更新，避免原地抖動亂轉。

**AC**
- AC-6.1 設定目標點後推進固定模擬時間，距離單調遞減。
- AC-6.2 以 dt=1/30 推進 30 次，與 dt=1/120 推進 120 次（同為 1 秒），最終顯示點差距 **< 0.5 像素**。
- AC-6.3 經過 **1 個半衰期**後，剩餘距離為初始的 **50% ± 5%**。
- AC-6.4 目標點不變且已抵達 → 後續更新不改變顯示點。
- AC-6.5 位移小於朝向門檻 → 朝向不變。
- AC-6.6 修改目標點向量後，顯示點的值不隨之改變（無別名）。

---

### REQ-C-07 `[P1]` 大跨距位移處理

**規則**
1. 觸發判準改用**公尺與速度**，不用像素：位移**超過 2 公里** 或 推算速度**超過 120 km/h**。
   > 像素門檻在非線性手繪地圖上物理不成立：現行過渡底圖為等距（370 公尺/像素），但最終手繪圖會重新引入尺度差異，實測既有錨點曾達 6.2 倍。
2. 觸發時不使用平滑，改發出**傳送事件**，由畫面層播放過場後指定顯示點。
3. 傳送事件必須攜帶：起點、終點、跨距（公尺與像素）、推算速度、時間、以及**成因**。
4. **成因**列舉：`continuousTracking`（連續追蹤中的真實高速移動）／`resumeFromBackground`（訂閱中斷後恢復）／`signalRecovered`（訊號遺失後恢復）／`modeSwitch`（切換移動模式）／`debugTeleport`。
5. **只有 `continuousTracking` 建議觸發遊戲後果**；其餘僅做視覺瞬移。
   > 否則「把手機收進口袋走 10 分鐘再拿出來」必然被判為傳送並受罰——這是每天都會發生的正常行為。
6. 本 SPEC **不得直接寫入任何遊戲數值**。後果由任務 A 依事件決定。

**AC**
- AC-7.1 位移 5 公里 → 發出傳送事件，顯示點在事件後等於目標點。
- AC-7.2 位移 300 公尺、速度 5 km/h → 不發事件，走一般平滑。
- AC-7.3 事件攜帶的跨距（公尺）與推算速度，與輸入兩點及時間差一致。
- AC-7.4 背景 30 秒後恢復造成的大跨距 → 成因為 `resumeFromBackground`。
- AC-7.5 連續追蹤中以 200 km/h 移動造成的大跨距 → 成因為 `continuousTracking`。
- AC-7.6 本 SPEC 的邏輯層原始碼不得 import 遊戲數值模組（架構測試；目錄邊界於 plan 定義）。

---

### REQ-C-08 `[P1]` 相機跟隨

**規則**
1. 跟隨行為必須實作為**純邏輯狀態機**：狀態 `following` / `free` / `returning`；輸入 `(玩家顯示點, 手勢事件, dt, zoom, viewportSize, mapSize)`；輸出目標相機中心。邊界限制為純函式。
   > 否則本需求只能寫成 widget test，違反 NFR-1。
2. 預設 `following`。
3. 手勢平移 → `free`。
4. `free` 下無操作達 **3 秒** → `returning`，平滑回歸後回到 `following`。
5. 提供「回到我的位置」操作，立即回到 `following`。
6. 相機中心在任何狀態下都不得越出地圖邊界。
7. **縮放不算「操作」**，不重置回歸計時器，也不改變跟隨狀態。

**AC**
- AC-8.1 手勢平移 → 狀態為 `free`。
- AC-8.2 停止操作滿 3 秒 → 進入 `returning`，回歸完成後為 `following`。
- AC-8.3 `free` 下玩家顯示點移動 → 輸出的相機中心不變。
- AC-8.4 任意狀態與 zoom 下，輸出的相機中心皆滿足邊界限制（含地圖小於視口的退化情形）。
- AC-8.5 `free` 期間僅縮放而不平移 → 回歸計時器不重置。

---

### REQ-C-09 `[P1]` 訊號健康度與中斷容錯

**規則**
1. **訊號健康度不得以「是否收到被接受的 Fix」判定。**
   > 平台層 10 公尺距離門檻的語意是「未移動就不推送」。玩家靜坐時平台根本不推 Fix，若以此判定，訊號滿格也會在 15 秒後誤報中斷。
2. 判定改以**來源心跳**：定位來源須提供不受距離門檻影響的存活訊號（週期 **5 秒**）。
3. 心跳靜默**超過 15 秒** → `signal = weak`；**超過 60 秒** → `signal = lost`。
4. `signal = lost` 時暫停依位置驅動的遊戲判定。
   > SAN 衰減由**顯著位移**驅動（REQ-C-13），中斷期間沒有位移即自然不衰減，**不需要額外的暫停機制**。
5. 中斷期間顯示點停在最後有效位置，不做推測位移。
6. 恢復後第一筆造成大跨距者，走 REQ-C-07，成因 `signalRecovered`。
7. **「有推送但品質不佳」不影響 `signal`**，屬 REQ-C-03 的職責，反映於診斷（REQ-C-15）。

**AC**
- AC-9.1 心跳靜默 15 秒 → `signal = weak`。
- AC-9.2 心跳靜默 60 秒 → `signal = lost`，位置驅動判定暫停。
- AC-9.3 心跳恢復 → `signal = live`。
- AC-9.4 中斷期間顯示點不變。
- AC-9.5 **玩家靜止 5 分鐘但心跳正常** → `signal` 維持 `live`，`motion = still`。
- AC-9.6 心跳正常但 Fix 連續被品質過濾丟棄 → `signal` 維持 `live`，丟棄計數於診斷中遞增。

---

### REQ-C-10 `[P0]` 虛擬定位來源

**敘述**：同時服務**正式的方向鍵玩法**與**開發除錯**，共用同一抽象，可用範圍不同。

**規則**
1. 虛擬來源與真實來源實作同一抽象，下游完全不知差異。
2. **正式建置可用**：方向鍵移動，依方向與固定速度連續產生 Fix（步行 5 km/h、車行 60 km/h）。
3. **僅除錯建置可用**：點擊尋路（反投影注入）、直接跳點、異常注入（低精度／跳點／心跳中斷）。
4. 虛擬來源**繞過平台層距離門檻與應用層節流**，以 **15 Hz** 產生 Fix。
   > 步行 5 km/h = 1.39 m/s，若套用 10 公尺距離門檻需 7.2 秒才產生一筆 Fix，方向鍵將完全不能玩。
5. 虛擬 Fix 的精度欄位須為足以通過所有閘門的固定小值，且位移必然通過顯著性閘門。
6. 每筆 Fix 必須帶正確的 `sourceMode` 標記，且不可由下游竄改。
7. 正式建置**不得提供**規則 3 的入口。（守衛方式屬 plan。）

**AC**
- AC-10.1 點擊已知像素座標 → 產生的 Fix 經投影後回到原像素，誤差 **< 2 像素**（反投影往返一致性；亦即圖資模組須保證反投影可逆到此精度）。
- AC-10.2 方向鍵步行 10 秒 → 產生約 **150 筆** Fix（15 Hz），累計位移約 **13.9 公尺**（5 km/h）。
- AC-10.3 同一組下游測試對虛擬與真實來源皆通過。
- AC-10.4 在 release 旗標下，除錯來源的工廠回傳空值或拋出。
- AC-10.5 虛擬來源產生的每筆 Fix，`sourceMode == virtual`。
- AC-10.6 虛擬 Fix 不被應用層節流合併。

---

### REQ-C-11 `[P1]` 省電

**規則**
1. 定位訂閱僅在需要時存在；不需要時完全取消，而非降頻空轉。
2. 兩段模式：`active` / `suspended`。
3. `suspended` 的觸發條件：背景超過 20 秒、權限不可用、`permission = approximate`、Mini-game 進行中、**`mode = virtual`**。
4. `mode = virtual` 期間 GPS 訂閱取消；定位可用性改以**回前景時檢查**與**低頻輪詢（60 秒）**判定，供 REQ-C-13 規則 4 的提示使用。
5. 平台層距離門檻是主要省電手段，不得為了追求滑順而取消（虛擬來源除外，見 REQ-C-10）。

**AC**
- AC-11.1 進入 Mini-game → 訂閱取消。
- AC-11.2 切換為 `virtual` → GPS 訂閱取消。
- AC-11.3 `suspended` 期間不產生任何位置更新。
- AC-11.4 `virtual` 期間定位由不可用變為可用 → 於 60 秒內產生可觀測的提示。

---

### REQ-C-12 `[P0]` 隱私

> 本節為安全需求，逐字說明，不做壓縮。與 `CROSS_CUTTING_CONSTRAINTS.md` §4 一致。

**規則**
1. 原始經緯度僅存在於裝置的執行期記憶體中。**永不離開裝置**（產品承諾，非僅技術現況）。
2. 禁止將原始經緯度寫入存檔、事件日誌，或任何未來的遠端服務。
3. 允許持久化並允許將來上傳的位置衍生資料，僅限：已投影的像素座標、POI 打卡結果、不含原始座標的事件欄位、分桶後的累計距離。
4. 正式建置不得將位置資料寫入日誌輸出。
5. 放寬此政策必須經過明確的產品決策與使用者同意流程，並補齊保存期限與刪除權。事後放寬遠比事後收緊容易，故第一版從嚴。

**AC**
- AC-12.1 本 SPEC 對外提供的持久化 DTO，其序列化結果不含 `lat`／`lng` 鍵。
- AC-12.2 注入假 logger，於 release 旗標下不含任何座標字串。

---

### REQ-C-13 `[P0]` 移動模式與位移歸屬

**敘述**：玩家可隨時在 GPS 與方向鍵模式間切換（已裁決）。本 SPEC 只負責**產生正確的來源標記與分桶距離**；**成果規則（御朱印、圖鑑、獎勵限縮）屬任務 A／B，不在此定義**。

**規則**

*模式切換*
1. 兩種模式：`gps` 與 `virtual`。玩家可隨時手動切換，即時生效。
2. 定位不可用時（`denied`／`deniedForever`／`serviceDisabled`／`approximate`）**自動切換**為 `virtual`，並記錄該次為自動切換。
   （**不含**圖資範圍外——見 REQ-C-05 規則 4。）
3. 定位恢復可用時**不自動切回**；HUD 提示可切回。
4. **持久化規則**：手動切換的模式**持久化**，跨進程保留；自動切換**不持久化**，重啟後重新依當時的權限狀態判定。
5. 切換模式造成的位置不連續，走 REQ-C-07，成因 `modeSwitch`。

*來源標記與分桶輸出*
6. 每一筆**顯著位移**都必須帶 `sourceMode` 標記，且此標記須能一路傳遞至結算，不可於中途遺失或被覆寫。
7. 對外輸出**依模式分桶的累計距離**：`realDistanceMeters`（`gps`）與 `virtualDistanceMeters`（`virtual`），單位一律**公尺**。
8. 僅**顯著位移**（通過 REQ-C-03 規則 6）計入。被丟棄或判定靜止者一律不計。
9. 範圍外（`coverage = outside`）期間不計入任何桶。
10. **`isMocked == true` 的 Fix，其位移一律標記為 `virtual`。**
    > 否則玩家安裝定位偽造程式即可以 `gps` 身分取得全部成果，任務 A／B 的成果限縮形同虛設。本規則不定義任何懲罰，只要求標記誠實，成本極低而封住整個漏洞。
11. **對任務 A 的契約要求**：一次遭遇的模式歸屬，**以遭遇開始當下的 `mode` 為準**，中途切換不改變該次歸屬。
    （此為跨 SPEC 契約，實作屬任務 A。）

**AC**
- AC-13.1 手動切換 → `mode` 即時改變。
- AC-13.2 權限被拒 → `mode` 自動變為 `virtual`，且標記為自動切換。
- AC-13.3 定位由不可用恢復為可用 → `mode` **維持** `virtual`，不自動切回。
- AC-13.4 `virtual` 模式移動 1 公里 → `virtualDistanceMeters` 約 1000，`realDistanceMeters` 為 0。
- AC-13.5 `gps` 模式移動 1 公里 → `realDistanceMeters` 約 1000。
- AC-13.6 被 REQ-C-03 判定為靜止的位移 → 兩個桶皆不變。
- AC-13.7 `coverage = outside` 期間的位移 → 兩個桶皆不變。
- AC-13.8 `isMocked == true` 且 `mode == gps` 的 Fix → 位移計入 `virtualDistanceMeters`。
- AC-13.9 手動切換後重啟 → `mode` 維持手動選擇值；自動切換後重啟 → 依當時權限重新判定。
- AC-13.10 每筆輸出的位移紀錄皆帶有 `sourceMode`。

---

### REQ-C-15 `[P1]` 診斷資料

**敘述**：多條 AC（AC-2.5、AC-1.5、AC-9.6、REQ-C-03 的統計）依賴診斷值才能斷言，故診斷不是非功能項，是有 AC 的功能需求。

**規則**：對外提供唯讀診斷快照，至少含
`activeSubscriptionCount`、`acceptedFixCount`、`rejectedFixCount`、`rejectionsByReason`（依規則分類）、`lastRejectionReason`、`currentAccuracyMeters`、`motion`、四維度狀態、`realDistanceMeters`、`virtualDistanceMeters`、`lastHeartbeatAge`。

**AC**
- AC-15.1 丟棄一筆低精度 Fix → `rejectedFixCount` +1 且 `rejectionsByReason['accuracy']` +1。
- AC-15.2 診斷快照為不可變值物件。
- AC-15.3 診斷資料不含原始經緯度（與 REQ-C-12 一致）。

---

## 4. 非功能需求

| ID | 項目 | 要求 |
|---|---|---|
| NFR-1 | 可測試性 | 所有邏輯需求須能在不啟動模擬器、不使用真實 GPS、不載入圖檔的條件下測試 |
| NFR-2 | 分層 | 邏輯層不得相依 Flutter widget、`dart:ui` 型別、`FlameGame` 生命週期或 Flame `Component`。**允許 `package:vector_math` 的純數學型別**（`Vector2` 由該套件提供，flame 僅轉出；直接 import `vector_math` 即可，不需自訂型別） |
| NFR-3 | 時基 | 相對時序與速度推算用 Fix 自帶時戳；**逾時與冷卻一律用單調時鐘**（見 CC-2） |
| NFR-4 | 效能 | 每幀位置計算為常數時間；吸附若為線性搜尋，須由圖資模組自行最佳化 |
| NFR-5 | 資源釋放 | 遊戲實例重建（熱重載）時所有訂閱與計時器完全釋放，不得重複訂閱 |
| NFR-6 | 平台降級 | 桌面開發環境、無 GNSS 裝置、模擬器無定位時，須進入 `permission = unavailable` 並可切換方向鍵模式繼續開發 |

**AC（NFR-6）**
- AC-NFR-6.1 注入回報「無定位硬體」的來源 → `permission = unavailable`，`mode` 自動為 `virtual`，遊戲可操作。

---

## 5. 參數表（已裁決）

門檻比較語意統一：**門檻值本身視為通過／合格**。

| 參數 | 值 | 備註 |
|---|---|---|
| 平台層距離門檻 | 10 m | 虛擬來源不適用 |
| 應用層節流窗 | 1 s（throttleLatest） | leading + trailing |
| 來源心跳週期 | 5 s | 不受距離門檻影響 |
| 精度丟棄門檻 | 100 m | |
| 精度降級判定 | 連續 3 筆 > 500 m | Android 概略位置／iOS reduced accuracy |
| 速度合理性門檻 | 350 km/h | 高鐵營運最高 300 km/h |
| 連續丟棄保護 | 計數達 5 後第 6 筆接受 | 僅豁免速度規則 |
| 時鐘異常門檻 | 1 天 | 重置基準而非丟棄 |
| 位移顯著性係數 | 0.75 ×（前後精度和） | 靜止判定 |
| 吸附上限 | 1500 m | 須小於最小 POI 觸發半徑 |
| 平滑半衰時間 | **1.0 s**（自 0.25 s 調高） | |
| 抵達門檻 | 1 px | |
| 大跨距門檻 | 2 km 或 120 km/h | 改用公尺，不用像素 |
| 相機回歸延遲 | 3 s | 縮放不重置 |
| 訊號微弱／遺失 | 心跳靜默 15 s／60 s | |
| 背景取消訂閱門檻 | 20 s | 短暫背景不取消 |
| 恢復訂閱 debounce | 2 s | |
| 冷啟提示門檻 | 10 s | TTFF |
| 虛擬來源頻率 | 15 Hz | 繞過節流 |
| 方向鍵速度 | 步行 5 km/h、車行 60 km/h | |
| 可用性輪詢（virtual 期間） | 60 s | |

---

## 6. 裁決紀錄

| # | 問題 | 裁決 |
|---|---|---|
| Q1 | 無定位權限時的降級 | 方向鍵，且**隨時可切換** |
| Q1-a | 方向鍵的成果 | **成果限縮**（規則本身移至任務 A／B；C 只提供標記與分桶距離） |
| Q2 | 大跨距傳送是否觸發後果 | **要**，但僅 `continuousTracking` 成因；C 只發事件 |
| Q3 | 久坐是否續扣 SAN | **不用**。改由顯著位移驅動 |
| Q4 | 多城市圖資提示 | **不做** |
| Q5 | 參數 | 全接受，速度門檻 200 → **350 km/h** |
| Q6 | NFR-2 型別邊界 | **用 `vector_math` + ISP 拆 `GeoProjection` / `OverworldMapManifest`** |
| Q7 | 平滑手感 | **半衰期拉長至 1.0 s** |
| Q8 | 圖資資產 | **用真實輪廓生成過渡圖**（已於 `cec1de8` 完成） |

### 6.1 v3 相對 v2 的變更

1. **新增 REQ-C-14**：可觀測狀態拆為四個正交維度。v2 把它們壓成單一互斥列舉，會產生無法表達的合法狀態。
2. **新增 REQ-C-15**：診斷由 NFR 升格為有 AC 的功能需求。
3. **REQ-C-13 大幅收斂**：移除成果表、圖鑑升級、SAN 衰減曲線（越界至任務 A／B，且與 REQ-C-07 規則 6 自相矛盾）。改為只輸出來源標記與分桶距離。新增 `isMocked` 標記規則與模式持久化規則。
4. **REQ-C-03 新增位移顯著性閘門**：擋住靜止抖動造成的假里程。修正連續丟棄保護的 off-by-one，明訂強制接受僅豁免速度規則。新增時鐘異常處理與裝置 speed 的使用策略。
5. **REQ-C-01 新增精度降級偵測**：Android 12+／iOS 14+ 的概略位置會造成「授權正常但小人不動」。
6. **REQ-C-09 改以心跳判定**：原以「是否收到被接受的 Fix」判定，會讓靜坐的玩家被誤報訊號中斷。
7. **REQ-C-07 改用公尺／速度門檻並新增成因欄位**：像素門檻在非線性地圖上物理不成立；成因欄位避免「把手機收進口袋」被當成傳送並受罰。
8. **REQ-C-05 不再自動切換模式**：原規則會使本需求永不生效。
9. **REQ-C-04 管線順序改為範圍檢查優先**，吸附改用公尺並要求小於 POI 觸發半徑。
10. **REQ-C-02 節流語意明確化**，新增生命週期 debounce 與冷啟提示。修正 AC-2.1／2.2 互相衝突。
11. **REQ-C-06 半衰期 1.0 s**，新增半衰期本身的 AC 與別名防護。
12. **REQ-C-08 要求純邏輯狀態機**，否則無法滿足 NFR-1。
13. **NFR-2 明確化**：允許 `vector_math`，禁止 Flutter／Flame 型別。§2.2 依 ISP 拆介面。
14. **多條 AC 改為可自動化**：AC-1.5、AC-2.5、AC-7.6、AC-10.4、AC-12.1、AC-12.2。
15. **新增 PRE-6～PRE-8**。

### 6.2 待決問題（不阻擋 plan）

| # | 問題 |
|---|---|
| Q9 | 方向鍵掛機自動繞圖的防濫用（與 SAN 衰減有交互作用，需實際數據） |
| Q10 | 車行速度是否需要解鎖條件 |
| Q11 | 定位偽造的處置政策（本 SPEC 已封住標記漏洞，處置屬產品決策） |
| Q12 | 最終手繪地圖完成後的錨點重測流程 |

---

## 7. 完成定義 (DoD)

1. REQ-C-01 至 **REQ-C-15** 的所有 AC 皆有對應的自動化測試且通過。
2. 全部邏輯測試可在無模擬器、無真機、無圖檔的條件下執行。
3. 存在純數學的 `FakeGeoProjection`，並用它通過全部邏輯測試。
4. 通用引擎的邏輯層不存在對具名城市模組的引用（靜態檢查）。
5. `flutter analyze` 維持 0 errors / 0 warnings。
6. 真機實地測試：步行 500 公尺，且診斷數據滿足 **丟棄率 < 20%**、**單次目標點跳躍 > 200 公尺的次數為 0**、**靜止 3 分鐘期間累計距離為 0**。
7. 模式歸屬測試：`virtual` 模式全程遊玩一輪，`realDistanceMeters` 為 0，且所有位移紀錄的 `sourceMode` 皆為 `virtual`。
