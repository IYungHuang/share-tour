# 任務 D 提案 — 地方層與馬賽克轉場

狀態：**方向已裁決，待寫 SPEC D**
日期：2026-09-06
來源：主筆 × 2D 地圖工程師兩輪對談，再經架構師與資深工程師以**玩家體驗角度**覆核，使用者裁決

本檔不是 SPEC。它是 spec 前的探索產出。
§7 記錄使用者已裁決的五個方向；§8 記錄覆核推翻的原始判斷。

---

## 1. 問題

大地圖 370.4 公尺/像素。實測：步行 500 公尺 = **1.35 像素**，騎車 774 公尺 = **2.1 像素**。
玩家看不見自己在動。投影再精確、閘門調得再好，都不改變這件事。

---

## 2. 核心判斷：控制變數不是公尺/像素

手感由「**一次顯著位移在螢幕上佔角色 sprite 的幾分之幾**」決定：

```
感知位移比 = (顯著位移公尺 / 有效公尺每像素) / 角色 sprite 像素高
```

現行 sprite 是 `CircleComponent(radius: 8)` → **直徑 16 px**。以顯著位移門檻 30 公尺代入：

| 有效 m/px | 30 m 位移 | 感知位移比 | 100 m 位移 | 判定 |
|---|---|---|---|---|
| 148（大地圖 zoom 2.5） | 0.2 px | 0.006 | 0.7 px | 看不見 |
| 4.0 | 7.5 px | 0.47 | 25 px | 可以 |
| **2.67** | **11.2 px** | **0.70** | **37 px** | **良好** |
| 2.0 | 15 px | 0.94 | 50 px | 明顯，但抖動也明顯 |

判準：相對比 ≥ 0.4 可用、≥ 0.8 明顯；絕對面 100 公尺的移動需 ≥ 24 螢幕像素才讀得出「持續在動」。

**目標是有效 2.7 m/px 左右，不是底圖必須畫成 1 m/px。** 這兩件事最初被我混為一談。

---

## 3. 建議規格

### 3.1 地方層 sheet

| 項目 | 值 | 依據 |
|---|---|---|
| 尺寸 | **1280 × 1280**（正方形） | 見下方「為什麼必須正方形」 |
| 比例 | **4.0 公尺/像素** | 覆蓋 5.12 × 5.12 km ≈ 一個行政區 |
| zoom 範圍 | 0.7 ~ 3.0 | 下限由直式視口高反推 |
| 預設 zoom | **1.5** | 有效 2.67 m/px，兩條判準都過 |
| 解碼記憶體 | 6.55 MB | 小於大地圖的 9.44 MB |

**為什麼必須正方形**：測試機直式視口約 392 × 850 邏輯像素。`CameraFollow._clamp` 在 `viewportSize / zoom ≥ mapSize` 時把中心釘在 `mapSize / 2`——576 像素高的 16:9 底圖在 zoom 1.0 下（需要 850）會直接觸發這條，畫面上下露出海洋底色，且**相機完全不跟隨 y 軸**。這不是美觀問題，是跟隨功能整條失效。

**大地圖 zoom 上限由 2.5 提高到 4.0**（→ 92.6 m/px）。成本是一個常數，把兩層之間的斷崖從 74 倍縮到 32 倍。

**不做中間層。** 32 倍的斷崖靠中間層補要再畫一整套 40 m/px 的縣市圖（全台約 6 張），為了「連續性」付一整條產線。而馬賽克在語意上是**切鏡**不是推軌，切鏡本來就允許尺度不連續——JRPG 的世界圖到城鎮從來沒有中間層。

真正解決「跳過去以後不知道自己在哪」的，是轉場後在角落留一張**大地圖縮圖**標出這張 sheet 的位置，兩秒後淡出。成本是一個 inset，不是一條產線。

### 3.2 固定格網（不是即時生成）

sheet 中心**由格子推導，不由玩家位置推導**：

```
cellLat = 0.0463°   // 5117 m
cellLng = 0.0500°   // 台灣緯度帶 5036 ~ 5166 m
i = floor(lng / cellLng);  j = floor(lat / cellLat)
sheetId   = 'L1_${i}_${j}'
centerLng = (i + 0.5) × cellLng
centerLat = (j + 0.5) × cellLat
```

**「任意位置即時生成」必須否決。** 若中心隨玩家浮動，同一地點在不同時刻、不同裝置會生成不同 sheet，同一地標落在不同像素。而唯一能持久化的就是像素座標（CC-5），事件日誌又必須決定性重播（CC-3 規則 5）——**sheet 幾何一浮動，日誌裡所有像素座標同時失去意義**。這是資料完整性問題，不是效能問題。

投影用逐軸精確公式：

```
x = size/2 + (lng − centerLng) × 111320 × cos(centerLat) / mpp
y = size/2 − (lat − centerLat) × 110540 / mpp
```

固定度數格網在台灣境內格寬變動 2.6%（5166 → 5036 m），`metersPerPixelAt` 因此各 sheet 略異（4.04 → 3.93），x/y 有約 1.3% 異向性。它只用於換算抵達門檻（6 m），1.3% 是 8 公分。**須在 manifest 註解裡寫死這個結論**，否則日後有人會為此發明一個逐軸介面。

**策展與全覆蓋不衝突**：每個格子都有程序生成的底圖（全覆蓋）；策展是**同一個 sheetId、同一組投影參數下的資產覆寫**。美術想手繪西門町，就畫那一格、照同樣的 5.12 km 邊界描圖，換掉 PNG——程式零改動、事件日誌零影響、可增量加。

### 3.3 觸發：over-zoom 手勢意圖，不是 zoom 門檻

**zoom 是連續量，拿它當門檻必然橫跳**——玩家把手指停在門檻交界，一個像素的抖動就來回換層，而換層是重載入加 500 ms 轉場。

改成離散事件：

```
ratio = requestedZoom / maxZoom     // requestedZoom 是手勢算出的值，未經 clamp

ratio < 1.35            → 計時器清空，phase = building 或 idle
ratio ≥ 1.35 且持續 ≥ 250 ms → phase = armed
放開手指未 armed        → 彈回，不觸發
```

**必須吃「手勢請求的 zoom」而非 `viewfinder.zoom`** —— 後者已被 clamp 在上限，比值恆為 1.0，狀態機永遠不會 armed。

計時起點設在 `ratio` 首次 ≥ 1.35 那一刻，**不是首次 > 1.0**——後者會讓玩家慢慢推大的過程早就超過 250 ms，「按住」的語意消失，而那正是防誤觸的部分。

進入還需三個前提，缺一則手勢降級為單純彈回（並在 HUD 顯示原因，不得靜默失敗）：

1. 有 sheet 的 `containsGeo` 為真
2. **當前 Fix 具品質標記**（SPEC C 增修草案的 `triggerSuitabilityThreshold`，目前 30 m）
3. 距上次換層 ≥ **30 秒**

畫面同步：over-zoom 期間馬賽克格子開始從 1 px 長大，作為 affordance——玩家看到畫面開始碎，就知道再推下去會進去。

### 3.4 換層與離開的滯後

原本設計的五個滯後參數在固定格網下退化成兩個（相鄰 cell 必然存在、中心距離固定）：

```
候選 = 所有 containsGeo 為真的 manifest
若候選含當前層 → 其到中心的距離減去 0.15 × cellSize（768 m）再參與比較
選距離最小者
若選中的不是當前層，且距上次換層 < 30 s → 不換
```

**須實測調校的只有「最小停留 30 秒」**——它是防止格線附近抖動觸發連續重載的硬保險，正確值取決於實機載入耗時與玩家在格線徘徊的頻率。黏性 15% 先寫死，若出現「沿路走反覆換層」再調到 25%。

### 3.5 馬賽克轉場

狀態機在 domain（`idle → dissolving → swapping → resolving → idle`），由單調時鐘驅動，對外只吐 `progress ∈ [0,1]` 與 `mosaicCellPixels`。Flame 端只讀，不決策。

**時長 500 ms**：dissolve 220 ms → swap（1 frame）→ resolve 220 ms，另留 60 ms 給資產未載入完成時的 hold。低於 300 ms 讀不出「轉場」語意，高於 800 ms 一天觸發五六次就開始煩。

格子大小走**量化階梯** 1 → 2 → 4 → 8 → 16 → 32 螢幕像素再反向，不用連續插值——階梯才有 retro 質感，且成本是階梯的。

**實作不需要 shader**：把場景 render 到 `1/N` 解析度的 image，再以 `FilterQuality.none` 放大貼滿。Flame 的 decorator 就能做。

**domain 必須保證的五條契約**

1. **轉場期間 pipeline 不得停止 ingest。** 丟掉一筆 Fix 會讓顯著性基準與速度基準錯位，轉場結束後第一筆會被誤判為大跨距。照收，只是不送顯示。
2. **swap 的瞬間依序做四件事**：以新 manifest 投影最後已知地理位置 → `resetBaselines()` → `smoother.jumpTo(newPixel)` → **`cameraFollow.recenter()`**。
   第四項現在會漏：`CameraFollow._frozenCenter` 持有的是**舊層座標系的像素**，換層後不 recenter 會把相機扔到新圖的隨機位置。（已複驗：`switchLayer` 完全沒碰相機。）
3. **換層不得產生任何移動事件、不得改變任何距離桶。** 須寫成 AC：換層前後 `realDistanceMeters` 差為 0.0。`resetBaselines()` 已達成，但要有測試釘住——日後有人「順手」改成 `markDiscontinuity` 就破了（那會發傳送事件）。
4. **事件日誌只寫 `LayerChanged { eventId, fromMapId, toMapId, pixelInNewLayer }`。** 馬賽克的格子序與動畫進度一律不進事件。若要隨機化格子消失順序，**用確定性的 Bayer 矩陣或螺旋序，不要用亂數**——重播決定性因此不需要存種子，也少一個出錯面。
5. **轉場期間輸入與 POI 判定全鎖。** 不鎖的話同一個 POI 會在舊層與新層各觸發一次（兩層都有該 POI、觸發半徑都是公尺制、都會命中）。這是必然發生的 bug，不是邊緣案例。

### 3.6 細尺度手感

**這是探索中最重要的發現。** `SignificanceGate` 目前同時扮演兩個角色：里程與事件的閘門、**以及畫面位置的閘門**。未達門檻 → 沒有 `targetPixel` → 小人不動。

在 370 m/px 下無所謂（動了也是 0.08 px）。在 4 m/px 下，這就是「小人靜止 40 秒然後跳 7.5 px」——**卡頓感的主因不是抖動，是這個閘門**。

**改法**：`PipelineOutput` 加 `displayPixel`——每一筆通過品質閘門的 Fix 都有（連續、含抖動），只餵 smoother；`targetPixel` 維持現狀，只有顯著位移才有，餵里程與事件。事件日誌與距離桶完全不變，故重播決定性與隱私承諾不受影響。

配套兩項：

- **精度死區**（單一參數）：
  ```
  deadband = 0.5 × accuracyMeters
  d = |target − rendered| 換算成公尺
  weight = smoothstep(deadband, 2 × deadband, d)
  有效步長 = 原步長 × (0.08 + 0.92 × weight)
  ```
  中位精度 32 m → 死區 16 m → 有效 4 m/px 下是 4 px。靜坐時小人幾乎不動；真的走 30 m 會超出 2× 死區，全速跟上。
- **抵達門檻 2 m → 6 m**（地方層）。2 m 在 4 m/px 下是 0.5 px，smoother 會永遠在跑，看起來像抖。

**速度自適應 half-life：不做。** 固定 1.0 s 的穩態落後量 = `v × HL / ln2`，步行 1.4 m/s 為 2.02 公尺 = 0.5 px；騎車 5 m/s 為 7.2 m = 1.8 px。地方層尺度下肉眼不可見，而它需要 3 個參數並讓 smoother 相依 `MotionTracker`——繞回剛解耦的東西。

**航位推算：不做。** 沒有 sensors 套件（約束禁止新增），只能用 GPS 導出的速度外推，步行速度下 SNR 太差，結果是橡皮筋感，且外推依賴當下時間、污染決定性。

### 3.7 資產與載入

- **各自獨立，不拼接。** sheet 彼此不相鄰，拼接的接縫校準成本遠高於收益，且會逼你統一 m/px 與投影，剝奪「這張圖可以誇張一點」的自由。
- **記憶體硬上限 3 張常駐**（當前 + 最近 2 張，LRU），世界圖獨立常駐永不淘汰（它是回退目標）。峰值 = 9.44 + 3 × 6.55 = **29.1 MB**，與 sheet 總數無關——這是全覆蓋能成立的關鍵。
  > **Flame 的 `Images` 是它自己的 Map，沒有任何上限，也不受 Flutter `imageCache` 管轄。** 載幾張就常駐幾張。一天玩下來 20 格 × 6.55 MB = 131 MB 永不釋放，中階機足以被系統 kill。必須顯式管理。
- **載入時機**：判定「可進入」的那一刻就開始 async 預載（玩家看到提示到按下去通常有 2~10 秒，藏得住）。離開後**延遲 30 秒**才 evict——邊界附近折返是常態。
- 轉場動畫跑完而圖未載入完成時，**停在全馬賽克等待**，不要拉長動畫時間（那會讓不同裝置的轉場長度不一致）。

---

## 4. 我與對談者的分歧

| 議題 | 對談者 | 主筆裁決 |
|---|---|---|
| sprite 尺寸 | 假設 32 px，據此推 2 m/px | **實際是 16 px**（`radius: 8`）。感知比全部乘 2，**4 m/px 即達標**，覆蓋面積因此是 2 m/px 方案的 4 倍 |
| 稀疏覆蓋 | 第一輪主張「稀疏是正確的單位，地方層是策展出的可逛場景」 | 得知底圖是**程式生成**後它自行撤回。全覆蓋成立，策展降級為同 sheetId 的資產覆寫 |
| 進入前提的精度門檻 | 建議 50 m | 改用 SPEC C 的品質標記（**30 m**）。同一個概念不該有兩個門檻 |
| 吸附上限 | 建議地方層宣告 0 | SPEC C 增修草案已**整條刪除吸附**，此議題消失 |

---

## 5. 主筆補充的三個風險

**5.1 兩套網格不可混為一談**

本提案的 sheet 格網（5.12 km）與 SPEC C 增修草案的探索網格（128 m）是**不同的東西**，用途、粒度、持久化語意都不同。命名必須明確區隔（例如 `sheetGrid` / `explorationGrid`），否則將來一定有人以為改一個就是改兩個。

**5.2 「程序生成全覆蓋」目前不成立，因為缺街道資料**（**此判斷的前提已於 §7.3 消失**）

現有 Python 管線畫的是**地形**（海岸線、山脈、河流、道路路廊），輸入是真實海岸線與少數城際航點。而 5.12 km 見方、4 m/px 的 sheet 需要的是**街廓級幾何**——那要 OSM 之類的來源，我們沒有。

沒有街道資料的 sheet 會是一整片綠地加幾條主幹道，在 4 m/px 下看起來是空的。**這是全覆蓋方案的真正阻斷項，不是資產大小。**

> **後續修正**：本節成立於「全覆蓋 1382 格」的前提。§7.2 裁決稀疏策展後，10 格約 262 km²，OSM 抽取降為幾百 KB 且不需拓撲——街道資料不再是阻斷項。見 §7.3 與 §7.6。

**5.3 單格檔案大小仍是估計值**

對談者的決策表以壓縮後大小分流（≤150 KB → 全部打包約 450 格）。我用現有大地圖的位元密度（117,063 bytes / 2,359,296 px = 0.0496 B/px）外推 1280×1280 約 **81 KB** → 450 格約 36 MB，落在「全部打包」那一列。

但地方層每單位面積的細節遠多於大地圖（街道、建物），熵更高，實際可能翻倍。**要先產一張真的 sheet 量過，才能定覆蓋策略。**

---

## 7. 已裁決的方向（2026-09-06）

三方（主筆、架構師、資深工程師）以玩家體驗角度覆核後收斂，使用者裁決通過。

### 7.1 地方層禁用方向鍵

**玩家在地方層的位置由 GPS 唯一決定。** 想看別處拖地圖（REQ-C-08 的 `free` 狀態已完成），想去別處走過去。

不能真的走的時候，玩家的去處是**局外層**——行李箱、圖鑑、天賦（任務 B）。旅行的人在旅館裡就是在整理行李，這符合世界觀且完全不碰 LBS 前提。

**否決「12 px/s 觀光模式」**，兩個獨立理由：

- 12 px/s × 4 m/px = 48 m/s = **173 km/h**。分桶是會計處理，玩家收到的訊息是「走路只是取得同樣內容的比較慢的方式」——這句話說出去收不回來。
- 玩家花 20 分鐘逛完一張 sheet，**探索的記憶被消耗掉了**，而御朱印沒有、圖鑑沒有、里程標示為不計入。Roguelite 的核心情緒是「這一趟我帶回了什麼」，一個明文告訴玩家「你現在做的事不算數」的系統，等於邀請他把時間投進已知無效的行為。

**「禁用 → 沒訊號時卡死」不成立**：沒訊號的玩家卡的是大地圖，而大地圖的方向鍵照舊（40 px/s）。地方層是一個他**進不去**的房間，不是把他鎖在裡面的房間。

**配套三項**

1. 方向鍵在地方層要「不存在」，不是「變灰」。灰掉的按鈕玩家會一直戳然後想「為什麼壞了」。
2. 進不去時要說原因：「訊號不足，無法進入街道視圖」。每個用過地圖 app 的人都懂這句話。
3. HUD 的職責從「說明現在是什麼狀況」擴充為「說明現在能做什麼」——沒訊號／室內／國外時主動導向局外層。

**實測依據**：室內精度中位數 25 m，在 4 m/px、預設 zoom 1.5（有效 2.67 m/px）下是**直徑 18.7 螢幕像素的誤差圈**，而角色 sprite 是 24 螢幕像素。小人會在自己身體大小的範圍裡持續游移。玩家的解讀不是「GPS 不準」，是**「有人在操作我的小人」**——不動可以歸因為沒訊號，自己動只能歸因為遊戲在亂跳。

### 7.2 遭遇系統綁世界層，地方層是加分項

**核心循環必須對每一個玩家、在每一個地方、每一天都成立。**

POI 帶經緯度、觸發半徑以公尺計（PRE-10 已改），與作用中是哪一層無關；遭遇本身是 overlay／Mini-game 場景，不在地圖上發生。地方層只影響「他在遭遇之前看到的那張底圖」。

御朱印與圖鑑的語意（到某地、蓋章、收進圖鑑）在世界層 + POI 打卡上完整成立，不需要任何街廓級資料。

**LBS 遊戲唯一不能犯的錯，是讓玩家真的走了卻沒有回應。** 畫面醜他會忍，走 40 分鐘沒事發生他會刪。

**稀疏必須做成看得見的設計。** 世界層上把有地方層的格子畫成發光城區。玩家看到「台北亮了 8 個我只看過 2 個」——稀疏從缺點變成旅行目標，同時是 DLC 邊界（京都篇＝一批新的亮格），而且美術產能與上線時程解耦（一格畫完上線一格）。

> **empty-by-design 與 empty-by-omission 對玩家是完全不同的兩件事，差別只在遊戲有沒有事先講。**

**不做全覆蓋的理由是產品決策，不是技術限制**：空的地方層比沒有地方層更糟。花蓮玩家的 sheet 大部分真的是山和田，程序生成的綠地接近正確；台北玩家的 sheet 是錯的。**兩人看到的畫面一模一樣，而玩家沒有能力分辨。** 於是正確的呈現與錯誤的呈現得到同一個評價。

### 7.3 地方層內容走「程序地形 + 真實街道」（檔次 B）

**放大之後的細節有兩個來源：**

| 真實世界的東西 | 世界層 370 m/px | 地方層 4 m/px |
|---|---|---|
| 30 公尺寬的河 | 0.08 px（不存在） | **7.5 px**（有兩岸、有轉彎） |
| 200×200 公尺的公園 | 0.5 px | **50×50 px**（走得進去） |
| POI 的 50 公尺觸發半徑 | 0.13 px | **12.5 px 的圈** |
| GPS 精度圈（25 m） | 0.07 px | **6 px 半徑** |
| 500 公尺的足跡 | **1.35 px** | **125 px** |
| 探索網格（128 公尺） | 0.35 px | **32 px 的方格** |
| 一個 16×16 磚塊代表 | 5.9 公里 | **64 公尺**（一個街廓） |

**來源一：同樣的地形，第一次看得見。** 河流、公園、山勢在世界層是次像素，到 4 m/px 才有形狀。

**來源二：遊戲自己的狀態層。** 足跡、POI 光暈與觸發圈、精度圈、探索網格的已探索著色——在世界層全部是次像素，在地方層是**畫面的主要內容**。玩家放大進去看到的細節，有一半是他自己留下的東西。

**三個內容檔次，按格選用，共用同一份 manifest**

| 檔次 | 內容 | 成本 |
|---|---|---|
| A. 程序風格化 | DEM 等高線、水體、綠地 ＋ 遊戲狀態層 | 每格約 30 秒，幾乎免費 |
| **B. 程序 ＋ 真實街道（已裁決）** | A ＋ OSM 街道折線畫成道路磚塊 | 每格多一次 OSM 抽取，幾百 KB |
| C. 手繪策展 | 美術照真實幾何描圖，加建物與地標 | 昂貴，重點區才做 |

**為什麼 B 可行**：「街道資料是阻斷項」這個判斷建立在**全覆蓋 1382 格**的前提上（全台 OSM 路網 10~20 MB、要建索引與拓撲）。稀疏策展後前提消失——10 格約 262 平方公里，OSM 抽取是幾百 KB 的事，而且**只需要把街道畫出來**，不做路網比對、不做尋路，那只是一組折線。

**「半寫實是最糟的落點」這條反對意見不適用於 B**：覆核者反對的是「畫得像地圖但其實是空的或錯的」。用真實 OSM 街道畫出來的圖不是半寫實，是寫實。

三個檔次的差別只在底圖怎麼來，`manifest` 相同、程式零改動。

### 7.4 轉場只留給自願進入

| 路徑 | 玩家感受 | 處置 |
|---|---|---|
| 主動 over-zoom（推大、按住 250 ms、看到格子長大） | **驚喜**——他做了一件事，遊戲回應了他 | **保留完整 500 ms 馬賽克** |
| 走過 sheet 邊界自動換層 | **障礙**——他什麼都沒做，畫面突然碎掉，第一反應是「當機了？」 | **不用馬賽克、不鎖輸入** |
| 離開地方層回世界層 | 32 倍尺度跳躍，需要重新定位 | 保留轉場 |

自動換層的處置（主筆裁決，兩位覆核者的方案折衷）：

- **前景且玩家在互動**：不自動換。HUD 出現「已離開本區街景／切換到相鄰街區」讓玩家自己按。走出邊界的當下玩家多半沒在看螢幕，等他下次拿起手機再由他決定，比在他口袋裡偷偷換掉整張地圖好。
- **背景或螢幕關閉**：**不換層**，回到前景再算一次該在哪一格。
- 任何情況都不用馬賽克。若判斷必須有過場，用 ≤120 ms 的交叉淡出。

**觸發頻率的修正**：原估「一天數次」不成立。固定 5.12 km 格網下，台中跨區通勤一趟穿過 2~3 條格線，加上外出往返與繞路，**一天 8~10 次是常態**，且集中在手機在口袋、玩家沒在看的時候——每次重載 6.55 MB、evict、recenter，純粹的電池與發熱支出，換來零體驗。

**轉場期間不得鎖 POI 判定，改用去重。** §3.5 契約 5 的理由是「同一個 POI 會在兩層各觸發一次」，但那是去重問題——POI 有 UUID，已觸發集合本來就該存在（冷卻、重複造訪都需要它）。用「鎖住」來解去重，代價是**玩家看不見地漏掉遭遇**：沒有錯誤訊息、查不出原因，只是那個御朱印沒拿到。這種看不見的不公平最傷信任。
正確做法：轉場期間 Fix 照常進管線、POI 判定照常跑、觸發的遭遇**排隊**，轉場結束後播。

**「圖沒載完就停在全馬賽克」需要可見的出口**：靜止的馬賽克雜訊在手機上讀起來就是當機。要有持續動作證明遊戲還活著，並設硬上限——超過 N 秒放棄換層、退回原層並說明原因。玩家可以接受「進不去」，不能接受「不知道發生什麼事」。

### 7.5 優先順序：回饋先於尺度

**這一項推翻了本提案 §2 的核心指標**（見 §8）。

| 順序 | 內容 | 資產成本 | 玩家看到的差別 |
|---|---|---|---|
| **第一** | 距最近 POI 的公尺數（走路時變小）、POI 隨接近變亮變大、收集進度可見 | **零** | **第一次知道自己為什麼要走那 300 公尺** |
| **第一** | 給小人畫**精度圈** | **零**（一個圓） | 同樣的像素游移，從「遊戲在亂動」變成「我的訊號在抖」 |
| **第一** | 顯著位移發生時給回饋（數字跳動、足跡點、8-bit 短音） | **零** | 建立「什麼算數」的直覺 |
| 第二 | `displayPixel` 分離 ＋ 精度死區 ＋ 世界層 zoom 上限 4.0 | 零 | **近乎不可見**（見下） |
| 第三 | 地方層，稀疏策展、檔次 B | 每格一張圖 | 場所感 |

**第二項不是體驗交付，是技術前置。** zoom 上限 4.0（有效 92.6 m/px）下：

- 走 500 公尺（約 7 分鐘）＝ **5.4 px**，角色身長的 34%
- 走 60 秒 ＝ 0.9 px
- 一次顯著位移（30 公尺）＝ **0.32 px**

本提案 §2 自己的判準是「感知比 ≥ 0.4 可用」。**走七分鐘得到 0.34，整趟還達不到「一次位移可用」的標準。**

必須先講清楚，否則做完會出現一種危險的挫折：「我們做了顯示與判定分離，手感沒變，所以問題不在這裡」——然後把唯一正確的前置判成無效。zoom 4.0 的真實收穫是**可讀性**（POI 變大、好點、字看得清），照可讀性去賣它，別照手感賣。

**`displayPixel` 分離會製造一個新的分歧，必須同時處理**：玩家坐在咖啡廳，小人在螢幕上緩緩游移（GPS 抖動的真實呈現），而里程 0、探索格 0。他的推論是

> 「遊戲有在追蹤我，但它不承認。」

這比小人不動嚴重，而且**玩家可以截圖證明**——前後兩張圖小人明顯位移、計數沒變。解法就是上表第一批的精度圈與位移回饋，加上一句文案：精度圈超過某個大小時，HUD 直接寫「訊號較差，移動可能不計入」。

> **玩家可以接受規則嚴格，不能接受規則隱形。**

### 7.6 圖資施作範圍

裁決後的施作範圍**大幅縮小**：未策展的格子不生成、不可進入，世界層底圖不重畫。

**A. 零圖資成本的部分（優先級最高）**

§7.5 第一批三項（近接回饋、位移回饋、精度圈）不碰圖資管線。用的是既有資料：POI 經緯度、Fix 精度、顯著位移事件。

**B. 世界層：兩件事，資料已在手上**

| 工作 | 現況 |
|---|---|
| `containsGeo` 接上分類遮罩 | `assets/maps/taiwan/mask.png` 已產出並複驗（分類與底圖零不一致）。要做的是讓 `TaiwanMapManifest` 查它，取代矩形框 |
| POI 光點與「發光城區」標記 | POI 已有 22 個；「哪些格子有地方層」是一份 id 清單，不是圖資 |

世界層底圖不需重畫：370 m/px 下它是示意圖，玩家不會拿去比對現實——這正是「半寫實最糟」的另一面，世界層夠抽象所以安全。

**C. 地方層：每格「四個數字 + 一張圖」**

```
centerLat, centerLng, metersPerPixel(4.0), size(1280×1280)
```

投影與反投影由這四個值算出（局部等距近似，5 公里內誤差 < 0.1 m），**零人工錨點、零控制網、零三角化**。與世界層的差別在此：世界層要 8 錨點加 IDW 是因為那張圖非線性誇張；地方層每格只有 5 公里，直接用公式。

**D. 檔次 B 的資料來源**

| 資料 | 畫出什麼 | 取得 |
|---|---|---|
| **OSM 街道折線**（僅策展格） | 道路磚塊、路口、橋 | 每格約 26 km²，幾百 KB。**只畫不比對**——不做拓撲、不做尋路、不做 map matching |
| **DEM 高程**（SRTM 30 m 網格） | 等高線、山勢、坡度著色 | 公開離線資料。30 m 網格在 4 m/px 下是 7.5 px 一個取樣點——對等高線夠用，而我們正好不要更細 |
| **水體多邊形**（OSM water/waterway） | 河流、湖、岸線 | 資料量遠小於路網 |
| 綠地／土地利用 | 公園、農地、林地色塊 | 同上 |
| 現有海岸線 | 沿海格子的岸線 | 已在管線裡 |

加上遊戲自己產生的層：足跡、POI 光暈與觸發圈、精度圈、探索網格著色。

**E. `tool/` 要長出的兩支**

| 工具 | 輸入 | 輸出 |
|---|---|---|
| 單格產生器 | 中心經緯度、比例尺 | 底圖 PNG、該格 `mask.png`、POI 子集、`manifest.json` |
| 單格驗證器 | 上述產出 | POI 兩兩間距是否滿足 `d > r_A + r_B + positionErrorBound`；遮罩與底圖分類一致；同輸入跑兩次位元組相同 |

> 幾何約束**在 5 公里見方的格子裡才第一次會咬人**——世界層最近一對 POI 相距 4969 m，是門檻的 32 倍。

**F. 明確不做**

- 全覆蓋程序生成（1382 格的產能問題隨稀疏策展消失）
- 全台 OSM 路網匯入與拓撲（只抽策展格，且只畫不比對）
- 地方層路網與吸附（吸附已於 SPEC C 增修草案整條刪除）
- on-device 即時生成（30 秒不可接受，且要求位元組等價）

**G. 第一格與驗收**

建議選**實測走過的那一帶**（台中，24.156 / 120.658 附近）。理由是那裡是唯一有真實軌跡的地方——可以把 2026-09-06 那 142 筆餵進地方層投影，**在畫任何美術之前就驗證手感**：軌跡是否落在合理範圍、抖動幅度是否如預估的 18.7 px、死區壓下去之後是否還看得出「在走」。

---

## 8. 覆核推翻的原始判斷

| 本提案原本主張 | 覆核後 | 理由 |
|---|---|---|
| §2「感知位移比」是核心設計指標——讓玩家看見自己在動 | **推翻** | 玩家掏出手機那一眼要回答的是「**我離下一個東西還有多遠**」，不是「我剛才有沒有動」。後者更貴、效果更差。1.35 px 看不見，但「距最近 POI 320 公尺」在任何尺度下都可讀 |
| §3.2 程序生成全覆蓋 | **推翻** | 能做到不等於該做。空的地方層比沒有地方層更糟；稀疏做成可見的設計就完全不是缺點 |
| §5.2 街道資料是全覆蓋的阻斷項 | **前提消失** | 該判斷建立在 1382 格上。稀疏策展後 10 格約 262 km²，OSM 抽取是幾百 KB，且只需畫出來不需拓撲 |
| §6.1 方向鍵二選一（禁用 vs 觀光模式） | **問題問錯了** | 方向鍵被要求同時服務「想看別處」與「不能真的走」兩個需求，前者拖地圖就好，後者該由局外層承接 |
| §3.3／3.4 所有換層共用同一個轉場 | **拆開** | 自願的是驚喜，非自願的是障礙，不該共用同一個表達 |
| §3.5 契約 5 轉場期間鎖 POI 判定 | **改為去重** | 用鎖解去重，代價是玩家看不見地漏掉遭遇 |
| §6.3 (a) 「立刻改善現有手感」 | **改為技術前置** | 對玩家零可見改變（走七分鐘 5.4 px） |

---

## 9. 原始的待裁決題（已於 §7 裁決，保留供追溯）

**6.1 地方層的方向鍵怎麼辦**

4 m/px 下真實步速 5 km/h = 0.35 px/s，慢到無法接受。若宣告 30 px/s，等於玩家用方向鍵能以 432 km/h 在街上飄——真實走路反而變成最爛的移動方式，整個 LBS 前提被自己拆掉。

| 選項 | 代價 |
|---|---|
| **地方層禁用方向鍵** | 符合世界觀，但沒訊號時完全卡死 |
| **宣告 12 px/s（≈48 m/s），標示為「觀光模式」且不計入真實桶** | 現有的分桶架構已支援。但玩家仍可用它逛完整張圖 |

**6.2 遭遇系統是否綁定地方層**

若綁定，且覆蓋不完整，大部分玩家在大部分時間碰不到核心循環。全覆蓋成立則此風險消失——但 5.2 指出全覆蓋目前缺街道資料。

這題會反過來決定「地方層是必需品還是加分項」，**應在寫 SPEC D 之前答**。

**6.3 先做哪一半**

地方層的價值來自「看得見自己在動」，而那需要 3.6 的 `displayPixel` 分離。但 `displayPixel` 分離**在大地圖上就能做、且能單獨驗證**（它只是把顯示與里程解耦）。

- 先做 `displayPixel` + 死區 + 世界層 zoom 上限 4.0 → 立刻改善現有手感，零資產成本
- 或直接進地方層 → 要先解 5.2 的街道資料

我建議前者：它是後者的前置，而且它讓「小人跟隨是否順暢」這件事在有地方層之前就先被驗證過。
