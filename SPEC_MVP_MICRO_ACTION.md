# SPEC — 《Share Tour：奇葩旅行策展人》Milestone M5：快門微動作與三態取材

狀態：**Draft v2 — 待覆核**
流程位置：`spec → 覆核 → plan → 覆核 → 執行計劃 → 覆核`
上位文件：`CROSS_CUTTING_CONSTRAINTS.md`、`CLAUDE.md`、`SPEC_MVP_CORE_LOOP.md`（牴觸時以其為準）
相關文件：`SPEC_MVP_POI_GATHERING.md`（本 SPEC 解除其 §1.3 的「微動作」排除條款）、`SPEC_MVP_CAUSAL_FEEDBACK.md`（**M5 必須排在其施工完成之後**，見 §5）、`SPEC_MVP_AMENDMENT_01.md`（數值包絡）

> **命名澄清**：本文件的「M5」指**里程碑 M5**。`PLAN_MVP_META_PROGRESSION.md` 內的 `Task M5` 是 M4 施工計劃的任務編號，兩者無關。

---

> ## v2 修訂摘要（相對 v1，雙軌覆核後全面改寫）
>
> v1 送交企劃軌與工程軌獨立覆核，兩軌合計提出 11 條 P0。**兩軌各自獨立算出同一個致命問題**：v1 的核心主張（三態影響產出側即可驗證）未經驗算，而實算結果推翻了它。主要變動：
>
> 1. **判定改以時間定義，不再以半徑相對偏差定義**（原 $d = |r - r_t| / r_t$ 全部作廢）。收縮環降為純表現，判定看 $|t_{\text{按下}} - t_{\text{吻合}}|$，參數直接以毫秒表達。原設計在 `修羅` / risk5 下的完美窗僅 50 ms，低於行動裝置觸控延遲抖動，且會被渲染幀率主導而非玩家主導。
> 2. **難度改為唯一的 QTE 旋鈕。** `riskLevel` 與 `cameraLevel` 對 QTE 的影響全部移除 —— 前者會造成「高熱度卡幾乎都是高風險」的三重懲罰疊加，後者的實際效果僅 +37 ms，低於可辨識差異卻要付出跨文件耦合代價。
> 3. **新增快門獎金（局外金幣）。** 解決三個 v1 無解的缺口：社畜端計分不含熱度項、網紅強行程滿意度撞 100 天花板、高難度零獎勵。走的是 v1 §1.4 自己開的合法通道（只動局外金幣，不動局內計分）。
> 4. **幅度不再由推導定案，改由分佈型 AC 反推。** v1 的 ±12 推導有三處錯誤（拿網紅公式論證社畜門檻、漏掉絕景階梯、用了過時的 ThemeFactor 形式）。
> 5. **絕景兩段式合併為單一手勢。** v1 的分段設計實測估 5~15 秒／張，四張絕景吃掉 40~60 秒野外時間。
> 6. **接上因果可視化系統。** v1 新增了一個影響 `totalHype` 數十點的來源卻無 `reasonCode`，會導致歸因面板指向錯誤原因。
> 7. **事件範圍縮小**：單局事件溯源管線目前完全不存在，不在本里程碑建立。
> 8. **修正三處過時引用**：`ΔHP` 公式（現行為 `riskLevel × 6`）、絕景張數（京都為 5 張，v1 誤計註解行）、滿意度公式。
> 9. **中斷改為凍結續跑**：v1 的「中斷即取消」在手機上是玩家可主動觸發的無限重試後門。
> 10. **文案規模減半並補調性契約**：`name` 三版改為相同，台灣卡表 descope。

---

## 1. 目的與範圍

### 1.1 目的

現行踩線取材是一次原子呼叫：玩家按下按鈕，結果 100% 固定（`SPEC_MVP_POI_GATHERING.md` REQ-M3-03）。玩家對單一景點的唯一決策是「按或不按」，取材過程本身沒有任何玩家表達空間。

本里程碑在該原子呼叫**之前**插入一段即時操作（快門 QTE），其成績分為三態，決定入袋素材的版本。

### 1.2 設計意圖（施工時不得順手改掉）

以下四條是本案價值的所在，SPEC 明文記載以免在施工或後續重構中被抹平：

1. **技巧軸與編排軸的免費咬合**：`perfect` 只加 `hypeValue`，而黃昏槽位乘相機倍率。一張 `perfect` 一般卡放黃昏槽（相機 Lv3）值 +26 Hype，放其他槽只值 +12。**「把完美的那張留給黃昏、把失手的那張藏進晨曦」因此成為一個新的編排決策**，且是零成本自然長出來的，不是額外設計。由 AC-M5-9.1 保護。

2. **`isSpotlight` 在三態間凍結**：`failed` 的絕景**仍計入絕景階梯**。絕景階梯是網紅端最大的單一槓桿（因果 SPEC 消融實測 mean|Δ| = 17.44），快門失手不奪走這個槓桿是刻意的減震 —— 手殘不該讓整個行程崩盤。由 AC-M5-2.2 保護。

3. **失手後可以走去下一個 POI，是本案的壓力閥**：16 個可達 POI 對 6 格腰包，失手一張不是絕路，代價是 HP 與時間。這比任何重試按鈕都健康。**不得以「防刷」為由堵掉。**

4. **技巧軸回饋到路線決策**：高熱度卡多為高風險（Amendment 01 附錄 A.2），採一張 risk 4 要 24 HP。玩家在「拿一張貴的」與「穩穩拿兩張便宜的」之間的取捨，因三態而多一層考量。

### 1.3 解閘依據

`SPEC_MVP_AMENDMENT_01.md` 曾裁定「先修數學，再做微動作 —— 在評分模型不可能失敗的前提下，微動作的三態判定驗證不出效果」。該增修已於 2026-09-12 全數執行並驗收（`MVP_AMENDMENT_01_VERIFICATION.md` §6，commit `0b9a30a`），閘門條件已清償。

**本 SPEC 的 AC-M5-9.2（分佈型驗收）是該裁定的直接延續**：只有存在性 AC（「perfect 時入袋 perfect 版」）證不出玩家感覺得到，必須以全母體分佈證明三態確實改變結算結果。

### 1.4 本 SPEC 涵蓋

1. 快門 QTE 的行為契約與三態判定（以時間定義，參數為毫秒）。
2. 三態素材版本契約：每張素材三個版本，差異限於 `hypeValue` 與 `description`。
3. 難度模式三檔，為 QTE 的唯一旋鈕。
4. 難度的單局鎖定與持久化。
5. 快門獎金（局外金幣）。
6. 與因果可視化系統的接線（第 15 個 `reasonCode`）。
7. 節奏預算上限。

### 1.5 本 SPEC 明確不涵蓋（Out of Scope）

- **Runner 衝刺微動作**：延後至里程碑 M6，屆時依標籤分派（`#拉車` / `#深夜` / `#高風險`），沿用本 SPEC 已驗證的三態框架。
- **單局事件溯源管線**：`CuratorEventType` 現有 4 種全為局外事件，單局狀態從未落入日誌。本里程碑不建立該管線（見 REQ-M5-06）。
- **社畜端計分模型的熱度不對稱**：見 §7 已知限制 1。屬客戶核心性格的變更，須另立 SPEC 與全母體搜尋。
- **台灣卡表的三態版本**：見 §7 已知限制 2。
- **`riskLevel` / `cameraLevel` 影響 QTE 難度**：v2 明確移除。
- **骨骼動畫與新動畫套件**：表現以 Flutter／Flame 內建能力實作（`SPEC_MVP_CAUSAL_FEEDBACK.md` v6 §5.3）。

---

## 2. 需求與行為契約

### REQ-M5-01 三態判定（時間定義） `[P0]`

1. **三態列舉**：`perfect` / `normal` / `failed`。完整且互斥，不存在第四種結果。

2. **判定以時間為準，不以半徑為準。** 收縮環是純表現。設 $t_{\text{吻合}}$ 為環與目標框吻合的時刻（自 QTE 開始起算）、$t_{\text{按下}}$ 為玩家放開／按下的時刻，定義**時間偏差**：

   $$\Delta t = |t_{\text{按下}} - t_{\text{吻合}}|$$

   判定：$\Delta t \le \frac{W_p}{2}$ → `perfect`；$\frac{W_p}{2} < \Delta t \le \frac{W_n}{2}$ → `normal`；否則 → `failed`。
   $W_p$ / $W_n$ 為完美窗／普通窗的**總寬度**（毫秒），見規則 4。

3. **逾時**：動畫全長結束仍未按下 → `failed`。逾時**優先於一切難度參數**，`觀光客` 亦不例外 —— 否則最低難度會退化為「不按也能拿普通版」，等同 §1.5 已排除的跳過機制。
   逾時在型別上是**輸入的一種**（`timedOut`），不是 $\Delta t$ 的極值。判定函式的輸入須為和型別（`pressed(Δt)` / `timedOut`），不得以 $\Delta t$ 的特殊值表示逾時。

4. **難度參數表**（初始值，以毫秒為調校單位）：

   | 難度 | 吻合時刻 $t_{\text{吻合}}$ | 完美窗 $W_p$ | 普通窗 $W_n$ | 動畫全長 |
   |---|---|---|---|---|
   | `tourist`（觀光客） | 1800 ms | 1040 ms | **不設上限** | 2600 ms |
   | `photographer`（攝影師） | 1100 ms | 240 ms | 720 ms | 1600 ms |
   | `decisiveMoment`（決定性瞬間） | 800 ms | 120 ms | 300 ms | 1200 ms |

   `tourist` 的 $W_n$ 不設上限 → 只要按下就不可能 `failed`（逾時除外）。

5. **完美窗硬下限**：任何難度的 $W_p \ge 120$ ms。行動裝置從實體觸碰到畫面更新的端到端延遲通常為 60~120 ms 且本身帶有數十毫秒抖動；低於此下限的判定是抽獎而非技巧，且無法透過練習改善。

6. **難度是 QTE 的唯一旋鈕。** `riskLevel` 與 `cameraLevel` 均不影響 $t_{\text{吻合}}$、$W_p$、$W_n$ 或動畫全長。
   **`riskLevel` 解耦的理由**：高熱度卡幾乎都是高風險（Amendment 01 附錄 A.2），而 `riskLevel` 已透過 `gatheringHpCost()` 與 Theme 疲勞兩條通道施加代價；第三條通道只會讓最有趣的卡最不好拿，並可能撐破 AC-A1-6.8a 的「五哲學最佳滿意度極差 ≤ 15 分」。

7. **時間來源**：$t_{\text{按下}}$ 與 $t_{\text{吻合}}$ 一律取自**單調時鐘**（CC-2 規則 3：任何「經過了多久」的判定必須使用單調時間），或由 Flame 的 `dt` 累加得出。**禁用 `DateTime.now()`。**

8. **取樣精度**：$\Delta t$ 必須由**輸入事件時戳**計算，不得以渲染幀時間近似。@60 fps 的單幀量化誤差為 16.7 ms，佔 `decisiveMoment` 完美窗 120 ms 的 14%，且在 60 Hz 與 120 Hz 裝置上結果不同。

9. **判定為純函式**：給定難度與輸入（`pressed(Δt)` 或 `timedOut`），結果唯一確定。不得依賴當下時間、亂數或外部狀態（CC-3）。

---

### REQ-M5-02 三態素材版本契約 `[P0]`

1. **每張素材具備三個手寫版本**。本里程碑範圍為京都卡表 **32 張 → 96 張**（台灣 descope，見 §7 已知限制 2）。

2. **版本間的差異僅限兩項**：`hypeValue`、與 `description` 的敘事文案。

3. **`name` 三版必須相同。** `八坂之塔清晨藍調時刻` 是一個**地點**，三個名字等於同一地點有三個名字，會破壞玩家在腰包抽屜、4 槽位卡面、結算歸因三處的卡片辨識 —— 而因果 SPEC 的歸因設計（元兇槽位、疲勞時段對）高度依賴玩家認得出卡。三態改由 UI 狀態徽章承載。

4. **以下欄位在三個版本間必須完全相同**：`name`、`themeValue`、`storyValue`、`cost`、`riskLevel`、`isSpotlight`、`tags`。
   **`themeValue` 凍結的理由**：它決定哲學契合與主題一致性，是玩家在夜間編排時的策略軸。若白天的操作失誤改變素材的主題屬性，等同以操作懲罰策略規劃，兩條軸會糊在一起。
   **`isSpotlight` 凍結的理由**：見 §1.2 設計意圖 2。
   **`cost` / `riskLevel` 凍結的附帶好處**：資格判定與代價預告完全不需要知道三態，`evaluateAttractionEligibility` 與 `nearestGatherablePoi` 零改動。

5. **`hypeValue` 幅度為固定增減量**（非比例），初始值：

   | 素材類別 | `perfect` | `normal` | `failed` |
   |---|---|---|---|
   | 一般（`isSpotlight == false`） | $H + 12$ | $H$ | $H - 12$ |
   | 絕景（`isSpotlight == true`） | $H + 20$ | $H$ | $H - 20$ |

   $H$ 為現行卡表既有值，即 `normal` 版沿用現值。

6. **幅度不由推導定案，由 AC-M5-9.2 的分佈搜尋反推。** 上表是搜尋的起點而非結論。
   （v1 曾以「±12 對應滿意度 ±8 分」論證，該推導有三處錯誤：拿網紅公式論證社畜門檻、漏掉絕景階梯 `[0.70, 0.75, 0.80, 0.85, 1.00]`、使用了過時的 ThemeFactor 形式。現行公式為 `effectiveHype = round(netHype × spotlightLadder[min(count,4)])`、`themeFactor = (floor + (100-floor) × theme/100)/100`、`rawScore = effectiveHype / client.targetHype × 100 × themeFactor`，其後 `.clamp(0, 100)`。）

7. **`hypeValue` 下限**：任一版本不得為負。現行卡表最低為 20。

8. **成本不受三態影響**：體力代價一律由 `gatheringHpCost(material)` 決定（現行為 `riskLevel × 6`，Amendment 01 D8 裁定），`Budget -= material.cost`，`failed` 亦照常扣除。
   **本 SPEC 一律以函式名引用成本，不複製算式** —— v1 寫死了已廢除的 `10 + riskLevel × 2`，照其字面實作的 AC 會當場失敗。

9. **三態版本的存放形狀**：三版本以 `(poiId, 三態)` 的變體表存放，**不進入既有的扁平卡表**。三版本**共用同一個 `id`**，版本識別以獨立的三態值表示。
   **理由**：`curatorMaterialPoolProvider`（`main.dart` 注入 `kyotoNightMaterials`）、`kyoto_night_layout.dart` 的 `materialById` 與地圖景點生成、`auditMaterialCatalog()` 的比例判定，皆以扁平卡表為分母；塞進 123 筆會讓地圖長出三倍景點並使既有測試轉紅。共用 `id` 則保住 `TimelineItinerary.operator ==`（只比對 `s?.id`）的相等語意。

10. **`PoiMaterialResolver` 保持原簽章**：`resolveMaterialFor(String poiId)` 繼續回傳 `normal` 版，供資格判定與代價預告使用；三態解析另加一個帶三態參數的方法。既有測試 Fake 不需改寫。

---

### REQ-M5-03 絕景快門：單一手勢的構圖＋時機 `[P0]`

1. **適用範圍**：`isSpotlight == true` 的素材。京都現行 **5 張**。

2. **單一手勢**：**按住** → 取景框跟隨手指移動（構圖），收縮環**同時**開始收縮；**放開即快門**。構圖與時機在同一次互動中完成，不設獨立的確認 tap。
   （v1 的兩段式設計實測估 5~15 秒／張，一局四張絕景吃掉 40~60 秒野外時間，且發生在一個會暫停 Flame 引擎的彈窗內。）

3. **構圖偏差** $d_c$：放開瞬間取景框中心與標的中心的距離，以標的半徑正規化。

4. **QTE 期間視口鎖定**：進入 QTE 時接管視口並以**固定 zoom** 呈現，世界座標與螢幕座標於該期間重合。
   **理由**：大世界有雙指縮放，以螢幕座標正規化可被「放到最大再對準」直接操弄，而那不是有趣的技巧；以世界座標正規化則與視覺不符。固定 zoom 使兩難消失，並讓構圖難度在所有裝置與所有縮放狀態下一致、可測。

5. **最終三態取兩項中較差者**：構圖依 $d_c$ 判出 $t_c$、快門依 $\Delta t$ 判出 $t_s$，最終結果為兩者中較差的一態（`failed` > `normal` > `perfect`）。

6. 構圖的判定門檻隨難度縮放，與 $W_p$ / $W_n$ 同表管理。

---

### REQ-M5-04 不可跳過、不可重試；中斷採凍結 `[P0]`

1. 進入 QTE 後**無跳過入口**，亦**不提供重試**。單次判定即為最終結果。

2. **中斷採凍結續跑，不採取消。** App 進入非 `resumed` 生命週期狀態時，QTE 暫停並保留當下進度；回到前景後以 3 秒倒數重新進入，自暫停點續跑剩餘時間。
   **理由**：v1 的「中斷即取消、零副作用」在手機上是玩家可主動觸發的後門 —— 上滑切換 App 不到半秒，準心眼看要超出普通帶時上滑一次即可零代價重來，次數無限。且 Flutter 端無法區分來電與手動切換（皆為 `AppLifecycleState.paused`）。

3. **App 遭系統終止**：單局狀態全在記憶體（見 REQ-M5-06 規則 1），整局隨之遺失。此為既有行為，非本 SPEC 引入，不需額外規則。

4. **無障礙由難度承接，且難度必須縮放時間而非僅縮放窗寬。**
   v1 宣稱最低難度承接無障礙，但其收縮時長不受難度影響，高風險 POI 只有 1.00 秒絕對反應窗 —— **`觀光客` 保障的是「按得準」，無障礙的門檻是「按得到」，兩者不是同一件事。** 故 REQ-M5-01 規則 4 的表中，動畫全長與吻合時刻皆隨難度縮放（`tourist` 2600 ms vs `decisiveMoment` 1200 ms）。

---

### REQ-M5-05 難度模式、單局鎖定與快門獎金 `[P0]`

1. **三檔難度**：`tourist`（觀光客）／`photographer`（攝影師）／`decisiveMoment`（決定性瞬間）。
   **命名理由**：採攝影師語彙而非難度形容詞，貼合本作的相機主軸與策展人身分，且與城市無關 —— `修羅` 之類的日式佛教語彙貼的是京都 DLC 的味道，而難度屬通用引擎設定，接入其他城市 DLC 時會語感打架。

2. **選定時機**：與旅行哲學同一個行前畫面（`CuratorBriefingModal`）選定，不新增畫面。

3. **單局鎖定**：出發時寫入單局快照，比照 `equipmentSnapshot` 的鎖定語意。單局進行中不可變更。

4. **持久化**：最後一次選用的難度存入存檔，作為下一局預設值。
   **實作路徑**：`CuratorSaveData` **刻意不提供 `toJson` / `fromJson`**（該型別是事件日誌重播出的投影，不是持久化格式，CC-3），故持久化須經新增 `CuratorEventType` 達成，而非為該型別加欄位或升 schema 版本。前置條件見 §5.2。

5. **難度不影響局內任何數值**：局內計分、素材掉落、HP／預算消耗一律與難度無關。

6. **快門獎金（局外金幣）**：結算時依本局 `perfect` 張數發放局外 Coins，與客戶、與滿意度天花板皆無關。

   $$\text{快門獎金} = \text{perfect 張數} \times \text{單價} \times k_{\text{難度}}$$

   $k$ 初始值：`tourist` 0.3／`photographer` 1.0／`decisiveMoment` 2.0。單價初始值 60 金（參照黑市升級花費 500 / 2000）。

   **這條通道解決三個否則無解的缺口**：
   - 社畜端計分不含熱度項，滿意度對三態幾乎不動（§7 已知限制 1）
   - 網紅強行程滿意度撞 100 天花板，完美與全失手同分
   - 高難度否則零回報，理性玩家永不選

   它走的是本 SPEC 自己開的合法通道（只動局外金幣，不動局內計分），故不與 §4.2 衝突。
   **數值由 AC-M5-5.5 夾住**：三檔難度的期望快門獎金必須嚴格遞增 —— 低難度的完美率高得多，倍率若不夠低會造成「越簡單賺越多」的反轉。

---

### REQ-M5-06 事件與決定性 `[P0]`

1. **範圍縮小**：本里程碑**不建立單局事件溯源管線**。`CuratorEventType` 現有 4 種（`profileCreated` / `philosophyRerolled` / `equipmentUpgraded` / `runSettled`）全為局外事件；`gatheredPoiIds`、腰包、時間線從未落入日誌，`replayCuratorEvents()` 重播出的 `CuratorSaveData` 不承載任何單局狀態。
   （v1 的 AC「重播 100 次最終狀態一致」在現行重播目標下是空洞的 —— 該型別沒有任何欄位承載三態結果。）

2. **本里程碑的要求**：三態結果與難度須以**純資料形式**表達並可跨層傳遞，且**不含輸入時序**。
   **禁止出現於任何可持久化資料**：$t_{\text{按下}}$、$\Delta t$、取景框座標、$d_c$，或任何輸入時序資料。
   **理由是 CC-3（重播決定性）與「這些欄位沒有用途」，不是 CC-5。** CC-5 管的是原始 GPS 座標不離開裝置；時間偏差不是位置資料。寫成 CC-5 會讓後人誤以為那是隱私約束而不敢動。

3. **新增的 `difficultySelected` 事件**（REQ-M5-05.4）只記錄難度值，不記錄任何 QTE 過程資料。

4. **重播模組不得依賴 QTE 判定**：以靜態檢查驗證（見 AC-M5-6.3），不以注入間諜驗證 —— 後者會逼出一個只為測試存在的注入點，違反 `CLAUDE.md` §3「抽象只在擋住已知會變的軸時才做」。

---

### REQ-M5-07 取材流程接線 `[P0]`

1. **QTE 開始時鎖定目標**：進入 QTE 時鎖定 `poiId` 與該 POI 的三態素材集合；判定完成後若鎖定的 POI 已不可取材，該次取材取消。
   **理由**：`curatorRunController.gatherPoi()` 在**呼叫當下**以 `nearestGatherablePoi()` 重算最近可取材景點並覆蓋傳入的 `poiId`，且不像 `replaceGatheredPoi()` 那樣具備 `expectedPoiId` 防護。插入 QTE 後，「按下取材」與「實際呼叫 domain」之間多了 1.2~2.6 秒，期間位置持續更新，最近 POI 可以換人 —— 玩家對 A 景點做的 QTE，判定結果會被套用到 B 景點的素材上。施工時須一併把 `expectedPoiId` 防護補到 `gatherPoi()`。

2. **腰包滿額時，換牌抽屜開在 QTE 之前**：`GatheringEligibility == inventoryFull` 時，先開抽屜讓玩家決定要捨棄哪張（或放棄本次取材），確認後才進 QTE。
   **理由**：v1 的順序會讓玩家跑完 QTE 才發現腰包滿，一次辛苦拿到的 `perfect` 當場蒸發，且不可重試。

3. **QTE 期間的按鈕態**：沿用 REQ-M3-06 既有的 loading 防連點態，**不新增 `GatheringEligibility` 列舉值**（該列舉現為 6 態，含 `unavailable`）。

4. 既有前置檢查與異常語意不變：`PoiAlreadyGatheredException`、`CuratorExhaustedException`、`InventoryFullException`。

---

### REQ-M5-08 因果可視化接線 `[P0]`

1. **新增第 15 個 `reasonCode`**（如 `shutter_quality`），三態各有符號與 `CuratorCodex` 詞條。

2. **理由**：`SPEC_MVP_CAUSAL_FEEDBACK.md` §1.2 原則 1 要求「每一條 `CausalFact` 都對應一段已存在的計算」，AC-CF-2.2 要求全部 `reasonCode` 有詞條。本 SPEC 新增了一個影響 `totalHype` 數十點的來源；若不接線，玩家失手三張後在結算看到熱度不足，歸因面板會把原因指向「絕景缺口」或「熱度稍差」—— **玩家真正的失分原因在報告裡沒有坑位可放**。這正是因果 SPEC 立案要消滅的病灶原型。

3. **順序約束**：因果 SPEC 明文要求以最新程式碼為基線，故 **M5 必須排在因果可視化施工完成並合入主線之後**。

4. **符號語言統一**：野外浮字採與因果系統一致的序數符號，不使用 `+XX` / `-XX` 的裸數值 —— 因果 SPEC §3.1.3 刻意在編排期只給方向不給量級，兩處用兩套語言會讓玩家學兩次。

---

### REQ-M5-09 節奏預算 `[P0]`

1. 單次 QTE 的完整儀式（轉場進 ＋ 收縮 ＋ 浮字回饋 ＋ 轉場出）的**邏輯時間**須有上限，且單局全部 QTE 的邏輯時間總和不得超過野外段預算的 **15%**（野外段基準 240 秒，見 AC-A1-4.1）。

2. **理由**：一局的 QTE 次數下界為腰包容量 6，上界為可達 POI 數 16。以單次 3~5 秒估算，合計 20~80 秒，佔野外段 8~33%。33% 那端會把探索切碎，而**節奏是這個機制最可能失敗的地方**，v1 沒有任何一條 AC 保護它。

---

### REQ-M5-10 架構隔離 `[P0]`

1. QTE 的計時迴圈、輸入處理與動畫住在 `game/` 或 `ui/`；`domain/` 只接收已判定的三態結果。
2. 三態判定的數學與難度參數表屬 `domain/`，為純函式，以 `dart test` 可測。
3. 通用引擎不得出現具名城市；三態卡表落在 `lib/data/core_loop/`。
4. `FakePoiMaterialResolver` 須支援三態解析，維持秒級測試。
5. 資料流沿既有形狀：UI 取得素材 → 呼叫 `curatorRunController.gatherPoi()` → domain 原子轉移。QTE 只需在 UI/game 產出一個三態值，沿同一條路多帶一個參數。**state 層不新增抽象。**

---

### REQ-M5-11 取材表現層 `[P1]`

1. 三態須有可辨識的即時回饋，沿用 `GatheringFloatingFeedbackOverlay` 既有通道。
2. 三態必須在**不看數字的情況下**可區分（色彩 ＋ 符號 ＋ 動態）。
3. 限於 Flutter／Flame 內建動畫能力，不新增相依套件。

---

### REQ-M5-12 文案撰寫契約 `[P0]`

1. **規模**：京都 32 張 × 2 個新版本 = **64 段新 `description`**（`name` 三版相同，不需新撰）。

2. **語域不變**：三版都是同一位攝影者在現場的第一人稱觀察，不進行評價、不吐槽。
   **這條必須明寫**：題目雖是「毒舌策展人」，但**實際卡表的 `description` 完全不毒舌**，是抒情的旅行散文。毒舌住在客戶台詞（小林／安娜）與阿導旁白，不在卡面。只被告知「毒舌風格」的寫手會交出 64 段語域錯誤、與現存 32 段格格不入的稿。

3. **`perfect` = 多一個只有完美快門才捕捉得到的具體細節**（露水、一縷煙、一個剛好走進畫面的人影）。

4. **`failed` = 一個具體且好笑的入侵物或失誤，不是否定句。** 「沒拍好」「糊掉了」一律不合格。

5. **範例三連**（以現行卡 `祇園白川辰巳大明神夜櫻` 為基準）：

   | 版本 | `description` |
   |---|---|
   | `normal`（現行，不動） | 白川潺潺流水與朱紅小橋映照垂櫻，藝妓足音在石板路上迴盪。 |
   | `perfect` | 白川潺潺流水與朱紅小橋映照垂櫻，快門落下那一瞬，一片花瓣正好停在水面上還沒被沖走。 |
   | `failed` | 白川潺潺流水與朱紅小橋映照垂櫻，畫面正中央是一位舉著自拍棒、對你比讚的觀光客。 |

6. **機械護欄**（AC-M5-12）：三版長度差 ≤ 40%；`failed` / `perfect` 版不得包含 `normal` 版作為子字串。
   （僅要求「字串不相等」擋不住「（失手版）沒拍好的八坂之塔。」這種東西。）

---

## 3. 驗收條件（Acceptance Criteria）

### AC-M5-1 三態判定純函式

- **AC-M5-1.1** `photographer`：$\Delta t = 100$ ms → `perfect`（$W_p/2 = 120$）；$\Delta t = 200$ ms → `normal`；$\Delta t = 400$ ms → `failed`。
- **AC-M5-1.2** 三檔難度各自的 $W_p/2$ 邊界值（含 $\pm\epsilon$）判定符合 REQ-M5-01 規則 4 的表，邊界採閉區間；$W_n/2$ 的邊界僅適用於 `photographer` 與 `decisiveMoment`（`tourist` 的 $W_n$ 不設上限，由 AC-M5-1.3 涵蓋）。
- **AC-M5-1.3** `tourist` 下，任意 `pressed(Δt)` 皆不產生 `failed`。
- **AC-M5-1.4** 三檔難度的 $W_p \ge 120$ ms（REQ-M5-01 規則 5 的硬下限）。
- **AC-M5-1.5** `timedOut` 輸入在**三檔難度下皆**判 `failed`，含 `tourist`。
- **AC-M5-1.6** 判定函式的輸入為和型別：以 `timedOut` 呼叫不需提供 $\Delta t$，且不存在能以 $\Delta t$ 表達逾時的路徑。
- **AC-M5-1.7** 靜態檢查：判定模組原始碼不出現 `DateTime.now`、不 import `dart:math`。
- **AC-M5-1.8** `riskLevel` 1~5 與 `cameraLevel` 1~3 的任意組合，不改變 $t_{\text{吻合}}$、$W_p$、$W_n$ 或動畫全長（REQ-M5-01 規則 6 的解耦）。

### AC-M5-2 三態素材版本契約（全卡表機械驗證）

- **AC-M5-2.1** 遍歷**實際注入的素材池**（非硬編碼清單）：每個素材 id 皆存在三個版本，無缺漏、無多餘。
  （遍歷注入池而非硬編碼，使台灣卡表日後復活時缺版本會自動轉紅。）
- **AC-M5-2.2** 三版本的 `name`、`themeValue`、`storyValue`、`cost`、`riskLevel`、`isSpotlight`、`tags` 完全相等。
- **AC-M5-2.3** 一般素材三版本 `hypeValue` 差值恰為表定值；絕景素材同理。
- **AC-M5-2.4** 任一版本 `hypeValue` $\ge 0$。
- **AC-M5-2.5** 三版本共用同一 `id`；三態值為獨立欄位。
- **AC-M5-2.6** `normal` 版逐欄等於**版控內的 golden fixture**（施工起點序列化而得，存於 `test/fixtures/`）。測試不得讀取 git。
- **AC-M5-2.7** 既有扁平卡表的長度不變：`kyotoNightMaterials.length` 與地圖景點數維持 32，`auditMaterialCatalog()` 的比例判定分母不變。

### AC-M5-3 取材流程接線

- **AC-M5-3.1** 三態結果對應入袋的版本正確。
- **AC-M5-3.2** 三態皆不改變 `gatheringHpCost(material)` 的回傳值與實際扣減量。
- **AC-M5-3.3** `failed` 時 `Budget` 仍扣除 `material.cost`，`gatheredPoiIds` 仍加入 `poiId`。
- **AC-M5-3.4** 既有三個例外的觸發條件與時機不變。
- **AC-M5-3.5** QTE 開始時鎖定的 `poiId`，在判定完成後仍為實際套用的目標；以「QTE 期間移動玩家位置使最近 POI 改變」的情境驗證。
- **AC-M5-3.6** `inventoryFull` 時，換牌抽屜的開啟先於 QTE；抽屜中選「放棄」則不進入 QTE、不扣任何資源。
- **AC-M5-3.7** 中斷凍結：以 `TestWidgetsFlutterBinding.handleAppLifecycleStateChanged` 送出離開 `resumed` 再回到 `resumed`，QTE 剩餘時間與暫停前相等，且未產生任何資源變動。

### AC-M5-4 絕景單一手勢

- **AC-M5-4.1** `isSpotlight == true` 走構圖＋時機，`false` 走純時機。以注入池遍歷驗證分派（京都 5 張／27 張）。
- **AC-M5-4.2** 構圖態與時機態組合的 9 種真值表，皆產出兩者中較差者。
- **AC-M5-4.3** QTE 期間視口 zoom 固定：以不同初始 zoom 進入，構圖偏差 $d_c$ 的判定結果相同。

### AC-M5-5 難度、鎖定與快門獎金

- **AC-M5-5.1** 行為 AC：`fieldTrip` / `nightEditing` 期間任何變更難度的路徑，皆不改變單局快照的難度值。
- **AC-M5-5.2** 靜態檢查：比照 `layer_boundaries_test.dart` 的字串掃描，單局期間不存在難度變更入口。
  （Dart 無反射，「方法不存在」測不出來；M4 面對同樣問題時亦採行為 AC。）
- **AC-M5-5.3** 難度經 `difficultySelected` 事件持久化，重播後為下一局預設值。
- **AC-M5-5.4** 三檔難度下，同一條路線與同一組三態序列產出的**局內**分數完全相等。
- **AC-M5-5.5** 三檔難度的**期望快門獎金嚴格遞增**（以各難度的完美率乘上倍率計算）。

### AC-M5-6 決定性與資料純度

- **AC-M5-6.1** 欄位白名單：任何可持久化資料不含 $t_{\text{按下}}$、$\Delta t$、$d_c$、取景框座標或任何輸入時序。
- **AC-M5-6.2** 三態結果與難度可以純資料形式跨層傳遞，且該資料不含 `dart:ui` 或框架型別。
- **AC-M5-6.3** 靜態檢查：重播模組的原始碼不 import 三態判定模組。

### AC-M5-7 架構隔離

- **AC-M5-7.1** `domain/` 的判定與參數表不 import `package:flutter`、`package:flame`、`dart:ui`。
- **AC-M5-7.2** 判定與版本解析的全部測試以 `dart test` 通過。
- **AC-M5-7.3** `FakePoiMaterialResolver` 可解析三態，全套 domain 測試通過。
- **AC-M5-7.4** 既有三條架構測試維持全綠。
- **AC-M5-7.5** `resolveMaterialFor(String poiId)` 簽章未變，既有測試 Fake 未經改寫即通過。

### AC-M5-8 因果接線

- **AC-M5-8.1** 新 `reasonCode` 三態皆有 `CuratorCodex` 詞條（`title` / `explanation` / `guideNote`）。
- **AC-M5-8.2** 全程 `failed` 的行程，因果報告中**存在**指向快門的 `CausalFact`；且該情境下 `primaryCulpritSlot` 的歸因不指向絕景缺口。
- **AC-M5-8.3** 因果 SPEC 既有的「每個 `reasonCode` 在 UI 皆有坑位」守門測試維持全綠。

### AC-M5-9 效果可驗證性（本 SPEC 的核心驗收）

- **AC-M5-9.1** 設計意圖保護：同一張 `perfect` 一般卡放黃昏槽與放其他槽，對 `totalHype` 的貢獻差值 > 單純 `perfect` 增量（證明 §1.2 意圖 1 的軸間互動存在）。
- **AC-M5-9.2** **分佈型驗收**：以當局可達素材池窮舉全部合法行程 × 5 哲學 × 2 客戶為母體，統計三態滿意度：
  - 三態**兩兩相異**的行程佔比 $\ge X\%$
  - 三態**完全同分**的行程佔比 $\le Y\%$
  - 三態滿意度極差的**上界** $\le Z$ 分（防止技巧軸壓過所有編排規則）

  $X$ / $Y$ / $Z$ 與 REQ-M5-02.5 的幅度一併由搜尋結果定案，寫回本 SPEC。
- **AC-M5-9.3** 回歸護欄：引入三態後，AC-A1-6.8a 的「五哲學最佳滿意度極差 ≤ 15 分」仍成立。

### AC-M5-10 節奏

- **AC-M5-10.1** 單次 QTE 儀式的邏輯時間不超過上限。
- **AC-M5-10.2** 以 AC-A1-4.1 的 240 秒基準路徑模擬，全部 QTE 的邏輯時間總和 $\le$ 36 秒（15%）。

### AC-M5-11 表現層

- **AC-M5-11.1** 三態各自觸發可區分的 Overlay 回饋（以 `Key` 驗證），`IgnorePointer` 不阻塞點擊。
- **AC-M5-11.2** `pubspec.yaml` 的相依清單在本里程碑前後無新增項目。

### AC-M5-12 文案護欄

- **AC-M5-12.1** 三版 `description` 兩兩相異。
- **AC-M5-12.2** 三版 `description` 長度差 $\le$ 40%。
- **AC-M5-12.3** `failed` / `perfect` 版不包含 `normal` 版作為子字串。

---

## 4. 邊界約束與非目標

1. **純領域零依賴**：判定與參數表屬 `domain/`，嚴禁 import Flutter / Flame / `dart:ui`。
2. **不改動既有計分規則**：本 SPEC 不調整 `ClientReviewEngine` 的任何公式或係數。技巧軸的回饋缺口改由快門獎金（局外金幣）承接。
3. **不改動成本語意**：`gatheringHpCost()` 與 `material.cost` 的扣減語意凍結。
4. **不引入新套件**。
5. **不新增抽象**：三態解析併入既有 `PoiMaterialResolver` 職責；state 層不新增抽象；不為測試開注入點。
6. **凍結資產不動**：SPEC C 的 GPS 公尺契約、`AC-18.3`、`OverworldMapManifest` 介面一律不改。
7. **跨文件引用一律引用來源位置，不複製數字**，且須於 plan 階段逐條對程式碼複驗。

---

## 5. 前置條件（施工前必須清償）

1. **因果可視化施工完成並合入主線**（REQ-M5-08 規則 3）。
2. **事件日誌的前向相容修復（獨立 bugfix，不併入 M5）**：
   `curator_event.dart` 遇未知事件型別 `throw FormatException`，而 `local_persistence_repository.dart` 接到解析失敗會將整份日誌搬至 `.bak` 並回傳空，`loadSave` 於是重建全新身分 —— **玩家金幣與裝備歸零**。只要有較舊的建置或一次回滾讀到含新事件型別的日誌即觸發。
   M5 要新增 `difficultySelected` 事件，會首次踩到這顆地雷。須先改為「重播對未知事件型別採忽略而非拋出」。**此為資料遺失類問題，應以獨立 commit 與獨立測試修復。**
3. **`SPEC_MVP_POI_GATHERING.md` §1.3 增修**：該節將快門 QTE 列為 Out of Scope，須註記由本 SPEC 承接。
4. **`SPEC_MVP_CORE_LOOP.md` 非涵蓋範圍表增修**：微動作現標註為「引擎層 / 里程碑 M3」，實際落於 M5。
5. **64 段 `description` 文案交付**：須依 REQ-M5-12 的契約撰寫並通過 AC-M5-12。**此為內容產出工作，會序列阻塞施工**，須確認執行者與交期。
6. **`gatherPoi()` 補 `expectedPoiId` 防護**（REQ-M5-07 規則 1）。

---

## 6. 待決問題

1. **難度參數的實機調校**：$t_{\text{吻合}}$ / $W_p$ / $W_n$ / 動畫全長的初始值須由使用者實機驗證手感（依既有分工：邏輯由測試驗，手感與實機由使用者驗）。調校後的定值回寫 REQ-M5-01 規則 4。
   **特別待驗**：`decisiveMoment` 的 $W_p = 120$ ms 是否為「難但可練」而非「仍在抽獎」。觸控延遲抖動使這件事測試驗不出來。
2. **快門獎金的單價與倍率**：初始值 60 金 × (0.3 / 1.0 / 2.0) 須以 AC-M5-5.5 夾住後，再對照黑市升級節奏（500 / 2000）調校。
3. **AC-M5-9.2 的 $X$ / $Y$ / $Z$ 門檻**：須先跑一次全母體搜尋取得現況分佈，才能訂出有意義的門檻。此工作應排在 plan 階段之前。
4. **絕景構圖的判定門檻**：REQ-M5-03 規則 6 說隨難度縮放，但具體門檻值未定，須與規則 4 同表補齊。

---

## 7. 已知限制（明文記載，不在本里程碑解決）

1. **社畜端的熱度不對稱**：`_evaluateBudgetWorker` 的滿意度為 `budgetScore + themeScore − boredomPenalty`，**不含熱度項**；`totalHype` 僅透過 `< boredomThreshold(168)` 扣 25 的二元門檻進入。反無聊實測觸發率為 24.70%，故**約四分之三的社畜局，三態對滿意度一分不動**，而客戶為開局隨機指派。
   本 SPEC 以快門獎金（局外金幣）繞過此缺口，玩家仍有技巧回饋，但「拍得好而小林無感」在敘事上仍是破口。
   **根因不是滿意度天花板，而是社畜的計分模型從一開始就不看熱度** —— 那是客戶核心性格（不在乎熱鬧、在乎省錢與不無聊）的一部分。要修須給社畜一個對熱度敏感的項，屬計分模型變更，應另立 SPEC 並沿用 Amendment 01 的全母體搜尋方法論。

2. **台灣卡表 descope**：`main.dart` 僅注入 `kyotoNightMaterials`；`taiwan_attraction_materials.dart` 掛在未上線的台灣 resolver 上，且 Amendment 01 已裁定「台灣真實地圖原地封存，留待 Reality Run 階段」。本里程碑規模因此為 32 → 96 張、64 段文案。
   AC-M5-2.1 遍歷實際注入池而非硬編碼清單，故台灣日後復活時缺三態版本會自動轉紅 —— 比在文件中加註記可靠。

3. **內容成本的長期承諾**：即使砍掉 `name` 並 descope 台灣，**每個城市 DLC 的文案量從 32 段變為 96 段**。以現行卡表密度（每段 35~45 字、地點專屬、感官具體）估，一段含修潤約 15~20 分鐘，**每城約 24~32 小時純寫作**，且序列阻塞該 DLC 的上線。
   退路（若 DLC 節奏吃緊）：只手寫 `failed` 版，`perfect` 版改用共用的點睛句庫 —— `failed` 是最需要變化的那版。**不得三版皆由句庫組合**：本機制的樂趣就在失手時那句具體又好笑的描述，那裡罐頭化則整個 M5 失去存在意義。

---

## 8. 設計裁決紀錄

### 8.1 v1 裁決（2026-09-12，使用者逐題裁決，v2 維持有效）

| 議題 | 裁決 |
|---|---|
| 三態影響哪一側 | **產出側**（素材品質），成本固定 |
| 更好的卡怎麼來 | **每張卡手寫三版本** |
| Runner 與 Snapshot 分工 | 本里程碑**只做快門**，Runner 延後 M6 |
| 快門玩法 | **收縮準心為主**，絕景加上構圖 |
| 數值幅度 | **固定增減量**，只動 `hypeValue` |
| 跳過與重試 | **皆不可**，無障礙由難度承接 |
| 難度影響面 | **不連動局內獎勵** |
| 難度選定時機 | 行前畫面與哲學同時選，寫入單局快照 |

### 8.2 v2 裁決（2026-09-12，雙軌覆核後）

| 議題 | 裁決 | 落點 |
|---|---|---|
| 三態可驗證性 | **加快門獎金（局外金幣）**，不動計分引擎 | REQ-M5-05.6 |
| 社畜熱度不對稱 | **記為已知限制**，另立 SPEC 解決 | §7 限制 1 |
| 事件範圍 | **縮小** —— 不建單局事件溯源管線 | REQ-M5-06.1 |
| 事件前向相容地雷 | **獨立 bugfix 先修**，不併入 M5 | §5.2 |
| 判定定義 | 半徑相對偏差 → **時間偏差，參數以毫秒表達** | REQ-M5-01.2 |
| QTE 難度旋鈕 | `riskLevel` / `cameraLevel` **全部解耦**，難度為唯一旋鈕 | REQ-M5-01.6 |
| 幅度數值 | 不由推導定案，**由分佈型 AC 反推** | AC-M5-9.2 |
| 絕景互動 | 兩段式 → **單一手勢**（按住取景、放開快門） | REQ-M5-03.2 |
| 中斷處理 | 取消 → **凍結續跑** | REQ-M5-04.2 |
| 無障礙 | 難度須**同時縮放時間**，非僅縮放窗寬 | REQ-M5-04.4 |
| 因果接線 | **新增第 15 個 `reasonCode`**，M5 排在因果之後 | REQ-M5-08 |
| 換牌順序 | 抽屜**開在 QTE 之前** | REQ-M5-07.2 |
| `name` 三版 | 相異 → **相同**，三態由 UI 徽章承載 | REQ-M5-02.3 |
| 難度命名 | `漫步/一般/修羅` → **`觀光客/攝影師/決定性瞬間`** | REQ-M5-05.1 |
| 台灣卡表 | **descope** | §7 限制 2 |
| 節奏 | **新增預算上限與 AC** | REQ-M5-09 |
