# SPEC — 《Share Tour：奇葩旅行策展人》Milestone M5：快門微動作與三態取材

狀態：**Draft v1 — 待覆核**
流程位置：`spec → 覆核 → plan → 覆核 → 執行計劃 → 覆核`
上位文件：`CROSS_CUTTING_CONSTRAINTS.md`、`CLAUDE.md`、`SPEC_MVP_CORE_LOOP.md`（牴觸時以其為準）
相關文件：`SPEC_MVP_POI_GATHERING.md`（本 SPEC 解除其 §1.3 的「微動作」排除條款）、`SPEC_MVP_META_PROGRESSION.md`（相機等級、單局快照）、`SPEC_MVP_AMENDMENT_01.md`（數值包絡）

> **命名澄清**：本文件的「M5」指**里程碑 M5**。`PLAN_MVP_META_PROGRESSION.md` 內的 `Task M5` 是 M4 施工計劃的任務編號，兩者無關。

---

## 1. 目的與範圍

### 1.1 目的

現行踩線取材是一次原子呼叫：玩家按下按鈕，結果 100% 固定（`SPEC_MVP_POI_GATHERING.md` REQ-M3-03）。玩家對單一景點的唯一決策是「按或不按」，取材過程本身沒有任何玩家表達空間。

本里程碑在該原子呼叫**之前**插入一段即時操作（快門 QTE），其成績分為三態，決定入袋素材的版本。技巧因此成為一條獨立於路線規劃與時間線編排的表達軸。

### 1.2 解閘依據

`SPEC_MVP_AMENDMENT_01.md` 曾裁定「先修數學，再做微動作 —— 在評分模型不可能失敗的前提下，微動作的三態判定驗證不出效果」。該增修已於 2026-09-12 全數執行並驗收（`MVP_AMENDMENT_01_VERIFICATION.md` §6，commit `0b9a30a`），閘門條件已清償。

### 1.3 本 SPEC 涵蓋

1. **快門 QTE 的行為契約與三態判定**：一般素材採收縮準心（單段），絕景素材採構圖＋快門（兩段）。
2. **三態素材版本契約**：每張素材具備完美／普通／失敗三個手寫版本，差異僅限 `hypeValue` 與敘事文案。
3. **難度模式**：`漫步` / `一般` / `修羅` 三檔，僅影響判定寬鬆度，不影響任何獎勵。
4. **難度的單局鎖定與持久化**：出發時選定，寫入單局快照，中途不可變更。
5. **既有欄位的再利用**：`riskLevel` 決定收縮速度，`cameraLevel` 決定完美判定帶寬度。
6. **決定性重播契約**：事件日誌記錄三態結果與難度，不記錄玩家輸入時序。

### 1.4 本 SPEC 明確不涵蓋（Out of Scope）

- **Runner 衝刺微動作**：延後至里程碑 M6。屆時依標籤分派（`#拉車` / `#深夜` / `#高風險` 走 Runner，其餘走快門），沿用本 SPEC 已驗證的三態框架。
- **難度連動獎勵**：`修羅` 不額外給局內分數或局外金幣。若日後要加，走獨立增修，且只允許動局外金幣（不得動局內計分）。
- **QTE 重試與跳過**：本 SPEC 明定不可跳過、不可重試（§2 REQ-M5-04）。無障礙需求由 `漫步` 難度承接。
- **三態影響 `themeValue` / `storyValue` / `cost` / `riskLevel`**：一律不動（§2 REQ-M5-02 規則 3）。
- **骨骼動畫與新動畫套件**：表現以 Flutter 內建 `AnimationController` / `Tween` 與 Flame 內建 `Effect` / `ParticleSystemComponent` 實作（`SPEC_MVP_CAUSAL_FEEDBACK.md` v6 §5.3）。

---

## 2. 需求與行為契約

### REQ-M5-01 三態判定與難度參數 `[P0]`

1. **三態列舉**：取材成績為三態之一 —— `perfect`（完美）／`normal`（普通）／`failed`（失敗）。三態為完整且互斥的分類，不存在第四種結果。

2. **收縮準心判定（一般素材）**：外圈自起始半徑朝目標半徑收縮，玩家於任意時刻按下一次。設 $r$ 為按下瞬間的外圈半徑、$r_t$ 為目標半徑，定義**相對偏差**：

   $$d = \frac{|r - r_t|}{r_t}$$

   判定：$d \le p$ → `perfect`；$p < d \le n$ → `normal`；$d > n$ → `failed`。

   **收縮終點與逾時**：外圈自 $2 r_t$ 起收縮，通過 $r_t$ 後繼續收縮至 $0$；玩家全程未按下即為**逾時**。逾時一律判 `failed`，此條款**優先於難度參數**，`stroll` 亦不例外 —— 否則漫步難度會退化為「不按也能拿普通版」，等同 §1.4 已排除的跳過機制。

3. **難度參數表**（初始值，調參後以本表為準）：

   | 難度 | `p`（完美帶） | `n`（普通帶） | 說明 |
   |---|---|---|---|
   | `stroll`（漫步） | 0.40 | **1.00** | $n = 1.00$ 使 `failed` 在單段判定中不可能發生 |
   | `standard`（一般） | 0.15 | 0.45 | 基準 |
   | `shura`（修羅） | 0.05 | 0.20 | |

4. **收縮時長由 `riskLevel` 決定**：

   $$T_{\text{收縮}} = 1.60 - 0.12 \times \text{riskLevel} \quad (\text{秒})$$

   `riskLevel` 1 → 1.48 秒；`riskLevel` 5 → 1.00 秒。時長僅影響反應窗口，不改變 $p$ / $n$。

5. **`cameraLevel` 加寬完美帶**：

   $$p_{\text{有效}} = p \times \bigl(1 + 0.10 \times (\text{cameraLevel} - 1)\bigr)$$

   `cameraLevel` 1 為基準值。黑市升級相機因此同時提升黃昏槽位倍率（既有效果）與快門容錯（本 SPEC 新增效果）。
   $p_{\text{有效}}$ 不得超過 $n$；超過時取 $n$。

6. **判定為純函式**：給定難度、`riskLevel`、`cameraLevel` 與相對偏差 $d$，三態結果唯一確定。不得依賴當下時間、亂數或外部狀態（CC-3）。

---

### REQ-M5-02 三態素材版本契約 `[P0]`

1. **每張素材具備三個手寫版本**。現行卡表 41 張（京都 32、台灣 9），完成後為 **123 張**。

2. **版本間的差異僅限兩項**：`hypeValue`、以及 `name` 與 `description` 的敘事文案。

3. **以下欄位在三個版本間必須完全相同**：`themeValue`、`storyValue`、`cost`、`riskLevel`、`isSpotlight`、`tags`。
   **理由**：`themeValue` 決定哲學契合與主題一致性，是玩家在夜間編排時的策略軸。若白天的操作失誤改變素材的主題屬性，等同以操作懲罰策略規劃，兩條軸會糊在一起。`cost` / `riskLevel` 同理歸屬成本軸，不受操作影響。

4. **`hypeValue` 幅度為固定增減量**（非比例）：

   | 素材類別 | `perfect` | `normal` | `failed` |
   |---|---|---|---|
   | 一般（`isSpotlight == false`） | $H + 12$ | $H$ | $H - 12$ |
   | 絕景（`isSpotlight == true`） | $H + 20$ | $H$ | $H - 20$ |

   $H$ 為現行卡表既有的 `hypeValue`，即 `normal` 版沿用現值，現有平衡在普通態下完全不變。

5. **幅度依據**：網紅端滿意度為 $\min(100, \text{round}(\frac{\text{EffectiveHype}}{150} \times 100 \times \text{ThemeFactor}))$，Hype 變動 12 約對應滿意度 ±8 分，跨得過社畜退件線 60 與普通線 70，確保三態在結算畫面上可辨識。

6. **`hypeValue` 下限**：任一版本的 `hypeValue` 不得為負。現行卡表最低 `hypeValue` 為 20，扣 12 後為 8，無違反；新增素材須自行滿足。

7. **成本不受三態影響**：`ΔHP = 10 + riskLevel × 2` 與 `Budget -= material.cost` 維持 `SPEC_MVP_POI_GATHERING.md` REQ-M3-03 原契約，`failed` 亦照常扣除。

---

### REQ-M5-03 絕景兩段式快門 `[P0]`

1. **適用範圍**：`isSpotlight == true` 的素材（現行 8 張：京都 6、台灣 2）。

2. **第一段（構圖）**：玩家拖曳取景框對準景點標的，產生**構圖偏差** $d_c$（取景框中心與標的中心的距離，以標的半徑正規化）。玩家自行決定何時確認，此段無時間壓力。

3. **第二段（快門）**：確認構圖後進入收縮準心，規則同 REQ-M5-01，產生**快門偏差** $d_s$。

4. **最終三態取兩段中較差者**：先各自依 REQ-M5-01 規則 3 的 $p$ / $n$ 判出 $t_c$ 與 $t_s$，最終結果為兩者中較差的一態（`failed` > `normal` > `perfect`）。

5. **理由**：絕景在評分模型中本就是「階梯 + 第 4 格暴擊」的特殊存在（`SPEC_MVP_CAUSAL_FEEDBACK.md` §3.1 冠狀星芒 pip）。讓它同時是唯一需要兩段操作的素材，機制與敘事一致；且僅 8 張，節奏影響可控。

---

### REQ-M5-04 不可跳過、不可重試 `[P0]`

1. 進入 QTE 後**無跳過入口**，亦**不提供重試**。單次判定即為最終結果。
2. 無障礙與難度需求一律由 `stroll`（漫步）難度承接 —— 該檔 $n = 1.00$，**只要玩家按下**，單段判定不可能落入 `failed`。逾時（全程未按下）不在此保障範圍內，見 REQ-M5-01 規則 2。
3. **絕景例外的明確後果**：`stroll` 下兩段式仍可能因構圖段落入 `normal`，但不可能 `failed`（兩段的 $n$ 皆為 1.00）。
4. 中斷處理：QTE 進行中若遭系統級中斷（來電、切換 App、鎖屏），該次取材**整體取消**，不扣 HP、不扣預算、`gatheredPoiIds` 不記錄，景點維持未踩線。

---

### REQ-M5-05 難度模式的選定與單局鎖定 `[P0]`

1. **選定時機**：與旅行哲學同一個行前畫面（`CuratorBriefingModal`）選定。不新增畫面。
2. **單局鎖定**：出發時寫入單局快照，比照 `equipmentSnapshot` 的鎖定語意（`SPEC_MVP_META_PROGRESSION.md` §4）。單局進行中不可變更，杜絕「難點先拿獎勵、卡關再降難度」。
3. **持久化**：最後一次選用的難度存入存檔，作為下一局的預設值。
4. **難度不影響任何獎勵**：局內計分、局外金幣、素材掉落、HP／預算消耗一律與難度無關。

---

### REQ-M5-06 決定性重播契約 `[P0]`

1. 事件日誌記錄的取材事件必須包含：`poiId`、三態結果、當局難度、素材版本識別。
2. **禁止記錄**：玩家的按壓時間點、相對偏差 $d$、取景框座標、任何輸入時序資料。
3. 重播時直接套用已記錄的三態結果解析素材版本，**不得重跑 QTE 判定**。
4. 此契約同時滿足 CC-3（重播決定性）與 CC-5（原始輸入不離開裝置的一致性）。

---

### REQ-M5-07 架構隔離 `[P0]`

1. **QTE 的計時迴圈、輸入處理與動畫住在 `game/` 或 `ui/`**，`domain/` 只接收已判定的三態結果。
2. **三態判定的數學（REQ-M5-01 規則 2~5）屬 `domain/`**，為純函式，以 `dart test` 可測，不需模擬器。
3. **難度參數表屬 `domain/`**，不得硬編碼於 UI。
4. 三態素材版本的解析屬既有 `PoiMaterialResolver` 的職責延伸，通用引擎**不得出現具名城市**（`CLAUDE.md` §9 架構測試第 2 條）。
5. `FakePoiMaterialResolver` 須支援三態解析，維持秒級測試。

---

### REQ-M5-08 取材表現層 `[P1]`

1. 三態結果須有可辨識的即時回饋，沿用 `GatheringFloatingFeedbackOverlay` 既有通道（`SPEC_MVP_POI_GATHERING.md` REQ-M3-06）：
   - `perfect`：金色浮字 + 完美標籤，數值以 `+XX` 呈現增量
   - `normal`：既有的金色／藍色浮字
   - `failed`：灰色浮字 + 失敗標籤，數值以 `-XX` 呈現減量
2. 三態必須在**不看數字的情況下**可區分（色彩 + 標籤 + 動態），不得僅靠數值差異傳達。
3. 實作限於 Flutter／Flame 內建動畫能力，不新增相依套件。

---

## 3. 驗收條件（Acceptance Criteria）

### AC-M5-1 三態判定純函式

- **AC-M5-1.1** `standard` 難度、`cameraLevel` 1：$d = 0.10$ → `perfect`；$d = 0.30$ → `normal`；$d = 0.50$ → `failed`。
- **AC-M5-1.2** 三檔難度各自的 $p$ / $n$ 邊界值（$d = p$、$d = p + \epsilon$、$d = n$、$d = n + \epsilon$）判定符合 REQ-M5-01 規則 3 的表，邊界採閉區間（$\le$）。
- **AC-M5-1.3** `stroll` 難度下，遍歷 $d \in [0, 1.00]$ 的取樣不產生任何 `failed`。
- **AC-M5-1.4** `cameraLevel` 自 1 遞增至上限，$p_{\text{有效}}$ 嚴格遞增且恆不超過 $n$。
- **AC-M5-1.5** 同一組輸入（難度、`riskLevel`、`cameraLevel`、$d$）重複呼叫 1000 次，結果完全一致（無亂數、無時間相依）。
- **AC-M5-1.6** `riskLevel` 1~5 的 $T_{\text{收縮}}$ 依公式為 1.48 / 1.36 / 1.24 / 1.12 / 1.00 秒，且 $T_{\text{收縮}}$ 不影響 $p$ / $n$。
- **AC-M5-1.7** 外圈收縮完畢未按下 → `failed`，且此結果在 `stroll` 難度下**仍為 `failed`**（規則 2 的逾時條款優先於 $n = 1.00$）。

### AC-M5-2 三態素材版本契約（全卡表機械驗證）

- **AC-M5-2.1** 遍歷全卡表：每個素材 id 皆存在 `perfect` / `normal` / `failed` 三個版本，無缺漏、無多餘。
- **AC-M5-2.2** 遍歷全卡表：三版本的 `themeValue`、`storyValue`、`cost`、`riskLevel`、`isSpotlight`、`tags` 完全相等。
- **AC-M5-2.3** 遍歷全卡表：一般素材三版本 `hypeValue` 差值恰為 $+12$ / $0$ / $-12$；絕景素材恰為 $+20$ / $0$ / $-20$。
- **AC-M5-2.4** 遍歷全卡表：任一版本 `hypeValue` $\ge 0$。
- **AC-M5-2.5** 遍歷全卡表：三版本的 `name` 兩兩相異，且 `description` 兩兩相異（防止罐頭複製）。
- **AC-M5-2.6** `normal` 版的每一個欄位與施工基線卡表逐欄相等（證明普通態不改動既有平衡）。基線以施工起點的 commit 為準，於施工計劃中釘選該 hash 並以快照測試比對。

### AC-M5-3 取材流程接線

- **AC-M5-3.1** 三態結果為 `perfect` 時，入袋素材為該 `poiId` 的 `perfect` 版本；`normal`、`failed` 同理。
- **AC-M5-3.2** 三態皆不改變 $\Delta\text{HP} = 10 + \text{riskLevel} \times 2$ 的扣減量。
- **AC-M5-3.3** `failed` 時 `Budget` 仍扣除 `material.cost`，`gatheredPoiIds` 仍加入 `poiId`（不可重刷）。
- **AC-M5-3.4** 既有前置檢查不受影響：`PoiAlreadyGatheredException`、`CuratorExhaustedException`、`InventoryFullException` 的觸發條件與時機不變。
- **AC-M5-3.5** QTE 中斷（REQ-M5-04 規則 4）→ HP、Budget、`gatheredPoiIds`、腰包內容四者皆與中斷前完全相等。
- **AC-M5-3.6** 腰包滿額換牌流程（REQ-M3-03 規則 4）在三態下行為一致：換入的是該次判定對應版本。

### AC-M5-4 絕景兩段式

- **AC-M5-4.1** `isSpotlight == true` 的素材走兩段式，`false` 走單段。以全卡表遍歷驗證分派正確（8 張兩段、33 張單段）。
- **AC-M5-4.2** 兩段結果組合的完整真值表：$(t_c, t_s)$ 的 9 種組合皆產出兩者中較差者。
- **AC-M5-4.3** 構圖段無時間壓力：不設逾時，未確認前不判定。
- **AC-M5-4.4** `stroll` 難度下兩段式不可能產出 `failed`（除逾時條款外）。

### AC-M5-5 難度鎖定與持久化

- **AC-M5-5.1** 出發時選定的難度寫入單局快照；單局進行中呼叫變更難度的入口不存在（以介面契約驗證）。
- **AC-M5-5.2** 局外變更難度不影響進行中的單局快照值。
- **AC-M5-5.3** 難度存入存檔並於下一局載入為預設值。
- **AC-M5-5.4** 三檔難度下，同一條路線與同一組三態結果序列產出的局內分數、局外金幣完全相等（證明難度不連動獎勵）。

### AC-M5-6 決定性重播

- **AC-M5-6.1** 取材事件包含 `poiId`、三態結果、難度、素材版本識別四項。
- **AC-M5-6.2** 事件欄位不含按壓時間點、相對偏差、取景框座標或任何輸入時序（以欄位白名單機械驗證）。
- **AC-M5-6.3** 同一份事件日誌重播 100 次，最終狀態完全一致。
- **AC-M5-6.4** 重播過程不呼叫 QTE 判定函式（以注入的間諜實作驗證呼叫次數為 0）。

### AC-M5-7 架構隔離

- **AC-M5-7.1** `domain/` 內的三態判定與難度參數表不 import `package:flutter`、`package:flame`、`dart:ui`。
- **AC-M5-7.2** 三態判定與版本解析的全部測試以 `dart test` 通過，不需模擬器。
- **AC-M5-7.3** `FakePoiMaterialResolver` 可解析三態版本，全套 domain 測試在其上通過。
- **AC-M5-7.4** `lib/domain`、`lib/state`、`lib/game/universal_overworld_game.dart` 不出現具名城市字串（既有架構測試不得轉紅）。

### AC-M5-8 表現層

- **AC-M5-8.1** 三態各自觸發可區分的 Overlay 回饋（以 `Key` 驗證），`IgnorePointer` 不阻塞點擊。
- **AC-M5-8.2** `pubspec.yaml` 的相依清單在本里程碑前後無新增項目。

---

## 4. 邊界約束與非目標

1. **純領域零依賴**：三態判定與難度參數屬 `domain/`，嚴禁 import Flutter / Flame / `dart:ui`。
2. **不改動既有計分規則**：本 SPEC 只新增素材版本與判定，**不得**調整 `ClientReviewEngine` 的任何公式或係數。數值調整走獨立增修。
3. **不改動成本公式**：`ΔHP = 10 + riskLevel × 2` 與 `material.cost` 的扣減語意凍結。
4. **不引入新套件**（`CLAUDE.md` §6）。
5. **不新增抽象**（`CLAUDE.md` §3）：三態解析併入既有 `PoiMaterialResolver` 職責，不為此新開抽象層。
6. **凍結資產不動**：SPEC C 的 GPS 公尺契約、`AC-18.3`、`OverworldMapManifest` 介面一律不改。

---

## 5. 前置條件（施工前必須清償）

1. **`SPEC_MVP_POI_GATHERING.md` §1.3 增修**：該節將「微動作衝刺、快門 QTE 敏捷檢定」列為 Out of Scope，與本 SPEC 直接牴觸。須以增修註記指明由本 SPEC 承接。
2. **`SPEC_MVP_CORE_LOOP.md` 非涵蓋範圍表增修**：該表將「微動作（Runner 衝刺、Snapshot 快門）的具體 Flame 碰撞實作」歸給「引擎層 / 里程碑 M3」，實際落於 M5，須更正里程碑歸屬。
3. **`SPEC_MVP_META_PROGRESSION.md` 增修**：`cameraLevel` 現行語意為「提升黃昏高潮槽位 Hype 倍率」，本 SPEC 新增「加寬快門完美帶」的第二效果，須於該文件補記。
4. **素材文案撰寫**：82 張新版本的 `name` 與 `description` 須先完成撰寫並通過 AC-M5-2.5（兩兩相異）。此為內容產出工作，非程式任務，須確認由誰執行、何時交付。

---

## 6. 待決問題

1. **難度參數的調校責任歸屬**：$p$ / $n$ / $T_{\text{收縮}}$ 的初始值為本 SPEC 推導，手感是否成立**必須由使用者實機驗證**（依既有分工：邏輯由測試驗，手感與實機由使用者驗）。調校後的定值須回寫本 SPEC。
2. **`cameraLevel` 上限與 $p_{\text{有效}}$ 的封頂**：現行 `cameraLevel` 上限未在本 SPEC 範圍內確認。若上限過高，$p_{\text{有效}}$ 會被 $n$ 封頂而使升級失去邊際效果，須確認上限值後決定係數 0.10 是否調整。
3. **絕景構圖段的偏差正規化基準**：$d_c$ 以「標的半徑」正規化，但標的在畫面上的呈現尺寸隨縮放變化。須確認以世界座標或螢幕座標為基準 —— 前者公平但與視覺不符，後者直觀但可被縮放操弄。
4. **難度命名的最終用詞**：`漫步` / `一般` / `修羅` 為提案。`修羅` 的語氣是否符合作品調性待定。
5. **台灣卡表的三態版本是否同批交付**：台灣 9 張目前的使用狀態需確認（京都為現行生產 DLC）。若台灣卡表暫不使用，其 18 張新版本可延後，但 AC-M5-2 的全卡表遍歷須相應限縮範圍並註明。

---

## 7. 設計裁決紀錄（2026-09-12，使用者逐題裁決）

| 議題 | 裁決 | 落點 |
|---|---|---|
| 三態影響哪一側 | **產出側**（素材品質），成本固定 | REQ-M5-02 規則 7 |
| 更好的卡怎麼來 | **卡表 ×3 手寫**，41 → 123 張 | REQ-M5-02 規則 1 |
| Runner 與 Snapshot 分工 | 本里程碑**只做快門**，Runner 延後 M6 | §1.4 |
| 快門玩法 | **收縮準心為主**，絕景走**兩段式構圖＋快門** | REQ-M5-01、REQ-M5-03 |
| 數值幅度 | **固定增減量 ±12**，絕景 **±20**，只動 `hypeValue` | REQ-M5-02 規則 3~4 |
| 跳過與重試 | **皆不可**，無障礙由難度承接 | REQ-M5-04 |
| 難度影響面 | **只影響判定寬鬆度**，不連動任何獎勵 | REQ-M5-05 規則 4 |
| 難度選定時機 | 行前畫面與哲學同時選，寫入單局快照 | REQ-M5-05 規則 1~2 |
| 決定性 | 記錄三態結果與難度，不記輸入時序 | REQ-M5-06 |
