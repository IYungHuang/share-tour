# SPEC — 《Share Tour：奇葩旅行策展人》Milestone M5：快門微動作與三態取材

狀態：**Draft v3 — 待覆核**
流程位置：`spec → 覆核 → plan → 覆核 → 執行計劃 → 覆核`
上位文件：`CROSS_CUTTING_CONSTRAINTS.md`、`CLAUDE.md`、`SPEC_MVP_CORE_LOOP.md`（牴觸時以其為準）
相關文件：`SPEC_MVP_POI_GATHERING.md`（本 SPEC 解除其 §1.3 的「微動作」排除條款）、`SPEC_MVP_CAUSAL_FEEDBACK.md`（**M5 必須排在其施工完成之後**，見 §5）、`SPEC_MVP_AMENDMENT_01.md`（數值包絡）

> **命名澄清**：本文件的「M5」指**里程碑 M5**。`PLAN_MVP_META_PROGRESSION.md` 內的 `Task M5` 是 M4 施工計劃的任務編號，兩者無關。

---

> ## v3 修訂摘要（相對 v2）
>
> v2 送交第二輪雙軌覆核，企劃軌 8 條 P0、工程軌 4 條 P0，**兩軌皆判定不可進 plan**。其後執行了 v2 §6 待決 3 指定的全母體搜尋（§4），數據推翻了 v2 的幅度並揭露一個結構性事實。主要變動：
>
> 1. **幅度由推導改為實測定案，且刻度縮小一個數量級**：v2 的 ±12/±20 使網紅端極差 mean 27.2 / p95 42，是哲學軸（`AC-A1-6.8a` ≤ 15 分）的 1.8~2.8 倍。v3 改為隨難度縮放的 ±2 / ±3 / ±4。
> 2. **承認機制的真實形狀是損害控制，不是獎勵**（§1.3）。搜尋顯示 `perfect` 在 59.7% 的網紅局與約 99% 的社畜局不產生任何作用，且該比例**與幅度無關**。
> 3. **中斷改判 `normal` + 每局一次配額**，取消 v2 的凍結續跑。凍結會造成必輸局面，且現有 `Clock` 無暫停能力。
> 4. **難度持久化補上投影欄位**：v2 禁止為 `CuratorSaveData` 加欄位，等於封死了唯一的讀取路徑。
> 5. **節奏預算重寫**：v2 引的「野外段 240 秒」是誤引，且 POI 數把下限當成了實際值（32 而非 16）。
> 6. **絕景手勢三處修正**：環不繞標的（避開手指遮擋）、構圖偏差改取放開前取樣、判定由「取較差者」改為「最多降一階」。
> 7. **快門獎金改為封頂提交行程的 `perfect` 張數（≤ 4）**，v2 按採集次數發放會獎勵「跑滿 POI 全採便宜卡」這條品質最差的路線。
> 8. **新增參考玩家模型**，使獎金與支配性的 AC 可自動化。
> 9. **重寫兩條恆真的 AC**（`AC-M5-9.1` / `9.3`），新增三態混合池對 Amendment 01 分佈 AC 的回歸。
> 10. **§1.2 設計意圖第 4 條刪除**（已被 v2 自己的 `riskLevel` 解耦證偽）、**第 1 條降級**（它描述的是強支配解，與 `AC-A1-5.1` 結論相反）。
> 11. **§5.2 的前向相容 bug 已清償**（commit `14f6e92`），且修復範圍比 v2 記載的更大。

---

## 1. 目的與範圍

### 1.1 目的

現行踩線取材是一次原子呼叫：玩家按下按鈕，結果 100% 固定（`SPEC_MVP_POI_GATHERING.md` REQ-M3-03）。玩家對單一景點的唯一決策是「按或不按」，取材過程本身沒有任何玩家表達空間。

本里程碑在該原子呼叫**之前**插入一段即時操作（快門 QTE），其成績分為三態，決定入袋素材的版本。

### 1.2 設計意圖（施工時不得順手改掉）

1. **失手卡的損害控制**：`failed` 卡進入手牌後，「把它藏進哪個槽位」是一個真實的編排決策 —— 黃昏槽有相機倍率，把失手卡放進去會把損害放大。
   **注意本條的邊界**：反過來的「把 `perfect` 放黃昏槽」**不是決策，是強支配解**，而 `AC-A1-5.1` 明文要求黃昏槽固定最佳解的比例 ≤ 30%。施工時不得把「完美卡放黃昏」當成賣點呈現或以 UI 引導，那會直接推高該比例。由 AC-M5-9.1 與 AC-M5-9.4 共同保護。

2. **`isSpotlight` 在三態間凍結**：`failed` 的絕景**仍計入絕景階梯**。絕景階梯是網紅端最大的單一槓桿（因果 SPEC 消融實測 mean|Δ| = 17.44），快門失手不奪走這個槓桿是刻意的減震 —— 手殘不該讓整個行程崩盤。由 AC-M5-2.2 保護。

3. **失手後可以走去下一個 POI，是本案的壓力閥**：32 個可達 POI 對 6 格腰包，失手一張不是絕路，代價是 HP 與時間。這比任何重試按鈕都健康。**不得以「防刷」為由堵掉。**

> v2 的設計意圖第 4 條（「技巧軸回饋到路線決策」）已刪除。v2 同時把 `riskLevel` 從 QTE 解耦，於是任一非絕景卡的期望熱度成為 $H + (p_p - p_f)\Delta$ —— 一個**與路線無關的常數加項**，不改變任何兩條路線的排序。該斷言已被同一份文件的另一個決策證偽，留著會誤導施工。

### 1.3 設計語言：這是損害控制機制，不是獎勵機制

§4 的全母體搜尋顯示：

- 網紅端 **59.7% 的手牌**在 `normal` 態已達滿意度上限（`.clamp(0, 100)`），`perfect` 不可能有作用；
- 社畜端 **約 99%** 的情形 `perfect` 無作用（熱度只透過 `totalHype < boredomThreshold` 的二元門檻進入）；
- **這兩個比例與幅度無關**，在 ±2 到 ±20 的全區間皆然。

因此本機制的真實形狀是：**失手會扣分，完美多半不加分。**

v3 不再試圖把它扭成獎勵機制，而是明文採用對應的設計語言：

> `normal` **不是「普通」，是這張素材原本的價值**；`perfect` 是少數情況下的額外收穫；`failed` 是你搞砸了。

推論出的兩條施工約束：

1. **文案重心反轉**：`failed` 版是主力（玩家最常看到、最需要變化），`perfect` 版是彩蛋。見 REQ-M5-12。
2. **`perfect` 的穩定回報必須來自局外**（快門獎金，REQ-M5-05.6），因為局內通道結構上堵死。

### 1.4 解閘依據

`SPEC_MVP_AMENDMENT_01.md` 曾裁定「先修數學，再做微動作 —— 在評分模型不可能失敗的前提下，微動作的三態判定驗證不出效果」。該增修已於 2026-09-12 全數執行並驗收（`MVP_AMENDMENT_01_VERIFICATION.md` §6，commit `0b9a30a`），閘門條件已清償。

**AC-M5-9.2（分佈型驗收）是該裁定的直接延續**：只有存在性 AC 證不出玩家感覺得到，必須以全母體分佈證明。其門檻已由 §4 的實測數據填入，不再是空白。

### 1.5 本 SPEC 明確不涵蓋（Out of Scope）

- **Runner 衝刺微動作**：延後至里程碑 M7，屆時依標籤分派（`#拉車` / `#深夜` / `#高風險`），沿用本 SPEC 已驗證的三態框架。
- **勳章系統**：排在 M6。`perfect` 的長期回報預期由它承接（§7 已知限制 4）。本 SPEC **不為它新增任何欄位或介面**。
- **單局事件溯源管線**：`CuratorEventType` 現有 4 種全為局外事件，單局狀態從未落入日誌。本里程碑不建立（REQ-M5-06）。
- **社畜端計分模型的熱度不對稱**：§7 已知限制 1。屬客戶核心性格變更，須另立 SPEC 與全母體搜尋。
- **台灣卡表的三態版本**：§7 已知限制 2。
- **`riskLevel` / `cameraLevel` 影響 QTE 難度**：明確移除。
- **骨骼動畫與新動畫套件**：表現以 Flutter／Flame 內建能力實作（`SPEC_MVP_CAUSAL_FEEDBACK.md` v6 §5.3）。

---

## 2. 需求與行為契約

### REQ-M5-01 三態判定（時間定義） `[P0]`

1. **三態列舉**：`perfect` / `normal` / `failed`。完整且互斥。

2. **判定以時間為準，收縮環是純表現。** 設 $t_{\text{吻合}}$ 為環與目標框吻合的時刻、$t_{\text{按下}}$ 為玩家放開／按下的時刻：

   $$\Delta t = |t_{\text{按下}} - t_{\text{吻合}}|$$

   判定：$\Delta t \le \frac{W_p}{2}$ → `perfect`；$\frac{W_p}{2} < \Delta t \le \frac{W_n}{2}$ → `normal`；否則 → `failed`。

3. **時基錨定（單一權威時間軸）**：兩個時刻必須落在同一條時間軸上，否則相減沒有定義。
   - 權威時間軸為**指標事件時戳**：QTE 開始時記錄起始事件的時戳 $T_0$，$t_{\text{按下}} = T_{up} - T_0$。
   - $t_{\text{吻合}}$ 是該時間軸上的**常數**（取自難度參數表），不由動畫幀推算。
   - 收縮環的視覺進度可由 Flame 的 `dt` 驅動，但**不得**用來計算 $\Delta t$ —— 它只是表現。
   - **禁用 `DateTime.now()`**（CC-2 規則 3：任何「經過了多久」的判定必須使用單調時間）。指標事件時戳滿足此要求。

4. **時戳來源**：$T_{up}$ 與 $T_0$ 須取自**原始指標事件**（`PointerEvent.timeStamp`），**不得取自手勢識別器的 details** —— `TapUpDetails` 與 `DragEndDetails` 皆不攜帶時戳，走 `onPanEnd` 或 Flame 的 drag 回呼會在框架邊界弄丟它，只剩幀時間可用，而規則 3 已排除幀時間。

5. **取樣精度**：@60 fps 的單幀量化誤差為 16.7 ms，佔 `decisiveMoment` 完美窗 120 ms 的 14%，且在 60 Hz 與 120 Hz 裝置上結果不同 —— 這是規則 3~4 存在的原因。

6. **逾時**：動畫全長結束仍未按下 → `failed`。逾時**優先於一切難度參數**，`tourist` 亦不例外，否則最低難度會退化為「不按也能拿普通版」，等同 §1.5 已排除的跳過機制。
   逾時在型別上是**輸入的一種**，判定函式的輸入須為和型別（`pressed(Δt)` / `timedOut`），不得以 $\Delta t$ 的特殊值表示。

7. **難度參數表**（初始值，以毫秒為調校單位）：

   | 難度 | $t_{\text{吻合}}$ | 完美窗 $W_p$ | 普通窗 $W_n$ | 動畫全長 |
   |---|---|---|---|---|
   | `tourist`（觀光客） | 1400 ms | 800 ms | **不設上限** | 2000 ms |
   | `photographer`（攝影師） | 1100 ms | 240 ms | 720 ms | 1600 ms |
   | `decisiveMoment`（決定性瞬間） | 800 ms | 120 ms | 300 ms | 1200 ms |

   `tourist` 的 $W_n$ 不設上限 → 只要按下就不可能 `failed`（逾時除外）。
   **難度同時縮放動畫全長**，不只縮放窗寬 —— 無障礙的門檻是「按得到」而非「按得準」，只縮窗寬不縮時間解決不了前者。

8. **完美窗硬下限**：任何難度的 $W_p \ge 120$ ms。行動裝置從實體觸碰到畫面更新的端到端延遲通常為 60~120 ms 且帶有數十毫秒抖動；低於此下限是抽獎而非技巧，無法透過練習改善。

9. **難度是 QTE 的唯一旋鈕。** `riskLevel` 與 `cameraLevel` 均不影響上表任何一欄。
   **`riskLevel` 解耦的理由**：高熱度卡幾乎都是高風險（Amendment 01 附錄 A.2），而它已透過 `gatheringHpCost()` 與 Theme 疲勞兩條通道施加代價；第三條通道只會讓最有趣的卡最不好拿。
   **`cameraLevel` 解耦的理由**：等級上限為 3，v2 的係數使完美窗僅增加約 37 ms，低於可辨識差異，卻要付出跨文件耦合的代價。

10. **判定為純函式**：給定難度與輸入，結果唯一確定。不依賴當下時間、亂數或外部狀態（CC-3）。

11. **參考玩家模型**（供 AC 計算用，非玩家真實分佈）：按下時刻 $\sim \mathcal{N}(t_{\text{吻合}}, \sigma^2)$，截尾於動畫全長。$\sigma$ 取三檔：**120 / 200 / 300 ms**（熟練／中等／生疏）。凡需要「完美率」的 AC 一律以此模型計算，且須對三個 $\sigma$ 皆成立。

---

### REQ-M5-02 三態素材版本契約 `[P0]`

1. **每張素材具備三個手寫版本**。本里程碑範圍為京都卡表 **32 張 → 96 張**（台灣 descope，見 §7 已知限制 2）。

2. **版本間的差異僅限兩項**：`hypeValue`、與 `description` 的敘事文案。

3. **`name` 三版必須相同。** `八坂之塔清晨藍調時刻` 是一個**地點**；三個名字會破壞玩家在腰包抽屜、4 槽位卡面、結算歸因三處的卡片辨識，而因果 SPEC 的歸因設計高度依賴玩家認得出卡。三態改由 UI 狀態徽章承載（REQ-M5-11.4）。

4. **以下欄位在三個版本間必須完全相同**：`name`、`themeValue`、`storyValue`、`cost`、`riskLevel`、`isSpotlight`、`tags`。
   **`themeValue` 凍結的理由**：它決定哲學契合與主題一致性，是玩家在夜間編排時的策略軸。若白天的操作失誤改變素材的主題屬性，等同以操作懲罰策略規劃。
   **`isSpotlight` 凍結的理由**：§1.2 意圖 2。
   **`cost` / `riskLevel` 凍結的附帶好處**：資格判定與代價預告不需要知道三態，`evaluateAttractionEligibility` 與 `nearestGatherablePoi` 零改動。
   **`storyValue` 凍結的理由**（v2 未給）：它以 `totalStory × 5` 換金幣，範圍 1~5；四張卡全動 ±1 只值 ±20 金，對照 `baseCommission` 1000/1500 是雜訊。動它換不到可感知的回報，卻會讓退件折現規則多一個變數。

5. **`hypeValue` 幅度隨難度縮放**（初始值，由 §4 搜尋定案）：

   | 難度 | 一般卡（`isSpotlight == false`） | 絕景（`isSpotlight == true`） |
   |---|---|---|
   | `tourist` | $H \pm 2$ | $H \pm 3$ |
   | `photographer` | $H \pm 3$ | $H \pm 5$ |
   | `decisiveMoment` | $H \pm 4$ | $H \pm 7$ |

   $H$ 為現行卡表既有值，即 `normal` 版一律沿用現值、與難度無關。
   **難度影響局內幅度是刻意的**（撤銷 v2 的 REQ-M5-05.5）：不這麼做，難度就沒有任何局內意義，而 `tourist` 會嚴格支配其餘兩檔（亂按即有 40% 完美、0% 失手）。

6. **幅度的依據是 §4 的全母體搜尋，不是推導。**
   v2 曾以「±12 對應滿意度 ±8 分」論證，該推導有三處錯誤：拿網紅公式論證社畜門檻、漏掉絕景階梯、使用過時的 ThemeFactor 形式。現行公式為 `effectiveHype = round(netHype × spotlightLadder[min(count,4)])`、`themeFactor = (floor + (100-floor) × theme/100)/100`、`rawScore = effectiveHype / client.targetHype × 100 × themeFactor`，其後 `.clamp(0, 100)`。

7. **`hypeValue` 下限**：任一版本不得為負。現行卡表最低為 20。

8. **成本不受三態影響**：體力代價一律由 `gatheringHpCost(material)` 決定（現行為 `riskLevel × 6`，Amendment 01 D8 裁定），`Budget -= material.cost`，`failed` 亦照常扣除。
   **本 SPEC 一律以函式名引用成本，不複製算式。**

9. **三態版本的存放形狀**：三版本以 `(poiId, 三態)` 的變體表存放，**不進入既有的扁平卡表**；三版本**共用同一個 `id`**。
   **理由**：`curatorMaterialPoolProvider`、`kyoto_night_layout.dart` 的地圖景點生成、`auditMaterialCatalog()` 的比例判定皆以扁平卡表為分母，塞進 96 筆會讓地圖長出三倍景點。

10. **契約約束（三態值掛在哪個型別上）**：SPEC 不指定實作，但施工不得違反下列任一條 ——
    - 不得改變 `TravelMaterial` 的相等語意（現行 `operator ==` 含 `hypeValue` 不含 `description`，故同 id 不同態在該層可區分）；
    - 不得改動 `MaterialInventory` 與 `TimelineItinerary` 的元素型別；
    - 不得改動 `ClientReviewEngine.evaluate` 與 `calculateStats` 的簽章。
    **已知語意**：`TimelineItinerary.operator ==` 只比對 `s?.id`，故同 id 不同態在時間線層**不可區分**。目前無害（態在採集時固定，不會原地換態），記載於此以免日後誤用。

11. **`PoiMaterialResolver` 保持原簽章**：`resolveMaterialFor(String poiId)` 繼續回傳 `normal` 版；三態解析另加一個帶三態參數的方法。既有測試 Fake 不需改寫。

---

### REQ-M5-03 絕景快門：單一手勢的構圖＋時機 `[P0]`

1. **適用範圍**：`isSpotlight == true` 的素材。京都現行 **5 張**（全卡表 32 張中，其餘 27 張走純時機）。

2. **單一手勢**：**按住** → 取景框跟隨手指移動（構圖），收縮同時進行；**放開即快門**。不設獨立的確認 tap。

3. **收縮的視覺表現不得環繞標的。** 取景框跟隨手指意味著手指必須壓在標的上；若收縮環也繞著標的，判定瞬間玩家的手指正好蓋住他必須盯的東西。這是結構性遮擋，不是可靠美術微調解決的。改以不被遮擋的呈現（例如取景框本身的邊框收束）。

4. **構圖偏差** $d_c$ **取自放開前約 50 ms 的取樣**（或最後數筆的中位數），不取 lift-off 當下的座標 —— 電容式觸控在 lift-off 時回報的座標通常漂移數 px。

5. **判定：構圖最多降一階，不取兩者較差者。**
   構圖依 $d_c$ 判出 $t_c$、快門依 $\Delta t$ 判出 $t_s$，最終結果為 $t_s$ **至多下降一階**（`perfect` → `normal` → `failed`），下降與否由 $t_c$ 決定：$t_c$ 為 `perfect` 不降、`normal` 降一階、`failed` 亦降一階。
   **理由**：「取較差者」使絕景失手率約為一般卡的 7.8 倍，而絕景幅度最大、階梯是網紅端最大槓桿、黃昏槽再加倍，且京都僅 5 張多為高風險（§1.2 意圖 3 的壓力閥在絕景上幾乎不存在）。那正是 REQ-M5-01.9 解耦 `riskLevel` 所要消滅的形狀，只是換了一扇門。

6. **構圖判定不得受玩家在大世界的縮放狀態影響**，且 **QTE 結束後相機狀態（模式、中心、zoom）須與進入前一致**。
   （此為性質陳述。實作方式屬 plan —— 但須注意 `CameraFollow` 目前**沒有存／還原 API**，`recenter()` 會清掉凍結中心與互動時戳，直接沿用會抹掉玩家的自由平移狀態。）

7. 構圖的判定門檻隨難度縮放，與 $W_p$ / $W_n$ 同表管理（初始值見 §6 待決 2）。

---

### REQ-M5-04 不可跳過、不可重試；中斷判 `normal` `[P0]`

1. 進入 QTE 後**無跳過入口**，亦**不提供重試**。單次判定即為最終結果。

2. **中斷判 `normal`，並照常扣除 HP / 預算 / 記入 `gatheredPoiIds`。**
   觸發條件：QTE 進行中 App 生命週期離開 `resumed`。
   **每局一次配額**：同一局第二次起的中斷判 `failed`。

   **理由**：
   - v1 的「中斷即取消、零副作用」是玩家可主動觸發的無限重試後門 —— 手機上上滑切換 App 不到半秒，且 Flutter 端無法區分來電與手動切換。
   - v2 的「凍結續跑」造成必輸局面：`decisiveMoment` 的 $t_{\text{吻合}}$ 為 800 ms，在 760 ms 被打斷則回前景只剩 40 ms，低於規則 8 自己認定的人類反應下限；且現有 `lib/core/time/` 的時鐘是自由奔跑的 `Stopwatch`，**沒有暫停能力**，實作凍結須擴張未核准的抽象。
   - 判 `normal` 同時關掉兩者：它劣於 `perfect` 故無刷取誘因；優於 `failed` 故真實來電不受懲罰；配額擋住「用切 App 把 `failed` 換成 `normal`」。

3. **生命週期門檻與定位層不同調，此為刻意。** `main.dart` 對 `AppLifecycleState.inactive` 刻意 `break`（不動定位訂閱），但 QTE 的中斷門檻**包含 `inactive`** —— iOS 的下拉通知欄與來電橫幅都停在該狀態，玩家在那期間按不到螢幕。施工時不得為了「統一」而把兩者對齊。

4. **App 遭系統終止**：單局狀態全在記憶體（REQ-M5-06.1），整局隨之遺失。此為既有行為，非本 SPEC 引入。

5. **無障礙由難度承接**，且難度必須縮放時間而非僅縮放窗寬（REQ-M5-01.7）。

---

### REQ-M5-05 難度、單局鎖定與快門獎金 `[P0]`

1. **三檔難度**：`tourist`（觀光客）／`photographer`（攝影師）／`decisiveMoment`（決定性瞬間）。
   **命名理由**：採攝影師語彙而非難度形容詞，貼合本作的相機主軸與策展人身分，且與城市無關 —— 難度屬通用引擎設定，日式語彙會在接入其他城市 DLC 時語感打架。

2. **選定時機**：與旅行哲學同一個行前畫面（`CuratorBriefingModal`）選定，不新增畫面。

3. **單局鎖定**：出發時寫入單局快照，比照 `equipmentSnapshot` 的鎖定語意。單局進行中不可變更。

4. **持久化**：最後一次選用的難度存入存檔，作為下一局預設值。
   **實作路徑**：新增 `CuratorEventType.difficultySelected` 事件，**並**在 `CuratorSaveData` 新增 `lastDifficulty` 投影欄位。
   `CuratorSaveData` 沒有 `toJson`／`fromJson`（它是重播出的投影，不是持久化格式），因此**往它加投影欄位不是 schema 變更**，`saveVersion` 不動。v2 把「不加欄位」與「不升 schema」綑在一起是誤判 —— 不加欄位就沒有任何東西承載「上次選的難度」，該需求無法成立。

5. **難度影響局內幅度**（REQ-M5-02.5），但**不影響**素材掉落、HP／預算消耗、或評分公式本身。

6. **快門獎金（局外金幣）**：結算時依**本局提交行程中**的 `perfect` 張數發放，與客戶、與滿意度天花板皆無關。

   $$\text{快門獎金} = \min(\text{提交行程中的 perfect 張數},\ 4) \times \text{單價} \times k_{\text{難度}}$$

   單價初始值 **40 金**；$k$ 初始值 `tourist` 0.5 / `photographer` 1.0 / `decisiveMoment` 1.5。
   **退件局比照故事折現打 30%**（與 `rejectedStoryRatePercent` 一致），否則會稀釋 Amendment 01「失敗要有可感知代價」的收斂。

   **按提交張數而非採集次數**：v2 的算法會獎勵「跑滿 32 個 POI、全採 risk 1 便宜卡」—— 行程品質最差的那條路線，且與「把行程編好」的核心動機相反。

7. **快門獎金須折進 `runSettled` 事件的 `earnedCoins`**（`replayCuratorEvents` 只累加該欄位），不另開欄位、不改 `ClientReviewEngine`。

8. **快門獎金須在結算面板獨立成行**，標示完美張數與難度倍率。
   **這不是裝飾**：依 §1.3，它是社畜局玩家唯一感知得到的技巧回饋通道。

---

### REQ-M5-06 事件與決定性 `[P0]`

1. **範圍縮小**：本里程碑**不建立單局事件溯源管線**。`gatheredPoiIds`、腰包、時間線從未落入日誌，`replayCuratorEvents()` 的投影不承載單局狀態。

2. **本里程碑的要求**：三態結果與難度須以**純資料形式**表達並可跨層傳遞，且**不含輸入時序**。
   **禁止出現於任何可持久化資料**：$t_{\text{按下}}$、$\Delta t$、$T_0$、$d_c$、取景框座標。
   **理由是 CC-3（重播決定性）與「這些欄位沒有用途」，不是 CC-5。** CC-5 管的是原始 GPS 座標不離開裝置；時間偏差不是位置資料。

3. `difficultySelected` 事件只記錄難度值，不記錄任何 QTE 過程資料。

4. **重播模組不得依賴 QTE 判定**：以靜態檢查驗證（AC-M5-6.3），不以注入間諜驗證 —— 後者會逼出一個只為測試存在的注入點，違反 `CLAUDE.md` §3。

5. **前向相容的前提已清償**：事件日誌對未知事件種類已改為原文保留、重播忽略、追加時原樣寫回（commit `14f6e92`）。`difficultySelected` 是第一個受惠於此的新種類 —— 在該修復之前，舊建置讀到它會把整份日誌搬去 `.bak` 並重建身分，玩家金幣與裝備歸零。

---

### REQ-M5-07 取材流程接線 `[P0]`

1. **QTE 開始時鎖定目標**：進入 QTE 時鎖定 `poiId` 與該 POI 的三態素材集合；判定完成後若鎖定的 POI 已不可取材，該次取材取消。
   **理由**：`curatorRunController.gatherPoi()` 在呼叫當下以 `nearestGatherablePoi()` 重算最近可取材景點並覆蓋傳入的 `poiId`，且不像 `replaceGatheredPoi()` 那樣具備 `expectedPoiId` 防護。插入 QTE 後，按下與實際呼叫之間多了 1.2~2.0 秒，期間位置持續更新 —— 玩家對 A 景點做的 QTE 會套用到 B 景點的素材上。施工時須一併把 `expectedPoiId` 防護補到 `gatherPoi()`（§5.4）。

2. **腰包滿額時，換牌抽屜開在 QTE 之前**：`GatheringEligibility == inventoryFull` 時先開抽屜讓玩家決定捨棄哪張（或放棄本次），確認後才進 QTE。
   **已知取捨**：若在 QTE 之後才因規則 1 取消，玩家的 `perfect` 一樣蒸發，只是換個時點。此處接受「零副作用優先」。

3. **QTE 期間的按鈕態**：沿用 REQ-M3-06 既有的 loading 防連點態，**不新增 `GatheringEligibility` 列舉值**（該列舉現為 6 態，含 `unavailable`）。

4. 既有三個例外的觸發條件與時機不變：`PoiAlreadyGatheredException`、`CuratorExhaustedException`、`InventoryFullException`。

---

### REQ-M5-08 因果可視化接線 `[P0]`

1. **新增第 15 個 `reasonCode`**（如 `shutter_quality`），三態各有符號與 `CuratorCodex` 詞條。

2. **理由**：因果 SPEC §1.2 原則 1 要求「每一條 `CausalFact` 都對應一段已存在的計算」。本 SPEC 新增了一個影響 `totalHype` 的來源；若不接線，玩家失手三張後在結算看到熱度不足，歸因面板會把原因指向「絕景缺口」或「熱度稍差」—— **玩家真正的失分原因在報告裡沒有坑位可放**。這正是因果 SPEC 立案要消滅的病灶原型。

3. **順序約束**：因果 SPEC 要求以最新程式碼為基線，故 **M5 必須排在因果可視化施工完成並合入主線之後**。

4. **符號語言統一**：野外浮字採與因果系統一致的序數符號，不使用裸數值 —— 因果 SPEC §3.1.3 刻意在編排期只給方向不給量級，兩處用兩套語言會讓玩家學兩次。

---

### REQ-M5-09 節奏預算 `[P0]`

1. **單次 QTE 儀式**（轉場進 ＋ 收縮 ＋ 浮字回饋 ＋ 轉場出）的邏輯時間上限 = **該難度的動畫全長 + 1200 ms**。

2. **單局總量上限 60 秒**，以 **Lv.1 體力上界 16 次** QTE 計。
   16 次的來源：`gatheringHpCost = riskLevel × 6`，Lv.1 球鞋 100 HP ÷ 最低成本 6 = 16 次。**上界由體力決定，不由 POI 數決定** —— 可達 POI 實為 **32**（`kyoto_reachable_pool_test.dart` 斷言 `reachablePool.length == 32`；`AC-A1-4.4` 的 `>= 16` 是產品下限，不是實際值）。

3. 依上表驗算：`tourist` (2.0+1.2)×16 = **51.2 s**、`photographer` **44.8 s**、`decisiveMoment` **38.4 s**，皆守得住。
   （v2 的 `tourist` 全長 2600 ms 會得到 60.8 s，超標；v3 壓到 2000 ms 一併解決，順帶讓 `tourist` 的 `normal` 態真的存在 —— v2 的 $W_p$ 佔全長 40%，`normal` 幾乎不可能出現。）

4. **v2 引的「野外段基準 240 秒」是誤引**：`AC-A1-4.1` 是可達性測試（240 秒內能採到 6 個 POI），不是時間預算。v3 不再以百分比表達，改用上述兩條絕對上限。

---

### REQ-M5-10 架構隔離 `[P0]`

1. QTE 的計時迴圈、輸入處理與動畫住在 `game/` 或 `ui/`；`domain/` 只接收已判定的三態結果。
2. 三態判定的數學與難度參數表屬 `domain/`，為純函式，以 `dart test` 可測。
3. 通用引擎不得出現具名城市；三態卡表落在 `lib/data/core_loop/`。
4. `FakePoiMaterialResolver` 須支援三態解析，維持秒級測試。
5. 資料流沿既有形狀：UI 取得素材 → 呼叫 `curatorRunController.gatherPoi()` → domain 原子轉移。QTE 只需在 UI/game 產出一個三態值，沿同一條路多帶一個參數。**state 層不新增抽象。**

---

### REQ-M5-11 取材表現層 `[P1]`

1. 三態須有可辨識的即時回饋，沿用 `GatheringFloatingFeedbackOverlay` 既有通道。
2. 三態必須在**不看數字的情況下**可區分（色彩 ＋ 符號 ＋ 動態）。
3. 限於 Flutter／Flame 內建動畫能力，不新增相依套件。
4. **三態徽章規格**：`name` 三版相同（REQ-M5-02.3），三態改由徽章承載，須在**腰包抽屜、4 槽位卡面、結算歸因**三處一致呈現，採 REQ-M5-08.4 的序數符號語言。
5. 依 §1.3 的設計語言，`failed` 的回饋須明確可讀（玩家最常遇到），`perfect` 可較為含蓄。

---

### REQ-M5-12 文案撰寫契約 `[P0]`

1. **規模**：京都 32 張 × 2 個新版本 = **64 段新 `description`**（`name` 三版相同，不需新撰）。

2. **語域不變**：三版都是同一位攝影者在現場的第一人稱觀察，不進行評價、不吐槽。
   **這條必須明寫**：題目雖是「毒舌策展人」，但**實際卡表的 `description` 完全不毒舌**，是抒情的旅行散文。毒舌住在客戶台詞（小林／安娜）與阿導旁白，不在卡面。

3. **`perfect` = 多一個只有完美快門才捕捉得到的具體細節**（露水、一縷煙、一個剛好走進畫面的人影）。

4. **`failed` = 一個具體且好笑的入侵物或失誤，不是否定句。** 「沒拍好」「糊掉了」一律不合格。
   依 §1.3，**`failed` 是主力版本**，撰寫資源應優先投入此處。

5. **範例三連**（以現行卡 `祇園白川辰巳大明神夜櫻` 為基準）：

   | 版本 | `description` |
   |---|---|
   | `normal`（現行，不動） | 白川潺潺流水與朱紅小橋映照垂櫻，藝妓足音在石板路上迴盪。 |
   | `perfect` | 白川潺潺流水與朱紅小橋映照垂櫻，快門落下那一瞬，一片花瓣正好停在水面上還沒被沖走。 |
   | `failed` | 白川潺潺流水與朱紅小橋映照垂櫻，畫面正中央是一位舉著自拍棒、對你比讚的觀光客。 |

6. **機械護欄**：三版兩兩相異；**三版長度的 $\max/\min \le 1.4$**；`failed` / `perfect` 版不得包含 `normal` 版作為子字串。
   （分母須明訂 —— 上列範例實測 28 / 41 / 39 字，以短版為分母是 46.4%，以長版為分母是 31.7%。採 $\max/\min \le 1.4$ 則 $41/28 = 1.46$ 仍不合格，故範例的 `perfect` 版須再收短，或門檻放寬至 1.5。此為 §6 待決 4。）

---

## 3. 驗收條件（Acceptance Criteria）

### AC-M5-1 三態判定純函式

- **AC-M5-1.1** `photographer`：$\Delta t = 100$ ms → `perfect`（$W_p/2 = 120$）；$\Delta t = 200$ ms → `normal`；$\Delta t = 400$ ms → `failed`。
- **AC-M5-1.2** 三檔難度的 $W_p/2$ 邊界值（含 $\pm\epsilon$）判定符合 REQ-M5-01.7 的表，邊界採閉區間；$W_n/2$ 的邊界僅適用於 `photographer` 與 `decisiveMoment`。
- **AC-M5-1.3** `tourist` 下任意 `pressed(Δt)` 皆不產生 `failed`。
- **AC-M5-1.4** 三檔難度的 $W_p \ge 120$ ms。
- **AC-M5-1.5** `timedOut` 在三檔難度下**皆**判 `failed`，含 `tourist`。
- **AC-M5-1.6** 判定函式的輸入為和型別：以 `timedOut` 呼叫不需提供 $\Delta t$，且不存在能以 $\Delta t$ 表達逾時的路徑。
- **AC-M5-1.7** 同一輸入重複呼叫 1000 次結果一致；且靜態檢查：判定模組原始碼不出現 `DateTime.now`、不出現 `Random`。
- **AC-M5-1.8** 靜態檢查：三態判定與難度參數表模組的原始碼**不出現** `riskLevel` 與 `cameraLevel`（REQ-M5-01.9 的解耦）。
- **AC-M5-1.9** 以 `TestGesture.up(timeStamp:)` 指定兩個相差 1 ms 的放開時戳，跨過 $W_p/2$ 邊界時判定結果必須改變（證明時戳取自指標事件而非幀時間）。

### AC-M5-2 三態素材版本契約（全卡表機械驗證）

- **AC-M5-2.1** 遍歷**實際注入的素材池**（非硬編碼清單）：每個素材 id 皆存在三個版本，無缺漏、無多餘。
- **AC-M5-2.2** 三版本的 `name`、`themeValue`、`storyValue`、`cost`、`riskLevel`、`isSpotlight`、`tags` 完全相等。
- **AC-M5-2.3** 三檔難度下，一般素材與絕景素材的 `hypeValue` 差值恰為 REQ-M5-02.5 的表定值。
- **AC-M5-2.4** 任一版本 `hypeValue` $\ge 0$。
- **AC-M5-2.5** 三版本共用同一 `id`；三態值為獨立於 `id` 的資料。
- **AC-M5-2.6** `normal` 版逐欄等於**版控內的 golden fixture**（`test/fixtures/`，施工起點序列化而得，格式與欄位順序須明訂）。測試不得讀取 git。
- **AC-M5-2.7** 既有扁平卡表長度不變：`kyotoNightMaterials.length` 與地圖景點數維持 32，`auditMaterialCatalog()` 的分母不變。
- **AC-M5-2.8** `TravelMaterial` 的 `operator ==` / `hashCode` 語意未變；`MaterialInventory` 與 `TimelineItinerary` 的元素型別未變；`ClientReviewEngine.evaluate` 與 `calculateStats` 的簽章未變（REQ-M5-02.10 的契約約束）。

### AC-M5-3 取材流程接線

- **AC-M5-3.1** 三態結果對應入袋的版本正確。
- **AC-M5-3.2** 三態皆不改變 `gatheringHpCost(material)` 的回傳值與實際扣減量。
- **AC-M5-3.3** `failed` 時 `Budget` 仍扣除 `material.cost`，`gatheredPoiIds` 仍加入 `poiId`。
- **AC-M5-3.4** 既有三個例外的觸發條件與時機不變。
- **AC-M5-3.5** QTE 開始時鎖定的 `poiId` 在判定完成後仍為實際套用的目標。情境以 **`LocationSource` 注入新座標**造成最近 POI 改變來構造（QTE 期間引擎暫停，方向鍵不會動）。
- **AC-M5-3.6** `inventoryFull` 時換牌抽屜的開啟先於 QTE；抽屜中選「放棄」則不進入 QTE、不扣任何資源。
- **AC-M5-3.7** 中斷判定：以 `tester.binding.handleAppLifecycleStateChanged` 送出離開 `resumed`，該次取材判 `normal` 且資源照常扣除；同一局第二次中斷判 `failed`。
- **AC-M5-3.8** **任何中斷序列都不得提高三態結果的期望值**（以參考玩家模型計算，比較有中斷與無中斷的期望三態分佈）。

### AC-M5-4 絕景單一手勢

- **AC-M5-4.1** `isSpotlight == true` 走構圖＋時機，`false` 走純時機。以注入池遍歷驗證分派（京都 5 張／27 張）。
- **AC-M5-4.2** 構圖態與時機態組合的 9 種真值表符合 REQ-M5-03.5 的「最多降一階」：構圖 `failed` + 時機 `perfect` → `normal`（**不是** `failed`）。
- **AC-M5-4.3** 以 `free` 相機模式與非預設 zoom 進入 QTE：構圖偏差的判定結果與預設 zoom 相同，且結束後相機模式、中心與 zoom 均回復進入前的值。
- **AC-M5-4.4** $d_c$ 取自放開前的取樣：在放開前 10 ms 注入一次偏移，判定結果不受該偏移影響。

### AC-M5-5 難度、鎖定與快門獎金

- **AC-M5-5.1** 行為 AC：`fieldTrip` / `nightEditing` 期間任何變更難度的路徑皆不改變單局快照的難度值。
- **AC-M5-5.2** 靜態檢查：比照 `layer_boundaries_test.dart` 的字串掃描，單局期間不存在難度變更入口。
- **AC-M5-5.3** 難度經 `difficultySelected` 事件持久化，重播後 `CuratorSaveData.lastDifficulty` 為該值並作為下一局預設。
- **AC-M5-5.4** 三檔難度下，同一條路線與同一組三態序列產出的**局內滿意度公式輸入**（`calculateStats` 的輸出）僅因幅度不同而異，評分公式本身未被改動。
- **AC-M5-5.5** 以參考玩家模型（$\sigma$ = 120 / 200 / 300 ms）計算，三檔難度的**期望快門獎金嚴格遞增**，且對三個 $\sigma$ 皆成立。
- **AC-M5-5.6** 快門獎金計入 `runSettled` 的 `earnedCoins`，重播後金幣總額相符。
- **AC-M5-5.7** 退件局的快門獎金為 30%。
- **AC-M5-5.8** **加入快門獎金後，`AC-A1-5.4`（三件套升至滿級需 6~10 局）仍成立**，母體為三態混合池 × 各難度的參考完美率。

### AC-M5-6 決定性與資料純度

- **AC-M5-6.1** 欄位白名單：任何可持久化資料不含 $t_{\text{按下}}$、$\Delta t$、$T_0$、$d_c$、取景框座標。
- **AC-M5-6.2** 三態結果與難度可以純資料形式跨層傳遞，且該資料不含 `dart:ui` 或框架型別。
- **AC-M5-6.3** 靜態檢查：重播模組的原始碼不 import 三態判定模組。

### AC-M5-7 架構隔離

- **AC-M5-7.1** `domain/` 的判定與參數表不 import `package:flutter`、`package:flame`、`dart:ui`。
- **AC-M5-7.2** 判定與版本解析的全部測試以 `dart test` 通過。
- **AC-M5-7.3** `FakePoiMaterialResolver` 可解析三態，全套 domain 測試通過。
- **AC-M5-7.4** 既有三條架構測試維持全綠。
- **AC-M5-7.5** `resolveMaterialFor(String poiId)` 簽章未變，既有測試 Fake 未經改寫即通過。

### AC-M5-8 因果接線

- **AC-M5-8.1** 新 `reasonCode` 三態皆有 `CuratorCodex` 詞條。
- **AC-M5-8.2** 全程 `failed` 的行程，因果報告中存在指向快門的 `CausalFact`，且該情境下 `primaryCulpritSlot` 的歸因不指向絕景缺口。
- **AC-M5-8.3** 因果 SPEC 既有的「每個 `reasonCode` 在 UI 皆有坑位」守門測試維持全綠。

### AC-M5-9 效果可驗證性（本 SPEC 的核心驗收）

- **AC-M5-9.1** 設計意圖保護（**失手卡的損害控制**）：同一張 `failed` 卡放黃昏槽與放其他槽，對 `totalHype` 的**損害差值**隨 `cameraLevel` 單調遞增，且在 `cameraLevel = 1` 時即大於 0。
  （v2 的版本驗的是 `perfect` 放黃昏，$0.5(H+\Delta) > \Delta$ 在卡表最低 `hypeValue` 20 之下恆真、且描述的是 `AC-A1-5.1` 要壓制的強支配解。）
- **AC-M5-9.2** **分佈型驗收**。母體定義（不得更動）：`reachablePool.take(16)` 的 16 張池、每手 6 張（$\binom{16}{6} = 8008$）、3 槽位排列、5 哲學 × 2 客戶、**全域齊一態**、相機 Lv.1。與 `AC-A1-5.1` 同基準，**執行時間約 30 秒**。
  門檻（由 §4 實測定案）：

  | 指標 | 門檻 | ±2 實測 | ±3 實測 | ±4 實測 |
  |---|---|---|---|---|
  | 網紅 三態兩兩相異 | $\ge 40\%$ | 40.3% | 40.3% | 40.3% |
  | 網紅 三態完全同分 | $\le 40\%$ | 37.0% | 25.4% | 17.6% |
  | 網紅 極差 p95 | $\le 18$ | 8 | 13 | 18 |
  | 社畜 三態完全同分 | $\le 75\%$ | 73.3% | 62.9% | 53.3% |

  **社畜端「三態兩兩相異」恆為 0% 是結構性事實，不列為 AC**（見 §7 已知限制 1）。
- **AC-M5-9.3** **三態混合池回歸**：以三態混合池為母體重跑 `AC-A1-6.8a`，且**須對每一種齊一態分別成立**（全 `perfect` / 全 `normal` / 全 `failed`），而非僅「引入三態後仍成立」。
  （v2 的版本恆真：`6.8a` 比各哲學最佳解，最佳解一律全 `perfect`，五家同加同一量則極差不變。）
- **AC-M5-9.4** **Amendment 01 分佈 AC 的三態回歸**：以三態混合池重跑 `AC-A1-5.1`（黃金槽固定最佳解 $\le 30\%$）、`AC-A1-6.3a/b`、`AC-A1-6.4`、`AC-A1-6.8b`，全部仍須成立。
  **`AC-A1-5.1` 是重點**：`perfect` 嚴格優於其他版本 × 黃昏槽有倍率，會直接推高固定最佳解的比例。

### AC-M5-10 節奏

- **AC-M5-10.1** 單次 QTE 儀式的邏輯時間 $\le$ 該難度動畫全長 + 1200 ms。
- **AC-M5-10.2** **最壞情況**：以 Lv.1 體力可支撐的最大採集次數（全低風險序列，16 次）模擬，三檔難度的總邏輯時間皆 $\le$ 60 秒。
  （不得改以 `AC-A1-4.1` 的 6 次基準路徑驗證 —— 那是最友善案例，會在真實破綻存在時亮綠燈。）

### AC-M5-11 表現層

- **AC-M5-11.1** 三態各自觸發可區分的 Overlay 回饋（以 `Key` 驗證），`IgnorePointer` 不阻塞點擊。
- **AC-M5-11.2** `pubspec.yaml` 的相依清單在本里程碑前後無新增項目。
- **AC-M5-11.3** 三態徽章在腰包抽屜、4 槽位卡面、結算歸因三處皆存在且符號一致。

### AC-M5-12 文案護欄

- **AC-M5-12.1** 三版 `description` 兩兩相異。
- **AC-M5-12.2** 三版 `description` 長度的 $\max/\min \le$ 門檻（見 §6 待決 4）。
- **AC-M5-12.3** `failed` / `perfect` 版不包含 `normal` 版作為子字串。

---

## 4. 幅度搜尋的實測數據

腳本：`test/analysis/micro_action_amplitude_search_test.dart`（已進版控，預設 `skip`，解除即可重跑，約 30 秒）。母體與 `AC-A1-5.1` 同基準：16-POI 子集、8,008 手 × 5 哲學 × 2 客戶 = 40,040 樣本、相機 Lv.1。排列以 `normal` 態的最佳解決定（排列選擇與幅度無關），再在同一排列上評估三態。

```
[社畜] ±2/±3   三態相異 0.0%  完全同分 73.3%  P=N 100.0%  F=N 73.4%  極差 mean 6.7  med 0  p95 25 max 25
[社畜] ±3/±5   三態相異 0.0%  完全同分 62.9%  P=N  99.8%  F=N 63.1%  極差 mean 9.3  med 0  p95 25 max 25
[社畜] ±4/±7   三態相異 0.0%  完全同分 53.3%  P=N  99.7%  F=N 53.6%  極差 mean 11.7 med 0  p95 25 max 25
[社畜] ±12/±20 三態相異 0.0%  完全同分 10.4%  P=N  97.2%  F=N 13.2%  極差 mean 22.4 med 25 p95 25 max 25

[網紅] ±2/±3   三態相異 40.3%  完全同分 37.0%  P=N 59.7%  F=N 37.0%  極差 mean  3.4 med  3 p95  8 max  9
[網紅] ±3/±5   三態相異 40.3%  完全同分 25.4%  P=N 59.7%  F=N 25.4%  極差 mean  5.9 med  5 p95 13 max 15
[網紅] ±4/±7   三態相異 40.3%  完全同分 17.6%  P=N 59.7%  F=N 17.6%  極差 mean  8.5 med  8 p95 18 max 20
[網紅] ±12/±20 三態相異 40.3%  完全同分  1.2%  P=N 59.7%  F=N  1.2%  極差 mean 27.2 med 26 p95 42 max 54
```

（完整七組含 ±6/±10、±8/±13、±20/±33，見腳本輸出。）

### 四個結論

1. **幅度上界在 ±4 附近**。網紅端 `±3` 的極差 p95 = 13、max = 15，剛好壓在哲學軸（`AC-A1-6.8a` 五哲學極差 ≤ 15 分）之內；`±4` 的 p95 18 已略超。**v2 的 ±12 是哲學軸的 1.8~2.8 倍，會讓手速蓋過所有編排與哲學。**

2. **社畜端「三態相異」恆為 0.0%**，所有幅度皆然。熱度只透過 `totalHype < boredomThreshold(168)` 的二元門檻進入，**永遠是 0 或 25，從不漸進**。幅度只改變遇到斷崖的頻率。

3. **網紅端 `P=N` 恆為 59.7%**，所有幅度皆然 —— 那是 `.clamp(0, 100)`，六成手牌在 `normal` 態已滿分。

4. 合起來：**`perfect` 在 59.7% 的網紅局與約 99% 的社畜局什麼都不做。** 在「只動素材、不動引擎」的前提下無解（`hypeValue` 撞天花板、`storyValue` 規模不夠、`themeValue` 已裁決凍結）。這是 §1.3 設計語言轉向與 REQ-M5-05.6 快門獎金的依據。

---

## 5. 前置條件（施工前必須清償）

1. **擷取 32 張 `normal` golden fixture 至 `test/fixtures/`**（AC-M5-2.6）。**必須早於一切卡表改動** —— 文案一旦回填，基線就髒了。
2. **因果可視化施工完成並合入主線**（REQ-M5-08.3）。判準包含其守門測試已提交且全綠。
3. **`SPEC_MVP_POI_GATHERING.md` §1.3 增修**：該節將快門 QTE 列為 Out of Scope，須註記由本 SPEC 承接。
4. **`gatherPoi()` 補 `expectedPoiId` 防護**（REQ-M5-07.1）。此為**既有缺陷**，建議以獨立 commit 與獨立測試修復，不併入 M5 施工。
5. **`SPEC_MVP_CORE_LOOP.md` 非涵蓋範圍表增修**：微動作現標註為「引擎層 / 里程碑 M3」，實際落於 M5。
6. **64 段 `description` 文案交付**：須依 REQ-M5-12 撰寫並通過 AC-M5-12。**此為內容產出工作，會序列阻塞施工**，須確認執行者與交期。

> **已清償**：事件日誌的前向相容修復（commit `14f6e92`）。v2 曾將其列為前置條件，但描述寫錯層（拋出點在解析不在重播），且漏掉 `_doAppend` 的整份寫回會永久抹掉被跳過的事件並使 `seq` 撞號。實際修復採原文保留。

---

## 6. 待決問題

1. **難度參數的實機調校**：$t_{\text{吻合}}$ / $W_p$ / $W_n$ / 動畫全長的初始值須由使用者實機驗證手感。
   **特別待驗**：`decisiveMoment` 的 $W_p = 120$ ms 是否為「難但可練」而非「仍在抽獎」。觸控延遲抖動使這件事測試驗不出來，且在不同機型差 2~3 倍 —— 建議在開發模式即時量測並顯示端到端延遲，用數據回答而非用感覺。
2. **絕景構圖的判定門檻**：REQ-M5-03.7 說隨難度縮放，具體門檻值未定。須與 $W_p$ / $W_n$ 同表補齊，並與 REQ-M5-03.5 的「最多降一階」一併調校。
3. **快門獎金的單價與倍率**：40 金 × (0.5 / 1.0 / 1.5) 為初始值，須經 AC-M5-5.5 與 AC-M5-5.8 夾住後，再對照黑市升級節奏（500 / 2000）調校。
4. **AC-M5-12.2 的長度門檻**：$\max/\min \le 1.4$ 會讓 REQ-M5-12.5 的範例（28 / 41 / 39 字，比值 1.46）不合格。須裁決是收短範例的 `perfect` 版，還是把門檻放寬至 1.5。

---

## 7. 已知限制（明文記載，不在本里程碑解決）

1. **社畜端的熱度不對稱**：`_evaluateBudgetWorker` 的滿意度為 `budgetScore + themeScore − boredomPenalty`，**不含熱度項**；`totalHype` 僅透過 `< boredomThreshold(168)` 扣 25 的二元門檻進入。
   **§4 實測**：社畜端「三態兩兩相異」在所有幅度下**恆為 0.0%** —— 三態要嘛不改變滿意度，要嘛整整改變 25 分，**從不漸進**。在 ±3 幅度下完全同分為 62.9%，即約 37% 的行程會遇到那道斷崖。
   （v2 曾寫「反無聊觸發率 24.70%，**故**約四分之三的社畜局三態一分不動」—— 那個「故」不成立：24.70% 是 `totalHype < 168` 的行程比例，而三態改變結果的條件是 `normal` 態的 `totalHype` 落在門檻 ±Δ 的帶內。實測比例與該推論不符。）
   **緩解**：本 SPEC 以快門獎金繞過，玩家仍有技巧回饋。另建議沿用因果 SPEC v5 §3 的階梯張力反饋，讓玩家在接近 168 門檻時得到預警 —— 否則 25 分斷崖會是無預警的。
   **根因**是社畜的計分模型從一開始就不看熱度，那是客戶核心性格的一部分。要修須另立 SPEC 並沿用 Amendment 01 的全母體搜尋方法論。

2. **台灣卡表 descope**：`main.dart` 僅注入 `kyotoNightMaterials`；Amendment 01 已裁定「台灣真實地圖原地封存，留待 Reality Run 階段」。本里程碑規模因此為 32 → 96 張、64 段文案。
   AC-M5-2.1 遍歷實際注入池而非硬編碼清單，故台灣日後復活時缺三態版本會自動轉紅。

3. **內容成本的長期承諾**：即使砍掉 `name` 並 descope 台灣，**每個城市 DLC 的文案量從 32 段變為 96 段**。以現行密度（每段 35~45 字、地點專屬、感官具體）估，一段含修潤約 15~20 分鐘，**每城約 24~32 小時純寫作**，且序列阻塞該 DLC 上線。
   退路（若 DLC 節奏吃緊）：只手寫 `failed` 版，`perfect` 版改用共用點睛句庫。**不得三版皆由句庫組合** —— 依 §1.3，本機制的樂趣就在失手時那句具體又好笑的描述。

4. **`perfect` 的長期回報預期由勳章系統（M6）承接。** §4 證明局內通道結構上堵死，快門獎金是 M5 的權宜解。本 SPEC **不為勳章新增任何欄位或介面**（`CLAUDE.md` §3）。

---

## 8. 設計裁決紀錄

### 8.1 v1 / v2 裁決（2026-09-12，使用者逐題裁決，v3 維持有效）

| 議題 | 裁決 |
|---|---|
| 三態影響哪一側 | **產出側**（素材品質），成本固定 |
| 更好的卡怎麼來 | **每張卡手寫三版本**，`name` 三版相同 |
| Runner 與 Snapshot | 本里程碑**只做快門** |
| 快門玩法 | **收縮準心**；絕景加構圖，**單一手勢** |
| 跳過與重試 | **皆不可**，無障礙由難度承接 |
| 難度 | 三檔，且**放大局內幅度**（(a) 案） |
| 事件範圍 | **縮小**，不建單局事件溯源管線 |
| 台灣卡表 | **descope** |
| 勳章 | 第一版**只做收集**，資源與解鎖延後；排在 M5 之後 |

### 8.2 v3 裁決（2026-09-12，第二輪雙軌覆核 + 全母體搜尋後）

| 議題 | 裁決 | 落點 |
|---|---|---|
| 幅度 | 由搜尋定案，**±2 / ±3 / ±4**（絕景 ±3 / ±5 / ±7） | REQ-M5-02.5 |
| 設計語言 | **損害控制**，非獎勵機制 | §1.3 |
| 中斷處理 | 凍結 → **判 `normal` + 每局一次配額** | REQ-M5-04.2 |
| 難度持久化 | 新增事件型別**並**加 `lastDifficulty` 投影欄位 | REQ-M5-05.4 |
| 節奏預算 | 改為單次儀式上限 + 16 次總量 60 秒 | REQ-M5-09 |
| 絕景判定 | 取較差者 → **最多降一階** | REQ-M5-03.5 |
| 快門獎金 | 封頂**提交行程的 `perfect` 張數 ≤ 4**，40 金 × (0.5/1.0/1.5) | REQ-M5-05.6 |
| 設計意圖 4 | **刪除**（已被 `riskLevel` 解耦證偽） | §1.2 |
| 設計意圖 1 | **降級**為「失手卡的損害控制」 | §1.2 |
