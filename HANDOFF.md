# HANDOFF — 交接紀錄

**最後更新**：2026-09-12 下午
**分支**：`feature/mvp-amendment-01`
**上一份交接**：停在 2026-09-07 任務 C 階段，已整份汰換。

---

## 0. 三十秒版

本次會話做了七件事：

1. **解除一條擋住所有動畫的規格禁令** —— `SPEC_MVP_CAUSAL_FEEDBACK.md` §5.3 原本禁止骨骼／粒子動畫，已撤銷（v6）。
2. **M5 快門微動作 SPEC 寫到 v4**，經**三輪雙軌覆核**（企劃＋工程各三次，合計 29 條 P0）。
3. **v4 改為雙層結算** —— 這是本次會話最重要的設計轉向，見 §3。
4. **跑了三支全母體分析腳本**取得實證數據，其中一支推翻了 v2 的幅度、另一支推翻了 v3 的整套立論。
5. **修掉一個會滅存檔的 bug**（commit `14f6e92`）。
6. **確立勳章系統構想**，排在 M6，構想記於 §5。
7. 更新 `CLAUDE.md`（新增「文件檔頭視為可能過期」）與本交接。

**下一步**：送 M5 SPEC v4 覆核（**建議第四輪雙軌**，重點見 §3.5）。v4 通過才進 plan。

---

## 1. 版控狀態

```
e3df329 docs(spec): move the shutter signal onto a second scoring layer   ← M5 SPEC v4
1361c7b test: correct the amplitude search population and restore candidates
573b36c test: measure the golden-slot ratio under three-tier pools
b340a08 docs: refresh the handoff after v3 and the event-log fix
f70e9f7 docs(spec): settle micro-action magnitudes from an exhaustive search  ← v3
ce4e2b8 test: stop the amplitude search from taxing every suite run
14f6e92 fix(core-loop): keep unknown event types instead of erasing the save
fbf4280 feat(lighting): dynamic 4-act time of day progression              ← 並行 agent
eb4f912 feat(map): ground 32 spots to true Kyoto geography and 5 districts  ← 並行 agent
239724e docs(spec): rewrite micro-action spec after dual-track review        ← v2
66f0629 docs(spec): add micro-action shutter QTE spec for milestone M5       ← v1
128400b docs: judge build status from git log, not document headers
9c2bd50 docs(spec): mark amendment 01 completed and permit engine-native animations
```

`flutter analyze` 0 issues、`flutter test` **549 passed / 3 skipped**（skip 的是三支分析腳本）。

**未進版控**：角色素材與京都底圖（`assets/images/guide_*_generated.png` 等，仍在增加），由並行 agent 產出。見 §6。

**並行 agent**：全程在同一 repo 施工。依序做了因果可視化 → 京都圖資接真實地理與 5 個行政區（`eb4f912`）→ 四幕制時段光照（`fbf4280`），並持續產出角色素材。**動 `lib/` 之前先確認它是否仍在跑。**

> **已驗**：`eb4f912` 改了 285 行 `kyoto_night_layout.dart`。重跑幅度搜尋後十組數字與原先完全相同 —— 那次改動只動座標與分區，未動素材池。

---

## 2. 已完成且已 commit

### 2.1 動畫禁令解除（commit `9c2bd50`）

`SPEC_MVP_CAUSAL_FEEDBACK.md` v5 §5 第 3 款原文：

> **禁止骨骼／粒子動畫**：表情以靜態圖示切換呈現，不引入新套件（CLAUDE.md §6）。

該款以「不引入新套件」為由，連帶禁止了**不需要任何新套件**的表現手法（Flutter `AnimationController`/`Tween`、Flame `Effect`/`ParticleSystemComponent`），是整個專案視覺靜態化的制度來源。已撤銷並改為明確區分：新增相依套件仍禁止，引擎內建動畫能力開放。骨骼動畫（Spine／Rive）因確實需要新套件，仍不在範圍。

同時修正兩處過期檔頭：`SPEC_MVP_AMENDMENT_01.md`（「v7 草案待覆核」→ 實為已驗收）、`SPEC_MVP_CAUSAL_FEEDBACK.md`（「待 G4 清償後實作」→ 實作中）。

### 2.2 `CLAUDE.md` 新增「文件檔頭視為可能過期」（commit `128400b`）

放在 §7 之後。核心：**判斷施工狀態一律以 `git log` 與驗收報告為準，文件自述的狀態行不算證據。** 判讀順序 `git log -- <spec>` → `*_VERIFICATION.md` 結論節 → 程式碼 → 最後才是檔頭。附帶「規格沒寫 ≠ 沒規劃」。

### 2.3 M5 SPEC v1 → v2 → v3（commit `66f0629` → `239724e` → `f70e9f7`）

`SPEC_MVP_MICRO_ACTION.md`，567 行。目前檔案內容是 **v3**，狀態 `Draft v3 — 待覆核`。
兩輪雙軌覆核合計 12 條 P0 已全數處理，§4 的搜尋數據已回填成 `AC-M5-9.2` 的門檻。

### 2.4 事件日誌前向相容修復（commit `14f6e92`）

原本的行為鏈：`CuratorEvent.fromJson` 對未知 `type` 拋 `FormatException` → `loadEvents` 把整份日誌搬去 `.bak` 回空 → `loadSave` 重建全新身分 → **玩家金幣與裝備歸零**。M5 的 `difficultySelected` 會是第一個踩到的新種類。

修法是**原文保留**：`type` 改可空（`null` = 本建置不認得），新增 `rawTypeName` 保住原文供往返，重播 `continue` 略過，`_doAppend` 的整份寫回因此把它原樣寫回、`seq` 不再塌陷。

守住兩條邊界並各有測試釘住：**真正損毀的 JSON 仍走 `.bak`**、**缺 `eventId`/`seq`/`occurredAtUtc` 的仍視為損毀** —— 前向相容不得變成吞掉真損壞的藉口。

測試：`test/data/core_loop/event_log_forward_compat_test.dart`，7 條全綠。

### 2.5 分析腳本降噪（commit `ce4e2b8`）

`test/analysis/micro_action_amplitude_search_test.dart` 預設 `skip`。它是求參數的分析腳本不是驗收條件，留在版控是為了讓 §4 的數字可重現（約 30 秒），但不該對每次 `flutter test` 收 28 秒的稅。**要重跑就拿掉那行 `skip:`。**

---

## 3. M5 SPEC v4：雙層結算

**v4 已出並 commit（`e3df329`，625 行），狀態 `Draft v4 — 待覆核`。**

### 3.1 為什麼從 v3 大改

第三輪覆核時發現 **v3 §4 的搜尋母體錯誤** —— 只列舉 3 槽行程，漏掉同樣合法的 4 槽全填。修正後重跑（`1361c7b`）：

| 指標 | v3 記載 | 修正後 |
|---|---|---|
| 網紅 `perfect` 無作用比例 | 59.7% | **88.3%** |
| 網紅 三態兩兩相異 | 40.3% | **11.7%** |
| 社畜 三態完全同分（±3） | 62.9% | **82.7%** |

且這些數字**在 ±2 到 ±20 的全區間完全不動** —— 只有硬性 `clamp(0,100)` 會產生對幅度免疫的行為。

**診斷因此從「獎勵不足」改成「訊號消失」**，而根因不在 M5：是 **100 分制在 4 槽情境下沒有上方空間**（社畜的 `boredomThreshold 168` 同理，是 3 槽時代的數字，4 槽行程的 `totalHype` 普遍 200~300）。任何加大幅度的方案都是對一面牆推得更用力。

### 3.2 v4 的解法

```
第一層（既有，一個字元不動）   滿意度 / 評等 / 佣金       ← 你「編」得好不好
第二層（新增，無上限）         網紅=擴散觸及 / 社畜=CP值   ← 你「拍」得好不好
```

- **`hypeValue` 凍結**，素材改攜帶 `shotTier`，只餵給第二層
- 第二層是 $\sum(\text{hype} \times \text{tierFactor})$ 形式的**乘性量** —— 無天花板、無門檻、**排列無關**
- **排列無關使野外 HUD 能即時預估** → 補上覆核指出「v3 完全沒有維持不失手的正向節拍感」
- **只有兩條 Amendment 01 的 AC 要重跑**（`AC-A1-5.4`、`AC-A1-6.4`），一維求解

### 3.3 v4 的參數（全部由實測反推，非推導）

| 難度 | `tierFactor`(F/N/P) | 跨度 | $t_{吻合}$ | $W_p$ | 動畫全長 | 期望 factor (σ=120/300) |
|---|---|---|---|---|---|---|
| `tourist` | 0.90 / 1.00 / 1.05 | 15% | 1400 | 800 | 2000 ms | 1.049 / 1.042 |
| `photographer` | 0.72 / 1.00 / 1.22 | 50% | 1100 | 240 | 1600 ms | 1.150 / 1.017 |
| `decisiveMoment` | 0.62 / 1.00 / 1.70 | 108% | 800 | 120 | 1200 ms | 1.188 / **0.903** |

**難度終於是賭注**：熟練者在最高檔拿 1.188，生疏者只拿 0.903 —— 比留在 `tourist` 還差 13%。

金幣：**$c = 0.75$、CP 分母地板 2000**。由**全母體最大值**反推（不是中位數）：

| | 中位數金幣 | 佔 Pass 佣金 | 最大值 | 上界 |
|---|---|---|---|---|
| 網紅 | 87 | 5.8% | 221 | 225 ✓ |
| 社畜 | 53 | 5.3% | 111 | 150 ✓ |

### 3.4 v4 連帶關掉的既有 P0

| 第三輪的發現 | v4 後 |
|---|---|
| 快門獎金期望非單調 | 獎金整條刪除 → 消失 |
| `tourist` 三欄全贏 | 期望 factor 隨技巧翻轉 → 關閉 |
| 中斷是正 EV | 改乘性折扣 ×0.9，負 EV 可證 → 關閉 |
| ±4 超過哲學軸 15 分 | `hypeValue` 凍結 → 消失 |
| 幅度小到沒感覺 | 15%/50%/108% 相對差 → 關閉 |
| 缺正向節拍感 | 野外 HUD 即時預估 → 關閉 |
| `$T_0$` 對 27/32 張無來源 | 全部統一「按住→放開」→ 關閉 |
| 節奏次數算錯 | Lv.1 **17** 次／Lv.3 **26** 次，上限改 90 秒 → 關閉 |
| `reasonCode` 1 個還是 3 個 | 裁決為 **1 個**，文案依三態切換 → 關閉 |
| 兩條恆真 AC | 重寫；新的效果驗收改用**單張卡降幅** → 關閉 |

### 3.5 建議送第四輪雙軌 —— 我自己看到的可疑點

1. **`AC-M5-9.2` 的單張降幅門檻（3.0/8.5/11.5%）只留約 10% 餘裕**。若 `tierFactor` 日後微調，這條會先紅。是好事（它該紅），但要知道它敏感。
2. **CP 地板 2000 的副作用沒實測**：它把低成本行程的 CP 壓平，可能削弱社畜端「省錢」的表達力 —— 而社畜的**第一層本來就獎勵省錢**，兩層訊息可能不一致。已列入 v4 §6 待決 4。
3. **`REQ-M5-03.6` 的相機性質仍是「plan 必須解」而非「已解」**：`CameraFollow` 的 `returnDelay` 3 秒 < `tourist` 儀式 3.2 秒，且它讀自由奔跑的 `Stopwatch`，暫停引擎擋不住。v4 把它寫成前置條件 5，但沒給解法。
4. **第二層對「編排」零獎勵**是刻意的，但 $c$ 若日後上調，玩家可能放棄編排去堆第二層。v4 用 15% 上界擋，沒有 AC 直接驗證這件事。
5. **兩層分數的認知負荷**：結算面板已有滿意度、評等、佣金、故事獎金、因果歸因，再加第二層與其金幣。v4 §5.5 只列為「美術與版面成本」，我認為低估了。

---

## 4. 三支分析腳本（皆已進版控、預設 `skip`）

拿掉各檔的 `skip:` 即可重跑。

| 腳本 | 量什麼 | 執行時間 | 結論 |
|---|---|---|---|
| `micro_action_amplitude_search_test.dart` | 三態動 `hypeValue` 時的滿意度分佈 | ~80 s | **第一層塞不進訊號**（`P=N` 88.3%、三態相異 11.7%，對幅度免疫） |
| `golden_slot_tier_regression_test.dart` | 三態對 `AC-A1-5.1` 黃金槽比例的影響 | ~8 min | 5.00~18.69%，門檻 30%，**全部安全**；且 `perfect` 是**降低**比例（天花板造成平手），與 v3 的推測方向相反 |
| `layer_two_score_search_test.dart` | 第二層分數落點與 CP 地板掃描 | ~3 s | 訊號存在率 **100%**、單張降幅 3.3/9.3/12.7%、`c` 與地板由最大值反推 |

**重要教訓（兩次）**：

1. **母體要與被對照的 AC 同基準。** 第一支腳本只列 3 槽，導致 §4 的每一個百分比都建立在被截斷的母體上，而 v3 的 `AC-M5-9.2` 四條門檻全部抄自那張表。
2. **上界要看最大值，不是中位數。** 第三支腳本第一次只印中位數，`c = 1.0` 看起來安全；改印 max 才發現社畜 CP 最大 530 對上界 150 大幅超標。

---

## 5. 勳章系統（構想已定，文件未寫）

**使用者裁決**：勳章第一版**只做收集**；「給局外資源（金幣、解鎖）」與「解鎖內容（新哲學、新裝備、新城市）」**都可以加在後面**。

**排程**：不塞進 M5。理由是 M5 已兩輪覆核未過、桌上還有十幾條 P0，再加子系統會讓「快門手感成不成立」與「勳章設計對不對」混在一起無法歸因；且**勳章等 M5 落地會設計得更好** —— `perfect` 的實際達成率要等實機調完難度參數才知道，那個數字直接決定「一局全完美」該是家常便飯還是稀有成就。

```
M5  快門微動作 → M6 勳章系統 → M7 Runner 衝刺
```

**三條會框住設計的既有約束**：

| 約束 | 影響 |
|---|---|
| 第一版禁止排行榜／好友／雲端／**遙測**，且不得提前埋鉤子（`CROSS_CUTTING_CONSTRAINTS.md`） | 勳章只能是**單機、給自己看的**。不能有「全球 3% 玩家擁有」這類東西 |
| **單局事件溯源管線不存在** | 「單局過程中」的條件無法重播 |
| 重播必須決定性（CC-3） | 判定不得依賴當下時間或亂數 |

**條件的三個類別**（第二條約束推導出的鐵律：**條件必須能在結算那一刻算出來，或由結算數字累加得出**）：

- **① 單局成就 —— 可行**。結算當下 run state 有的：4 槽位（含三態）、哲學、難度、客戶、滿意度、評等、`gatheredPoiIds`。
  例：「以決定性瞬間難度通關」「4 槽全部完美」「五張絕景同一局全部完美」
- **② 累計成就 —— 可行**。只要結算事件 payload 帶出彙總數字，重播就能累加終身計數（`CuratorSaveData` 現在就是這樣累積金幣與通關數）。
  例：「累計 100 張完美快門」「五種哲學各通關一次」「京都 32 個 POI 全部踩過」
- **③ 單局過程中的序列條件 —— 不可行**。例：「連續 10 次完美不失手」。需要記住採集順序，而單局狀態全在記憶體。除非在結算時壓成一個數字（如「本局最長連續完美數」）帶進 payload —— 那就退化成 ②。

**框架的抽象邊界**（`CLAUDE.md` §3）：會變的軸是**條目本身**（未來一定會加），不是判定機制。所以框架該薄到只擋住那條軸：**一個條件表、一個發放事件、一個持有清單**。不做外掛式的成就引擎。

**已做**：M5 SPEC v3 的 §7 已知限制 4 已註明「`perfect` 的長期回報預期由勳章系統（M6）承接」，且未新增任何欄位或介面。

**待辦**：開 `SPEC_MVP_BADGES_PROPOSAL.md`（提案，不是 SPEC，比照 `TASK_D_LOCAL_TIER_PROPOSAL.md` 的格式）。**建議等 M5 落地、`perfect` 的實際達成率出來後再寫** —— 那個數字直接決定「一局全完美」該訂成家常便飯還是稀有成就，現在訂條件是憑空猜。本節的內容已足以讓構想不散掉。

---

## 6. 角色素材（本次會話期間出現，未進版控）

素材缺口原本是 M6 Runner 的硬阻塞（`PlayerComponent` 是 17 行的 `CircleComponent` 紅圓，沒有任何角色 sprite）。現在 `assets/images/` 下已有：

```
共 18 張，分三類（阿導與女性版各一套）：

  大世界行走表                                   2 張  例 1448x1086
  動作表                                      2 張  例 1246x1262
  對話立繪（含 8bit、半身、怒／笑／哀等表情變體）               14 張  例 1024x1536
```

**數量仍在增加中**（交接當下 8 → 18 張），清單以 `ls assets/images/guide_*` 為準。

**尚未做的事**：

1. **切圖與 sheet 契約**。這些是大圖，不是可直接播放的 sprite sheet。現有前例是 `ocean_wave_sheet.png` = **48×16**（3 frame × 16×16），播放器範本是 `ocean_waves_component.dart`（149 行，含 `update(dt)` + frame 輪播 + 相位錯開）。
2. **尺寸決策**。現行小人是 `CircleComponent(radius: 8)` = 直徑 16 px；`TASK_D_LOCAL_TIER_PROPOSAL.md:236` 的分析假設角色為 **24 螢幕像素**。
3. `pubspec.yaml` **不用改** —— 宣告的是目錄 `assets/images/`，丟檔進去就能用。
4. M5 **不需要**角色 sprite（快門 QTE 是取景框與準心，幾何 UI）。這是 M6/M7 的前置。

---

## 7. 已清償：事件日誌的資料遺失 bug

**已於本次會話修復（commit `14f6e92`），詳見 §2.4。** 本節保留標題以免其他章節的交叉引用斷掉。

另一條同類的既有缺陷**尚未修**，已列為 M5 SPEC v3 的 §5 前置條件第 4 條，同樣**建議獨立 commit**：

`curator_run_controller.gatherPoi()` 會用 `nearestGatherablePoi()` **重算並覆蓋傳入的 `poiId`**，且沒有 `replaceGatheredPoi()` 那樣的 `expectedPoiId` 防護。插入 QTE 後，「按下取材」與「實際呼叫 domain」之間多了 1.2~2.0 秒，期間位置持續更新 → 玩家對 A 景點做的 QTE 會套用到 B 景點的素材上。

---

## 8. 本次會話的錯誤紀錄（同一形狀重複六次，接手者請引以為戒）

`CLAUDE.md` 新增的那條規則就是為此而寫。四次都是**讀到一個數字／狀態就拿來用，沒確認它是什麼語意**：

| # | 錯誤 | 真相 |
|---|---|---|
| 1 | 讀 `SPEC_MVP_AMENDMENT_01.md` 檔頭「v7 草案，待覆核」，據此判斷微動作的閘門未開 | 檔頭過期。T0~T14 共 16 個 commit 早已執行並驗收（`MVP_AMENDMENT_01_VERIFICATION.md` §6、commit `0b9a30a`） |
| 2 | SPEC v1 三處寫 `ΔHP = 10 + riskLevel × 2`，抄自 `SPEC_MVP_POI_GATHERING.md` | 該公式已被 Amendment 01 D8 廢除。現行是 `gatheringHpCost() = riskLevel × 6`（`curator_run_state.dart:17`）。照字面實作的 AC 會當場紅 |
| 3 | 用 `grep -c "isSpotlight: true"` 得到京都絕景 6 張 | 第 12 行是**註解**提到該字串。實為 **5 張** |
| 4 | 把 `AC-A1-4.4` 的 `>= 16` 當成可達 POI 的實際值 | 那是**產品下限**。同一條測試緊接著 `expect(reachablePool.length, 32)`。真正把 QTE 次數壓在 16 附近的是 **HP**（100 ÷ 6），不是 POI 數 |

| 5 | 幅度搜尋腳本只列舉 3 槽行程，且 commit 時還帶著細分run 的窄候選清單 | 4 槽全填同樣合法且常見。§4 的每一個百分比都建立在被截斷的母體上，而 v3 的 `AC-M5-9.2` 四條門檻全部抄自那張表；SPEC 引用的 ±12/±20 兩列更是**版控裡的腳本重現不出來** |
| 6 | 第二層分數只看中位數就定 $c = 1.0$ | `AC-M5-5.8` 要求上界在**全母體**成立。改看最大值才發現社畜 CP 最大 530 對上界 150 大幅超標 |

另有兩次**推論**錯誤（性質不同但同樣承重）：

**（i）** 把可疑的 `AC-M5-9.4` 直接寫進 SPEC 並 commit，才去驗證 —— 使用者明確指出這個順序是錯的。量測只要 30 秒、零件現成、而且當天已因同類問題被打回四次。**正確順序：懷疑 → 量 → 依結果寫 → commit。** 凡是自己都在懷疑的條款，不准進檔案；要嘛當場驗完再寫，要嘛寫成「待驗證」而不是寫成 AC。把可疑的東西寫成 AC、再用註解說它可疑，是最差的組合。
（順帶：那條的實測結果是**安全的**，而我寫的理由**方向還是錯的** —— `perfect` 會**降低**黃金槽固定比例，因為天花板造成平手；推高它的是 `failed`。）

**（ii）** SPEC v2 §7 限制 1 寫「反無聊觸發率 24.70%，**故**約四分之三的社畜局三態一分不動」。那個「故」不成立 —— 24.70% 是 `totalHype < 168` 的行程比例，而三態改變社畜結果的條件是 `normal` 態的 `totalHype` 落在門檻 ±Δ 的帶內。§4 的實測顯示在 ±3 幅度下**完全同分只有 62.9%**（不是 75%），且在 ±12 下只剩 10.4% —— 方向與我寫的相反。**未經驗算的推論不要放進論證鏈的承重位置。**

---

## 9. 覆核流程的操作筆記

雙軌覆核（企劃軌＋工程軌並行，各自獨立不互通）在本次會話**兩輪都有效**：

- 第一輪：兩軌**各自獨立**算出同一個致命問題（三態對結算不可見）。這種收斂度是強訊號。
- 第二輪：工程軌 4 條 P0、企劃軌 8 條，重疊 2 條，分工乾淨（工程抓架構與可測性，企劃抓數值與體驗）。

**紀律**：agent 報告一律**逐項複驗再採信**。本次會話複驗了十餘條承重主張，**全部屬實** —— 但正因為每條都查過，才敢把「絕景 5 張」「可達 POI 32」這兩條反過來推翻我自己先前報給使用者的數字。

**給覆核 agent 的 prompt 要點**（本次有效的寫法）：明確給出要驗證的前一輪問題清單、要求「標示哪些已關閉／哪些沒關乾淨／哪些是新引入」、要求「實際讀程式碼驗證，不要憑印象」、要求算式要算出數字、並明確禁止改檔案。

---

## 10. 接手第一步

```bash
dart run build_runner build --delete-conflicting-outputs
flutter analyze            # 須 0 errors / 0 warnings
flutter test               # 交接當下 549 passed / 3 skipped
```

然後讀 `SPEC_MVP_MICRO_ACTION.md`（v4，625 行）。**下一步是送第四輪雙軌覆核**，重點與我自己看到的五個可疑點列在 §3.5。覆核通過才進 plan（`CLAUDE.md` §1 的流程不可跳級）。

§3.3 的數字全部由實測反推，不必重算；要重跑就拿掉 §4 各腳本裡那行 `skip:`。

**三輪覆核累積的判斷**：v4 的結構性裁決（雙層結算、`hypeValue` 凍結、乘性 `tierFactor`、統一按住→放開、中斷乘性折扣）建立在實測上，建議不要在 plan 階段推翻。真正還沒清償的是 §3.5 的第 3 條（相機性質）與 §6 的文案交付。
