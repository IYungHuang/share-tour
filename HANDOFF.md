# 交接 — 2026-09-07

## 一句話

任務 C 的 GPS 管線昨天第一次通過真機驗證，過程中修掉四個 bug、推翻三個自己的判斷；
現在卡在**三件需要使用者裁決的事**，裁決完才能把 SPEC C 增修草案併入本文、進 plan。

---

## 昨天發生了什麼

出門前的問題是「GPS 模式下 REAL 里程始終是 0」。實際查出來是**四個獨立的 bug 疊在一起**：

| # | Bug | 為什麼測試抓不到 |
|---|---|---|
| 1 | 相機平移被每幀覆寫（雙頭寫入） | 相機測試只驗狀態機，AC-8.3「free 下中心不變」由一個忽略手勢的實作完美滿足 |
| 2 | `geolocator_android` 弄丟所有 `has*` 旗標 → **100% 丟棄** | 所有測試都自己造 `GeoFix`，沒有一條跨過「從 `Position` 轉換」這一步 |
| 3 | 切換來源時基準沒重置 → 首筆灌入 **13 萬公尺** | `resetBaselines()` 寫好、測過、**沒有任何呼叫端** |
| 4 | 節流與訂閱管理整組沒接線 | `FixThrottle`、`LocationSubscriptionManager` 在 `lib/` 內零引用，只有測試在用 |

**共通形狀：元件正確，接線沒人測。** 四個都已修復並提交。

其後三趟實測（見下）推翻了我自己的三個判斷，也讓 SPEC C 增修草案改寫兩次。

---

## 目前狀態

```
6d01f8a  地圖美術：台北101 重繪、地標放大、夜市、山峰、動態海浪   ← 另一個 agent
c569da4  速度欄位日誌 + 匿名化的實測軌跡夾具
87fb9a5  GPS 實測發現與地方層裁決（兩份文件）
0775396  地圖、遮罩、路網從地理資料生成                        ← 另一個 agent
adaedbb  修正過期的圖資違反記載
abc2424  接線節流、訂閱生命週期、權限串流
23b79c4  切換來源的落差不再計入里程
a0978a3  修復 Android 掉失的量測旗標
0df3361  相機平移
```

`flutter test` → **192 passed, 1 skipped**｜`flutter analyze` → **0 issues**｜工作區乾淨

**手機（CPH2783）**：裝著含速度欄位日誌的除錯建置。
⚠️ **螢幕休眠被我改成 30 分鐘做實驗用，記得還原**：
```bash
adb shell settings put system screen_off_timeout 120000
```

---

## 卡在哪裡：三件要裁決的事

全部在 `SPEC_C_AMENDMENT_01.md` §5。**這三件不決，SPEC C 增修草案無法併入本文。**

### 1. 里程的角色（路徑 A / B）

| | A：降級為顯示數字 | B：維持機制輸入 |
|---|---|---|
| 成果載體 | POI 打卡 + 探索網格計數 | 里程 |
| 里程機制 | 只留品質標記 | 品質標記 + 冷啟動有界抑制 |
| 校準協定 | T2~T4 降為附錄 | 全部必要 |
| Q21/Q22 | 消失 | 仍阻擋 |

三方（主筆、架構師、資深工程師）建議 A。

### 2. `CROSS_CUTTING_CONSTRAINTS.md` 要不要改

CC-5 規則 3 是**封閉清單**（「僅限」四項）。下列兩項不在上面：

- **探索網格 id**（路徑 A 的成果載體）
- **離線重播用的 ENU 相對位移序列**

無論匿名化做得多好，條文層級都要改上位文件。§5.2 有三選項取捨表。

### 3. 確認刪除道路吸附

縮減 §2.2 的既有契約。三方技術意見一致（它是死碼，而且「吸附到離散節點」這個形式沒有正確用法），但需要使用者確認。

---

## 裁決後的下一步，依優先序

**1. 零圖資成本的三項（最高優先，但屬任務 A/B）**

- 距最近 POI 的公尺數，走路時變小
- 小人的**精度圈**
- 顯著位移發生時給回饋（數字跳動、足跡點、短音）

兩位覆核者獨立地認為這是「玩家會不會再打開一次 app」的關鍵，而且**零資產成本**。
它同時解掉 `displayPixel` 分離必然帶來的新困惑（畫面一直動、計數不動 → 玩家會覺得「遊戲有在追蹤我但它不承認」，而且可以截圖證明）。

**2. SPEC C 增修草案併入本文 → plan 階段**

八項修訂，內容見該檔 §2 總表。

**3. 實地驗收（DoD 7a / 7b）**

- 靜止 30 分鐘 × 3 趟（不同時段），至少一趟**清 GNSS 快取後立即開始**
- 每趟記錄 `accuracyGatedFixCount`、`acceptedFixCount`、精度分佈（Q22 唯一的資料來源）
- 趟次有效性：品質標記通過率 > 50%，未達則該趟無效（不是通過）

**4. 任務 D**

方向已裁決，見 `TASK_D_LOCAL_TIER_PROPOSAL.md` §7。等 SPEC C 落地再寫 SPEC D。

---

## 昨天的實測數據（參數爭論的唯一仲裁依據）

| 趟次 | 條件 | 結果 |
|---|---|---|
| A | 40 分鐘，螢幕自動休眠 | **12 筆** Fix，中位間隔 219 s |
| B | 38.4 分鐘步行，螢幕常亮 | **142 筆**，中位間隔 8.2 s，里程 2321 m（真值估 1100~1400） |
| C | 16.4 分鐘靜止 | 78 筆，里程 179 m（應為 0） |

B 趟已匿名化存為 `test/fixtures/trace_taichung_20260906.json`，可離線重播。
**任何參數改動都該先用它跑一次再談。**

---

## 陷阱（都是昨天踩過的）

1. **另一個 agent 與你共用這個 repo。** 它會直接覆寫受版控的檔案、也會自己 commit。
   **動工前先把自己的東西 commit**，否則被蓋掉救不回來。
   它的報告要逐項複驗——第一份有一條不實（宣稱是「原線稿備份」的檔案其實是新圖的中間版本）。要求它「只回報實測數字、不要形容詞」有效，第二份之後都屬實。

2. **「達成 0」和「正確地達成 0」在數字上長得一模一樣。** 靜止趟的里程 0 曾被當成暖機機制有效的證據，實際是暖機**永不結束**（該趟只有 11% 的 Fix ≤15 m，從未收斂）。

3. **用來調參數的資料不能同時用來證明參數穩健。** 消融實驗才是能證偽的形式。

4. **樣本最大值不是上界。** 「位置誤差 33 公尺」由單一筆 Fix 決定，且精度 ≤35 m 與 ≤25 m 兩組同值——平台自報精度與實際誤差的相關性在尾端是斷的。

5. **原始經緯度不得進版控。** 昨天差點把實測軌跡直接寫進 `test/fixtures/`，CC-5 擋下的是開發過程的一次外洩，不是產品功能。

6. **實測前先確認測試協定。** A 趟的 12 筆是因為螢幕休眠導致訂閱被取消（設計行為），那組數據無法用來判定 DoD。

---

## 文件地圖（新增的兩份）

| 檔案 | 用途 |
|---|---|
| `SPEC_C_AMENDMENT_01.md` | SPEC C 增修草案 v3。八項修訂 + 三件待裁決 + §10 方法論教訓 |
| `TASK_D_LOCAL_TIER_PROPOSAL.md` | 地方層與馬賽克轉場。§7 是已裁決的五個方向，§8 是覆核推翻的原始判斷 |
| `test/fixtures/trace_taichung_20260906.json` | 匿名化的實測軌跡，142 筆 |
