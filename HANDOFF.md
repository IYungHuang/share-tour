# HANDOFF — 2026-09-11

跨階段交接。不只交接當前施工計劃，而是讓接手者知道：**現在在哪、哪些東西被刻意凍結、哪些不准提前做、以及每個決定背後的理由**。

---

## 0. 三十秒版本

- 遊戲的**核心迴圈已經全部實作完畢**（M1~M4），383 條測試全綠，`flutter analyze` 0/0。
- 但**核心迴圈的數值是壞的**：主題分恆為 100、客戶滿意度不可能失敗、採集觸發窗只有 0.135 像素導致虛擬移動走不完一局。修這三件事的 SPEC 與 PLAN 都已寫完並經多輪覆核，**尚未施工**。
- **GPS 與真實地圖已經做完了，但那是提前做的**（屬第三階段）。現在**凍結**，不准再投入工時。
- 下一步是把大世界從台灣真實地圖換成**小尺度手繪京都夜間街區**，台灣圖資原地封存留待第三階段。

---

## 1. 產品四階段地圖與現在的位置

| 階段 | 核心問題 | 狀態 |
|---|---|---|
| **P1 — MVP** | 這遊戲好不好玩？ | **← 現在在這裡。** 迴圈完成、數值待修 |
| P2 — Creation | 玩家會不會一直創造不同旅行？ | 未開始 |
| P3 — Reality | 玩家真的想去嗎？ | **GPS 管線與台灣真實地圖已提前完成，凍結中** |
| P4 — Ecosystem | 能不能形成旅行經濟？ | 未開始 |

**P1 的驗收不是功能清單，是四個問題**（依序）：① 玩家會不會玩完第一局？② 會不會主動開始第二局？③ 知不知道自己為什麼失敗？④ 會不會想用不同策略再玩一次？

最重要的質性訊號：**「我知道我哪裡搞砸了，我想再試一次。」**

第三條現在**驗不了** —— 客戶滿意度在數學上不可能失敗，玩家沒有失敗可以歸因。這就是為什麼數值修訂排在所有其他事情之前。

### 每階段不可提前做的事

這條規則是專案失控的主要防線，已經被違反過一次（GPS 提前到 P1 之前）：

- **P1 禁止問**「GPS 怎麼做？」→ 應該問「玩家為什麼想再玩？」
- **P2 禁止問**「怎麼讓真人開始直播？」→ 應該問「玩家能不能創造出不同而且有個性的旅行？」
- **P3 禁止問**「怎麼做完整旅行社？」→ 應該問「遊戲裡創造的旅行，能不能真的被走完？」
- **P4 才問**「這些旅行能不能被其他人參與，進而形成經濟？」

---

## 2. 現況快照

branch `main`，工作區乾淨。`flutter analyze` 0 errors / 0 warnings，`flutter test` **383 passed**。

### 已實作（P1 範圍內）

| 里程碑 | 內容 |
|---|---|
| M1 | 純領域核心迴圈：3+1 資源（HP/Budget/Theme/Hype）、5 種旅行哲學、素材契約 |
| M2 | 4 槽位時間線編輯器、雙客戶 Review 彈窗 |
| M3 | 大世界 POI 踩線取材、野外 HUD、體力透支返程 |
| M4 | 行前委託、黑市裝備升級、本機存檔（事件日誌） |

### 本次交接期間完成的工作

```
4576b7f  M4 基準點（另一 session 的成果，原樣提交以便回退）
5000207  修五個審查抓到的 bug
93a158e  存檔改為 append-only 事件日誌（CC-3）
32d74ea  架構測試補兩條規則，並修掉抓到的倒向相依
3a08fcc  main.dart 接線測試（引擎暫停協調器、彈窗路由）
0cadd89  事件日誌韌性強化（併發、壞資料、seq 全序）
9342771  seq 改由日誌指派（消除 provider 間的隱形耦合）
0b20017 ~ f32170d  SPEC 增修草案 v2→v6（四輪企劃覆核）
2d03a90, 2d7a8b8   施工計劃 v1→v2（企劃 + 工程師雙軌覆核）
```

---

## 3. 進行中：核心迴圈數值修訂

**`SPEC_MVP_AMENDMENT_01.md` v6** 與 **`PLAN_MVP_AMENDMENT_01.md` v2**，都已寫完、都**待最終覆核**，**尚未動任何數值程式碼**。

### 要修的三件事（都已複驗）

| 症狀 | 證據 |
|---|---|
| `finalTheme` 恆為 100 | `timeline_itinerary.dart:153` 累加素材全額 `themeValue`，`:197` 以 50 為基底 clamp。素材值域 15~45，四槽最低組合即溢出 |
| 滿意度不可能失敗 | 主題得分恆滿；反無聊門檻 `totalHype < 30` 在實測 200~330 下不可達 |
| 採集在虛擬移動下不可用 | `triggerRadiusMeters = 50` ÷ `metersPerPixelAt() = 370.4` = **0.135 px** 觸發窗，而方向鍵單幀位移 0.67 px |

連帶後果：5 種哲學、槽位加成、節奏互補、拉車疲勞**全部無效**。

### 施工順序的關鍵

PLAN 的核心不是任務清單，是**決定順序**。最重要的一點：

**D4（疲勞）、D5（絕景階梯）、D6（純度獎勵）必須聯立求解，不可依序決定。** 三者同時餵進 `AC-A1-6.8a`（五種哲學滿意度極差 ≤15 分），而疲勞的量級是從那條 AC **反推**出來的。若三維可行域為空，**回報並退回 SPEC，不得硬選**。

---

## 4. 已凍結的資產（不准動，但也不准刪）

| 資產 | 為什麼凍結 | 解凍條件 |
|---|---|---|
| GPS 追蹤管線（`domain/location/`、`data/location/`） | 屬 P3。已通過真機驗證，能跑，但對「好不好玩」零貢獻 | 進入 P3 |
| 台灣真實地圖（`taiwan_map_manifest.dart` 及其 40 個 POI） | 同上。它是為真實步行設計的尺度，不利於 3~10 分鐘單局 | 進入 P3 |
| `metersPerPixelAt` 與 `triggerRadiusMeters` | **`position_smoother.dart:79` 的 GPS 平滑仍在用** | 隨 GPS 管線 |

**像素觸發要用「新增 `triggerRadiusPixels` 欄位」達成**，不得修改既有公尺欄位、不得動台灣 manifest 的 40 個字面值、**不得移除 `metersPerPixelAt`**。契約本體（`map_manifest.dart:14`）有一段明文論證「觸發半徑以公尺表達，不以像素」——改它就是修改契約，須是事前授權的決定。

---

## 5. 明確延後、不得主動實作

來自 CLAUDE.md §3 與歷次裁決：

- 軌跡錄製／重播
- A* 路網尋路
- 條件式物品效果系統
- 多段 GPS 功率模式（只做 active/suspended 兩段）
- 自訂 `Result<T,E>` 型別
- **事件日誌壓縮** —— 已評估：一天 20 局約 10KB，短期不需要
- **微動作（Runner / Snapshot）** —— 審查結論是「先修數學，再做微動作」。評分模型不可能失敗時，微動作的三態判定驗證不出效果
- **混亂冒險以外的哲學規則改寫** —— MVP 只允許一條具名例外

⚠️ **陷阱**：PLAN 的 T12 要做「以邏輯 tick 驅動的採集節奏模擬」，它離「軌跡錄製／重播」只有一步。**該模擬器必須住在 `test/`，不得進 `lib/`**，否則就是延後項換個名字混進來。

---

## 6. 跨階段約束與已知地雷

`CROSS_CUTTING_CONSTRAINTS.md` 優先於任何子系統 SPEC。摘要與**本專案特有的地雷**：

- **CC-1**：實體識別一律 UUID，禁用本機自增序號。
- **CC-2**：時戳一律 UTC；「經過多久」必須用單調時鐘。
  - ⚠️ `domain/core_loop` 目前對 `core/` 的 `Clock` **零引用**，時戳走 `DateTime.now()`。尚未踩到地雷（M4 沒有冷卻判定），但加任何「經過多久」的功能前必須先接上。
- **CC-3**：存檔是 append-only 事件日誌，最終狀態由**決定性重播**得出。
  - ⚠️ **`EquipmentType` 的 enum 名稱已凍結**。`curator_event_replay.dart:33-45` 用**字串**比對裝備種類，改名會讓所有既有存檔在 `loadSave()` 拋 `FormatException`。
  - 價格與佣金係數的改動是**安全的** —— 歷史事件的 `cost`/`earnedCoins` 存在 payload 裡，重播拿當時的值。這正是事件日誌該有的行為。
- **CC-5**：原始 GPS 座標永不離開裝置。已有靜態檢查（`layer_boundaries_test.dart` 掃 domain 的序列化鍵）。
- **第一版禁止且不得提前埋鉤子**：帳號、雲端同步、排行榜、好友、遠端 DLC、伺服器驗證、**遙測**。
  - 產品路線圖列了「基礎 Telemetry」為 P1 交付物，**與此直接衝突**。解法：Run/Retry/Fail 本來就是事件，從本機事件日誌離線讀取即可，不上傳、不埋鉤子。

---

## 7. 重要決策與理由

接手者最容易重新爭論的幾件事，連同當初的理由：

| 決策 | 理由 |
|---|---|
| **地圖改京都小街區，台灣封存** | 既有 32 張素材卡本來就是京都夜間的；台灣真實地圖的尺度為步行設計，不利短局；換 manifest 比補寫 89 張台灣卡便宜得多 |
| **標籤對齊改哲學去貼合卡表**（非改卡表） | 成本較低。具體方案見 SPEC 附錄 A.1，經實測窮舉搜尋得出 |
| **存檔用事件日誌（非快照）** | CC-3 的 P0 要求。M4 原本交付的是快照式，每次存檔抹掉前一份，後端接上後無法稽核，且 CC-4 說明原始依據事後補不回來 |
| **本次不新增第四條抽象** | 混亂冒險的例外用 `TravelPhilosophy` 上一個**按效果命名**的旗標。沒有新介面、沒有註冊機制、相依方向不變 —— 是資料不是抽象。按身分命名（`isChaosAdventure`）只是把 `if` 換個地方寫 |
| **允許提交 3 槽** | 原本 4 槽必填，所謂「刪除」只是「從 6 張選 4 張」。GDD Rule 11 要求 Theme Coherence 必須獎勵刪除，但玩家根本不被允許不放 |
| **疲勞反轉只給混亂冒險，且在 Hype 側** | 卡表中高熱度卡幾乎都是高風險，比例制疲勞會懲罰所有追熱度的玩家。反轉 Theme 側會與「主題上限被壓低」的定位矛盾 |
| **禁止抽樣，一律窮舉** | `AC-A1-6.8` 是最大值斷言、`AC-A1-2.5` 是分佈斷言。抽樣求得的最大值不是最大值。實算量級最大 12 萬，秒級可行 |

---

## 8. 已知債務

| # | 內容 |
|---|---|
| **S5** | `lastPersistError` / `persistFailureCount` 在 `lib/` 內**零讀者**。存檔失敗對玩家仍是靜默的——金幣消失與沒修是一樣的體感。提示形式是產品決策，故未實作 |
| **CC-2 未接線** | `domain/core_loop` 未接 `Clock`，見 §6 |
| **`SPEC_MVP_META_PROGRESSION.md` 過期** | 描述的是快照式存檔，與程式現況不符。該 SPEC 本身未覆核通過，修改須走關卡 |
| **CLAUDE.md §0 過期** | `dart test test/domain/` **不可用**（會報缺 `package:test`），正確是 `flutter test test/domain/`；「316 passed」實為 383 |
| **`manifest_geometry_check.dart:29`** | 現行**禁止**觸發窗重疊，與 SPEC v6 新的「容許重疊」牴觸。已在 PLAN 的 T9a 認領 |
| **兩套 POI 通道** | 契約有 `poiNodes` 與 `districtAttractions` 兩條平行通道，採集資格走後者。京都 manifest 用哪一條尚未裁決 |
| **抽象的真實漏洞** | `OverworldMapManifest` 強制每份 manifest 提供**真實地理投影**，但手繪街區沒有真實經緯度。「城市即 DLC」的下一張圖就不是地理參照的。這一點比單位問題更值得回報 |

---

## 9. 踩過的坑（別再踩）

1. **接線比元件更容易出事。** 任務 C 真機驗證抓到的四個 bug 全是同一形狀：元件正確且測過，但接線沒人測。M4 又重演了一次（`main.dart` 的引用計數狀態機零覆蓋、整合測試自抄一份 `ref.listen` 導致改 main.dart 不會變紅）。**接線必須有測試。**
2. **兜底規則會讓整組驗收空轉。** `taiwan_attraction_materials.dart:116` 的通配規則讓任何 poiId 都能合成出一張卡。若不換成「查表未命中即回 null」，所有標籤相關的 AC 量到的都是合成卡。
3. **審查報告要逐項複驗。** 五輪覆核數十條主張，複驗後絕大多數屬實，但有誤判（例如「髒 JSON 會靜默吃掉等級」—— 實際上 `as int?` 遇 double 會拋 TypeError，走損毀路徑，不是靜默）。
4. **涉及資料統計的斷言，先跑腳本再寫進文件。** 本次交接期間我自己數錯三次：絕景卡 6→5（把註解行算進去）、未使用標籤 35%→24%、`AC-A1-0.4` 不通過→通過。三次都是手算，三次都錯。
5. **SPEC 不要夾進程式 commit。** 曾把未覆核的草案夾進一個 refactor commit，違反關卡規則與單一意圖慣例。SPEC 一律獨立 docs commit。
6. **「屬於容器的屬性」不要讓呼叫端指派。** 事件 `seq` 原本由 controller 編號、種子取自另一個可獨立覆寫的 provider，脫鉤就撞號。改成呼叫端只能造 `CuratorEventDraft`（無 seq 欄位），由日誌封緘 —— 不合法狀態在型別上即不存在。
7. **紙上覆核有邊界。** 四輪 SPEC 覆核，前三輪各抓到一條結構性缺陷，第四輪開始多半在挑改寫時新造的矛盾。剩下的「量級對不對」在紙上判不了，得把數字寫進程式跑窮舉。

---

## 10. 文件地圖（本次新增／變動）

| 檔案 | 用途 |
|---|---|
| `SPEC_MVP_AMENDMENT_01.md` | **v6，待覆核。** 核心迴圈數值與取材尺度的增修。含附錄 A：實測窮舉得出的哲學標籤對齊方案 |
| `PLAN_MVP_AMENDMENT_01.md` | **v2，待覆核。** 施工計劃。核心是決定順序（D0~D12）與驗收歸屬，不是任務清單 |
| `SPEC_MVP_META_PROGRESSION.md` | **過期**，見 §8 |

CLAUDE.md §7 的文件地圖尚未納入上述兩份新文件。

---

## 11. 接手後的第一件事

1. 讀 `SPEC_MVP_AMENDMENT_01.md` v6 與 `PLAN_MVP_AMENDMENT_01.md` v2。
2. 確認使用者是否已覆核通過 —— **兩份都還沒有最終點頭，不得直接施工**（CLAUDE.md §1 的關卡規則）。
3. 若已通過，從 **T0（卡表加稅）** 開始，那是唯一不需要窮舉就能定的數字，且必須排在所有調參之前。
4. 每個 commit 必須全綠。期望值更新的五條紀律見 PLAN §5。
