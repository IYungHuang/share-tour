# HANDOFF — 2026-09-11

接續 2026-09-07 任務 C 之後的進度快照。前一版內容（任務 C 階段）已整份汰換。

## 現況

branch `main`，`flutter analyze` 0 errors / 0 warnings，`flutter test` **383 passed**。

M1~M4 核心迴圈全數實作完畢並已提交。M4（行前委託、黑市裝備、本機存檔）由另一 session 完成，本 session 接手審查與修正。

## 本次做了什麼

```
4576b7f  M4 基準點（另一 session 的成果，原樣提交以便回退）
5000207  修五個審查抓到的 bug
93a158e  存檔改為 append-only 事件日誌（CC-3）
32d74ea  架構測試補兩條規則，並修掉抓到的倒向相依
3a08fcc  main.dart 接線測試（引擎暫停協調器、彈窗路由）
0cadd89  事件日誌韌性強化（併發、壞資料、seq 全序）
9342771  seq 改由日誌指派（消除 provider 間的隱形耦合）
0b20017  SPEC 增修草案 v2
fafdc24  標籤詞彙表對齊方案（附錄 A）
```

## 進行中

**`SPEC_MVP_AMENDMENT_01.md` v2 的第二輪企劃覆核**已送出，結果未回。

草案處理三件事：核心迴圈數值塌陷（Theme 恆 100、滿意度不可能失敗）、取材尺度（觸發窗 0.135 px，虛擬移動走不完一局）、以及評分模型四處失衡。

已裁決：
- 地圖採**甲案** —— 小尺度手繪京都夜間街區，台灣真實地圖封存留待 Reality Run。
- 標籤對齊方向 —— **改哲學標籤貼合既有 32 張京都卡**，不改卡表。方案見草案附錄 A。

覆核通過後才進 plan 階段。**尚未動任何數值程式碼。**

## 已知待辦

- `lastPersistError` / `persistFailureCount` 在 `lib/` 內零讀者。存檔失敗對玩家仍是靜默的，需要 UI 提示，但提示形式是產品決策，未實作。
- `SPEC_MVP_META_PROGRESSION.md` 描述的是快照式存檔，與程式現況不符（已改事件日誌）。該 SPEC 本身尚未覆核通過，修改需走關卡。
- `CuratorEvent` 本身沒有 schema 版本欄位，版本只藏在鍵名 `curator_event_log_v1`。目前不構成問題，記錄備查。
- 事件日誌無壓縮機制。估算一天 20 局約 10KB，短期內不需要，**不要預先實作**。
- 微動作（Runner / Snapshot）完全未實作。審查結論是「先修數學，再做微動作」——評分模型不可能失敗時，微動作的三態判定驗證不出效果。

## 踩過的坑

- **審查報告要逐項複驗再採信。** 兩輪審查共十餘條主張，複驗後全部屬實，但其中一條（髒 JSON 會「靜默」吃掉等級）判斷有誤——`as int?` 遇到 double 是拋 TypeError，會被損毀路徑接住，不是靜默。
- **SPEC 不要夾進程式 commit。** 曾把未覆核的草案夾進一個 refactor commit，違反關卡規則與單一意圖慣例。SPEC 一律獨立 docs commit。
- **`seq` 這類「屬於容器的屬性」不要讓呼叫端指派。** 原本由 controller 編號、種子取自另一個可獨立覆寫的 provider，兩者脫鉤就撞號。改成呼叫端只能造 `CuratorEventDraft`（無 seq 欄位），由日誌封緘。不合法狀態在型別上即不存在。
