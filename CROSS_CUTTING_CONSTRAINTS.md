# 跨系統約束 (Cross-Cutting Constraints)

狀態：**v1，已裁決**
適用範圍：任務 A（遭遇）、任務 B（行李箱背包）、任務 C（GPS 追蹤）**全部**。

本文記錄**不屬於任何單一子系統、但拘束所有子系統**的決策。各子系統的 SPEC 不得與本文牴觸；若牴觸，以本文為準並回報。

---

## 1. 後端定位

**第一版純本機運作，但後端服務是既定終點，不是「可能需要」。**

這使下列抽象具備正當性（符合「抽象只用來擋住已知會變的軸」原則）：

| 抽象 | 擋住的變動軸 |
|---|---|
| `PersistenceRepository` | 存檔後端從本機換成遠端 |
| `LocationSource` | 真實 GPS vs 虛擬來源（另見 SPEC C） |
| `OverworldMapManifest` | 城市圖資模組化注入 |

**第一版明確禁止實作**：帳號系統、雲端同步、排行榜、好友、DLC 遠端下載、伺服器端驗證、遙測。
不得為這些功能提前埋鉤子——提前埋的鉤子多半猜錯形狀，反而成為負債。

---

## 2. 資料識別與時間

### CC-1 實體識別 `[P0]`
1. 所有需要跨裝置或跨存檔存活的實體（Run、遭遇紀錄、物品實例、圖鑑條目、事件），識別碼一律使用 **UUID**。
2. **禁止**使用本機自增序號作為持久化識別碼。
3. UUID 由客戶端產生即可，不需要伺服器配號。

**不做的後果**：多裝置同步時識別碼直接碰撞，屆時需要一次性遷移把所有既有存檔重新編號，且無法保證正確對應。

**AC**
- AC-CC-1.1 兩個獨立產生的實體，識別碼不相同。
- AC-CC-1.2 存檔序列化與反序列化後，識別碼不變。

### CC-2 時間表示 `[P0]`
1. 所有持久化的時戳一律為 **UTC**，不得存本地時間。
2. 需區分兩種時間來源，且不得混用：
   - **牆鐘時間 (wall clock)**：可被使用者或時區變更影響，用於顯示與紀錄。
   - **單調時間 (monotonic)**：不受時鐘調整影響，用於量測經過時間（冷卻、逾時、節流）。
3. 任何「經過了多久」的判定**必須**使用單調時間。

**不做的後果**：本專案的核心情境就是玩家跨時區旅行。用牆鐘時間量測經過時間，跨時區會產生負數時距與亂序事件，冷卻與逾時邏輯直接失效；玩家也可藉調整系統時間跳過冷卻。

**AC**
- AC-CC-2.1 存檔中的時戳欄位皆為 UTC。
- AC-CC-2.2 在量測期間變更裝置時區或時鐘，經過時間的計算結果不受影響。
- AC-CC-2.3 跨時區的兩筆事件，排序結果與實際發生順序一致。

---

## 3. 存檔形狀：事件日誌 `[P0]`

**已裁決：存檔採事件日誌 (event log)，而非只存最終狀態。**

### CC-3 規則
1. 存檔記錄的是**發生了什麼**（append-only 的事件序列），最終遊戲狀態由事件重播 (replay) 得出。
2. 每筆事件至少包含：事件 UUID、類型、UTC 時戳、單調序號、**產生該事件時的移動模式**（`gps` / `virtual`，見 SPEC C REQ-C-13）、以及該事件類型專屬的酬載。
3. 事件一經寫入**不得修改或刪除**。更正只能以新事件表達（補償事件）。
4. 為避免存檔無限膨脹，允許定期產生**快照 (snapshot)**：快照 = 某序號為止的重播結果，其後的事件續接在快照之上。快照是最佳化手段，**不得**取代事件日誌的權威地位。
5. 重播必須是**決定性的**：同一組事件在任何裝置、任何時間重播，必得出完全相同的狀態。因此事件處理器**不得**依賴目前時間、亂數（除非亂數種子存在事件內）、或外部狀態。

### CC-4 為什麼現在就要做
一旦有後端，客戶端自報的結果（例如「這次遭遇我是 GPS 模式，請發御朱印」）伺服器沒有理由相信。若存檔只有最終狀態，伺服器拿到的是一個**無法回溯、無法驗證的數字**，且**事後補不回來**——原始依據已經不存在。

事件日誌在第一版純本機時，成本只是一個 append-only 清單；有後端後，它就是可驗證的憑據。同時它天然支援除錯重播與斷線事件佇列。

**AC**
- AC-CC-3.1 對同一組事件序列重播兩次，得到完全相同的狀態。
- AC-CC-3.2 在不同時區、不同時間重播同一組事件，結果相同。
- AC-CC-3.3 每筆事件皆帶有移動模式標記。
- AC-CC-3.4 從快照 + 後續事件重播的結果，等於從頭重播全部事件的結果。
- AC-CC-3.5 事件日誌不存在修改或刪除既有事件的 API。

---

## 4. 隱私 `[P0]`

> 本節為安全需求，逐字說明，不做壓縮。

**已裁決：原始 GPS 座標永不離開裝置。** 此為產品承諾，非僅技術現況。

### CC-5 規則
1. 原始經緯度僅存在於裝置的執行期記憶體中。
2. **禁止**將原始經緯度寫入存檔、事件日誌、或任何未來的遠端服務。
3. 允許持久化並允許將來上傳的位置衍生資料，僅限：
   - 已投影的地圖像素座標
   - POI 打卡結果（到訪與否、時間、模式標記）
   - 事件日誌中不含原始座標的欄位
4. 上述資料足以支撐將來的成果驗證與排行榜，不需要原始軌跡。
5. 正式建置不得將位置資料寫入日誌輸出；除錯建置的位置日誌必須以除錯旗標守衛。
6. 將來若要放寬此政策，必須經過明確的產品決策與使用者同意流程，並補齊保存期限與刪除權的處理。**事後放寬遠比事後收緊容易，故第一版從嚴。**

**AC**
- AC-CC-5.1 存檔與事件日誌的序列化結果中，不存在原始經緯度欄位（可靜態檢查）。
- AC-CC-5.2 正式建置下，位置資料不出現在日誌輸出。

---

## 5. 對既有 SPEC 的影響

| 文件 | 影響 |
|---|---|
| `SPEC_C_GPS_TRACKING.md` | REQ-C-12 隱私條款與本文 §4 一致，並升格為產品承諾；REQ-C-13 的模式標記成為事件日誌的必填欄位（CC-3 規則 2）；經過時間類的參數判定須改用單調時間（CC-2） |
| 任務 A SPEC（未寫） | 遭遇結算須以事件表達，不得直接寫最終狀態；重播必須決定性，故結算不得依賴未存入事件的亂數 |
| 任務 B SPEC（未寫） | 背包狀態由事件重播得出；局外持久化同樣走事件日誌；快照策略在此定義 |

---

## 6. 待決問題

| # | 問題 | 為何可延後 |
|---|---|---|
| CC-Q1 | 快照產生的頻率與觸發時機 | 屬最佳化參數，有實際存檔大小數據後再定 |
| CC-Q2 | 事件日誌的大小上限與超限處理 | 同上 |
| CC-Q3 | 補償事件的種類與語意 | 待任務 A／B 的實際結算需求浮現 |
| CC-Q4 | 亂數種子的產生與存放方式 | 待任務 A 定義哪些結算含亂數 |
