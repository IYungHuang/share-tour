# SPEC C 增修草案 v3 — 實地測試後的修訂

狀態：**待使用者裁決**（三方技術覆核已收斂）
日期：2026-09-06
上位文件：`CROSS_CUTTING_CONSTRAINTS.md`
修訂對象：`SPEC_C_GPS_TRACKING.md`（v5.1）

v1、v2 已作廢。本版為主筆與兩位覆核者（架構師、資深工程師）三輪往返後的收斂結果。
§3 標明三方一致的項目，§4 標明分歧與裁決，§5 是**需要使用者裁決**的三件事。

---

## 1. 實測證據

三趟真機資料，CPH2783（Android 16），台中。全部為逐筆日誌的離線統計。

| 趟次 | 條件 | 結果 |
|---|---|---|
| A | 40 分鐘，螢幕自動休眠 | **12 筆** Fix，中位間隔 219 s，丟棄率 33% |
| B | 38.4 分鐘步行，螢幕常亮 | **142 筆**，中位間隔 8.2 s，丟棄率 7%，累計里程 **2321 m** |
| C | 16.4 分鐘靜止（手機置於窗邊未觸碰） | 78 筆，累計里程 **179 m**（應為 0） |

**F1 背景取消訂閱使核心迴圈無法成立。** A 與 B 的唯一差異是螢幕是否休眠，Fix 數相差 11.8 倍。A 趟的 12 筆與使用者點亮螢幕的次數吻合。

**F2 里程系統性高估。** B 趟移動時間佔 52.7%，精度最佳區段（6~8 m）平均速度 1.29 m/s，推估真值 1100~1400 m。C 趟真值 0，累計 179 m。

**F3 高估集中於未收斂期。** B 趟前 5 分鐘：路徑 1348 m、淨位移 152 m、方向一致性 **0.31**、平均精度 18.8 m。其後三段：一致性 0.78~0.92、精度 6~8 m。C 趟兩次假位移（88.0 m、90.8 m，方向相反）全在冷啟動第一分鐘，其後 14 分鐘零次。

**F4 裝置回報速度不是獨立證據源。** `hasSpeed` 全程為真、`speedAccuracy` 有值，但手機靜止時裝置回報速度中位數 **1.85 m/s**、53% 超過 1 m/s、最大 **14.08 m/s**。都卜勒解算與位置解算共用同一組衛星幾何。

**F5 精度閘門對位置誤差有效，且存在窗口。** C 趟以中位數位置為真值，位置誤差中位數 3.8 m、90% 為 33.0 m、最大 94.2 m。以精度 ≤ 35 m 過濾後誤差上界降為 33.0 m。

**F6 里程與觸發的誤差性質不同。** 里程是路徑積分，雜訊累加且只增不減；觸發是瞬時判定，雜訊不累加。里程高估不影響觸發正確性。

**F7 消融實驗**（兩趟資料離線重播真管線）

| 組合 | 步行（真值 1100~1400） | 靜止（真值 0） |
|---|---|---|
| 無 | 2321 m（98 次） | 179 m（2 次） |
| 只有暖機（連續 3 筆 ≤15 m） | 1259 m（89 次） | 0 m（0 次） |
| 只有精度上限 35 m | 1394 m（91 次） | 0 m（0 次） |
| 只有門檻下限 30 m | 2240 m（42 次） | 179 m（2 次） |
| 暖機 + 下限 | 1204 m（34 次） | 0 m（0 次） |

**F8 精度上限的有效窗口為 [27.5, 35]，中心約 31。**

| 上限 | 25 | 27.5 | 30 | 32.5 | 35 | 37.5 | 40 |
|---|---|---|---|---|---|---|---|
| 步行 | 1394 | 1394 | 1394 | 1394 | 1394 | 1394 | 1757 |
| 靜止 | 66 | **0** | **0** | **0** | **0** | 176 | 176 |

**F9 收斂判準 15 m 不可靠。** 步行趟 85% 的 Fix ≤15 m、首次連續 3 筆收斂於 150 秒；**靜止趟 11%，從未收斂**。C 趟的 0 是「暖機永不結束」造成的，不是機制正確。

---

## 2. 修訂總表

| # | 內容 | 狀態 |
|---|---|---|
| 一 | REQ-C-16 追蹤期間的裝置喚醒 | 三方一致 |
| 二 | REQ-C-13 里程與位置的品質標記（單一機制） | 三方一致 |
| 三 | REQ-C-18 觸發品質契約與幾何約束 | 三方一致（式子見 §4.2） |
| 四 | 刪除道路吸附與道路節點列舉，`containsGeo` 改查分類遮罩 | 三方一致（遮罩為主筆追加） |
| 五 | REQ-C-03 規則 5 刪除裝置速度交叉檢查 | 工程師提出，主筆採納 |
| 六 | DoD 第 7 條拆分與趟次有效性 | 三方一致 |
| 七 | REQ-C-17（防誤觸）移出 SPEC C | 三方一致 |
| 八 | 修正 v5.1 既有的 AC-3.9 內部矛盾 | 工程師發現 |
| — | **里程的角色（路徑 A / B）** | **需使用者裁決，見 §5.1** |

---

## 3. 修訂內容

### 3.1 修訂一：新增 REQ-C-16 `[P0]` 追蹤期間的裝置喚醒

**問題**：核心迴圈是「真實移動累積成果」，而移動時玩家不注視螢幕。REQ-C-02 規則 7 於背景逾 20 秒取消訂閱，§1.3 又將背景定位劃入第二階段。兩者相加使第一版核心玩法僅在玩家注視螢幕時成立（F1）。

**前提已證實**：目標裝置（ColorOS）於螢幕常亮下未對前景 app 施加省電凍結，Fix 密度回到中位 8.2 秒。**此結論僅在 CPH2783 / ColorOS 成立**，其他 Android 廠商與 iOS 未測（見 §6 Q25）。

**規則**

1. 定義衍生述詞：
   ```
   shouldKeepAwake = (mode == gps) ∧ (permission == ready)
                     ∧ (應用程式在前景) ∧ (玩家未關閉本功能)
   ```
2. 喚醒抑制狀態必須等於此述詞的值。
3. 述詞須為可在不啟動平台的條件下求值的純函式（NFR-1）。
4. 抑制的是**自動休眠**，不是使用者的主動操作。玩家按電源鍵鎖屏時依 REQ-C-02 規則 7 與 REQ-C-11 處理。
5. **須提供可關閉本功能的設定，預設開啟。**
   > 本需求沒有自動釋放路徑：一旦釋放，螢幕熄滅 → 20 秒後取消訂閱 → `motion` 永遠回不到 `moving` → 無法自動恢復。玩家唯一的耗電煞車只能是手動開關。
6. 述詞的值以 `keepAwakeActive` 之名同時進入狀態物件與診斷快照。
   > 畫面層需要它做互動決策（§3.7），故它是對外契約而非僅診斷欄位。
7. 不得因本需求放寬 REQ-C-11 的任何一條。
8. 平台能力的取得方式屬 plan 裁決。SPEC 僅記載：Flutter framework 未提供此能力。

**述詞不含 `powerMode` 的理由（規範性註解）**

REQ-C-11 規則 3 的五個 `suspended` 觸發條件中，四個已被述詞的其他合取項排除（背景逾時↔前景、權限不可用與 `approximate`↔`permission == ready`、`mode == virtual`↔`mode == gps`），僅餘 Mini-game。若將 `powerMode == active` 納入述詞，Mini-game 期間會錯誤地釋放喚醒——而那正是玩家全程注視螢幕的時刻。

**REQ-C-11 規則 3 須加註：本清單為封閉列舉；新增觸發條件時必須重新驗證本述詞的化簡是否仍成立。** 否則新增條件將造成無聲失效。

**AC**

- AC-16.1 `mode` 由 `virtual` 切為 `gps`、`permission == ready`、在前景 → `keepAwakeActive` 為真。
- AC-16.2 `mode` 切回 `virtual` → 為假；`activeSubscriptionCount` 與 `powerMode` 行為不變。
- AC-16.3 `permission` 離開 `ready` → 為假。
- AC-16.4 進入背景 → 為假；回到前景且其餘條件成立 → 為真。
- AC-16.5 `powerMode` 變為 `suspended`（Mini-game 情境）而其餘條件不變 → **維持為真**。
- AC-16.6 玩家關閉本功能 → 為假，其餘五個狀態維度不受影響。
- AC-16.7 dispose 後 → 為假且抑制被釋放。
  > NFR-5 須補上「與外部副作用（含喚醒抑制）」。漏放的後果是玩家手機不再自動熄屏，且無從得知原因。

---

### 3.2 修訂二：REQ-C-13 品質標記（取代 v2 的三道防線）

**問題**：里程是路徑積分，每筆位置帶獨立誤差；真實位移趨近零時，量到的距離退化為誤差本身的量級且只增不減。顯著位移門檻 `0.75 × (基準精度 + 當前精度)` 意在隨誤差浮動，但精度值本身是估計值且偏樂觀。

**規則（新增為 REQ-C-13 規則 16）**

16. **品質標記**：精度 ≤ **30 公尺**（門檻值視為合格）的 Fix 標記為「品質合格」。不合格的 Fix：
    - 不產生位移事件、不累計任何距離桶
    - **不得成為顯著性閘門的比較基準**
    - **不得成為大跨距判定（REQ-C-07）的比較基準**
    - **不得消耗待處理的不連續標記**（`markDiscontinuity` 設定的標記須由第一筆合格 Fix 消耗）
    - **不推進 `motion`**
    - 仍更新顯示點與 `coverage`
    - 計入 `accuracyGatedFixCount`，不計入 `rejectedFixCount`

    > 「不得消耗不連續標記」防的是已發生過的缺陷：模式切換後首筆若為不合格 Fix 而消耗了標記，第二筆將走連續路徑，虛擬位置到真實位置的落差會被計入里程（實測曾灌入 13 萬公尺）。

17. **虛擬 Fix 一律標記為合格。** 合成 Fix 無量測誤差（REQ-C-10 規則 5 的理由逐字適用）。
    > 不寫成「旁路品質閘門」：旁路會讓標記處於未初始化狀態，而 REQ-C-18 要把該標記交給任務 A——方向鍵模式下遭遇系統會因此完全無法觸發。

**參數依據**

有效窗口為 **[27.5, 35]**（F8）：27.5 以下靜止趟出現 66 m 假里程，37.5 以上出現 176 m。取 **30** 為窗口中心附近的整數值，兩側各留 2.5 / 5 的餘裕。步行趟 95% 的 Fix 通過此門檻。

**只留一個機制的理由**

F7 顯示：暖機與上限對兩趟資料等效（1259/0 對 1394/0），下限 30 幾乎無效（2240/179）且把顯著位移次數從 98 砍到 42——它不是噪聲過濾器而是位移合併器，路徑積分幾乎不變。

選上限而非暖機的理由不是數字更好，是**上限是無狀態的逐筆純述詞，不可能死鎖；暖機是有狀態的閂鎖，而 F9 已實證那個閂鎖會鎖死**（靜止趟從未收斂）。同一效果選無狀態者。

**未被此機制擋下的殘留**：F3 的冷啟動段（步行趟 1394 − 1204 = 190 m，約 15%）通過 30 m 標記。處置見 §5.1 路徑 A / B。

**AC**

以下三項為全體 AC 的通則，缺任一項視為未完成：**時戳間隔**、**速度已量測旗標**、**是否共線**。

- AC-13.14 基準精度 24 m、當前精度 40 m、相距 96 m、間隔 5 s、兩筆速度旗標皆為否 → 顯示點更新、`motion` 不變、兩桶不變、`accuracyGatedFixCount` 為 1、`rejectedFixCount` 為 0。
  > 無標記閘門的實作會計入 96 m：`0.75 × (24+40) = 48 < 96`，顯著性閘門本就放行。
- AC-13.15 承上，其後第三筆精度 20 m、與**第一筆**相距 200 m、時戳為第一筆 +10 s、三點共線同向、速度未量測 → 計入 **200 m**（單一筆位移事件）。
  > 唯一能區分「不合格 Fix 是否帶走基準」的條件：帶走的錯誤實作得 104 m。時戳必須明寫——第二筆到第三筆為 104 m / 5 s = 74.9 km/h；若間隔寫 1 s，正確實作也會被速度規則丟棄。
- AC-13.16 兩筆精度**恰為 30 m**、相距 100 m、間隔 5 s、速度未量測 → 計入 100 m。同一組資料把當前精度改為 **30.1 m** → 不計入，`accuracyGatedFixCount` 為 1。
  > 兩半寫在同一條，否則「門檻值視為合格」的全文統一語意在此處沒有測試。
- AC-13.17 `markDiscontinuity(modeSwitch)` 後：第一筆精度 50 m 於 P1（距切換前最後已知位置 5 km）；第二筆精度 20 m 於 P2（距 P1 100 m、+5 s、速度未量測）→ 第一筆不產生任何事件、不成為任何基準；**第二筆**產生**恰好一筆**傳送事件（`discontinuity` / `modeSwitch`）、零筆位移事件，兩桶維持 0。
- AC-13.18 虛擬 Fix（精度 1 m）→ 標記為合格，行為與現行 AC-13.4、AC-10.7 一致。

---

### 3.3 修訂三：新增 REQ-C-18 `[P0]` 觸發品質契約

**問題**：觸發正確性與里程正確性是不同的問題（F6）。本 SPEC 不裁決觸發半徑與遭遇規則（屬任務 A），但必須提供「這筆位置是否夠好到可用於觸發判定」的契約。

**規則**

1. 每筆位置對外提供**觸發適用性標記**，判準與 REQ-C-13 規則 16 的品質標記相同（精度 ≤ 30 m）。
   為**三個具名參數、目前同值 30 m**：`mileageQualityThreshold`、`triggerSuitabilityThreshold`、`gridEligibilityThreshold`，參數表三列、各自寫依據，另加一條斷言三者相等的測試。
   > 不共用單一參數：Q22 若放寬里程門檻，觸發契約與網格資格不應跟著漂移。真正的風險不是共用一個值，是**隱式**共用一個值——三個名字加一條相等性斷言，使「它們分家」成為一次刻意的編輯。
2. **`positionErrorBound` 為必填注入、無預設值**，由任務 A 於裁決觸發半徑時一併決定。
   本 SPEC 提供的是量測結果作為輸入資料：單裝置、單時段、24 筆、以自身中位數位置為真值的離散度上界 **33.0 m**。
   > 不給預設值的理由：安全裕度的大小應由誤觸發的代價決定，而只有任務 A 知道那個代價。無預設值使「忘記決定」成為啟動期失敗，而非靜默沿用一個沒人推導過的數字。
   > 本數字**不得**用以論證任何 POI 佈點合格，其樣本不足以支撐統計保證。
3. **幾何約束**（AC-4.6 改寫）：
   ```
   d(A, B) > r_A + r_B + positionErrorBound
   ```
   推導：玩家於 A 的觸發範圍內任一點（距 A 最遠 r_A），量測點偏離真位置最多 `positionErrorBound`，要求量測點距 B 超過 r_B。
   本約束為**對稱**式（含 r_A 與 r_B 兩項），故每對 POI 驗一次即可。它**不保證** A 自身觸發得到（偽陰性方向屬任務 A）。
4. 本 SPEC 不規定觸發半徑、停留筆數、冷卻時間。

**AC**

- AC-18.1 精度 30 m 的 Fix → 標記為適用；30.1 m → 不適用。
- AC-18.2 不適用的 Fix 仍更新顯示點與 `coverage`。
- AC-18.3 參數化幾何驗證：給定（POI 清單、各自觸發半徑、`positionErrorBound`），驗證每對 POI 滿足規則 3。
  - 以合法的 Fake 資料驗證 → 綠燈。
  - **以刻意違規的 Fake 資料（兩個 POI 相距 0.2 像素、r 各 50 m、e = 52.5 m）驗證 → 必須被抓到。** 否則規則本身未被驗證。
  - 以 `TaiwanMapManifest` 驗證 → 目前綠燈，但測試名稱與註解須寫明：**綠燈源自 370.4 公尺/像素的尺度（最近一對 POI 相距 4969 m，為門檻的 32 倍），非佈點品質；地方層（任務 D）上線後必須重驗。**

---

### 3.4 修訂四：刪除道路吸附與道路節點列舉

**問題**：吸附的實際門檻為 `50 ÷ 370.4 = 0.135 像素`——投影點須落在節點的 0.135 像素內才觸發，實質上是死碼。但問題不在數值：**吸附到離散節點這個形式沒有正確用法**——節點密度低於 GPS 精度時把玩家傳送到數百公尺外，高於時等於沒作用。而它的輸出（像素座標）是唯一被持久化的位置資料，錯誤不可回復。

**規則**

1. §2.2 刪除「道路吸附」與「列舉道路節點」兩項能力；`snapToRoad`、`snapLimitMeters`、`roadNodes` 一併移除。
2. §3.0 管線刪除吸附步驟。
3. **不保留設為 0 的介面。**
4. §2.1 的 PRE-3、PRE-8 隨之作廢。

> `roadNodes` 為 `List<Vector2>`——無序、無連通性、無方向，表達不了路段折線也表達不了弧長。保留它不會為將來的路網比對保住任何選項，只會提供一個名字讓後人硬套。將來若引入路網比對，新增的是「有序、含連通性的路段折線 + 弧長投影」能力，**不得復用 `snapToRoad` 或 `roadNodes` 之名**。

**須同步修改的條文**（機械性，但漏改即成矛盾條文）：§1.2、§2.2 規則 4/5、§5 參數表刪除「吸附上限 50 m」、REQ-C-04 標題與規則 1/3/4、AC-4.1 措辭、AC-4.3 刪除、REQ-C-15 規則 3、§6.1 第 5/8/12 點的引用。

**刪除後留下的洞，與其替代品**

吸附雖是死碼，但它在文件上曾是「讓玩家待在合理位置」的唯一機制。刪掉它之後，唯一還在守這條線的是 `containsGeo`——而它目前是矩形框（`lat ∈ [21.4, 25.7]`、`lng ∈ [119.7, 122.3]`），台灣海峽、巴士海峽、太平洋全在框內。

方向鍵的路徑是像素累加、無邊界檢查，所以玩家按著方向鍵往西會一路走進海裡，全程 `coverage == inside`，管線一切正常；真實 GPS 在海峽中亦然，且投影會給出落在錨點凸包內、看似合理但完全錯誤的點（REQ-C-04 規則 1 已預告此風險）。

**規則（新增）**

5. **`containsGeo` 由矩形框改為查詢分類遮罩。** 遮罩為圖資模組的資產，解析度由模組宣告。方向鍵與真實 GPS 共用同一條判定路徑，不得分歧。

> 遮罩與吸附解的是同一個問題，但方式不同，而差別正是刪除吸附的理由：
> **吸附是把玩家拉到某個點**——結果寫進唯一被持久化的位置資料（CC-5），錯誤不可回復。
> **遮罩是判斷這個位置合不合法**——不改動座標，只標記 `coverage`，玩家看到的是「你在圖資範圍外」而不是被瞬移。後者不會說謊。

**兩項須實機確認的後果**

- **方向鍵手感改變**：走到海岸線會停（`coverage = outside`），現況是可一路走到地圖外。這是行為變更，須實測沿海岸移動是否流暢。
- **遮罩粒度**：現有遮罩 256×144，每格對應 8×8 地圖像素 = 大地圖上約 **3 公里**。當邊界判定夠用，當視覺約束太粗——小人可能在離岸 3 公里處被擋，或深入陸地 3 公里才被擋。地方層（任務 D）需另一份較細的遮罩。

**實作排程**：本草案負責交代刪除吸附後的邊界由誰守；遮罩接線的實作可排入任務 D，但**不得在遮罩接上之前刪除吸附**——那會留下一段完全沒有邊界守衛的期間。

**AC**

- AC-4.3（刪除）
- AC-4.7（新增）圖資介面不存在吸附與道路節點相關成員（靜態檢查）。
- AC-4.8（新增）以 `FakeMapManifest` 提供一份標記部分區域為範圍外的遮罩：位於範圍外的座標 → `containsGeo` 為假、投影未被呼叫（同 AC-0.1）；方向鍵驅動的虛擬 Fix 與真實 Fix 在同一座標上得到相同判定。

---

### 3.5 修訂五：REQ-C-03 規則 5 刪除裝置速度交叉檢查

**問題**：現行規則取「兩點差分」與「裝置回報速度」的**較小值**。取小值只會讓速度閘門更難丟棄，而 F4 已證明裝置回報速度不帶獨立資訊（靜止時中位數 1.85 m/s、最大 14.08 m/s）。AC-3.10 明文編碼了這個洞：裝置回報 2 m/s、兩點差分 300 m/s → 不丟棄。**一個亂報低速的裝置可以整條關掉 350 km/h 閘門。**

**規則**：REQ-C-03 規則 5 的推算速度**僅用兩點差分**。刪除 min 運算與 AC-3.10、AC-3.11。`hasSpeed` / `hasSpeedAccuracy` 欄位保留於 `GeoFix`（診斷用），但管線不再消費。

F4 的結論須以註解形式併入 REQ-C-03 規則 5，否則日後必有人重新提議以裝置速度過濾。

---

### 3.6 修訂六：DoD 第 7 條拆分

**7a（管線正確性）** 全程前景、GPS 模式、不主動鎖屏。判定：

1. **假顯著位移事件筆數為 0** ——「假」定義為裝置未被移動期間產生的任何位移事件或傳送事件。三趟不同時段皆須為 0。
2. **趟次有效性**：該趟的品質標記通過率須 > 50%，未達則該趟**判定為無效、不計入**（不是判定為通過）。
   > 沒有這條，室內窗邊必然拿到 0 筆綠燈，而機制有沒有用完全沒被驗證——F9 的靜止趟即為實例。測試環境本身要先被驗證，才有資格驗證被測物。
3. **至少一趟為清除 GNSS 快取後立即開始**（冷啟動）。
   > 品質標記不擋冷啟動雜訊（§3.2 殘留 15%），刪掉暖機後這是唯一能量到該失效模式的測試。
4. 每趟一併記錄 `accuracyGatedFixCount`、`acceptedFixCount` 與精度分佈。這是 Q22 唯一的資料來源。
5. **行走側（路徑 A 之下）**：沿已知路線走完，**首次進入格數與幾何真值相差 ≤ 1 格**（含端點半格的容許）。
   > 這比里程比值硬得多：真值由「路線幾何 ∩ 格網」精確算出，是整數、不需 GPS、對訊號品質偏誤免疫（格子固定，與門檻無關），也不需要跑道級的 ±1% 真值。它取代了 v2「里程比值 ∈ [0.85, 1.15]」那條在自己選定的參數下必然紅燈的判定。
   > **路徑 B 之下**改為：已知路線三趟，里程比值 ∈ [0.7, 1.3] 且三趟變異係數 < 25%（變異係數量的是「這個數字穩不穩」，那才是玩家覺得系統壞掉與否的關鍵）。

**7b（真實情境誠實性）** 手機收在身上、允許鎖屏、步行 500 公尺。判定：**未產生假里程**（中斷區間不計入）、且**中斷區間對玩家可見**。

7b 的第二項需要新規則：**不連續事件（`backgroundResume` 等）須同時提供給畫面層**，並補 AC。目前該事件只餵給任務 A。
> 玩家鎖屏 13.6 分鐘、實際走了 773 公尺，回來後里程沒動而 HUD 顯示正常——與 REQ-C-14 規則 4 當初要修的缺陷同構，且更嚴重。

**離線重播能力必須保留。** 本次的消融、參數窗口、下限 30 無效等結論全部依賴它，它也是未來所有參數爭論的唯一仲裁方式。落盤格式與匿名化見 §5.2。

---

### 3.7 修訂七：REQ-C-17（防誤觸）移出 SPEC C

v2 的 REQ-C-17 以 `motion == moving` 為觸發條件，但 `motion` 由顯著位移驅動，在「手機在口袋 + 精度差」時必為 `still`——正是它宣稱要防的情境。且它約束的是輸入層，落在 §1.3 的邊界紀律之外。

**改為**：SPEC C 以 `keepAwakeActive`（§3.1 規則 6）暴露「追蹤中且螢幕被強制常亮」的狀態，畫面層據此自行決定是否鎖觸控或要求二次確認。

**交接給任務 A／畫面層的具體危害**（不記下來會被遺忘）：

- `gps` 模式下虛擬來源已 `stop()` 且未被訂閱，**方向鍵誤觸不產生 Fix、不寫事件日誌**。
- 但 `VirtualLocationSource.setDirection` 會**記住方向**，該方向會在日後切回 `virtual` 時（含權限掉落**自動**切換）生效，小人自己開始走。
- 模式切換誤觸的後果不可逆：產生 `modeChanged` 事件（append-only）、改變後續位移的 `sourceMode` 歸屬，且依 REQ-C-13 規則 4 手動切換**持久化跨進程**——玩家口袋誤觸一次，重開 app 仍是 `virtual`。

---

### 3.8 修訂八：修正 v5.1 既有的 AC-3.9 內部矛盾

**此項與本次修訂無關，是 v5.1 帶進來的錯誤，順手修掉。**

現行 AC-3.9 同時寫「恰有第 3 筆顯著」「第 4、5 筆距新基準 12 m、24 m」與「累計 36 m」。三者不可能同時成立：精度 20 m 時門檻為 `0.75 × 40 = 30`，第 3 筆距基準 24 m 未達門檻；事件在**第 4 筆**（36 m）觸發，累計 36 m。照字面實作測試，正確實作會紅燈。

**改寫**

> AC-3.9 連續 5 筆沿直線、位置為 0 / 12 / 24 / 36 / 48 公尺、精度皆 20 m、時戳各間隔 12 s、速度未量測 → **恰有第 4 筆**判定為顯著位移（第 2、3 筆距基準 12 m、24 m 未達門檻 30 m；第 5 筆距新基準 12 m 未達門檻），累計 **36 m**。
> 此條是唯一能區分「基準凍結」與「基準前移」的驗收條件：凍結得 36 m，前移得 0 m。措辭寫「恰有第 4 筆」而非「自第 4 筆起」。

**AC-13.5 在本修訂下不需改寫**：精度 20 m ≤ 30 m 標記合格，門檻 30 m，每 10 m 一筆、基準凍結 → 每 40 m 一次事件，500 m 共 12 次 = 480 m ∈ [400, 550]。

---

## 4. 分歧與裁決

### 4.1 里程降級後的成果載體

**架構師**主張改用**固定地理網格的首次進入計數**（例如 50 m 網格，記網格 id）。
**工程師**主張維持 **POI 打卡**，反對引入「顯著位移次數」。

兩人**一致反對顯著位移次數**，理由互相印證：門檻 `0.75 × (a₁+a₂)` 使精度好時計數快、精度差時計數慢，形成「開闊地掛機比逛街划算」的系統性偏誤；且 F7 顯示次數對參數的變異（98→34，2.88 倍）大於里程（2321→1204，1.93 倍）。

**裁決：採用 POI 打卡 + 網格計數，捨棄顯著位移次數。**

網格計數另有一個決定性優點：**它免疫於冷啟動的刷分面**。§3.2 的殘留（冷啟動 2 次假位移、約 135 m）在里程口徑下只是 9.7% 誤差，但它**每次冷啟動都發生且玩家可控**——反覆重啟 app 即可刷計數，完全不需移動。誤差是隨機的，這個是確定性的。而冷啟動假位移是圍繞起點的隨機遊走（淨位移 152 m 對路徑 1348 m），在 50 m 網格下幾乎全落在起點及緊鄰格，玩家本來就會踩到。

**網格的四項結構性規定（缺一即不可用）**

1. **網格 id 必須是 `(緯度, 經度)` 的純函式，常數由通用引擎持有。**
   **絕對不得自像素空間推導**——Q14 已預告控制點會重標、三角化會重算，那天所有既有事件會被靜默重新編號，而重播出來的探索度會憑空改變。副作用是好的：網格不進 §2.2，圖資契約不變，測試連 `FakeMapManifest` 都不需要。

2. **格子邊長 ≥ 2 × `positionErrorBound`。**
   以現行量測（誤差上界 33 m、任務 A 若取 1.5 倍裕度則為 ~50 m）推算，50 公尺格是**不成立的**：合格 Fix 的誤差上界會超過一整格，相鄰格的歸屬在結構上不可靠。這不是調參問題，是語意問題——「玩家在這一格」必須是一句真話。
   建議邊長 **128 公尺**，並寫成 SPEC 的不變式，配一條在 `cellMeters < 2 × positionErrorBound` 時必紅的測試。
   > 附帶好處：冷啟動的殘留漂移（淨位移 152 m、最大偏移 88~90 m）在 50 m 格下是 3~5 格，在 128 m 格下降為 1~2 格。

3. **格子大小以度數定義，非公尺。** 固定度數格網跨緯度會變形：緯度方向穩定，經度方向在台灣境內變動 ±1.6%（可忽略），但在 lat 60° 只剩約一半。本作的核心情境是跨時區旅行（CC-2 的理由欄），故參數定義為 `cellDegrees`，並註明公尺值是它在參考緯度的標稱值。等面積網格（S2 / H3）列入 §6 延後項，**屆時 id 必須換 scheme，不得沿用**。

4. **事件必須帶 scheme 版本號**（`gridScheme` + `cellDegrees`）。沒有它，日後改一次格子大小就會讓全部歷史悄悄失效且無法偵測。現在加是一個欄位，事後補是不可能。

**重播決定性**：事件記錄「**進入了哪一格**」，重播端做**集合摺疊**得出計數；不得在執行期先去重再記錄。
理由一：集合摺疊天生冪等，重播兩次結果相同（CC-3 規則 5）。
理由二：執行期去重的日誌在有後端、要合併多裝置時會重複計數，而那正是 CC-4 存在的理由。

**對任務 A 的性質告知**：首次進入計數是**單調且不可重複**的，適合「探索度」，不適合「今天走了多少」。若 A 要做每日循環，期間切分必須用事件自身的 UTC 時戳，不得用重播當下的時間（CC-3 規則 5）。

**網格對品質標記的需求嚴格高於里程**：里程的過度累計是可被後續低估抵銷的連續量；網格首次進入是**單調且不可回復**的——一筆爛 Fix 把玩家丟進沒去過的格子，那一格永遠回不來。

### 4.2 幾何約束的式子

**工程師**主張 `d > r_B + e`（站在 A 的中心不得誤觸 B）。
**架構師**主張 `d > r_A + r_B + e`（在 A 的觸發範圍內任一點皆不得誤觸 B）。

**裁決：採架構師版本。** 玩家在 A 的觸發範圍內任一點打卡是常態，站在座標正中心是唯一不會發生的情況。工程師的「非對稱、須驗兩個方向」在含 `r_A` 的對稱式下自動滿足。

### 4.3 `positionErrorBound` 的值

**工程師**主張 `品質門檻 × 1.5 = 45 m`，並註明 1.5 為安全裕度非量測值。
**架構師**主張必填注入、無預設值，由任務 A 決定。

**裁決：採架構師版本**（§3.3 規則 2）。以乘法綁定品質門檻會使 Q22 調參時靜默改變任務 A 的幾何約束，違反「兩個具名參數」的初衷。工程師「1.5 的性質須寫明」的要求已納入——本 SPEC 改為提供量測資料與其局限，不提供數值。

### 4.4 T5（冷啟動校準）

**主筆原提案**刪除 T2~T5。
**工程師**反對刪 T5：砍掉暖機後冷啟動是唯一被放掉的失效模式，刪掉暖機又刪掉唯一能量到它的測試，是把守門員和球門一起搬走。

**裁決：採工程師版本**，T5 併入 DoD 7a 第 3 項（§3.6）。T2~T4 降為附錄協定，重啟條件見 §6。

---

## 5. 需要使用者裁決的三件事

### 5.1 里程的角色（路徑 A / B）

兩條路徑各自完整。若否決路徑 A，§3.6 的判定與 §6 的延後項須整組回退。

| | **路徑 A：里程降級** | **路徑 B：里程仍是機制輸入** |
|---|---|---|
| 里程定位 | 顯示用數字，不驅動任何機制 | 驅動獎勵解鎖 |
| 成果載體 | POI 打卡 + 網格計數（§4.1） | 里程 |
| 里程機制 | 只留品質標記（§3.2） | 品質標記 + **冷啟動有界抑制**（前 N 筆合格 Fix 不計，N=3；有界故無 F9 的死路） |
| 冷啟動殘留 | 網格載體天生免疫 | 需上述第二個機制 |
| 校準協定 | T2~T4 為附錄，非 DoD | T2~T5 全部必要 |
| 誤差宣告 | 不需要 | **必須明文宣告不保證優於 ±25%**，且任何以里程為輸入的機制須在該誤差下仍合理 |
| 待決問題 | Q21、Q22 消失 | Q21、Q22 仍阻擋 |
| 上位文件 | 仍須修改（見 §5.2） | 同樣須修改 |

**主筆建議路徑 A。** 理由：F6 已證明觸發不吃里程誤差，而觸發是遊戲機制的骨幹；路徑 A 讓三個待決問題與一套校準協定一起消失。

**但這是產品決策**：它把獎勵設計從「走了多少公尺」改為「打了幾個卡、踩過幾個網格」，屬手感範疇。

**工程師的但書**：「里程不得作為任何遊戲機制的輸入」這句話 SPEC C **執行不了**（C 管不到 A）。可執行的形式是誠實宣告誤差區間，並讓任務 A 在該區間下自行判斷。故路徑 A 的條文措辭應為「本 SPEC 不保證里程誤差優於 ±25%」，而非禁止性語句。

### 5.2 上位文件 `CROSS_CUTTING_CONSTRAINTS.md` 的修改

CC-5 規則 3 是**封閉清單**（「僅限：已投影的地圖像素座標／POI 打卡結果／不含原始座標的事件欄位／依移動模式分桶的累計距離」）。以下兩項不在清單上：

1. **網格 id**（§4.1 的成果載體）
2. **離線重播用的 ENU 相對位移序列**（§3.6）

無論匿名化做得多好，在條文層級都需要修改上位文件。**依 CLAUDE.md §5 與 CC 文件開頭，此事必須由使用者裁決。**

**網格 id 不是「POI 打卡結果的一般化」**，兩者量級不同而非程度不同：POI 打卡是 22 個人工策展點的到訪集合，洩漏的是「他去過台北 101」；台灣範圍的網格約 10⁷ 格，而**已到訪格集合就是一條被量化到格寬的軌跡**。把後者說成前者的一般化，正是會侵蝕產品承諾的那種推理。

**給使用者的三個選項**

| 選項 | 隱私 | 代價 |
|---|---|---|
| 只持久化**計數**，不存 id 集合 | 不需改 CC-5 | 失去去重與重播決定性；多裝置合併必然重複計數；CC-4 的可驗證性歸零 |
| **持久化格 id 集合** | 需修改 CC-5 規則 3，並補保存期限與刪除權（規則 6） | 冪等、可重播、可合併、可驗證 |
| 存加鹽雜湊 | **假的** | 10⁷ 個候選格可暴力枚舉；鹽若留在裝置則伺服器無法驗證，CC-4 落空。不建議 |

**順帶回報上位文件的一處既存不一致**：CC-5 規則 3 允許持久化「已投影的地圖像素座標」，而地方層的 1 像素遠小於一個網格——**現行清單已經允許了比網格更高解析度的軌跡資料**。這不是放行的理由，是要順手修的洞。

建議的例外條文形狀：

- 網格 id 加入 CC-5 規則 3 的封閉清單（其資訊量低於累計距離：只存集合、不存順序與路徑）
- 離線重播資料：僅除錯建置、以除錯旗標守衛（呼應 CC-5 規則 5）；轉換發生在寫入路徑上，原點與絕對時戳不落盤；固定寫入版控忽略目錄；補靜態檢查禁止落盤格式出現 `lat`/`lng`/`latitude`/`longitude` 鍵；明訂保存期限與刪除責任；不得隨 app 分發或上傳
- 條文中**不得**出現「地理身分銷毀」這類說法——保留完整軌跡形狀與時間關係，配合公開路網比對仍可重新定位。誠實的措辭是「顯著降低但未消除」

### 5.3 修訂四（刪除道路吸附與道路節點）

它縮減 §2.2 的既有契約，屬規格範圍的實質變更。三方技術意見一致，但需要使用者確認。

---

## 6. 延後項與重啟條件

| 項目 | 判定 | 重啟條件 |
|---|---|---|
| **路網比對（map matching）** | 延後 | 完成本修訂後，若都市峽谷路線的里程比值仍在 [0.7, 1.3] 之外，**且**失效樣態經診斷為「沿真實道路行走時的橫向噪聲」。重啟時新增的是「有序、含連通性的路段折線 + 弧長投影」能力，不得復用舊名 |
| **卡爾曼濾波** | 不做 | 無。要壓掉 2.65 m/s 的假跳點，過程噪聲須小到 ≈0.05 m/s²，而真實步行起停加速度為 0.5~1.5 m/s²——同一量級，濾波器換不出資訊 |
| **裝置回報速度作為證據** | 否決 | 無。依據 F4（靜止時中位數 1.85 m/s、最大 14.08 m/s，與位置解算共用同一組衛星幾何） |
| **計步器 / 活動辨識** | 不做 | 使用者裁決：騎乘為主要移動方式，計步涵蓋不到 |
| **T2~T4 校準協定** | 附錄 | 里程若成為機制輸入或進入跨玩家比較，升為必要 |
| **背景定位（前景服務）** | 維持第二階段 | 不變 |

> 路網比對延後的主因不是成本，而是**它對本失效模式無效**：C 趟的兩次假位移為 2.65 m/s 與 3.81 m/s，落在慢跑速度區間內；僅憑「位置與時間」的任何演算法都無法將其與真實移動區分，因為區分所需的資訊不在這兩個通道裡。F4 排除裝置速度後，`accuracy` 是唯一存活的獨立證據源。

---

## 7. 對既有條文的影響

| 條文 | 影響 |
|---|---|
| REQ-C-02 規則 5 | 新增平台層更新間隔 **1 秒（Android 專屬；iOS `CLLocationManager` 無此參數）** |
| REQ-C-03 規則 5 | 刪除裝置速度交叉檢查（§3.5）；併入 F4 的註解 |
| REQ-C-04 | 刪除吸附（§3.4） |
| REQ-C-11 規則 3 | 加註「本清單為封閉列舉」（§3.1） |
| REQ-C-13 | 新增規則 16、17 |
| REQ-C-14 規則 5 | 診斷快照新增 `accuracyGatedFixCount`、`keepAwakeActive` |
| §1.2 / §2.1 / §2.2 / §3.0 / §5 | 刪除吸附與道路節點的所有引用；PRE-3、PRE-8 作廢 |
| §2.2 圖資能力 | 新增「分類遮罩查詢」；`containsGeo` 的實作由模組自決，解析度由模組宣告 |
| AC-3.9 | 改寫（§3.8，修正既有錯誤） |
| AC-3.10 / AC-3.11 | 刪除（§3.5） |
| AC-4.3 / AC-4.6 | 刪除 / 改寫為 AC-18.3 |
| DoD 第 7 條 | 拆為 7a / 7b（§3.6） |
| NFR-5 | 補上「與外部副作用（含喚醒抑制）」 |

**§3.0 管線新版步驟表**

```
 1. 接收原始 Fix
 2. 精度等級評估        （REQ-C-01，對原始串流評估）
 3. 已量測旗標檢查      （REQ-C-03 規則 1）      ┐
 4. 品質閘門            （REQ-C-03 規則 2~5）    ├ sourceMode == virtual 時整段跳過
 5. 連續丟棄保護        （REQ-C-03 規則 6）      ┘
 6. 品質標記判定        （REQ-C-13 規則 16、17。虛擬 Fix 一律合格）
 7. 顯著位移閘門        （REQ-C-03 規則 7。不合格者不參與，且不更新基準）
 8. 地理範圍檢查        （REQ-C-05）
 9. 投影                （REQ-C-04）
10. 大跨距判定          （REQ-C-07。不合格者不更新基準、不消耗不連續標記）
11. 更新目標點
12. 分桶距離累計        （REQ-C-13。用第 8 步之前的地理距離）
13. 平滑（每幀，與上列串流事件非同步）（REQ-C-06）
```

---

## 8. 待決問題

| # | 問題 | 阻擋誰 |
|---|---|---|
| Q19 | 螢幕常亮的耗電量級與可玩時長上限 | 需一輪完整實地耗電數據 |
| Q21 | 丟棄率 20% 門檻在取樣密度提高後是否適當 | 分母已改變（12 → 142 筆）。路徑 A 之下影響降低 |
| Q22 | 品質標記門檻 30 m 是否過嚴 | 需 `accuracyGatedFixCount` 的實地資料 |
| Q23 | 里程數字在遊戲中的用途 | **見 §5.1，本次應一併裁決** |
| Q25 | 其他 Android 廠商與 iOS 的螢幕常亮是否同樣使 Fix 密度回到 8 秒級 | 不阻擋第一版（開發機即目標裝置），但 §3.1 的前提僅在 ColorOS 證實 |
| Q26 | REQ-C-16 規則 5 的開關是否跨進程持久化 | 若持久化則相依任務 B 的持久化層（同 Q18 的先例）。不持久化則玩家每次啟動都要重關 |

---

## 9. 不在本草案範圍

- 顯示點與目標點的分離（`displayPixel`）：屬地方層前置，隨任務 D 處理。世界層下兩者差距 0.08 像素。
- 背景恢復的假里程：已由 `backgroundResume` 不連續機制涵蓋，程式碼完成、**尚未實地驗證**。DoD 7b 的通過為其前置。
- 地方層地圖與尺度（Q15）：屬任務 D。
- 遭遇系統的觸發半徑、停留筆數、冷卻：屬任務 A。本 SPEC 只提供品質標記與幾何約束的形式。

---

## 10. 本次覆核的方法論教訓

記錄於此，因為它們會重複發生：

1. **一個機制「達成 0」和「正確地達成 0」在數字上長得一模一樣。** 靜止趟的 0 曾被當成暖機有效的證據，實際是暖機永不結束（F9）。
2. **用來調參數的資料不能同時用來證明參數穩健。** v2 曾以同一組資料主張「參數敏感度消失」。消融實驗（F7）才是能證偽的形式。
3. **參數的非單調性是耦合的訊號。** 精度上限 25 m 比 30 m 更差（F8），暴露了「上限收緊 → 門檻下沉」的耦合。
4. **樣本最大值不是上界。** 33.0 m 由單一筆 Fix 決定，且 F5 表中 ≤35 m 與 ≤25 m 兩列同值，說明平台自報精度與實際誤差的相關性在尾端是斷的。
