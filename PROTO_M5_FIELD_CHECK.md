# M5 快門手感原型 —— 實機驗證清單

分支：`proto/m5-shutter-feel`（worktree，不影響主線）
對象：`SPEC_MVP_MICRO_ACTION.md` v4 的 §6 待決 1~2，與五件只有實機能判斷的事。

```bash
flutter run -t lib/proto/shutter_proto_app.dart -d <device-id>
# 或用已建好的 debug APK
adb install -r build/app/outputs/flutter-apk/app-debug.apk
```

**操作**：按住畫面 → 白框開始收縮、下緣時間軸出現走針 → 在琥珀色帶（完美窗）內放開。
上緣三檔難度可隨時切換，不用重開。「絕景」開關讓取景框跟著手指。

---

## ① 唯一的量化題：抖動 σ（畫面底部「抖動」列）

判準已寫進 v4 §6 待決 1：

> 若實機抖動 σ > 40 ms，則 `decisiveMoment` 的 $W_p = 120$ ms **判定為抽獎** ——
> ±60 ms 只有 1.5 個抖動標準差，練習改善不了。

畫面會直接給裁決：

| 顯示 | 意義 |
|---|---|
| `樣本不足` | 不到 30 次，σ 還不可信 —— 繼續按 |
| `可練的技巧`（綠） | σ ≤ 40 ms，120 ms 窗成立 |
| `★ 抽獎`（紅） | σ > 40 ms，**最高難度的參數必須上調** |

同一列還給「建議最小完美窗」（$\ge 3\sigma$）。**若它超過 120，就把 v4 REQ-M5-01.8 的 $W_p$ 改成那個數字**，三檔一起重算。

**請至少按 30 次再看。** 這個數字量的是指標事件時戳相對本地單調時鐘的離散度 ——
兩者 epoch 不同，所以絕對值無意義，只有 σ 有意義。

---

## ② 三檔難度的體感（主觀，但有判準）

各檔按 15~20 次，回答「症狀欄有沒有出現」：

| 難度 | 該有的感覺 | 症狀 = 要改 |
|---|---|---|
| 觀光客 | 幾乎不會失手，但**還是有在操作** | 「按一下就過，這步驟多餘」 |
| 攝影師 | 要專心，會失手但不常 | 「太簡單」或「一直失手」 |
| 決定性瞬間 | **難但練得起來** | 「這是抽獎」 |

最後一格最關鍵 —— 就算 ① 的數據漂亮，若主觀仍覺得是抽獎，參數還是要改。

底部「累計」列有 `P / N / F` 計數，可以對照你的主觀感受與實際分佈。

---

## ③ 節奏：連續按 20 次

**這題單次試玩答不出來，只有重複之後才顯形。**

底部會累計次數、總耗時、每次平均。v4 的預算是：

- 單次儀式 = 動畫全長 + 1200 ms（轉場與浮字）
- 最壞情況 Lv.3 球鞋 **26 次**，三檔皆須 ≤ 90 秒

（原型沒有轉場與浮字，所以實測的「每次」會比 v4 的估算短約 1.2 秒。）

**判準**：按到第 15 次的時候，你是還在專心，還是已經想跳過？

---

## ④ 收縮的可讀性

收縮表現刻意**不繞標的** —— v4 REQ-M5-03.3 預判取景框跟手指會讓手指蓋住要盯的東西，
所以改成「畫面下緣的時間軸 + 白框收束」。

**判準**：你是看時間軸的走針，還是看白框？若兩個都要看，那就是設計失敗。

---

## ⑤ 絕景的單一手勢

開「絕景」開關。取景框跟手指，綠色小圈是構圖容差範圍，框變綠表示構圖會通過。

構圖是**二元判定**（v4 REQ-M5-03.5）：不通過則時機態下降一階，`perfect` 只會掉到 `normal`，不會直接 `failed`。

**兩個判準**：

1. 手指會不會蓋住你要盯的東西？
2. 你會覺得這是**一個整合的動作**，還是兩件不相干的事被綁在一起？

第 2 點是覆核指出的疑慮 —— 兩軸在時間上其實解耦（最優策略是先把框移到位、手不動、純等時機）。
若答案是「兩件事」，構圖這半就白做了，該退回純時機或重新設計。

構圖容差初始值（v4 §6 待決 2）：觀光客 25% / 攝影師 18% / 決定性瞬間 12%（佔標的半徑）。

---

## ⑥ 逾時會不會誤觸發

尤其**觀光客**的全長 2000 ms。底部「上次」列會標 `(逾時)`。

**判準**：你會不會因為「以為還有時間」而錯過？若會，觀光客對無障礙的保障就是假的 ——
它保障的是「按得準」，不是「按得到」。

---

## 回報格式

驗完把這些丟回來就好：

```
裝置：
① σ = ___ ms，裁決 = ___，建議最小完美窗 = ___ ms
② 觀光客 / 攝影師 / 決定性瞬間 各自的症狀（或「沒症狀」）
③ 20 次之後：還在專心 / 想跳過
④ 看走針還是看白框
⑤ 手指遮擋：有 / 無；整合動作：是 / 否
⑥ 逾時誤觸發：有 / 無
```

拿到之後我會改 v4 §REQ-M5-01.8 的參數表與 §6 的待決項，然後才送第四輪覆核。

---

## 這個原型不驗什麼

滿意度分佈、三態訊號存在率、單張卡降幅、`c` 係數與 CP 地板、黃金槽回歸、期望 `tierFactor`
隨難度的走向 —— 都是純算術，三支分析腳本已量完，實機只會給出同樣的數字。

原型也**不接**第二層分數、計分、文案、存檔、因果面板。它是丟棄式的：產出一組參數與一個
「機制成不成立」的判斷，然後刪掉。
